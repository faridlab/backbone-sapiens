-- MFADevice Seed Data
-- Generated by backbone-schema
--
-- This file contains initial seed data for the mfa_devices table.
-- Customize this file to add your initial data.

-- INSERT INTO mfa_devices (
--     id,
--     user_id,
--     device_type,
--     device_name,
--     phone_number,
--     email_address,
--     totp_secret,
--     secret,
--     hardware_key_id,
--     push_token,
--     device_fingerprint,
--     manufacturer,
--     model,
--     operating_system,
--     app_version,
--     is_primary,
--     is_backup,
--     requires_verification,
--     auto_approval_enabled,
--     trusted_duration_hours,
--     enrolled_at,
--     enrolled_by,
--     enrollment_method,
--     enrollment_ip,
--     enrollment_user_agent,
--     verification_attempts,
--     backup_codes_generated,
--     verified_at,
--     last_used,
--     last_used_at,
--     usage_count,
--     successful_verifications,
--     failed_verifications,
--     is_locked,
--     locked_at,
--     locked_until,
--     lock_reason,
--     risk_score,
--     is_active,
--     status,
--     backup_codes_data,
--     metadata
-- ) VALUES
-- (
--     -- Add values here
-- );

-- Example: Insert reference data
-- Uncomment and modify the following:

-- INSERT INTO mfa_devices (id, code, name, is_active, created_at, updated_at) VALUES
--     (gen_random_uuid(), 'SAMPLE1', 'Sample 1', true, NOW(), NOW()),
--     (gen_random_uuid(), 'SAMPLE2', 'Sample 2', true, NOW(), NOW());

-- <<< CUSTOM SEED DATA >>>
-- Add your custom seed data below

