-- Migration: Create session_limits table
-- Generated by backbone-schema

CREATE TABLE IF NOT EXISTS session_limits (
    id UUID NOT NULL DEFAULT gen_random_uuid(),
    user_id UUID NOT NULL,
    max_sessions INTEGER NOT NULL DEFAULT 5,
    max_sessions_per_device INTEGER,
    enforce_limit BOOLEAN NOT NULL DEFAULT TRUE,
    current_session_count INTEGER NOT NULL DEFAULT 0,
    last_session_revoke_at TIMESTAMPTZ,
    metadata JSONB NOT NULL DEFAULT '{"created_at":null,"updated_at":null,"deleted_at":null,"created_by":null,"updated_by":null,"deleted_by":null}'::jsonb,
    PRIMARY KEY (id)
);

CREATE INDEX IF NOT EXISTS idx_session_limits_user_id ON session_limits (user_id);

CREATE INDEX IF NOT EXISTS idx_session_limits_enforce_limit ON session_limits (enforce_limit);

CREATE UNIQUE INDEX IF NOT EXISTS idx_session_limits_user_id ON session_limits (user_id);

-- GIN index for audit metadata JSONB queries
CREATE INDEX IF NOT EXISTS idx_session_limits_metadata_gin ON session_limits USING GIN (metadata);
CREATE INDEX IF NOT EXISTS idx_session_limits_metadata_deleted_at ON session_limits ((metadata->>'deleted_at'));

-- Triggers for automatic metadata timestamp management
-- Automatically sets created_at on INSERT and updated_at on UPDATE

-- Function to set metadata->'created_at' on INSERT
CREATE OR REPLACE FUNCTION session_limits_audit_timestamp() RETURNS trigger AS $$
BEGIN
    IF TG_OP = 'INSERT' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{created_at}', to_jsonb(NOW()));
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    ELSIF TG_OP = 'UPDATE' THEN
        NEW.metadata = jsonb_set(NEW.metadata::jsonb, '{updated_at}', to_jsonb(NOW()));
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Trigger to set timestamps on INSERT
DROP TRIGGER IF EXISTS session_limits_insert_audit ON session_limits;
CREATE TRIGGER session_limits_insert_audit BEFORE INSERT ON session_limits
    FOR EACH ROW EXECUTE FUNCTION session_limits_audit_timestamp();

-- Trigger to set updated_at on UPDATE
DROP TRIGGER IF EXISTS session_limits_update_audit ON session_limits;
CREATE TRIGGER session_limits_update_audit BEFORE UPDATE ON session_limits
    FOR EACH ROW EXECUTE FUNCTION session_limits_audit_timestamp();

