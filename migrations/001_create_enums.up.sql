-- Enum types for sapiens module
-- Generated by backbone-schema

-- Create analytics_event_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'analytics_event_type') THEN
        CREATE TYPE analytics_event_type AS ENUM ('user_login', 'user_logout', 'user_registration', 'password_change', 'mfa_setup', 'mfa_verification', 'role_assignment', 'permission_grant', 'api_access', 'page_view', 'feature_usage', 'security_event', 'system_event');
    END IF;
END
$$;

-- Create analytics_metric_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'analytics_metric_type') THEN
        CREATE TYPE analytics_metric_type AS ENUM ('counter', 'gauge', 'histogram', 'timer');
    END IF;
END
$$;

-- Create aggregation_level enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'aggregation_level') THEN
        CREATE TYPE aggregation_level AS ENUM ('minute', 'hour', 'day', 'week', 'month', 'quarter', 'year');
    END IF;
END
$$;

-- Create analytics_report_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'analytics_report_type') THEN
        CREATE TYPE analytics_report_type AS ENUM ('user_activity', 'login_trends', 'feature_usage', 'security_events', 'performance_metrics', 'compliance_audit', 'custom');
    END IF;
END
$$;

-- Create report_format enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'report_format') THEN
        CREATE TYPE report_format AS ENUM ('json', 'csv', 'pdf', 'excel');
    END IF;
END
$$;

-- Create report_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'report_status') THEN
        CREATE TYPE report_status AS ENUM ('pending', 'generating', 'completed', 'failed', 'expired');
    END IF;
END
$$;

-- Create anonymization_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'anonymization_method') THEN
        CREATE TYPE anonymization_method AS ENUM ('full', 'partial', 'pseudonymization');
    END IF;
END
$$;

-- Create anonymization_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'anonymization_status') THEN
        CREATE TYPE anonymization_status AS ENUM ('pending', 'processing', 'completed', 'failed');
    END IF;
END
$$;

-- Create audit_log_severity enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'audit_log_severity') THEN
        CREATE TYPE audit_log_severity AS ENUM ('info', 'warning', 'error', 'critical');
    END IF;
END
$$;

-- Create bulk_operation_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'bulk_operation_type') THEN
        CREATE TYPE bulk_operation_type AS ENUM ('user_import', 'user_export', 'role_assignment', 'permission_grant', 'user_update', 'user_suspension', 'user_deletion');
    END IF;
END
$$;

-- Create bulk_operation_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'bulk_operation_status') THEN
        CREATE TYPE bulk_operation_status AS ENUM ('pending', 'running', 'completed', 'failed', 'cancelled', 'paused');
    END IF;
END
$$;

-- Create bulk_operation_result_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'bulk_operation_result_status') THEN
        CREATE TYPE bulk_operation_result_status AS ENUM ('pending', 'processing', 'success', 'failed', 'skipped', 'warning');
    END IF;
END
$$;

-- Create data_export_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'data_export_status') THEN
        CREATE TYPE data_export_status AS ENUM ('pending', 'processing', 'completed', 'failed', 'expired');
    END IF;
END
$$;

-- Create data_export_format enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'data_export_format') THEN
        CREATE TYPE data_export_format AS ENUM ('json', 'csv', 'pdf', 'xml');
    END IF;
END
$$;

-- Create device_trust_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'device_trust_type') THEN
        CREATE TYPE device_trust_type AS ENUM ('desktop', 'laptop', 'mobile', 'tablet', 'browser', 'application');
    END IF;
END
$$;

-- Create trust_level enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'trust_level') THEN
        CREATE TYPE trust_level AS ENUM ('temporary', 'standard', 'trusted', 'permanent');
    END IF;
END
$$;

-- Create direct_permission_grant_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'direct_permission_grant_status') THEN
        CREATE TYPE direct_permission_grant_status AS ENUM ('active', 'expired', 'revoked', 'suspended');
    END IF;
END
$$;

-- Create email_verification_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'email_verification_type') THEN
        CREATE TYPE email_verification_type AS ENUM ('account_creation', 'email_change', 'password_reset', 'account_reactivation');
    END IF;
END
$$;

-- Create email_verification_token_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'email_verification_token_status') THEN
        CREATE TYPE email_verification_token_status AS ENUM ('pending', 'verified', 'expired', 'revoked');
    END IF;
END
$$;

-- Create impersonation_session_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'impersonation_session_status') THEN
        CREATE TYPE impersonation_session_status AS ENUM ('active', 'ended', 'expired', 'terminated');
    END IF;
END
$$;

-- Create ldap_directory_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'ldap_directory_status') THEN
        CREATE TYPE ldap_directory_status AS ENUM ('draft', 'active', 'inactive', 'error');
    END IF;
END
$$;

-- Create generation_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'generation_method') THEN
        CREATE TYPE generation_method AS ENUM ('user_initiated', 'admin_enforced', 'automatic', 'device_enrollment', 'emergency');
    END IF;
END
$$;

-- Create compromise_detection_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'compromise_detection_method') THEN
        CREATE TYPE compromise_detection_method AS ENUM ('failed_attempts', 'unusual_usage', 'admin_report', 'automatic_scan', 'user_report');
    END IF;
END
$$;

-- Create mfa_device_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'mfa_device_type') THEN
        CREATE TYPE mfa_device_type AS ENUM ('totp', 'sms', 'email', 'hardware_key', 'biometric', 'push_notification');
    END IF;
END
$$;

-- Create enrollment_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'enrollment_method') THEN
        CREATE TYPE enrollment_method AS ENUM ('self_service', 'admin_enforced', 'automatic', 'emergency');
    END IF;
END
$$;

-- Create mfa_device_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'mfa_device_status') THEN
        CREATE TYPE mfa_device_status AS ENUM ('active', 'inactive', 'suspended', 'compromised', 'expired', 'revoked');
    END IF;
END
$$;

-- Create primary_auth_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'primary_auth_method') THEN
        CREATE TYPE primary_auth_method AS ENUM ('password', 'sso', 'oauth', 'certificate', 'biometric');
    END IF;
END
$$;

-- Create mfa_trust_level enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'mfa_trust_level') THEN
        CREATE TYPE mfa_trust_level AS ENUM ('low', 'medium', 'high', 'critical');
    END IF;
END
$$;

-- Create mfa_verification_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'mfa_verification_method') THEN
        CREATE TYPE mfa_verification_method AS ENUM ('totp', 'sms', 'email', 'hardware_key', 'push_notification', 'backup_code', 'biometric');
    END IF;
END
$$;

-- Create device_trust_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'device_trust_status') THEN
        CREATE TYPE device_trust_status AS ENUM ('trusted', 'untrusted', 'new_device', 'compromised');
    END IF;
END
$$;

-- Create network_quality enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'network_quality') THEN
        CREATE TYPE network_quality AS ENUM ('excellent', 'good', 'fair', 'poor', 'unknown');
    END IF;
END
$$;

-- Create mfa_session_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'mfa_session_status') THEN
        CREATE TYPE mfa_session_status AS ENUM ('initiated', 'pending_verification', 'verified', 'failed', 'expired', 'terminated');
    END IF;
END
$$;

-- Create notification_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'notification_type') THEN
        CREATE TYPE notification_type AS ENUM ('welcome', 'email_verification', 'password_reset', 'security_alert', 'account_suspended', 'role_assigned', 'permission_granted', 'mfa_enabled', 'login_alert', 'system_maintenance', 'general');
    END IF;
END
$$;

-- Create notification_channel enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'notification_channel') THEN
        CREATE TYPE notification_channel AS ENUM ('in_app', 'email', 'sms', 'push');
    END IF;
END
$$;

-- Create notification_priority enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'notification_priority') THEN
        CREATE TYPE notification_priority AS ENUM ('low', 'normal', 'high', 'urgent');
    END IF;
END
$$;

-- Create notification_log_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'notification_log_status') THEN
        CREATE TYPE notification_log_status AS ENUM ('pending', 'sent', 'delivered', 'failed', 'bounced');
    END IF;
END
$$;

-- Create digest_frequency enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'digest_frequency') THEN
        CREATE TYPE digest_frequency AS ENUM ('immediate', 'hourly', 'daily', 'weekly');
    END IF;
END
$$;

-- Create o_auth_provider_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'o_auth_provider_type') THEN
        CREATE TYPE o_auth_provider_type AS ENUM ('google', 'github', 'microsoft', 'facebook', 'apple');
    END IF;
END
$$;

-- Create user_o_auth_link_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'user_o_auth_link_status') THEN
        CREATE TYPE user_o_auth_link_status AS ENUM ('pending_verification', 'active', 'expired', 'revoked');
    END IF;
END
$$;

-- Create organization_membership_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'organization_membership_status') THEN
        CREATE TYPE organization_membership_status AS ENUM ('active', 'inactive', 'pending', 'suspended');
    END IF;
END
$$;

-- Create password_creation_method enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'password_creation_method') THEN
        CREATE TYPE password_creation_method AS ENUM ('registration', 'password_change', 'reset_by_admin', 'self_reset', 'forced_change');
    END IF;
END
$$;

-- Create password_history_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'password_history_status') THEN
        CREATE TYPE password_history_status AS ENUM ('active', 'expired', 'compromised', 'replaced', 'rotated');
    END IF;
END
$$;

-- Create password_reset_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'password_reset_status') THEN
        CREATE TYPE password_reset_status AS ENUM ('pending', 'verified', 'completed', 'expired', 'revoked');
    END IF;
END
$$;

-- Create password_strength enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'password_strength') THEN
        CREATE TYPE password_strength AS ENUM ('very_weak', 'weak', 'moderate', 'strong', 'very_strong');
    END IF;
END
$$;

-- Create conflict_severity enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'conflict_severity') THEN
        CREATE TYPE conflict_severity AS ENUM ('low', 'medium', 'high', 'critical');
    END IF;
END
$$;

-- Create conflict_resolution_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'conflict_resolution_status') THEN
        CREATE TYPE conflict_resolution_status AS ENUM ('pending', 'resolved', 'escalated', 'ignored');
    END IF;
END
$$;

-- Create request_source enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'request_source') THEN
        CREATE TYPE request_source AS ENUM ('web', 'mobile', 'api', 'admin', 'cli', 'webhook', 'service');
    END IF;
END
$$;

-- Create resource_permission_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'resource_permission_status') THEN
        CREATE TYPE resource_permission_status AS ENUM ('active', 'expired', 'revoked');
    END IF;
END
$$;

-- Create conflict_resolution_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'conflict_resolution_type') THEN
        CREATE TYPE conflict_resolution_type AS ENUM ('priority', 'latest', 'manual', 'escalation');
    END IF;
END
$$;

-- Create role_assignment_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'role_assignment_status') THEN
        CREATE TYPE role_assignment_status AS ENUM ('active', 'inactive', 'expired', 'suspended', 'pending_approval');
    END IF;
END
$$;

-- Create saml_provider_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'saml_provider_status') THEN
        CREATE TYPE saml_provider_status AS ENUM ('draft', 'active', 'inactive');
    END IF;
END
$$;

-- Create security_event_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'security_event_type') THEN
        CREATE TYPE security_event_type AS ENUM ('login_failed', 'suspicious_login', 'password_reused', 'account_locked', 'privilege_escalation', 'brute_force_attack', 'malicious_content', 'impersonation_started', 'impersonation_ended', 'mfa_enabled', 'mfa_disabled', 'data_exported', 'data_anonymized', 'rate_limit_exceeded', 'password_reset_request', 'password_changed');
    END IF;
END
$$;

-- Create security_event_severity enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'security_event_severity') THEN
        CREATE TYPE security_event_severity AS ENUM ('low', 'medium', 'high', 'critical');
    END IF;
END
$$;

-- Create device_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'device_type') THEN
        CREATE TYPE device_type AS ENUM ('web', 'mobile', 'tablet', 'desktop', 'unknown');
    END IF;
END
$$;

-- Create temporary_permission_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'temporary_permission_status') THEN
        CREATE TYPE temporary_permission_status AS ENUM ('pending', 'active', 'expired', 'revoked');
    END IF;
END
$$;

-- Create user_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'user_status') THEN
        CREATE TYPE user_status AS ENUM ('active', 'inactive', 'suspended', 'pending_verification');
    END IF;
END
$$;

-- Create gender enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'gender') THEN
        CREATE TYPE gender AS ENUM ('male', 'female');
    END IF;
END
$$;

-- Create user_saml_link_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'user_saml_link_status') THEN
        CREATE TYPE user_saml_link_status AS ENUM ('active', 'inactive', 'revoked');
    END IF;
END
$$;

-- Create theme enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'theme') THEN
        CREATE TYPE theme AS ENUM ('light', 'dark', 'system');
    END IF;
END
$$;

-- Create workflow_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'workflow_type') THEN
        CREATE TYPE workflow_type AS ENUM ('user_registration', 'email_verification', 'password_reset', 'account_suspension', 'role_assignment', 'permission_grant', 'mfa_setup', 'email_change', 'account_recovery', 'bulk_user_import', 'oauth_linking', 'device_trust', 'security_review', 'compliance_audit');
    END IF;
END
$$;

-- Create workflow_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'workflow_status') THEN
        CREATE TYPE workflow_status AS ENUM ('pending', 'running', 'paused', 'completed', 'failed', 'cancelled', 'expired');
    END IF;
END
$$;

-- Create workflow_step_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'workflow_step_type') THEN
        CREATE TYPE workflow_step_type AS ENUM ('validation', 'database_operation', 'external_service', 'notification', 'approval', 'email_send', 'security_check', 'audit_log', 'user_interaction', 'background_job');
    END IF;
END
$$;

-- Create workflow_step_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'workflow_step_status') THEN
        CREATE TYPE workflow_step_status AS ENUM ('pending', 'running', 'completed', 'failed', 'skipped', 'cancelled');
    END IF;
END
$$;

-- Create trigger_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'trigger_type') THEN
        CREATE TYPE trigger_type AS ENUM ('manual', 'scheduled', 'event_based', 'webhook', 'api_call');
    END IF;
END
$$;

-- Create action_type enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'action_type') THEN
        CREATE TYPE action_type AS ENUM ('send_email', 'create_task', 'update_record', 'call_api', 'approve', 'reject', 'notify_user', 'schedule_job');
    END IF;
END
$$;

-- Create workflow_execution_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'workflow_execution_status') THEN
        CREATE TYPE workflow_execution_status AS ENUM ('pending', 'running', 'paused', 'completed', 'failed', 'cancelled', 'timeout');
    END IF;
END
$$;

-- Create workflow_action_execution_status enum type
DO $$
BEGIN
    IF NOT EXISTS (SELECT 1 FROM pg_type WHERE typname = 'workflow_action_execution_status') THEN
        CREATE TYPE workflow_action_execution_status AS ENUM ('pending', 'processing', 'success', 'failed', 'skipped', 'warning');
    END IF;
END
$$;

