//! OrganizationPermission REST handlers
//!
//! Auto-generated by backbone-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc};

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::{AuthContext, AuthMiddleware};

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{OrganizationPermissionService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateOrganizationPermissionDto, UpdateOrganizationPermissionDto, PatchOrganizationPermissionDto, OrganizationPermissionResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum OrganizationPermissionError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for OrganizationPermissionError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for OrganizationPermissionError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "ORGANIZATIONPERMISSION_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "ORGANIZATIONPERMISSION_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "ORGANIZATIONPERMISSION_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "ORGANIZATIONPERMISSION_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for OrganizationPermission.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /organization_permissions | List with pagination |
/// | POST | /organization_permissions | Create new |
/// | GET | /organization_permissions/:id | Get by ID |
/// | PUT | /organization_permissions/:id | Full update |
/// | PATCH | /organization_permissions/:id | Partial update |
/// | DELETE | /organization_permissions/:id | Soft delete |
/// | POST | /organization_permissions/bulk | Bulk create |
/// | POST | /organization_permissions/upsert | Upsert |
/// | GET | /organization_permissions/trash | List deleted |
/// | POST | /organization_permissions/:id/restore | Restore |
/// | DELETE | /organization_permissions/empty | Empty trash |
/// | GET | /organization_permissions/:id/deleted | Get deleted by ID |
/// | DELETE | /organization_permissions/trash/:id | Permanent delete from trash |
/// | GET | /organization_permissions/count | Count active entities |
/// | GET | /organization_permissions/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(OrganizationPermissionService::with_repository(repository));
/// let router = create_organization_permission_routes(service);
/// ```
pub fn create_organization_permission_routes(service: Arc<OrganizationPermissionService>) -> Router {
    BackboneCrudHandler::<OrganizationPermissionService, OrganizationPermission, CreateOrganizationPermissionDto, UpdateOrganizationPermissionDto, OrganizationPermissionResponseDto>::routes(
        service,
        "/organization_permissions",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_organization_permission_routes<A: AuthMiddleware + 'static>(
    service: Arc<OrganizationPermissionService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_organization_permission_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                match auth.authenticate(&req).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

