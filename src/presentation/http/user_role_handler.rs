//! UserRole REST handlers
//!
//! Auto-generated by backbone-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc};

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::{AuthContext, AuthMiddleware};

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{UserRoleService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateUserRoleDto, UpdateUserRoleDto, PatchUserRoleDto, UserRoleResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum UserRoleError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for UserRoleError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for UserRoleError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "USERROLE_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "USERROLE_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "USERROLE_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "USERROLE_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for UserRole.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /user_roles | List with pagination |
/// | POST | /user_roles | Create new |
/// | GET | /user_roles/:id | Get by ID |
/// | PUT | /user_roles/:id | Full update |
/// | PATCH | /user_roles/:id | Partial update |
/// | DELETE | /user_roles/:id | Soft delete |
/// | POST | /user_roles/bulk | Bulk create |
/// | POST | /user_roles/upsert | Upsert |
/// | GET | /user_roles/trash | List deleted |
/// | POST | /user_roles/:id/restore | Restore |
/// | DELETE | /user_roles/empty | Empty trash |
/// | GET | /user_roles/:id/deleted | Get deleted by ID |
/// | DELETE | /user_roles/trash/:id | Permanent delete from trash |
/// | GET | /user_roles/count | Count active entities |
/// | GET | /user_roles/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(UserRoleService::with_repository(repository));
/// let router = create_user_role_routes(service);
/// ```
pub fn create_user_role_routes(service: Arc<UserRoleService>) -> Router {
    BackboneCrudHandler::<UserRoleService, UserRole, CreateUserRoleDto, UpdateUserRoleDto, UserRoleResponseDto>::routes(
        service,
        "/user_roles",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_user_role_routes<A: AuthMiddleware + 'static>(
    service: Arc<UserRoleService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_user_role_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                match auth.authenticate(&req).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

