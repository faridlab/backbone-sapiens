//! EffectivePermissionCache REST handlers
//!
//! Auto-generated by backbone-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::{AuthContext, AuthMiddleware};

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{EffectivePermissionCacheService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateEffectivePermissionCacheDto, UpdateEffectivePermissionCacheDto, PatchEffectivePermissionCacheDto, EffectivePermissionCacheResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum EffectivePermissionCacheError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for EffectivePermissionCacheError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for EffectivePermissionCacheError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "EFFECTIVEPERMISSIONCACHE_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "EFFECTIVEPERMISSIONCACHE_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "EFFECTIVEPERMISSIONCACHE_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "EFFECTIVEPERMISSIONCACHE_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for EffectivePermissionCache.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /effective_permission_caches | List with pagination |
/// | POST | /effective_permission_caches | Create new |
/// | GET | /effective_permission_caches/:id | Get by ID |
/// | PUT | /effective_permission_caches/:id | Full update |
/// | PATCH | /effective_permission_caches/:id | Partial update |
/// | DELETE | /effective_permission_caches/:id | Soft delete |
/// | POST | /effective_permission_caches/bulk | Bulk create |
/// | POST | /effective_permission_caches/upsert | Upsert |
/// | GET | /effective_permission_caches/trash | List deleted |
/// | POST | /effective_permission_caches/:id/restore | Restore |
/// | DELETE | /effective_permission_caches/empty | Empty trash |
/// | GET | /effective_permission_caches/:id/deleted | Get deleted by ID |
/// | DELETE | /effective_permission_caches/trash/:id | Permanent delete from trash |
/// | GET | /effective_permission_caches/count | Count active entities |
/// | GET | /effective_permission_caches/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(EffectivePermissionCacheService::with_repository(repository));
/// let router = create_effective_permission_cache_routes(service);
/// ```
pub fn create_effective_permission_cache_routes(service: Arc<EffectivePermissionCacheService>) -> Router {
    BackboneCrudHandler::<EffectivePermissionCacheService, EffectivePermissionCache, CreateEffectivePermissionCacheDto, UpdateEffectivePermissionCacheDto, EffectivePermissionCacheResponseDto>::routes(
        service,
        "/effective_permission_caches",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_effective_permission_cache_routes<A: AuthMiddleware + 'static>(
    service: Arc<EffectivePermissionCacheService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_effective_permission_cache_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                match auth.authenticate(&req).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

