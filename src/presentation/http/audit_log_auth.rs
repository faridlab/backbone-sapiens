//! Auth configuration for AuditLog handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use backbone_auth::{AuthContext, AuthMiddleware};
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for AuditLog operations
pub mod audit_logs {
    /// Permission to list audit_log entities
    pub const LIST: &str = "audit_log:list";
    /// Permission to read audit_log entity details
    pub const READ: &str = "audit_log:read";
    /// Permission to create audit_log entities
    pub const CREATE: &str = "audit_log:create";
    /// Permission to update audit_log entities
    pub const UPDATE: &str = "audit_log:update";
    /// Permission to delete audit_log entities
    pub const DELETE: &str = "audit_log:delete";
    /// Permission to bulk create audit_log entities
    pub const BULK_CREATE: &str = "audit_log:bulk_create";
    /// Permission to upsert audit_log entities
    pub const UPSERT: &str = "audit_log:upsert";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            BULK_CREATE,
            UPSERT,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.permissions.iter().any(|p| p == permission)
}

/// Check if auth context has any of the required roles
pub fn has_role(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.roles.iter().any(|r| roles.contains(&r.as_str()))
}

/// Check if auth context has all of the required permissions
pub fn has_all_permissions(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().all(|p| has_permission(auth, p))
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| has_permission(auth, p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Create a permission guard that checks for a specific permission
pub fn require_permission(permission: &'static str) -> impl Fn(
    axum::extract::Extension<AuthContext>,
    Request,
    Next,
) -> impl std::future::Future<Output = Result<Response, StatusCode>> + Send + Clone {
    move |auth, request, next| async move {
        if has_permission(&auth, permission) {
            Ok(next.run(request).await)
        } else {
            Err(StatusCode::FORBIDDEN)
        }
    }
}

/// Create a role guard that checks for any of the specified roles
pub fn require_role(roles: &'static [&'static str]) -> impl Fn(
    axum::extract::Extension<AuthContext>,
    Request,
    Next,
) -> impl std::future::Future<Output = Result<Response, StatusCode>> + Send + Clone {
    move |auth, request, next| async move {
        if has_role(&auth, roles) {
            Ok(next.run(request).await)
        } else {
            Err(StatusCode::FORBIDDEN)
        }
    }
}

// ==========================================================================
// AuditLog Specific Guards
// ==========================================================================

/// Guard that requires list permission for audit_log
pub async fn require_list_audit_log(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, audit_logs::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for audit_log
pub async fn require_read_audit_log(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, audit_logs::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for audit_log
pub async fn require_create_audit_log(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, audit_logs::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for audit_log
pub async fn require_update_audit_log(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, audit_logs::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for audit_log
pub async fn require_delete_audit_log(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, audit_logs::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

