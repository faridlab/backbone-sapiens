//! Auth configuration for SAMLProvider handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for SAMLProvider operations
pub mod saml_providers {
    /// Permission to list saml_provider entities
    pub const LIST: &str = "saml_provider:list";
    /// Permission to read saml_provider entity details
    pub const READ: &str = "saml_provider:read";
    /// Permission to create saml_provider entities
    pub const CREATE: &str = "saml_provider:create";
    /// Permission to update saml_provider entities
    pub const UPDATE: &str = "saml_provider:update";
    /// Permission to delete saml_provider entities
    pub const DELETE: &str = "saml_provider:delete";
    /// Permission to restore deleted saml_provider entities
    pub const RESTORE: &str = "saml_provider:restore";
    /// Permission to view saml_provider trash
    pub const TRASH: &str = "saml_provider:trash";
    /// Permission to empty saml_provider trash (permanent delete)
    pub const EMPTY_TRASH: &str = "saml_provider:empty_trash";
    /// Permission to bulk create saml_provider entities
    pub const BULK_CREATE: &str = "saml_provider:bulk_create";
    /// Permission to upsert saml_provider entities
    pub const UPSERT: &str = "saml_provider:upsert";

    // State transition permissions
    /// Permission to execute activate transition
    pub const TRANSITION_ACTIVATE: &str = "saml_provider:transition:activate";
    /// Permission to execute deactivate transition
    pub const TRANSITION_DEACTIVATE: &str = "saml_provider:transition:deactivate";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_ACTIVATE,
            TRANSITION_DEACTIVATE,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// SAMLProvider Specific Guards
// ==========================================================================

/// Guard that requires list permission for saml_provider
pub async fn require_list_saml_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, saml_providers::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for saml_provider
pub async fn require_read_saml_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, saml_providers::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for saml_provider
pub async fn require_create_saml_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, saml_providers::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for saml_provider
pub async fn require_update_saml_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, saml_providers::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for saml_provider
pub async fn require_delete_saml_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, saml_providers::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for saml_provider
pub async fn require_restore_saml_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, saml_providers::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for saml_provider
pub async fn require_trash_saml_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, saml_providers::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for saml_provider
pub async fn require_empty_trash_saml_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, saml_providers::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

