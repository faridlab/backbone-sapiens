//! Auth configuration for EffectivePermissionCache handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for EffectivePermissionCache operations
pub mod effective_permission_caches {
    /// Permission to list effective_permission_cache entities
    pub const LIST: &str = "effective_permission_cache:list";
    /// Permission to read effective_permission_cache entity details
    pub const READ: &str = "effective_permission_cache:read";
    /// Permission to create effective_permission_cache entities
    pub const CREATE: &str = "effective_permission_cache:create";
    /// Permission to update effective_permission_cache entities
    pub const UPDATE: &str = "effective_permission_cache:update";
    /// Permission to delete effective_permission_cache entities
    pub const DELETE: &str = "effective_permission_cache:delete";
    /// Permission to restore deleted effective_permission_cache entities
    pub const RESTORE: &str = "effective_permission_cache:restore";
    /// Permission to view effective_permission_cache trash
    pub const TRASH: &str = "effective_permission_cache:trash";
    /// Permission to empty effective_permission_cache trash (permanent delete)
    pub const EMPTY_TRASH: &str = "effective_permission_cache:empty_trash";
    /// Permission to bulk create effective_permission_cache entities
    pub const BULK_CREATE: &str = "effective_permission_cache:bulk_create";
    /// Permission to upsert effective_permission_cache entities
    pub const UPSERT: &str = "effective_permission_cache:upsert";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// EffectivePermissionCache Specific Guards
// ==========================================================================

/// Guard that requires list permission for effective_permission_cache
pub async fn require_list_effective_permission_cache(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, effective_permission_caches::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for effective_permission_cache
pub async fn require_read_effective_permission_cache(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, effective_permission_caches::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for effective_permission_cache
pub async fn require_create_effective_permission_cache(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, effective_permission_caches::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for effective_permission_cache
pub async fn require_update_effective_permission_cache(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, effective_permission_caches::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for effective_permission_cache
pub async fn require_delete_effective_permission_cache(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, effective_permission_caches::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for effective_permission_cache
pub async fn require_restore_effective_permission_cache(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, effective_permission_caches::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for effective_permission_cache
pub async fn require_trash_effective_permission_cache(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, effective_permission_caches::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for effective_permission_cache
pub async fn require_empty_trash_effective_permission_cache(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, effective_permission_caches::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

