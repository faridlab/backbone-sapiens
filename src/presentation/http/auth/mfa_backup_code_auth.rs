//! Auth configuration for MFABackupCode handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for MFABackupCode operations
pub mod mfa_backup_codes {
    /// Permission to list mfa_backup_code entities
    pub const LIST: &str = "mfa_backup_code:list";
    /// Permission to read mfa_backup_code entity details
    pub const READ: &str = "mfa_backup_code:read";
    /// Permission to create mfa_backup_code entities
    pub const CREATE: &str = "mfa_backup_code:create";
    /// Permission to update mfa_backup_code entities
    pub const UPDATE: &str = "mfa_backup_code:update";
    /// Permission to delete mfa_backup_code entities
    pub const DELETE: &str = "mfa_backup_code:delete";
    /// Permission to restore deleted mfa_backup_code entities
    pub const RESTORE: &str = "mfa_backup_code:restore";
    /// Permission to view mfa_backup_code trash
    pub const TRASH: &str = "mfa_backup_code:trash";
    /// Permission to empty mfa_backup_code trash (permanent delete)
    pub const EMPTY_TRASH: &str = "mfa_backup_code:empty_trash";
    /// Permission to bulk create mfa_backup_code entities
    pub const BULK_CREATE: &str = "mfa_backup_code:bulk_create";
    /// Permission to upsert mfa_backup_code entities
    pub const UPSERT: &str = "mfa_backup_code:upsert";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// MFABackupCode Specific Guards
// ==========================================================================

/// Guard that requires list permission for mfa_backup_code
pub async fn require_list_mfa_backup_code(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_backup_codes::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for mfa_backup_code
pub async fn require_read_mfa_backup_code(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_backup_codes::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for mfa_backup_code
pub async fn require_create_mfa_backup_code(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_backup_codes::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for mfa_backup_code
pub async fn require_update_mfa_backup_code(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_backup_codes::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for mfa_backup_code
pub async fn require_delete_mfa_backup_code(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_backup_codes::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for mfa_backup_code
pub async fn require_restore_mfa_backup_code(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_backup_codes::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for mfa_backup_code
pub async fn require_trash_mfa_backup_code(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_backup_codes::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for mfa_backup_code
pub async fn require_empty_trash_mfa_backup_code(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_backup_codes::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

