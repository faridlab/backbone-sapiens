//! Auth configuration for User handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for User operations
pub mod users {
    /// Permission to list user entities
    pub const LIST: &str = "user:list";
    /// Permission to read user entity details
    pub const READ: &str = "user:read";
    /// Permission to create user entities
    pub const CREATE: &str = "user:create";
    /// Permission to update user entities
    pub const UPDATE: &str = "user:update";
    /// Permission to delete user entities
    pub const DELETE: &str = "user:delete";
    /// Permission to restore deleted user entities
    pub const RESTORE: &str = "user:restore";
    /// Permission to view user trash
    pub const TRASH: &str = "user:trash";
    /// Permission to empty user trash (permanent delete)
    pub const EMPTY_TRASH: &str = "user:empty_trash";
    /// Permission to bulk create user entities
    pub const BULK_CREATE: &str = "user:bulk_create";
    /// Permission to upsert user entities
    pub const UPSERT: &str = "user:upsert";

    // State transition permissions
    /// Permission to execute verify transition
    pub const TRANSITION_VERIFY: &str = "user:transition:verify";
    /// Permission to execute activate transition
    pub const TRANSITION_ACTIVATE: &str = "user:transition:activate";
    /// Permission to execute deactivate transition
    pub const TRANSITION_DEACTIVATE: &str = "user:transition:deactivate";
    /// Permission to execute reactivate transition
    pub const TRANSITION_REACTIVATE: &str = "user:transition:reactivate";
    /// Permission to execute suspend transition
    pub const TRANSITION_SUSPEND: &str = "user:transition:suspend";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_VERIFY,
            TRANSITION_ACTIVATE,
            TRANSITION_DEACTIVATE,
            TRANSITION_REACTIVATE,
            TRANSITION_SUSPEND,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// User Specific Guards
// ==========================================================================

/// Guard that requires list permission for user
pub async fn require_list_user(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, users::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for user
pub async fn require_read_user(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, users::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for user
pub async fn require_create_user(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, users::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for user
pub async fn require_update_user(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, users::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for user
pub async fn require_delete_user(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, users::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for user
pub async fn require_restore_user(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, users::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for user
pub async fn require_trash_user(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, users::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for user
pub async fn require_empty_trash_user(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, users::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

