//! Auth configuration for UserSAMLLink handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for UserSAMLLink operations
pub mod user_saml_links {
    /// Permission to list user_saml_link entities
    pub const LIST: &str = "user_saml_link:list";
    /// Permission to read user_saml_link entity details
    pub const READ: &str = "user_saml_link:read";
    /// Permission to create user_saml_link entities
    pub const CREATE: &str = "user_saml_link:create";
    /// Permission to update user_saml_link entities
    pub const UPDATE: &str = "user_saml_link:update";
    /// Permission to delete user_saml_link entities
    pub const DELETE: &str = "user_saml_link:delete";
    /// Permission to restore deleted user_saml_link entities
    pub const RESTORE: &str = "user_saml_link:restore";
    /// Permission to view user_saml_link trash
    pub const TRASH: &str = "user_saml_link:trash";
    /// Permission to empty user_saml_link trash (permanent delete)
    pub const EMPTY_TRASH: &str = "user_saml_link:empty_trash";
    /// Permission to bulk create user_saml_link entities
    pub const BULK_CREATE: &str = "user_saml_link:bulk_create";
    /// Permission to upsert user_saml_link entities
    pub const UPSERT: &str = "user_saml_link:upsert";

    // State transition permissions
    /// Permission to execute deactivate transition
    pub const TRANSITION_DEACTIVATE: &str = "user_saml_link:transition:deactivate";
    /// Permission to execute reactivate transition
    pub const TRANSITION_REACTIVATE: &str = "user_saml_link:transition:reactivate";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_DEACTIVATE,
            TRANSITION_REACTIVATE,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// UserSAMLLink Specific Guards
// ==========================================================================

/// Guard that requires list permission for user_saml_link
pub async fn require_list_user_saml_link(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_saml_links::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for user_saml_link
pub async fn require_read_user_saml_link(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_saml_links::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for user_saml_link
pub async fn require_create_user_saml_link(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_saml_links::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for user_saml_link
pub async fn require_update_user_saml_link(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_saml_links::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for user_saml_link
pub async fn require_delete_user_saml_link(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_saml_links::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for user_saml_link
pub async fn require_restore_user_saml_link(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_saml_links::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for user_saml_link
pub async fn require_trash_user_saml_link(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_saml_links::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for user_saml_link
pub async fn require_empty_trash_user_saml_link(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, user_saml_links::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

