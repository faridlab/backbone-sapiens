//! Auth configuration for SessionLimit handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for SessionLimit operations
pub mod session_limits {
    /// Permission to list session_limit entities
    pub const LIST: &str = "session_limit:list";
    /// Permission to read session_limit entity details
    pub const READ: &str = "session_limit:read";
    /// Permission to create session_limit entities
    pub const CREATE: &str = "session_limit:create";
    /// Permission to update session_limit entities
    pub const UPDATE: &str = "session_limit:update";
    /// Permission to delete session_limit entities
    pub const DELETE: &str = "session_limit:delete";
    /// Permission to restore deleted session_limit entities
    pub const RESTORE: &str = "session_limit:restore";
    /// Permission to view session_limit trash
    pub const TRASH: &str = "session_limit:trash";
    /// Permission to empty session_limit trash (permanent delete)
    pub const EMPTY_TRASH: &str = "session_limit:empty_trash";
    /// Permission to bulk create session_limit entities
    pub const BULK_CREATE: &str = "session_limit:bulk_create";
    /// Permission to upsert session_limit entities
    pub const UPSERT: &str = "session_limit:upsert";

    // State transition permissions
    /// Permission to execute enable transition
    pub const TRANSITION_ENABLE: &str = "session_limit:transition:enable";
    /// Permission to execute disable transition
    pub const TRANSITION_DISABLE: &str = "session_limit:transition:disable";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_ENABLE,
            TRANSITION_DISABLE,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// SessionLimit Specific Guards
// ==========================================================================

/// Guard that requires list permission for session_limit
pub async fn require_list_session_limit(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, session_limits::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for session_limit
pub async fn require_read_session_limit(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, session_limits::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for session_limit
pub async fn require_create_session_limit(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, session_limits::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for session_limit
pub async fn require_update_session_limit(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, session_limits::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for session_limit
pub async fn require_delete_session_limit(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, session_limits::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for session_limit
pub async fn require_restore_session_limit(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, session_limits::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for session_limit
pub async fn require_trash_session_limit(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, session_limits::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for session_limit
pub async fn require_empty_trash_session_limit(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, session_limits::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

