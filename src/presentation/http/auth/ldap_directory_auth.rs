//! Auth configuration for LDAPDirectory handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for LDAPDirectory operations
pub mod ldap_directorys {
    /// Permission to list ldap_directory entities
    pub const LIST: &str = "ldap_directory:list";
    /// Permission to read ldap_directory entity details
    pub const READ: &str = "ldap_directory:read";
    /// Permission to create ldap_directory entities
    pub const CREATE: &str = "ldap_directory:create";
    /// Permission to update ldap_directory entities
    pub const UPDATE: &str = "ldap_directory:update";
    /// Permission to delete ldap_directory entities
    pub const DELETE: &str = "ldap_directory:delete";
    /// Permission to restore deleted ldap_directory entities
    pub const RESTORE: &str = "ldap_directory:restore";
    /// Permission to view ldap_directory trash
    pub const TRASH: &str = "ldap_directory:trash";
    /// Permission to empty ldap_directory trash (permanent delete)
    pub const EMPTY_TRASH: &str = "ldap_directory:empty_trash";
    /// Permission to bulk create ldap_directory entities
    pub const BULK_CREATE: &str = "ldap_directory:bulk_create";
    /// Permission to upsert ldap_directory entities
    pub const UPSERT: &str = "ldap_directory:upsert";

    // State transition permissions
    /// Permission to execute activate transition
    pub const TRANSITION_ACTIVATE: &str = "ldap_directory:transition:activate";
    /// Permission to execute deactivate transition
    pub const TRANSITION_DEACTIVATE: &str = "ldap_directory:transition:deactivate";
    /// Permission to execute report_error transition
    pub const TRANSITION_REPORT_ERROR: &str = "ldap_directory:transition:report_error";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_ACTIVATE,
            TRANSITION_DEACTIVATE,
            TRANSITION_REPORT_ERROR,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// LDAPDirectory Specific Guards
// ==========================================================================

/// Guard that requires list permission for ldap_directory
pub async fn require_list_ldap_directory(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, ldap_directorys::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for ldap_directory
pub async fn require_read_ldap_directory(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, ldap_directorys::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for ldap_directory
pub async fn require_create_ldap_directory(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, ldap_directorys::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for ldap_directory
pub async fn require_update_ldap_directory(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, ldap_directorys::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for ldap_directory
pub async fn require_delete_ldap_directory(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, ldap_directorys::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for ldap_directory
pub async fn require_restore_ldap_directory(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, ldap_directorys::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for ldap_directory
pub async fn require_trash_ldap_directory(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, ldap_directorys::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for ldap_directory
pub async fn require_empty_trash_ldap_directory(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, ldap_directorys::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

