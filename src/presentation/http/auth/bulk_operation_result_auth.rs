//! Auth configuration for BulkOperationResult handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for BulkOperationResult operations
pub mod bulk_operation_results {
    /// Permission to list bulk_operation_result entities
    pub const LIST: &str = "bulk_operation_result:list";
    /// Permission to read bulk_operation_result entity details
    pub const READ: &str = "bulk_operation_result:read";
    /// Permission to create bulk_operation_result entities
    pub const CREATE: &str = "bulk_operation_result:create";
    /// Permission to update bulk_operation_result entities
    pub const UPDATE: &str = "bulk_operation_result:update";
    /// Permission to delete bulk_operation_result entities
    pub const DELETE: &str = "bulk_operation_result:delete";
    /// Permission to restore deleted bulk_operation_result entities
    pub const RESTORE: &str = "bulk_operation_result:restore";
    /// Permission to view bulk_operation_result trash
    pub const TRASH: &str = "bulk_operation_result:trash";
    /// Permission to empty bulk_operation_result trash (permanent delete)
    pub const EMPTY_TRASH: &str = "bulk_operation_result:empty_trash";
    /// Permission to bulk create bulk_operation_result entities
    pub const BULK_CREATE: &str = "bulk_operation_result:bulk_create";
    /// Permission to upsert bulk_operation_result entities
    pub const UPSERT: &str = "bulk_operation_result:upsert";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// BulkOperationResult Specific Guards
// ==========================================================================

/// Guard that requires list permission for bulk_operation_result
pub async fn require_list_bulk_operation_result(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, bulk_operation_results::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for bulk_operation_result
pub async fn require_read_bulk_operation_result(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, bulk_operation_results::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for bulk_operation_result
pub async fn require_create_bulk_operation_result(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, bulk_operation_results::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for bulk_operation_result
pub async fn require_update_bulk_operation_result(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, bulk_operation_results::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for bulk_operation_result
pub async fn require_delete_bulk_operation_result(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, bulk_operation_results::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for bulk_operation_result
pub async fn require_restore_bulk_operation_result(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, bulk_operation_results::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for bulk_operation_result
pub async fn require_trash_bulk_operation_result(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, bulk_operation_results::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for bulk_operation_result
pub async fn require_empty_trash_bulk_operation_result(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, bulk_operation_results::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

