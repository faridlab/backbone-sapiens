//! Auth configuration for PasswordHistorySettings handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for PasswordHistorySettings operations
pub mod password_history_settingss {
    /// Permission to list password_history_settings entities
    pub const LIST: &str = "password_history_settings:list";
    /// Permission to read password_history_settings entity details
    pub const READ: &str = "password_history_settings:read";
    /// Permission to create password_history_settings entities
    pub const CREATE: &str = "password_history_settings:create";
    /// Permission to update password_history_settings entities
    pub const UPDATE: &str = "password_history_settings:update";
    /// Permission to delete password_history_settings entities
    pub const DELETE: &str = "password_history_settings:delete";
    /// Permission to restore deleted password_history_settings entities
    pub const RESTORE: &str = "password_history_settings:restore";
    /// Permission to view password_history_settings trash
    pub const TRASH: &str = "password_history_settings:trash";
    /// Permission to empty password_history_settings trash (permanent delete)
    pub const EMPTY_TRASH: &str = "password_history_settings:empty_trash";
    /// Permission to bulk create password_history_settings entities
    pub const BULK_CREATE: &str = "password_history_settings:bulk_create";
    /// Permission to upsert password_history_settings entities
    pub const UPSERT: &str = "password_history_settings:upsert";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// PasswordHistorySettings Specific Guards
// ==========================================================================

/// Guard that requires list permission for password_history_settings
pub async fn require_list_password_history_settings(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, password_history_settingss::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for password_history_settings
pub async fn require_read_password_history_settings(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, password_history_settingss::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for password_history_settings
pub async fn require_create_password_history_settings(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, password_history_settingss::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for password_history_settings
pub async fn require_update_password_history_settings(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, password_history_settingss::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for password_history_settings
pub async fn require_delete_password_history_settings(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, password_history_settingss::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for password_history_settings
pub async fn require_restore_password_history_settings(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, password_history_settingss::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for password_history_settings
pub async fn require_trash_password_history_settings(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, password_history_settingss::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for password_history_settings
pub async fn require_empty_trash_password_history_settings(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, password_history_settingss::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

