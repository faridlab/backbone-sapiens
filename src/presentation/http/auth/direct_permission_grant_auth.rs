//! Auth configuration for DirectPermissionGrant handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for DirectPermissionGrant operations
pub mod direct_permission_grants {
    /// Permission to list direct_permission_grant entities
    pub const LIST: &str = "direct_permission_grant:list";
    /// Permission to read direct_permission_grant entity details
    pub const READ: &str = "direct_permission_grant:read";
    /// Permission to create direct_permission_grant entities
    pub const CREATE: &str = "direct_permission_grant:create";
    /// Permission to update direct_permission_grant entities
    pub const UPDATE: &str = "direct_permission_grant:update";
    /// Permission to delete direct_permission_grant entities
    pub const DELETE: &str = "direct_permission_grant:delete";
    /// Permission to restore deleted direct_permission_grant entities
    pub const RESTORE: &str = "direct_permission_grant:restore";
    /// Permission to view direct_permission_grant trash
    pub const TRASH: &str = "direct_permission_grant:trash";
    /// Permission to empty direct_permission_grant trash (permanent delete)
    pub const EMPTY_TRASH: &str = "direct_permission_grant:empty_trash";
    /// Permission to bulk create direct_permission_grant entities
    pub const BULK_CREATE: &str = "direct_permission_grant:bulk_create";
    /// Permission to upsert direct_permission_grant entities
    pub const UPSERT: &str = "direct_permission_grant:upsert";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// DirectPermissionGrant Specific Guards
// ==========================================================================

/// Guard that requires list permission for direct_permission_grant
pub async fn require_list_direct_permission_grant(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, direct_permission_grants::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for direct_permission_grant
pub async fn require_read_direct_permission_grant(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, direct_permission_grants::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for direct_permission_grant
pub async fn require_create_direct_permission_grant(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, direct_permission_grants::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for direct_permission_grant
pub async fn require_update_direct_permission_grant(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, direct_permission_grants::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for direct_permission_grant
pub async fn require_delete_direct_permission_grant(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, direct_permission_grants::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for direct_permission_grant
pub async fn require_restore_direct_permission_grant(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, direct_permission_grants::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for direct_permission_grant
pub async fn require_trash_direct_permission_grant(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, direct_permission_grants::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for direct_permission_grant
pub async fn require_empty_trash_direct_permission_grant(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, direct_permission_grants::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

