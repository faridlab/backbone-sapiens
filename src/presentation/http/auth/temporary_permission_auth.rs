//! Auth configuration for TemporaryPermission handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for TemporaryPermission operations
pub mod temporary_permissions {
    /// Permission to list temporary_permission entities
    pub const LIST: &str = "temporary_permission:list";
    /// Permission to read temporary_permission entity details
    pub const READ: &str = "temporary_permission:read";
    /// Permission to create temporary_permission entities
    pub const CREATE: &str = "temporary_permission:create";
    /// Permission to update temporary_permission entities
    pub const UPDATE: &str = "temporary_permission:update";
    /// Permission to delete temporary_permission entities
    pub const DELETE: &str = "temporary_permission:delete";
    /// Permission to restore deleted temporary_permission entities
    pub const RESTORE: &str = "temporary_permission:restore";
    /// Permission to view temporary_permission trash
    pub const TRASH: &str = "temporary_permission:trash";
    /// Permission to empty temporary_permission trash (permanent delete)
    pub const EMPTY_TRASH: &str = "temporary_permission:empty_trash";
    /// Permission to bulk create temporary_permission entities
    pub const BULK_CREATE: &str = "temporary_permission:bulk_create";
    /// Permission to upsert temporary_permission entities
    pub const UPSERT: &str = "temporary_permission:upsert";

    // State transition permissions
    /// Permission to execute activate transition
    pub const TRANSITION_ACTIVATE: &str = "temporary_permission:transition:activate";
    /// Permission to execute revoke transition
    pub const TRANSITION_REVOKE: &str = "temporary_permission:transition:revoke";
    /// Permission to execute auto_expire transition
    pub const TRANSITION_AUTO_EXPIRE: &str = "temporary_permission:transition:auto_expire";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_ACTIVATE,
            TRANSITION_REVOKE,
            TRANSITION_AUTO_EXPIRE,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// TemporaryPermission Specific Guards
// ==========================================================================

/// Guard that requires list permission for temporary_permission
pub async fn require_list_temporary_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, temporary_permissions::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for temporary_permission
pub async fn require_read_temporary_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, temporary_permissions::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for temporary_permission
pub async fn require_create_temporary_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, temporary_permissions::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for temporary_permission
pub async fn require_update_temporary_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, temporary_permissions::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for temporary_permission
pub async fn require_delete_temporary_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, temporary_permissions::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for temporary_permission
pub async fn require_restore_temporary_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, temporary_permissions::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for temporary_permission
pub async fn require_trash_temporary_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, temporary_permissions::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for temporary_permission
pub async fn require_empty_trash_temporary_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, temporary_permissions::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

