//! Auth configuration for DataExport handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for DataExport operations
pub mod data_exports {
    /// Permission to list data_export entities
    pub const LIST: &str = "data_export:list";
    /// Permission to read data_export entity details
    pub const READ: &str = "data_export:read";
    /// Permission to create data_export entities
    pub const CREATE: &str = "data_export:create";
    /// Permission to update data_export entities
    pub const UPDATE: &str = "data_export:update";
    /// Permission to delete data_export entities
    pub const DELETE: &str = "data_export:delete";
    /// Permission to restore deleted data_export entities
    pub const RESTORE: &str = "data_export:restore";
    /// Permission to view data_export trash
    pub const TRASH: &str = "data_export:trash";
    /// Permission to empty data_export trash (permanent delete)
    pub const EMPTY_TRASH: &str = "data_export:empty_trash";
    /// Permission to bulk create data_export entities
    pub const BULK_CREATE: &str = "data_export:bulk_create";
    /// Permission to upsert data_export entities
    pub const UPSERT: &str = "data_export:upsert";

    // State transition permissions
    /// Permission to execute start_processing transition
    pub const TRANSITION_START_PROCESSING: &str = "data_export:transition:start_processing";
    /// Permission to execute complete transition
    pub const TRANSITION_COMPLETE: &str = "data_export:transition:complete";
    /// Permission to execute fail transition
    pub const TRANSITION_FAIL: &str = "data_export:transition:fail";
    /// Permission to execute expire transition
    pub const TRANSITION_EXPIRE: &str = "data_export:transition:expire";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_START_PROCESSING,
            TRANSITION_COMPLETE,
            TRANSITION_FAIL,
            TRANSITION_EXPIRE,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// DataExport Specific Guards
// ==========================================================================

/// Guard that requires list permission for data_export
pub async fn require_list_data_export(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, data_exports::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for data_export
pub async fn require_read_data_export(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, data_exports::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for data_export
pub async fn require_create_data_export(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, data_exports::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for data_export
pub async fn require_update_data_export(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, data_exports::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for data_export
pub async fn require_delete_data_export(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, data_exports::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for data_export
pub async fn require_restore_data_export(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, data_exports::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for data_export
pub async fn require_trash_data_export(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, data_exports::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for data_export
pub async fn require_empty_trash_data_export(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, data_exports::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

