//! Auth configuration for OAuthProvider handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for OAuthProvider operations
pub mod o_auth_providers {
    /// Permission to list o_auth_provider entities
    pub const LIST: &str = "o_auth_provider:list";
    /// Permission to read o_auth_provider entity details
    pub const READ: &str = "o_auth_provider:read";
    /// Permission to create o_auth_provider entities
    pub const CREATE: &str = "o_auth_provider:create";
    /// Permission to update o_auth_provider entities
    pub const UPDATE: &str = "o_auth_provider:update";
    /// Permission to delete o_auth_provider entities
    pub const DELETE: &str = "o_auth_provider:delete";
    /// Permission to restore deleted o_auth_provider entities
    pub const RESTORE: &str = "o_auth_provider:restore";
    /// Permission to view o_auth_provider trash
    pub const TRASH: &str = "o_auth_provider:trash";
    /// Permission to empty o_auth_provider trash (permanent delete)
    pub const EMPTY_TRASH: &str = "o_auth_provider:empty_trash";
    /// Permission to bulk create o_auth_provider entities
    pub const BULK_CREATE: &str = "o_auth_provider:bulk_create";
    /// Permission to upsert o_auth_provider entities
    pub const UPSERT: &str = "o_auth_provider:upsert";

    // State transition permissions
    /// Permission to execute activate transition
    pub const TRANSITION_ACTIVATE: &str = "o_auth_provider:transition:activate";
    /// Permission to execute deactivate transition
    pub const TRANSITION_DEACTIVATE: &str = "o_auth_provider:transition:deactivate";
    /// Permission to execute maintenance transition
    pub const TRANSITION_MAINTENANCE: &str = "o_auth_provider:transition:maintenance";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_ACTIVATE,
            TRANSITION_DEACTIVATE,
            TRANSITION_MAINTENANCE,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// OAuthProvider Specific Guards
// ==========================================================================

/// Guard that requires list permission for o_auth_provider
pub async fn require_list_o_auth_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, o_auth_providers::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for o_auth_provider
pub async fn require_read_o_auth_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, o_auth_providers::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for o_auth_provider
pub async fn require_create_o_auth_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, o_auth_providers::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for o_auth_provider
pub async fn require_update_o_auth_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, o_auth_providers::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for o_auth_provider
pub async fn require_delete_o_auth_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, o_auth_providers::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for o_auth_provider
pub async fn require_restore_o_auth_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, o_auth_providers::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for o_auth_provider
pub async fn require_trash_o_auth_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, o_auth_providers::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for o_auth_provider
pub async fn require_empty_trash_o_auth_provider(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, o_auth_providers::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

