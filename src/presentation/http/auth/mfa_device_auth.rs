//! Auth configuration for MFADevice handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for MFADevice operations
pub mod mfa_devices {
    /// Permission to list mfa_device entities
    pub const LIST: &str = "mfa_device:list";
    /// Permission to read mfa_device entity details
    pub const READ: &str = "mfa_device:read";
    /// Permission to create mfa_device entities
    pub const CREATE: &str = "mfa_device:create";
    /// Permission to update mfa_device entities
    pub const UPDATE: &str = "mfa_device:update";
    /// Permission to delete mfa_device entities
    pub const DELETE: &str = "mfa_device:delete";
    /// Permission to restore deleted mfa_device entities
    pub const RESTORE: &str = "mfa_device:restore";
    /// Permission to view mfa_device trash
    pub const TRASH: &str = "mfa_device:trash";
    /// Permission to empty mfa_device trash (permanent delete)
    pub const EMPTY_TRASH: &str = "mfa_device:empty_trash";
    /// Permission to bulk create mfa_device entities
    pub const BULK_CREATE: &str = "mfa_device:bulk_create";
    /// Permission to upsert mfa_device entities
    pub const UPSERT: &str = "mfa_device:upsert";

    // State transition permissions
    /// Permission to execute verify transition
    pub const TRANSITION_VERIFY: &str = "mfa_device:transition:verify";
    /// Permission to execute disable transition
    pub const TRANSITION_DISABLE: &str = "mfa_device:transition:disable";
    /// Permission to execute cancel transition
    pub const TRANSITION_CANCEL: &str = "mfa_device:transition:cancel";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_VERIFY,
            TRANSITION_DISABLE,
            TRANSITION_CANCEL,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// MFADevice Specific Guards
// ==========================================================================

/// Guard that requires list permission for mfa_device
pub async fn require_list_mfa_device(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_devices::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for mfa_device
pub async fn require_read_mfa_device(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_devices::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for mfa_device
pub async fn require_create_mfa_device(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_devices::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for mfa_device
pub async fn require_update_mfa_device(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_devices::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for mfa_device
pub async fn require_delete_mfa_device(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_devices::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for mfa_device
pub async fn require_restore_mfa_device(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_devices::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for mfa_device
pub async fn require_trash_mfa_device(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_devices::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for mfa_device
pub async fn require_empty_trash_mfa_device(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, mfa_devices::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

