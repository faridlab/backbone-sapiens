//! Auth configuration for ResourcePermission handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides:
//! - Permission definitions for CRUD operations
//! - Role-based guards
//! - Auth middleware configuration

use std::sync::Arc;

use crate::application::middleware::auth::AuthContext;
use axum::{
    extract::{Request, State},
    http::StatusCode,
    middleware::Next,
    response::Response,
};

// ==========================================================================
// Permission Constants
// ==========================================================================

/// Permission identifiers for ResourcePermission operations
pub mod resource_permissions {
    /// Permission to list resource_permission entities
    pub const LIST: &str = "resource_permission:list";
    /// Permission to read resource_permission entity details
    pub const READ: &str = "resource_permission:read";
    /// Permission to create resource_permission entities
    pub const CREATE: &str = "resource_permission:create";
    /// Permission to update resource_permission entities
    pub const UPDATE: &str = "resource_permission:update";
    /// Permission to delete resource_permission entities
    pub const DELETE: &str = "resource_permission:delete";
    /// Permission to restore deleted resource_permission entities
    pub const RESTORE: &str = "resource_permission:restore";
    /// Permission to view resource_permission trash
    pub const TRASH: &str = "resource_permission:trash";
    /// Permission to empty resource_permission trash (permanent delete)
    pub const EMPTY_TRASH: &str = "resource_permission:empty_trash";
    /// Permission to bulk create resource_permission entities
    pub const BULK_CREATE: &str = "resource_permission:bulk_create";
    /// Permission to upsert resource_permission entities
    pub const UPSERT: &str = "resource_permission:upsert";

    // State transition permissions
    /// Permission to execute auto_expire transition
    pub const TRANSITION_AUTO_EXPIRE: &str = "resource_permission:transition:auto_expire";
    /// Permission to execute revoke transition
    pub const TRANSITION_REVOKE: &str = "resource_permission:transition:revoke";

    /// Get all permissions for this entity
    pub fn all() -> Vec<&'static str> {
        vec![
            LIST,
            READ,
            CREATE,
            UPDATE,
            DELETE,
            RESTORE,
            TRASH,
            EMPTY_TRASH,
            BULK_CREATE,
            UPSERT,
            TRANSITION_AUTO_EXPIRE,
            TRANSITION_REVOKE,
        ]
    }
}

// ==========================================================================
// Permission Checking
// ==========================================================================

/// Check if auth context has a specific permission
pub fn has_permission(auth: &AuthContext, permission: &str) -> bool {
    auth.has_permission(permission)
}

/// Check if auth context has a specific role
pub fn has_role(auth: &AuthContext, role: &str) -> bool {
    auth.has_role(role)
}

/// Check if auth context has all of the required roles
pub fn has_all_roles(auth: &AuthContext, roles: &[&str]) -> bool {
    auth.has_all_roles(roles)
}

/// Check if auth context has any of the required permissions
pub fn has_any_permission(auth: &AuthContext, permissions: &[&str]) -> bool {
    permissions.iter().any(|p| auth.has_permission(p))
}

// ==========================================================================
// Permission Guards (Axum Middleware)
// ==========================================================================

/// Note: Axum middleware should be implemented as async fn, not impl Fn
/// This avoids E0562: `impl Trait` is not allowed in the return type of `Fn` trait bounds

// ==========================================================================
// ResourcePermission Specific Guards
// ==========================================================================

/// Guard that requires list permission for resource_permission
pub async fn require_list_resource_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, resource_permissions::LIST) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires read permission for resource_permission
pub async fn require_read_resource_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, resource_permissions::READ) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires create permission for resource_permission
pub async fn require_create_resource_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, resource_permissions::CREATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires update permission for resource_permission
pub async fn require_update_resource_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, resource_permissions::UPDATE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires delete permission for resource_permission
pub async fn require_delete_resource_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, resource_permissions::DELETE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires restore permission for resource_permission
pub async fn require_restore_resource_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, resource_permissions::RESTORE) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires view trash permission for resource_permission
pub async fn require_trash_resource_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, resource_permissions::TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

/// Guard that requires empty trash permission for resource_permission
pub async fn require_empty_trash_resource_permission(
    auth: axum::extract::Extension<AuthContext>,
    request: Request,
    next: Next,
) -> Result<Response, StatusCode> {
    if has_permission(&auth, resource_permissions::EMPTY_TRASH) {
        Ok(next.run(request).await)
    } else {
        Err(StatusCode::FORBIDDEN)
    }
}

