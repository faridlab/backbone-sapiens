//! Sapiens Module Routes
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This module provides route composition with dual pattern support:
//! - Stateless routes: `create_stateless_routes()` -> `Router<()>`
//! - Stateful routes: `create_stateful_routes()` -> `Router<AppState>`

use axum::Router;
use std::sync::Arc;

// Import handlers
use crate::presentation::http::{
    create_analytics_event_routes,
    create_analytics_metric_routes,
    create_analytics_report_routes,
    create_anonymization_record_routes,
    create_audit_log_routes,
    create_backup_code_routes,
    create_bulk_operation_routes,
    create_bulk_operation_result_routes,
    create_data_export_routes,
    create_device_trust_routes,
    create_direct_permission_grant_routes,
    create_effective_permission_cache_routes,
    create_email_verification_token_routes,
    create_impersonation_session_routes,
    create_l_d_a_p_directory_routes,
    create_m_f_a_backup_code_routes,
    create_m_f_a_device_routes,
    create_m_f_a_session_routes,
    create_notification_routes,
    create_notification_template_routes,
    create_notification_log_routes,
    create_notification_preference_routes,
    create_o_auth_provider_routes,
    create_user_o_auth_link_routes,
    create_organization_permission_routes,
    create_organization_role_routes,
    create_organization_user_routes,
    create_password_creation_context_routes,
    create_password_expiration_settings_routes,
    create_password_history_routes,
    create_password_history_settings_routes,
    create_password_policy_routes,
    create_password_requirements_routes,
    create_password_reset_routes,
    create_password_reset_security_routes,
    create_password_reset_token_routes,
    create_password_reset_verification_details_routes,
    create_permission_routes,
    create_permission_conflict_routes,
    create_resource_permission_routes,
    create_role_routes,
    create_role_assignment_routes,
    create_role_permission_routes,
    create_s_a_m_l_provider_routes,
    create_security_event_routes,
    create_session_routes,
    create_session_limit_routes,
    create_system_settings_routes,
    create_temporary_permission_routes,
    create_user_routes,
    create_profile_routes,
    create_user_permission_routes,
    create_user_role_routes,
    create_user_s_a_m_l_link_routes,
    create_user_settings_routes,
    create_workflow_routes,
    create_workflow_step_routes,
    create_workflow_definition_routes,
    create_workflow_action_routes,
    create_workflow_execution_routes,
    create_workflow_action_execution_routes
};

// Import AppState for stateful routes
use crate::handlers::AppState;

// ============================================================
// PATTERN 1: STATELESS ROUTES
// ============================================================

/// Create stateless routes for the Sapiens module.
///
/// These routes don't require shared state and are simpler to use.
/// Use this when you only need standard CRUD operations.
///
/// # Example
///
/// ```ignore
/// let module = SapiensModule::builder().with_database(pool).build()?;
/// let routes = create_stateless_routes(&module);
/// let app = Router::new().merge(routes);
/// ```
pub fn create_stateless_routes(module: &crate::SapiensModule) -> Router<()> {
    Router::new()
        .merge(create_analytics_event_routes(module.analytics_event_service.clone()))
        .merge(create_analytics_metric_routes(module.analytics_metric_service.clone()))
        .merge(create_analytics_report_routes(module.analytics_report_service.clone()))
        .merge(create_anonymization_record_routes(module.anonymization_record_service.clone()))
        .merge(create_audit_log_routes(module.audit_log_service.clone()))
        .merge(create_backup_code_routes(module.backup_code_service.clone()))
        .merge(create_bulk_operation_routes(module.bulk_operation_service.clone()))
        .merge(create_bulk_operation_result_routes(module.bulk_operation_result_service.clone()))
        .merge(create_data_export_routes(module.data_export_service.clone()))
        .merge(create_device_trust_routes(module.device_trust_service.clone()))
        .merge(create_direct_permission_grant_routes(module.direct_permission_grant_service.clone()))
        .merge(create_effective_permission_cache_routes(module.effective_permission_cache_service.clone()))
        .merge(create_email_verification_token_routes(module.email_verification_token_service.clone()))
        .merge(create_impersonation_session_routes(module.impersonation_session_service.clone()))
        .merge(create_l_d_a_p_directory_routes(module.l_d_a_p_directory_service.clone()))
        .merge(create_m_f_a_backup_code_routes(module.m_f_a_backup_code_service.clone()))
        .merge(create_m_f_a_device_routes(module.m_f_a_device_service.clone()))
        .merge(create_m_f_a_session_routes(module.m_f_a_session_service.clone()))
        .merge(create_notification_routes(module.notification_service.clone()))
        .merge(create_notification_template_routes(module.notification_template_service.clone()))
        .merge(create_notification_log_routes(module.notification_log_service.clone()))
        .merge(create_notification_preference_routes(module.notification_preference_service.clone()))
        .merge(create_o_auth_provider_routes(module.o_auth_provider_service.clone()))
        .merge(create_user_o_auth_link_routes(module.user_o_auth_link_service.clone()))
        .merge(create_organization_permission_routes(module.organization_permission_service.clone()))
        .merge(create_organization_role_routes(module.organization_role_service.clone()))
        .merge(create_organization_user_routes(module.organization_user_service.clone()))
        .merge(create_password_creation_context_routes(module.password_creation_context_service.clone()))
        .merge(create_password_expiration_settings_routes(module.password_expiration_settings_service.clone()))
        .merge(create_password_history_routes(module.password_history_service.clone()))
        .merge(create_password_history_settings_routes(module.password_history_settings_service.clone()))
        .merge(create_password_policy_routes(module.password_policy_service.clone()))
        .merge(create_password_requirements_routes(module.password_requirements_service.clone()))
        .merge(create_password_reset_routes(module.password_reset_service.clone()))
        .merge(create_password_reset_security_routes(module.password_reset_security_service.clone()))
        .merge(create_password_reset_token_routes(module.password_reset_token_service.clone()))
        .merge(create_password_reset_verification_details_routes(module.password_reset_verification_details_service.clone()))
        .merge(create_permission_routes(module.permission_service.clone()))
        .merge(create_permission_conflict_routes(module.permission_conflict_service.clone()))
        .merge(create_resource_permission_routes(module.resource_permission_service.clone()))
        .merge(create_role_routes(module.role_service.clone()))
        .merge(create_role_assignment_routes(module.role_assignment_service.clone()))
        .merge(create_role_permission_routes(module.role_permission_service.clone()))
        .merge(create_s_a_m_l_provider_routes(module.s_a_m_l_provider_service.clone()))
        .merge(create_security_event_routes(module.security_event_service.clone()))
        .merge(create_session_routes(module.session_service.clone()))
        .merge(create_session_limit_routes(module.session_limit_service.clone()))
        .merge(create_system_settings_routes(module.system_settings_service.clone()))
        .merge(create_temporary_permission_routes(module.temporary_permission_service.clone()))
        .merge(create_user_routes(module.user_service.clone()))
        .merge(create_profile_routes(module.profile_service.clone()))
        .merge(create_user_permission_routes(module.user_permission_service.clone()))
        .merge(create_user_role_routes(module.user_role_service.clone()))
        .merge(create_user_s_a_m_l_link_routes(module.user_s_a_m_l_link_service.clone()))
        .merge(create_user_settings_routes(module.user_settings_service.clone()))
        .merge(create_workflow_routes(module.workflow_service.clone()))
        .merge(create_workflow_step_routes(module.workflow_step_service.clone()))
        .merge(create_workflow_definition_routes(module.workflow_definition_service.clone()))
        .merge(create_workflow_action_routes(module.workflow_action_service.clone()))
        .merge(create_workflow_execution_routes(module.workflow_execution_service.clone()))
        .merge(create_workflow_action_execution_routes(module.workflow_action_execution_service.clone()))
}

/// Get all routes (stateless) for the Sapiens module.
///
/// This is a convenience function that wraps routes with API versioning.
pub fn get_routes(module: &crate::SapiensModule) -> Router<()> {
    Router::new()
        .nest("/api/v1", create_stateless_routes(module))
}

// ============================================================
// PATTERN 2: STATEFUL ROUTES (WITH APPSTATE)
// ============================================================

/// Create stateful routes for the Sapiens module.
///
/// These routes require AppState and support custom handlers
/// that need access to services via dependency injection.
///
/// # Example
///
/// ```ignore
/// let module = SapiensModule::builder().with_database(pool).build()?;
/// let state = AppState::from_module(&module);
/// let routes = create_stateful_routes();
/// let app = Router::new().merge(routes).with_state(state);
/// ```
pub fn create_stateful_routes() -> Router<AppState> {
    Router::new()
        // Add custom handlers here that need AppState
        // .route("/custom", get(custom_handler))
}

/// Create combined routes (stateless CRUD + stateful custom handlers).
///
/// This merges both stateless CRUD routes and stateful custom routes.
pub fn create_combined_routes(module: &crate::SapiensModule) -> Router<AppState> {
    // First create stateless routes
    let crud_routes: Router<()> = create_stateless_routes(module);

    // Convert to stateful by adding state requirement
    // Note: The CRUD routes don't actually use the state,
    // but this allows them to be merged with stateful routes.
    let crud_routes_stateful: Router<AppState> = crud_routes.with_state(()).into();

    // Merge with stateful routes
    Router::new()
        .merge(crud_routes_stateful)
        .merge(create_stateful_routes())
}

/// Get all routes (stateful) for the Sapiens module.
///
/// This is a convenience function that wraps routes with API versioning.
pub fn get_routes_with_state(module: &crate::SapiensModule) -> Router<AppState> {
    Router::new()
        .nest("/api/v1", create_combined_routes(module))
}

// ============================================================
// CUSTOM HANDLERS
// ============================================================

// <<< CUSTOM HANDLERS START >>>
// Add custom route handlers here
// Example:
//
// use axum::extract::State;
// use axum::response::IntoResponse;
//
// pub async fn custom_handler(
//     State(state): State<AppState>,
// ) -> impl IntoResponse {
//     // Use state.user_service, etc.
//     "Custom response"
// }
// <<< CUSTOM HANDLERS END >>>
