//! SAMLProvider seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial SAMLProvider data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SAMLPROVIDER SEEDER
// ============================================================================

/// Seeder for saml_provider
pub struct SeedSAMLProviderSeeder;

impl SeedSAMLProviderSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedSAMLProviderSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedSAMLProviderSeeder {
    fn name(&self) -> &'static str {
        "SeedSAMLProviderSeeder"
    }

    fn order(&self) -> i32 {
        39
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if saml_providers table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM saml_providers")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/saml_provider_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM saml_providers")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

