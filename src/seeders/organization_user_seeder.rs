//! OrganizationUser seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial OrganizationUser data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ORGANIZATIONUSER SEEDER
// ============================================================================

/// Seeder for organization_user
pub struct SeedOrganizationUserSeeder;

impl SeedOrganizationUserSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedOrganizationUserSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedOrganizationUserSeeder {
    fn name(&self) -> &'static str {
        "SeedOrganizationUserSeeder"
    }

    fn order(&self) -> i32 {
        60
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if organization_users table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM organization_users")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/organization_user_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM organization_users")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

