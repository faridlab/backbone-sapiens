//! DirectPermissionGrant seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial DirectPermissionGrant data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// DIRECTPERMISSIONGRANT SEEDER
// ============================================================================

/// Seeder for direct_permission_grant
pub struct SeedDirectPermissionGrantSeeder;

impl SeedDirectPermissionGrantSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedDirectPermissionGrantSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedDirectPermissionGrantSeeder {
    fn name(&self) -> &'static str {
        "SeedDirectPermissionGrantSeeder"
    }

    fn order(&self) -> i32 {
        10
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if direct_permission_grants table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM direct_permission_grants")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/direct_permission_grant_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM direct_permission_grants")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

