//! OrganizationPermission seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial OrganizationPermission data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ORGANIZATIONPERMISSION SEEDER
// ============================================================================

/// Seeder for organization_permission
pub struct SeedOrganizationPermissionSeeder;

impl SeedOrganizationPermissionSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedOrganizationPermissionSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedOrganizationPermissionSeeder {
    fn name(&self) -> &'static str {
        "SeedOrganizationPermissionSeeder"
    }

    fn order(&self) -> i32 {
        58
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if organization_permissions table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM organization_permissions")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/organization_permission_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM organization_permissions")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

