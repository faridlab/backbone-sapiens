//! DataExport seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial DataExport data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// DATAEXPORT SEEDER
// ============================================================================

/// Seeder for data_export
pub struct SeedDataExportSeeder;

impl SeedDataExportSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedDataExportSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedDataExportSeeder {
    fn name(&self) -> &'static str {
        "SeedDataExportSeeder"
    }

    fn order(&self) -> i32 {
        8
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if data_exports table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM data_exports")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/data_export_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM data_exports")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

