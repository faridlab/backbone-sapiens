//! SessionLimit seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial SessionLimit data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SESSIONLIMIT SEEDER
// ============================================================================

/// Seeder for session_limit
pub struct SeedSessionLimitSeeder;

impl SeedSessionLimitSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedSessionLimitSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedSessionLimitSeeder {
    fn name(&self) -> &'static str {
        "SeedSessionLimitSeeder"
    }

    fn order(&self) -> i32 {
        42
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if session_limits table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM session_limits")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/session_limit_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM session_limits")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

