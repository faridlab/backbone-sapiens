//! OAuthProvider seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial OAuthProvider data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// OAUTHPROVIDER SEEDER
// ============================================================================

/// Seeder for o_auth_provider
pub struct SeedOAuthProviderSeeder;

impl SeedOAuthProviderSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedOAuthProviderSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedOAuthProviderSeeder {
    fn name(&self) -> &'static str {
        "SeedOAuthProviderSeeder"
    }

    fn order(&self) -> i32 {
        22
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if oauth_providers table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM oauth_providers")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/o_auth_provider_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM oauth_providers")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

