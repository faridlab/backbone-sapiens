//! DeviceTrust seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial DeviceTrust data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// DEVICETRUST SEEDER
// ============================================================================

/// Seeder for device_trust
pub struct SeedDeviceTrustSeeder;

impl SeedDeviceTrustSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedDeviceTrustSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedDeviceTrustSeeder {
    fn name(&self) -> &'static str {
        "SeedDeviceTrustSeeder"
    }

    fn order(&self) -> i32 {
        9
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if device_trusts table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM device_trusts")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/device_trust_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM device_trusts")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

