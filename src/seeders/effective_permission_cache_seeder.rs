//! EffectivePermissionCache seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial EffectivePermissionCache data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// EFFECTIVEPERMISSIONCACHE SEEDER
// ============================================================================

/// Seeder for effective_permission_cache
pub struct SeedEffectivePermissionCacheSeeder;

impl SeedEffectivePermissionCacheSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedEffectivePermissionCacheSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedEffectivePermissionCacheSeeder {
    fn name(&self) -> &'static str {
        "SeedEffectivePermissionCacheSeeder"
    }

    fn order(&self) -> i32 {
        11
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if effective_permission_cache table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM effective_permission_cache")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/effective_permission_cache_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM effective_permission_cache")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

