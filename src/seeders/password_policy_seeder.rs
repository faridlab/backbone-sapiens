//! PasswordPolicy seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial PasswordPolicy data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PASSWORDPOLICY SEEDER
// ============================================================================

/// Seeder for password_policy
pub struct SeedPasswordPolicySeeder;

impl SeedPasswordPolicySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPasswordPolicySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPasswordPolicySeeder {
    fn name(&self) -> &'static str {
        "SeedPasswordPolicySeeder"
    }

    fn order(&self) -> i32 {
        61
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if password_policies table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM password_policies")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/password_policy_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM password_policies")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

