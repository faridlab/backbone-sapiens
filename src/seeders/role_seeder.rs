//! Role seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial Role data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ROLE SEEDER
// ============================================================================

/// Seeder for role
pub struct SeedRoleSeeder;

impl SeedRoleSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRoleSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRoleSeeder {
    fn name(&self) -> &'static str {
        "SeedRoleSeeder"
    }

    fn order(&self) -> i32 {
        36
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if roles table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM roles")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/role_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM roles")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

