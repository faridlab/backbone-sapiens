//! AnonymizationRecord seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial AnonymizationRecord data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ANONYMIZATIONRECORD SEEDER
// ============================================================================

/// Seeder for anonymization_record
pub struct SeedAnonymizationRecordSeeder;

impl SeedAnonymizationRecordSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedAnonymizationRecordSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedAnonymizationRecordSeeder {
    fn name(&self) -> &'static str {
        "SeedAnonymizationRecordSeeder"
    }

    fn order(&self) -> i32 {
        3
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if anonymization_records table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM anonymization_records")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/anonymization_record_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM anonymization_records")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

