//! BackupCode seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial BackupCode data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// BACKUPCODE SEEDER
// ============================================================================

/// Seeder for backup_code
pub struct SeedBackupCodeSeeder;

impl SeedBackupCodeSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedBackupCodeSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedBackupCodeSeeder {
    fn name(&self) -> &'static str {
        "SeedBackupCodeSeeder"
    }

    fn order(&self) -> i32 {
        5
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if backup_codes table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM backup_codes")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/backup_code_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM backup_codes")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

