//! MFABackupCode seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial MFABackupCode data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// MFABACKUPCODE SEEDER
// ============================================================================

/// Seeder for mfa_backup_code
pub struct SeedMFABackupCodeSeeder;

impl SeedMFABackupCodeSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedMFABackupCodeSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedMFABackupCodeSeeder {
    fn name(&self) -> &'static str {
        "SeedMFABackupCodeSeeder"
    }

    fn order(&self) -> i32 {
        15
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if mfa_backup_codes table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM mfa_backup_codes")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/mfa_backup_code_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM mfa_backup_codes")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

