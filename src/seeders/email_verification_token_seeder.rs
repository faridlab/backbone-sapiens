//! EmailVerificationToken seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial EmailVerificationToken data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// EMAILVERIFICATIONTOKEN SEEDER
// ============================================================================

/// Seeder for email_verification_token
pub struct SeedEmailVerificationTokenSeeder;

impl SeedEmailVerificationTokenSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedEmailVerificationTokenSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedEmailVerificationTokenSeeder {
    fn name(&self) -> &'static str {
        "SeedEmailVerificationTokenSeeder"
    }

    fn order(&self) -> i32 {
        12
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if email_verification_tokens table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM email_verification_tokens")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/email_verification_token_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM email_verification_tokens")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

