//! NotificationTemplate seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial NotificationTemplate data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// NOTIFICATIONTEMPLATE SEEDER
// ============================================================================

/// Seeder for notification_template
pub struct SeedNotificationTemplateSeeder;

impl SeedNotificationTemplateSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedNotificationTemplateSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedNotificationTemplateSeeder {
    fn name(&self) -> &'static str {
        "SeedNotificationTemplateSeeder"
    }

    fn order(&self) -> i32 {
        19
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if notification_templates table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM notification_templates")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/notification_template_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM notification_templates")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

