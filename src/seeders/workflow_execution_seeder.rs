//! WorkflowExecution seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial WorkflowExecution data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// WORKFLOWEXECUTION SEEDER
// ============================================================================

/// Seeder for workflow_execution
pub struct SeedWorkflowExecutionSeeder;

impl SeedWorkflowExecutionSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedWorkflowExecutionSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedWorkflowExecutionSeeder {
    fn name(&self) -> &'static str {
        "SeedWorkflowExecutionSeeder"
    }

    fn order(&self) -> i32 {
        55
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if workflow_executions table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM workflow_executions")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/workflow_execution_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM workflow_executions")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

