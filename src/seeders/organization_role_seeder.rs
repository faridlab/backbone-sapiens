//! OrganizationRole seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial OrganizationRole data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ORGANIZATIONROLE SEEDER
// ============================================================================

/// Seeder for organization_role
pub struct SeedOrganizationRoleSeeder;

impl SeedOrganizationRoleSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedOrganizationRoleSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedOrganizationRoleSeeder {
    fn name(&self) -> &'static str {
        "SeedOrganizationRoleSeeder"
    }

    fn order(&self) -> i32 {
        59
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if organization_roles table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM organization_roles")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/organization_role_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM organization_roles")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

