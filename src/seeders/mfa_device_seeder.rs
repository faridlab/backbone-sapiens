//! MFADevice seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial MFADevice data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// MFADEVICE SEEDER
// ============================================================================

/// Seeder for mfa_device
pub struct SeedMFADeviceSeeder;

impl SeedMFADeviceSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedMFADeviceSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedMFADeviceSeeder {
    fn name(&self) -> &'static str {
        "SeedMFADeviceSeeder"
    }

    fn order(&self) -> i32 {
        16
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if mfa_devices table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM mfa_devices")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/mfa_device_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM mfa_devices")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

