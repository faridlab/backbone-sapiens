//! NotificationLog seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial NotificationLog data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// NOTIFICATIONLOG SEEDER
// ============================================================================

/// Seeder for notification_log
pub struct SeedNotificationLogSeeder;

impl SeedNotificationLogSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedNotificationLogSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedNotificationLogSeeder {
    fn name(&self) -> &'static str {
        "SeedNotificationLogSeeder"
    }

    fn order(&self) -> i32 {
        20
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if notification_logs table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM notification_logs")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/notification_log_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM notification_logs")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

