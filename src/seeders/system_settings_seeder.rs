//! SystemSettings seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial SystemSettings data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// SYSTEMSETTINGS SEEDER
// ============================================================================

/// Seeder for system_settings
pub struct SeedSystemSettingsSeeder;

impl SeedSystemSettingsSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedSystemSettingsSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedSystemSettingsSeeder {
    fn name(&self) -> &'static str {
        "SeedSystemSettingsSeeder"
    }

    fn order(&self) -> i32 {
        43
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if system_settings table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM system_settings")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/system_settings_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM system_settings")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

