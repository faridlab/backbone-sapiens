//! LDAPDirectory seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial LDAPDirectory data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// LDAPDIRECTORY SEEDER
// ============================================================================

/// Seeder for ldap_directory
pub struct SeedLDAPDirectorySeeder;

impl SeedLDAPDirectorySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedLDAPDirectorySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedLDAPDirectorySeeder {
    fn name(&self) -> &'static str {
        "SeedLDAPDirectorySeeder"
    }

    fn order(&self) -> i32 {
        14
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if ldap_directories table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM ldap_directories")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/ldap_directory_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM ldap_directories")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

