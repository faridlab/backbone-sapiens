//! RoleAssignment seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial RoleAssignment data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// ROLEASSIGNMENT SEEDER
// ============================================================================

/// Seeder for role_assignment
pub struct SeedRoleAssignmentSeeder;

impl SeedRoleAssignmentSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedRoleAssignmentSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedRoleAssignmentSeeder {
    fn name(&self) -> &'static str {
        "SeedRoleAssignmentSeeder"
    }

    fn order(&self) -> i32 {
        37
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if role_assignments table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM role_assignments")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/role_assignment_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM role_assignments")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

