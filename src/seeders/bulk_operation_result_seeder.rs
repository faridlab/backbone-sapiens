//! BulkOperationResult seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial BulkOperationResult data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// BULKOPERATIONRESULT SEEDER
// ============================================================================

/// Seeder for bulk_operation_result
pub struct SeedBulkOperationResultSeeder;

impl SeedBulkOperationResultSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedBulkOperationResultSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedBulkOperationResultSeeder {
    fn name(&self) -> &'static str {
        "SeedBulkOperationResultSeeder"
    }

    fn order(&self) -> i32 {
        7
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if bulk_operation_results table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM bulk_operation_results")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/bulk_operation_result_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM bulk_operation_results")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

