//! Permission seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial Permission data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PERMISSION SEEDER
// ============================================================================

/// Seeder for permission
pub struct SeedPermissionSeeder;

impl SeedPermissionSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPermissionSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPermissionSeeder {
    fn name(&self) -> &'static str {
        "SeedPermissionSeeder"
    }

    fn order(&self) -> i32 {
        33
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if permissions table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM permissions")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/permission_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM permissions")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

