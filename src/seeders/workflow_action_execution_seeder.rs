//! WorkflowActionExecution seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial WorkflowActionExecution data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// WORKFLOWACTIONEXECUTION SEEDER
// ============================================================================

/// Seeder for workflow_action_execution
pub struct SeedWorkflowActionExecutionSeeder;

impl SeedWorkflowActionExecutionSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedWorkflowActionExecutionSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedWorkflowActionExecutionSeeder {
    fn name(&self) -> &'static str {
        "SeedWorkflowActionExecutionSeeder"
    }

    fn order(&self) -> i32 {
        56
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if workflow_action_executions table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM workflow_action_executions")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/workflow_action_execution_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM workflow_action_executions")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

