//! PermissionConflict seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial PermissionConflict data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PERMISSIONCONFLICT SEEDER
// ============================================================================

/// Seeder for permission_conflict
pub struct SeedPermissionConflictSeeder;

impl SeedPermissionConflictSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPermissionConflictSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPermissionConflictSeeder {
    fn name(&self) -> &'static str {
        "SeedPermissionConflictSeeder"
    }

    fn order(&self) -> i32 {
        34
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if permission_conflicts table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM permission_conflicts")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/permission_conflict_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM permission_conflicts")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

