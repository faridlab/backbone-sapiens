//! WorkflowDefinition seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial WorkflowDefinition data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// WORKFLOWDEFINITION SEEDER
// ============================================================================

/// Seeder for workflow_definition
pub struct SeedWorkflowDefinitionSeeder;

impl SeedWorkflowDefinitionSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedWorkflowDefinitionSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedWorkflowDefinitionSeeder {
    fn name(&self) -> &'static str {
        "SeedWorkflowDefinitionSeeder"
    }

    fn order(&self) -> i32 {
        53
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if workflow_definitions table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM workflow_definitions")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/workflow_definition_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM workflow_definitions")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

