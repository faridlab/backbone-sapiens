//! PasswordReset seeder
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Seeds initial PasswordReset data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PASSWORDRESET SEEDER
// ============================================================================

/// Seeder for password_reset
pub struct SeedPasswordResetSeeder;

impl SeedPasswordResetSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPasswordResetSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPasswordResetSeeder {
    fn name(&self) -> &'static str {
        "SeedPasswordResetSeeder"
    }

    fn order(&self) -> i32 {
        29
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if password_resets table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM password_resets")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/password_reset_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM password_resets")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

