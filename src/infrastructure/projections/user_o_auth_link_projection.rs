//! UserOAuthLink Projection (Read Model)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This is a CQRS projection - an optimized read model for queries.
//! It is updated asynchronously from domain events.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

// ============================================================================
// PROJECTION (READ MODEL)
// ============================================================================

/// UserOAuthLink read model optimized for queries
///
/// This projection is denormalized for fast reads.
/// It is updated when domain events are published.
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct UserOAuthLinkProjection {
    pub id: Uuid,
    pub user_id: Uuid,
    pub oauth_provider_id: Uuid,
    pub provider_user_id: String,
    pub provider_email: String,
    pub provider_username: Option<String>,
    pub is_active: bool,
    pub access_token: Option<String>,
    pub refresh_token: Option<String>,
    pub token_expires_at: Option<DateTime<Utc>>,
    pub is_primary: bool,
    pub link_status: UserOAuthLinkStatus,
    pub last_synced: Option<DateTime<Utc>>,
    pub sync_enabled: bool,
    pub metadata: serde_json::Value,

    // Projection metadata
    /// Version of the projection (for optimistic locking)
    pub projection_version: i64,
    /// Last event sequence number applied
    pub last_event_sequence: i64,
    /// When this projection was last updated
    pub projection_updated_at: DateTime<Utc>,
}

impl UserOAuthLinkProjection {
    /// Create a new projection from entity data
    pub fn new(
        id: Uuid,
        user_id: Uuid,
        oauth_provider_id: Uuid,
        provider_user_id: String,
        provider_email: String,
        provider_username: Option<String>,
        is_active: bool,
        access_token: Option<String>,
        refresh_token: Option<String>,
        token_expires_at: Option<DateTime<Utc>>,
        is_primary: bool,
        link_status: UserOAuthLinkStatus,
        last_synced: Option<DateTime<Utc>>,
        sync_enabled: bool,
        metadata: serde_json::Value
    ) -> Self {
        Self {
            id,
            user_id,
            oauth_provider_id,
            provider_user_id,
            provider_email,
            provider_username,
            is_active,
            access_token,
            refresh_token,
            token_expires_at,
            is_primary,
            link_status,
            last_synced,
            sync_enabled,
            metadata,
            projection_version: 1,
            last_event_sequence: 0,
            projection_updated_at: Utc::now(),
        }
    }

    /// Get the projection ID
    pub fn id(&self) -> &Uuid {
        &self.id
    }

    /// Update projection version after applying event
    pub fn apply_event(&mut self, event_sequence: i64) {
        self.projection_version += 1;
        self.last_event_sequence = event_sequence;
        self.projection_updated_at = Utc::now();
    }
}

// ============================================================================
// SUMMARY PROJECTION (LIST VIEW)
// ============================================================================

/// Summary view of UserOAuthLink for list displays
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct UserOAuthLinkSummary {
    pub id: Uuid,
}

// ============================================================================
// CUSTOM PROJECTIONS
// ============================================================================
// <<< CUSTOM PROJECTIONS START >>>

// <<< CUSTOM PROJECTIONS START >>>
// Add custom read models here for specific query use cases
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct UserOAuthLinkDashboard {
//     pub id: Uuid,
//     pub key_metric: i64,
// }
// <<< CUSTOM PROJECTIONS END >>>