//! DeviceTrust Projection (Read Model)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This is a CQRS projection - an optimized read model for queries.
//! It is updated asynchronously from domain events.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

// ============================================================================
// PROJECTION (READ MODEL)
// ============================================================================

/// DeviceTrust read model optimized for queries
///
/// This projection is denormalized for fast reads.
/// It is updated when domain events are published.
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct DeviceTrustProjection {
    pub id: Uuid,
    pub user_id: Uuid,
    pub device_fingerprint: String,
    pub device_name: String,
    pub device_type: DeviceTrustType,
    pub trust_level: TrustLevel,
    pub is_trusted: bool,
    pub trusted_at: DateTime<Utc>,
    pub expires_at: Option<DateTime<Utc>>,
    pub last_used_at: DateTime<Utc>,
    pub usage_count: i32,
    pub ip_address: Option<String>,
    pub location: Option<serde_json::Value>,
    pub risk_score: f64,
    pub requires_mfa: bool,
    pub metadata: serde_json::Value,

    // Projection metadata
    /// Version of the projection (for optimistic locking)
    pub projection_version: i64,
    /// Last event sequence number applied
    pub last_event_sequence: i64,
    /// When this projection was last updated
    pub projection_updated_at: DateTime<Utc>,
}

impl DeviceTrustProjection {
    /// Create a new projection from entity data
    pub fn new(
        id: Uuid,
        user_id: Uuid,
        device_fingerprint: String,
        device_name: String,
        device_type: DeviceTrustType,
        trust_level: TrustLevel,
        is_trusted: bool,
        trusted_at: DateTime<Utc>,
        expires_at: Option<DateTime<Utc>>,
        last_used_at: DateTime<Utc>,
        usage_count: i32,
        ip_address: Option<String>,
        location: Option<serde_json::Value>,
        risk_score: f64,
        requires_mfa: bool,
        metadata: serde_json::Value
    ) -> Self {
        Self {
            id,
            user_id,
            device_fingerprint,
            device_name,
            device_type,
            trust_level,
            is_trusted,
            trusted_at,
            expires_at,
            last_used_at,
            usage_count,
            ip_address,
            location,
            risk_score,
            requires_mfa,
            metadata,
            projection_version: 1,
            last_event_sequence: 0,
            projection_updated_at: Utc::now(),
        }
    }

    /// Get the projection ID
    pub fn id(&self) -> &Uuid {
        &self.id
    }

    /// Update projection version after applying event
    pub fn apply_event(&mut self, event_sequence: i64) {
        self.projection_version += 1;
        self.last_event_sequence = event_sequence;
        self.projection_updated_at = Utc::now();
    }
}

// ============================================================================
// SUMMARY PROJECTION (LIST VIEW)
// ============================================================================

/// Summary view of DeviceTrust for list displays
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct DeviceTrustSummary {
    pub id: Uuid,
    pub device_name: String,
}

// ============================================================================
// CUSTOM PROJECTIONS
// ============================================================================
// <<< CUSTOM PROJECTIONS START >>>

// <<< CUSTOM PROJECTIONS START >>>
// Add custom read models here for specific query use cases
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct DeviceTrustDashboard {
//     pub id: Uuid,
//     pub key_metric: i64,
// }
// <<< CUSTOM PROJECTIONS END >>>