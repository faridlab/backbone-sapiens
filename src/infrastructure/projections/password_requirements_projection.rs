//! PasswordRequirements Projection (Read Model)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This is a CQRS projection - an optimized read model for queries.
//! It is updated asynchronously from domain events.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

// ============================================================================
// PROJECTION (READ MODEL)
// ============================================================================

/// PasswordRequirements read model optimized for queries
///
/// This projection is denormalized for fast reads.
/// It is updated when domain events are published.
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct PasswordRequirementsProjection {
    pub id: Uuid,
    pub min_length: i32,
    pub max_length: i32,
    pub require_uppercase: bool,
    pub require_lowercase: bool,
    pub require_digits: bool,
    pub require_special_characters: bool,
    pub require_no_common_passwords: bool,
    pub require_no_user_info: bool,
    pub min_entropy_bits: i32,
    pub metadata: serde_json::Value,

    // Projection metadata
    /// Version of the projection (for optimistic locking)
    pub projection_version: i64,
    /// Last event sequence number applied
    pub last_event_sequence: i64,
    /// When this projection was last updated
    pub projection_updated_at: DateTime<Utc>,
}

impl PasswordRequirementsProjection {
    /// Create a new projection from entity data
    pub fn new(
        id: Uuid,
        min_length: i32,
        max_length: i32,
        require_uppercase: bool,
        require_lowercase: bool,
        require_digits: bool,
        require_special_characters: bool,
        require_no_common_passwords: bool,
        require_no_user_info: bool,
        min_entropy_bits: i32,
        metadata: serde_json::Value
    ) -> Self {
        Self {
            id,
            min_length,
            max_length,
            require_uppercase,
            require_lowercase,
            require_digits,
            require_special_characters,
            require_no_common_passwords,
            require_no_user_info,
            min_entropy_bits,
            metadata,
            projection_version: 1,
            last_event_sequence: 0,
            projection_updated_at: Utc::now(),
        }
    }

    /// Get the projection ID
    pub fn id(&self) -> &Uuid {
        &self.id
    }

    /// Update projection version after applying event
    pub fn apply_event(&mut self, event_sequence: i64) {
        self.projection_version += 1;
        self.last_event_sequence = event_sequence;
        self.projection_updated_at = Utc::now();
    }
}

// ============================================================================
// SUMMARY PROJECTION (LIST VIEW)
// ============================================================================

/// Summary view of PasswordRequirements for list displays
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct PasswordRequirementsSummary {
    pub id: Uuid,
}

// ============================================================================
// CUSTOM PROJECTIONS
// ============================================================================
// <<< CUSTOM PROJECTIONS START >>>

// <<< CUSTOM PROJECTIONS START >>>
// Add custom read models here for specific query use cases
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct PasswordRequirementsDashboard {
//     pub id: Uuid,
//     pub key_metric: i64,
// }
// <<< CUSTOM PROJECTIONS END >>>