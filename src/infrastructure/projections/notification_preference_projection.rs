//! NotificationPreference Projection (Read Model)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This is a CQRS projection - an optimized read model for queries.
//! It is updated asynchronously from domain events.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

// ============================================================================
// PROJECTION (READ MODEL)
// ============================================================================

/// NotificationPreference read model optimized for queries
///
/// This projection is denormalized for fast reads.
/// It is updated when domain events are published.
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct NotificationPreferenceProjection {
    pub id: Uuid,
    pub user_id: Uuid,
    pub notification_type: String,
    pub channel_enabled: bool,
    pub channels: serde_json::Value,
    pub quiet_hours_start: Option<String>,
    pub quiet_hours_end: Option<String>,
    pub quiet_timezone: String,
    pub digest_enabled: bool,
    pub digest_frequency: DigestFrequency,
    pub enabled: bool,
    pub metadata: serde_json::Value,

    // Projection metadata
    /// Version of the projection (for optimistic locking)
    pub projection_version: i64,
    /// Last event sequence number applied
    pub last_event_sequence: i64,
    /// When this projection was last updated
    pub projection_updated_at: DateTime<Utc>,
}

impl NotificationPreferenceProjection {
    /// Create a new projection from entity data
    pub fn new(
        id: Uuid,
        user_id: Uuid,
        notification_type: String,
        channel_enabled: bool,
        channels: serde_json::Value,
        quiet_hours_start: Option<String>,
        quiet_hours_end: Option<String>,
        quiet_timezone: String,
        digest_enabled: bool,
        digest_frequency: DigestFrequency,
        enabled: bool,
        metadata: serde_json::Value
    ) -> Self {
        Self {
            id,
            user_id,
            notification_type,
            channel_enabled,
            channels,
            quiet_hours_start,
            quiet_hours_end,
            quiet_timezone,
            digest_enabled,
            digest_frequency,
            enabled,
            metadata,
            projection_version: 1,
            last_event_sequence: 0,
            projection_updated_at: Utc::now(),
        }
    }

    /// Get the projection ID
    pub fn id(&self) -> &Uuid {
        &self.id
    }

    /// Update projection version after applying event
    pub fn apply_event(&mut self, event_sequence: i64) {
        self.projection_version += 1;
        self.last_event_sequence = event_sequence;
        self.projection_updated_at = Utc::now();
    }
}

// ============================================================================
// SUMMARY PROJECTION (LIST VIEW)
// ============================================================================

/// Summary view of NotificationPreference for list displays
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct NotificationPreferenceSummary {
    pub id: Uuid,
}

// ============================================================================
// CUSTOM PROJECTIONS
// ============================================================================
// <<< CUSTOM PROJECTIONS START >>>

// <<< CUSTOM PROJECTIONS START >>>
// Add custom read models here for specific query use cases
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct NotificationPreferenceDashboard {
//     pub id: Uuid,
//     pub key_metric: i64,
// }
// <<< CUSTOM PROJECTIONS END >>>