//! DirectPermissionGrant Projection (Read Model)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This is a CQRS projection - an optimized read model for queries.
//! It is updated asynchronously from domain events.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

// ============================================================================
// PROJECTION (READ MODEL)
// ============================================================================

/// DirectPermissionGrant read model optimized for queries
///
/// This projection is denormalized for fast reads.
/// It is updated when domain events are published.
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct DirectPermissionGrantProjection {
    pub id: Uuid,
    pub user_id: Uuid,
    pub permission_id: Uuid,
    pub granted_by: Uuid,
    pub granted_at: DateTime<Utc>,
    pub expires_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub scope: Option<serde_json::Value>,
    pub conditions: Option<serde_json::Value>,
    pub last_used_at: Option<DateTime<Utc>>,
    pub usage_count: i32,
    pub access_log: Option<serde_json::Value>,
    pub status: DirectPermissionGrantStatus,
    pub revoked_at: Option<DateTime<Utc>>,
    pub revoked_by: Option<Uuid>,
    pub revocation_reason: Option<String>,
    pub metadata: serde_json::Value,

    // Projection metadata
    /// Version of the projection (for optimistic locking)
    pub projection_version: i64,
    /// Last event sequence number applied
    pub last_event_sequence: i64,
    /// When this projection was last updated
    pub projection_updated_at: DateTime<Utc>,
}

impl DirectPermissionGrantProjection {
    /// Create a new projection from entity data
    pub fn new(
        id: Uuid,
        user_id: Uuid,
        permission_id: Uuid,
        granted_by: Uuid,
        granted_at: DateTime<Utc>,
        expires_at: Option<DateTime<Utc>>,
        reason: Option<String>,
        scope: Option<serde_json::Value>,
        conditions: Option<serde_json::Value>,
        last_used_at: Option<DateTime<Utc>>,
        usage_count: i32,
        access_log: Option<serde_json::Value>,
        status: DirectPermissionGrantStatus,
        revoked_at: Option<DateTime<Utc>>,
        revoked_by: Option<Uuid>,
        revocation_reason: Option<String>,
        metadata: serde_json::Value
    ) -> Self {
        Self {
            id,
            user_id,
            permission_id,
            granted_by,
            granted_at,
            expires_at,
            reason,
            scope,
            conditions,
            last_used_at,
            usage_count,
            access_log,
            status,
            revoked_at,
            revoked_by,
            revocation_reason,
            metadata,
            projection_version: 1,
            last_event_sequence: 0,
            projection_updated_at: Utc::now(),
        }
    }

    /// Get the projection ID
    pub fn id(&self) -> &Uuid {
        &self.id
    }

    /// Update projection version after applying event
    pub fn apply_event(&mut self, event_sequence: i64) {
        self.projection_version += 1;
        self.last_event_sequence = event_sequence;
        self.projection_updated_at = Utc::now();
    }
}

// ============================================================================
// SUMMARY PROJECTION (LIST VIEW)
// ============================================================================

/// Summary view of DirectPermissionGrant for list displays
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct DirectPermissionGrantSummary {
    pub id: Uuid,
    pub status: DirectPermissionGrantStatus,
}

// ============================================================================
// CUSTOM PROJECTIONS
// ============================================================================
// <<< CUSTOM PROJECTIONS START >>>

// <<< CUSTOM PROJECTIONS START >>>
// Add custom read models here for specific query use cases
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct DirectPermissionGrantDashboard {
//     pub id: Uuid,
//     pub key_metric: i64,
// }
// <<< CUSTOM PROJECTIONS END >>>