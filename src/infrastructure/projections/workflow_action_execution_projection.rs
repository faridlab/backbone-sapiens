//! WorkflowActionExecution Projection (Read Model)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This is a CQRS projection - an optimized read model for queries.
//! It is updated asynchronously from domain events.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

// ============================================================================
// PROJECTION (READ MODEL)
// ============================================================================

/// WorkflowActionExecution read model optimized for queries
///
/// This projection is denormalized for fast reads.
/// It is updated when domain events are published.
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct WorkflowActionExecutionProjection {
    pub id: Uuid,
    pub workflow_execution_id: Uuid,
    pub workflow_action_id: Uuid,
    pub action_order: i32,
    pub status: WorkflowActionExecutionStatus,
    pub input_data: Option<serde_json::Value>,
    pub output_data: Option<serde_json::Value>,
    pub started_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub failed_at: Option<DateTime<Utc>>,
    pub duration_ms: Option<i32>,
    pub error_message: Option<String>,
    pub retry_count: i32,
    pub metadata: serde_json::Value,

    // Projection metadata
    /// Version of the projection (for optimistic locking)
    pub projection_version: i64,
    /// Last event sequence number applied
    pub last_event_sequence: i64,
    /// When this projection was last updated
    pub projection_updated_at: DateTime<Utc>,
}

impl WorkflowActionExecutionProjection {
    /// Create a new projection from entity data
    pub fn new(
        id: Uuid,
        workflow_execution_id: Uuid,
        workflow_action_id: Uuid,
        action_order: i32,
        status: WorkflowActionExecutionStatus,
        input_data: Option<serde_json::Value>,
        output_data: Option<serde_json::Value>,
        started_at: Option<DateTime<Utc>>,
        completed_at: Option<DateTime<Utc>>,
        failed_at: Option<DateTime<Utc>>,
        duration_ms: Option<i32>,
        error_message: Option<String>,
        retry_count: i32,
        metadata: serde_json::Value
    ) -> Self {
        Self {
            id,
            workflow_execution_id,
            workflow_action_id,
            action_order,
            status,
            input_data,
            output_data,
            started_at,
            completed_at,
            failed_at,
            duration_ms,
            error_message,
            retry_count,
            metadata,
            projection_version: 1,
            last_event_sequence: 0,
            projection_updated_at: Utc::now(),
        }
    }

    /// Get the projection ID
    pub fn id(&self) -> &Uuid {
        &self.id
    }

    /// Update projection version after applying event
    pub fn apply_event(&mut self, event_sequence: i64) {
        self.projection_version += 1;
        self.last_event_sequence = event_sequence;
        self.projection_updated_at = Utc::now();
    }
}

// ============================================================================
// SUMMARY PROJECTION (LIST VIEW)
// ============================================================================

/// Summary view of WorkflowActionExecution for list displays
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct WorkflowActionExecutionSummary {
    pub id: Uuid,
    pub status: WorkflowActionExecutionStatus,
}

// ============================================================================
// CUSTOM PROJECTIONS
// ============================================================================
// <<< CUSTOM PROJECTIONS START >>>

// <<< CUSTOM PROJECTIONS START >>>
// Add custom read models here for specific query use cases
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct WorkflowActionExecutionDashboard {
//     pub id: Uuid,
//     pub key_metric: i64,
// }
// <<< CUSTOM PROJECTIONS END >>>