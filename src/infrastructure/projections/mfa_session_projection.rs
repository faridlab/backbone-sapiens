//! MFASession Projection (Read Model)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This is a CQRS projection - an optimized read model for queries.
//! It is updated asynchronously from domain events.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

// ============================================================================
// PROJECTION (READ MODEL)
// ============================================================================

/// MFASession read model optimized for queries
///
/// This projection is denormalized for fast reads.
/// It is updated when domain events are published.
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct MFASessionProjection {
    pub id: Uuid,
    pub user_id: Uuid,
    pub device_id: Uuid,
    pub session_token: String,
    pub session_hash: String,
    pub primary_authentication_method: PrimaryAuthMethod,
    pub mfa_required: bool,
    pub mfa_enforced_at: Option<DateTime<Utc>>,
    pub risk_score: i32,
    pub trust_level: MfaTrustLevel,
    pub adaptive_authentication_triggered: bool,
    pub additional_factors_required: i32,
    pub ip_risk_score: Option<i32>,
    pub device_risk_score: Option<i32>,
    pub location_anomaly: bool,
    pub time_anomaly: bool,
    pub verification_method: MFAVerificationMethod,
    pub verification_initiated_at: DateTime<Utc>,
    pub verification_completed_at: Option<DateTime<Utc>>,
    pub verification_attempts: i32,
    pub max_attempts_allowed: i32,
    pub verification_success: Option<bool>,
    pub verification_time_ms: Option<i32>,
    pub device_trust_status: DeviceTrustStatus,
    pub is_terminated: bool,
    pub terminated_at: Option<DateTime<Utc>>,
    pub termination_reason: Option<String>,
    pub ip_address: String,
    pub user_agent: Option<String>,
    pub device_fingerprint: Option<String>,
    pub country: Option<String>,
    pub region: Option<String>,
    pub city: Option<String>,
    pub latitude: Option<f64>,
    pub longitude: Option<f64>,
    pub expires_at: DateTime<Utc>,
    pub max_duration_minutes: i32,
    pub idle_timeout_minutes: i32,
    pub auto_renew_enabled: bool,
    pub last_activity_at: DateTime<Utc>,
    pub extension_count: i32,
    pub initiation_time_ms: i32,
    pub total_session_time_ms: Option<i32>,
    pub device_performance_score: Option<i32>,
    pub network_quality: NetworkQuality,
    pub audit_log_required: bool,
    pub data_retention_hours: i32,
    pub status: MFASessionStatus,
    pub metadata: serde_json::Value,

    // Projection metadata
    /// Version of the projection (for optimistic locking)
    pub projection_version: i64,
    /// Last event sequence number applied
    pub last_event_sequence: i64,
    /// When this projection was last updated
    pub projection_updated_at: DateTime<Utc>,
}

impl MFASessionProjection {
    /// Create a new projection from entity data
    pub fn new(
        id: Uuid,
        user_id: Uuid,
        device_id: Uuid,
        session_token: String,
        session_hash: String,
        primary_authentication_method: PrimaryAuthMethod,
        mfa_required: bool,
        mfa_enforced_at: Option<DateTime<Utc>>,
        risk_score: i32,
        trust_level: MfaTrustLevel,
        adaptive_authentication_triggered: bool,
        additional_factors_required: i32,
        ip_risk_score: Option<i32>,
        device_risk_score: Option<i32>,
        location_anomaly: bool,
        time_anomaly: bool,
        verification_method: MFAVerificationMethod,
        verification_initiated_at: DateTime<Utc>,
        verification_completed_at: Option<DateTime<Utc>>,
        verification_attempts: i32,
        max_attempts_allowed: i32,
        verification_success: Option<bool>,
        verification_time_ms: Option<i32>,
        device_trust_status: DeviceTrustStatus,
        is_terminated: bool,
        terminated_at: Option<DateTime<Utc>>,
        termination_reason: Option<String>,
        ip_address: String,
        user_agent: Option<String>,
        device_fingerprint: Option<String>,
        country: Option<String>,
        region: Option<String>,
        city: Option<String>,
        latitude: Option<f64>,
        longitude: Option<f64>,
        expires_at: DateTime<Utc>,
        max_duration_minutes: i32,
        idle_timeout_minutes: i32,
        auto_renew_enabled: bool,
        last_activity_at: DateTime<Utc>,
        extension_count: i32,
        initiation_time_ms: i32,
        total_session_time_ms: Option<i32>,
        device_performance_score: Option<i32>,
        network_quality: NetworkQuality,
        audit_log_required: bool,
        data_retention_hours: i32,
        status: MFASessionStatus,
        metadata: serde_json::Value
    ) -> Self {
        Self {
            id,
            user_id,
            device_id,
            session_token,
            session_hash,
            primary_authentication_method,
            mfa_required,
            mfa_enforced_at,
            risk_score,
            trust_level,
            adaptive_authentication_triggered,
            additional_factors_required,
            ip_risk_score,
            device_risk_score,
            location_anomaly,
            time_anomaly,
            verification_method,
            verification_initiated_at,
            verification_completed_at,
            verification_attempts,
            max_attempts_allowed,
            verification_success,
            verification_time_ms,
            device_trust_status,
            is_terminated,
            terminated_at,
            termination_reason,
            ip_address,
            user_agent,
            device_fingerprint,
            country,
            region,
            city,
            latitude,
            longitude,
            expires_at,
            max_duration_minutes,
            idle_timeout_minutes,
            auto_renew_enabled,
            last_activity_at,
            extension_count,
            initiation_time_ms,
            total_session_time_ms,
            device_performance_score,
            network_quality,
            audit_log_required,
            data_retention_hours,
            status,
            metadata,
            projection_version: 1,
            last_event_sequence: 0,
            projection_updated_at: Utc::now(),
        }
    }

    /// Get the projection ID
    pub fn id(&self) -> &Uuid {
        &self.id
    }

    /// Update projection version after applying event
    pub fn apply_event(&mut self, event_sequence: i64) {
        self.projection_version += 1;
        self.last_event_sequence = event_sequence;
        self.projection_updated_at = Utc::now();
    }
}

// ============================================================================
// SUMMARY PROJECTION (LIST VIEW)
// ============================================================================

/// Summary view of MFASession for list displays
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct MFASessionSummary {
    pub id: Uuid,
    pub status: MFASessionStatus,
}

// ============================================================================
// CUSTOM PROJECTIONS
// ============================================================================
// <<< CUSTOM PROJECTIONS START >>>

// <<< CUSTOM PROJECTIONS START >>>
// Add custom read models here for specific query use cases
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct MFASessionDashboard {
//     pub id: Uuid,
//     pub key_metric: i64,
// }
// <<< CUSTOM PROJECTIONS END >>>