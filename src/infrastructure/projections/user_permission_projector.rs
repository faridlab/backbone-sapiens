//! UserPermission Projector (Event Handler)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This projector listens to domain events and updates the projection.

use std::sync::Arc;

use anyhow::Result;
use async_trait::async_trait;
use chrono::Utc;

use super::UserPermissionProjection;
use crate::domain::entity::UserPermission;
use crate::domain::events::{
    UserPermissionCreatedEvent, UserPermissionUpdatedEvent, UserPermissionDeletedEvent,
};

// ============================================================================
// EVENT HANDLER TRAIT
// ============================================================================

/// Trait for handling UserPermission domain events
///
/// All handler methods receive the event sequence number for tracking
/// which events have been processed by the projector.
#[async_trait]
pub trait UserPermissionEventHandler: Send + Sync {
    /// Handle entity created event
    async fn on_created(&self, event: UserPermissionCreatedEvent, sequence: i64) -> Result<()>;
    /// Handle entity updated event
    async fn on_updated(&self, event: UserPermissionUpdatedEvent, sequence: i64) -> Result<()>;
    /// Handle entity deleted event
    async fn on_deleted(&self, event: UserPermissionDeletedEvent, sequence: i64) -> Result<()>;
}

// ============================================================================
// PROJECTION REPOSITORY
// ============================================================================

/// Repository for UserPermission projections
#[async_trait]
pub trait UserPermissionProjectionRepository: Send + Sync {
    async fn save(&self, projection: &UserPermissionProjection) -> Result<()>;
    async fn find_by_id(&self, id: uuid::Uuid) -> Result<Option<UserPermissionProjection>>;
    async fn delete(&self, id: uuid::Uuid) -> Result<()>;
    async fn rebuild_all(&self) -> Result<u64>;
}

// ============================================================================
// PROJECTOR
// ============================================================================

/// Projector that updates UserPermission projection from events
pub struct UserPermissionProjector<R: UserPermissionProjectionRepository> {
    repository: Arc<R>,
}

impl<R: UserPermissionProjectionRepository> UserPermissionProjector<R> {
    /// Create a new projector
    pub fn new(repository: Arc<R>) -> Self {
        Self { repository }
    }

    /// Rebuild all projections from event history
    pub async fn rebuild(&self) -> Result<u64> {
        self.repository.rebuild_all().await
    }

    /// Process a stored event from the event store
    ///
    /// This method dispatches the event to the appropriate handler based on event type.
    /// Returns Ok(true) if the event was processed, Ok(false) if it was skipped.
    pub async fn process_stored_event(&self, event: &crate::infrastructure::event_store::StoredEvent) -> Result<bool>
    where
        Self: UserPermissionEventHandler,
    {
        let sequence = event.sequence;

        match event.event_type.as_str() {
            "UserPermissionCreatedEvent" => {
                let created: UserPermissionCreatedEvent = serde_json::from_value(event.payload.clone())?;
                self.on_created(created, sequence).await?;
                Ok(true)
            }
            "UserPermissionUpdatedEvent" => {
                let updated: UserPermissionUpdatedEvent = serde_json::from_value(event.payload.clone())?;
                self.on_updated(updated, sequence).await?;
                Ok(true)
            }
            "UserPermissionDeletedEvent" => {
                let deleted: UserPermissionDeletedEvent = serde_json::from_value(event.payload.clone())?;
                self.on_deleted(deleted, sequence).await?;
                Ok(true)
            }
            _ => Ok(false), // Event type not handled by this projector
        }
    }
}

#[async_trait]
impl<R: UserPermissionProjectionRepository> UserPermissionEventHandler for UserPermissionProjector<R> {

    async fn on_created(&self, event: UserPermissionCreatedEvent, sequence: i64) -> Result<()> {
        let mut projection = UserPermissionProjection::new(
            event.id,
            event.user_id,
            event.permission_id,
            event.granted_at,
            event.granted_by,
            event.reason,
            event.expires_at,
            event.resource_id,
            event.resource_type,
            event.is_active,
            event.revoked_at,
            event.revoked_by,
            event.revoked_reason,
            event.metadata
        );

        // Track the event sequence for this projection
        projection.apply_event(sequence);

        self.repository.save(&projection).await
    }

    async fn on_updated(&self, event: UserPermissionUpdatedEvent, sequence: i64) -> Result<()> {
        if let Some(mut projection) = self.repository.find_by_id(event.id).await? {
            // Apply updates from event
            projection.user_id = event.user_id;
            projection.permission_id = event.permission_id;
            projection.granted_at = event.granted_at;
            projection.granted_by = event.granted_by;
            projection.reason = event.reason;
            projection.expires_at = event.expires_at;
            projection.resource_id = event.resource_id;
            projection.resource_type = event.resource_type;
            projection.is_active = event.is_active;
            projection.revoked_at = event.revoked_at;
            projection.revoked_by = event.revoked_by;
            projection.revoked_reason = event.revoked_reason;
            projection.metadata = event.metadata;
            // Track the event sequence for this projection
            projection.apply_event(sequence);

            self.repository.save(&projection).await?;
        }

        Ok(())
    }

    async fn on_deleted(&self, event: UserPermissionDeletedEvent, _sequence: i64) -> Result<()> {
        self.repository.delete(event.id).await
    }
}

// ============================================================================
// CUSTOM EVENT HANDLERS
// ============================================================================
// <<< CUSTOM HANDLERS START >>>

// <<< CUSTOM HANDLERS START >>>
// Add custom event handlers here
// <<< CUSTOM HANDLERS END >>>