//! EmailVerificationToken Projection (Read Model)
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This is a CQRS projection - an optimized read model for queries.
//! It is updated asynchronously from domain events.

use chrono::{DateTime, Utc};
use serde::{Deserialize, Serialize};
use sqlx::FromRow;
use uuid::Uuid;

// ============================================================================
// PROJECTION (READ MODEL)
// ============================================================================

/// EmailVerificationToken read model optimized for queries
///
/// This projection is denormalized for fast reads.
/// It is updated when domain events are published.
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct EmailVerificationTokenProjection {
    pub id: Uuid,
    pub user_id: Uuid,
    pub token: String,
    pub email: String,
    pub token_type: EmailVerificationType,
    pub old_email: Option<String>,
    pub expires_at: DateTime<Utc>,
    pub verified_at: Option<DateTime<Utc>>,
    pub attempts: i32,
    pub max_attempts: i32,
    pub status: EmailVerificationTokenStatus,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub metadata: serde_json::Value,

    // Projection metadata
    /// Version of the projection (for optimistic locking)
    pub projection_version: i64,
    /// Last event sequence number applied
    pub last_event_sequence: i64,
    /// When this projection was last updated
    pub projection_updated_at: DateTime<Utc>,
}

impl EmailVerificationTokenProjection {
    /// Create a new projection from entity data
    pub fn new(
        id: Uuid,
        user_id: Uuid,
        token: String,
        email: String,
        token_type: EmailVerificationType,
        old_email: Option<String>,
        expires_at: DateTime<Utc>,
        verified_at: Option<DateTime<Utc>>,
        attempts: i32,
        max_attempts: i32,
        status: EmailVerificationTokenStatus,
        ip_address: Option<String>,
        user_agent: Option<String>,
        metadata: serde_json::Value
    ) -> Self {
        Self {
            id,
            user_id,
            token,
            email,
            token_type,
            old_email,
            expires_at,
            verified_at,
            attempts,
            max_attempts,
            status,
            ip_address,
            user_agent,
            metadata,
            projection_version: 1,
            last_event_sequence: 0,
            projection_updated_at: Utc::now(),
        }
    }

    /// Get the projection ID
    pub fn id(&self) -> &Uuid {
        &self.id
    }

    /// Update projection version after applying event
    pub fn apply_event(&mut self, event_sequence: i64) {
        self.projection_version += 1;
        self.last_event_sequence = event_sequence;
        self.projection_updated_at = Utc::now();
    }
}

// ============================================================================
// SUMMARY PROJECTION (LIST VIEW)
// ============================================================================

/// Summary view of EmailVerificationToken for list displays
#[derive(Debug, Clone, Serialize, Deserialize, FromRow)]
pub struct EmailVerificationTokenSummary {
    pub id: Uuid,
    pub email: String,
    pub status: EmailVerificationTokenStatus,
}

// ============================================================================
// CUSTOM PROJECTIONS
// ============================================================================
// <<< CUSTOM PROJECTIONS START >>>

// <<< CUSTOM PROJECTIONS START >>>
// Add custom read models here for specific query use cases
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct EmailVerificationTokenDashboard {
//     pub id: Uuid,
//     pub key_metric: i64,
// }
// <<< CUSTOM PROJECTIONS END >>>