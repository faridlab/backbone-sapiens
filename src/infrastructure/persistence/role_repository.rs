//! Repository for Role entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Role, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Role;

/// Table name for Role entities
pub const TABLE_NAME: &str = "roles";

/// Repository for Role entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct RoleRepository(
    backbone_orm::GenericCrudRepository<Role, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for RoleRepository {
    type Target = backbone_orm::GenericCrudRepository<Role, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl RoleRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl RoleRepository {
    /// Find role by name (excludes soft-deleted)
    pub async fn find_by_name(&self, name: &str) -> Result<Option<Role>> {
        self.find_by_text_field("name", name).await
    }

    /// Check if role exists by name (excludes soft-deleted)
    pub async fn exists_by_name(&self, name: &str) -> Result<bool> {
        self.exists_by_text_field("name", name).await
    }

}

backbone_core::impl_crud_repository!(RoleRepository, Role, soft_delete);
