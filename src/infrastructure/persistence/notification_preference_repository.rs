//! Repository for NotificationPreference entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<NotificationPreference, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;
use uuid::Uuid;

use crate::domain::entity::NotificationPreference;

/// Table name for NotificationPreference entities
pub const TABLE_NAME: &str = "notification_preferences";

/// Repository for NotificationPreference entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct NotificationPreferenceRepository(
    backbone_orm::GenericCrudRepository<NotificationPreference, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for NotificationPreferenceRepository {
    type Target = backbone_orm::GenericCrudRepository<NotificationPreference, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl NotificationPreferenceRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl NotificationPreferenceRepository {
    /// Find notification_preference by user_id (excludes soft-deleted)
    pub async fn find_by_user_id(&self, user_id: uuid::Uuid) -> Result<Option<NotificationPreference>> {
        self.find_by_uuid_field("user_id", user_id).await
    }

    /// Check if notification_preference exists by user_id (excludes soft-deleted)
    pub async fn exists_by_user_id(&self, user_id: uuid::Uuid) -> Result<bool> {
        self.exists_by_uuid_field("user_id", user_id).await
    }

}

backbone_core::impl_crud_repository!(NotificationPreferenceRepository, NotificationPreference, soft_delete);
