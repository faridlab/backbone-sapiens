//! Repository for DeviceTrust entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<DeviceTrust, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::DeviceTrust;

/// Table name for DeviceTrust entities
pub const TABLE_NAME: &str = "device_trusts";

/// Repository for DeviceTrust entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct DeviceTrustRepository(
    backbone_orm::GenericCrudRepository<DeviceTrust, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for DeviceTrustRepository {
    type Target = backbone_orm::GenericCrudRepository<DeviceTrust, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl DeviceTrustRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl DeviceTrustRepository {
    /// Find device_trust by device_fingerprint (excludes soft-deleted)
    pub async fn find_by_device_fingerprint(&self, device_fingerprint: &str) -> Result<Option<DeviceTrust>> {
        self.find_by_text_field("device_fingerprint", device_fingerprint).await
    }

    /// Check if device_trust exists by device_fingerprint (excludes soft-deleted)
    pub async fn exists_by_device_fingerprint(&self, device_fingerprint: &str) -> Result<bool> {
        self.exists_by_text_field("device_fingerprint", device_fingerprint).await
    }

}

backbone_core::impl_crud_repository!(DeviceTrustRepository, DeviceTrust, soft_delete);
