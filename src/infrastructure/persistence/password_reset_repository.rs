//! Repository for PasswordReset entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<PasswordReset, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::PasswordReset;

/// Table name for PasswordReset entities
pub const TABLE_NAME: &str = "password_resets";

/// Repository for PasswordReset entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct PasswordResetRepository(
    backbone_orm::GenericCrudRepository<PasswordReset, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for PasswordResetRepository {
    type Target = backbone_orm::GenericCrudRepository<PasswordReset, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl PasswordResetRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl PasswordResetRepository {
    /// Find password_reset by token (excludes soft-deleted)
    pub async fn find_by_token(&self, token: &str) -> Result<Option<PasswordReset>> {
        self.find_by_text_field("token", token).await
    }

    /// Find password_reset by token_hash (excludes soft-deleted)
    pub async fn find_by_token_hash(&self, token_hash: &str) -> Result<Option<PasswordReset>> {
        self.find_by_text_field("token_hash", token_hash).await
    }

    /// Check if password_reset exists by token (excludes soft-deleted)
    pub async fn exists_by_token(&self, token: &str) -> Result<bool> {
        self.exists_by_text_field("token", token).await
    }

    /// Check if password_reset exists by token_hash (excludes soft-deleted)
    pub async fn exists_by_token_hash(&self, token_hash: &str) -> Result<bool> {
        self.exists_by_text_field("token_hash", token_hash).await
    }

}

backbone_core::impl_crud_repository!(PasswordResetRepository, PasswordReset, soft_delete);
