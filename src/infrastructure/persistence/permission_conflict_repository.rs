//! Repository for PermissionConflict entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<PermissionConflict, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::PermissionConflict;

/// Table name for PermissionConflict entities
pub const TABLE_NAME: &str = "permission_conflicts";

/// Repository for PermissionConflict entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct PermissionConflictRepository(
    backbone_orm::GenericCrudRepository<PermissionConflict, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for PermissionConflictRepository {
    type Target = backbone_orm::GenericCrudRepository<PermissionConflict, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl PermissionConflictRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(PermissionConflictRepository, PermissionConflict, soft_delete);
