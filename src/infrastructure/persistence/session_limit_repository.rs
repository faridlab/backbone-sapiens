//! Repository for SessionLimit entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<SessionLimit, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;
use uuid::Uuid;

use crate::domain::entity::SessionLimit;

/// Table name for SessionLimit entities
pub const TABLE_NAME: &str = "session_limits";

/// Repository for SessionLimit entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct SessionLimitRepository(
    backbone_orm::GenericCrudRepository<SessionLimit, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for SessionLimitRepository {
    type Target = backbone_orm::GenericCrudRepository<SessionLimit, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl SessionLimitRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl SessionLimitRepository {
    /// Find session_limit by user_id (excludes soft-deleted)
    pub async fn find_by_user_id(&self, user_id: uuid::Uuid) -> Result<Option<SessionLimit>> {
        self.find_by_uuid_field("user_id", user_id).await
    }

    /// Check if session_limit exists by user_id (excludes soft-deleted)
    pub async fn exists_by_user_id(&self, user_id: uuid::Uuid) -> Result<bool> {
        self.exists_by_uuid_field("user_id", user_id).await
    }

}

backbone_core::impl_crud_repository!(SessionLimitRepository, SessionLimit, soft_delete);
