//! Repository for Workflow entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Workflow, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Workflow;

/// Table name for Workflow entities
pub const TABLE_NAME: &str = "workflows";

/// Repository for Workflow entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct WorkflowRepository(
    backbone_orm::GenericCrudRepository<Workflow, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for WorkflowRepository {
    type Target = backbone_orm::GenericCrudRepository<Workflow, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl WorkflowRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(WorkflowRepository, Workflow, soft_delete);
