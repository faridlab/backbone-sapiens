//! Repository for LDAPDirectory entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<LDAPDirectory, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::LDAPDirectory;

/// Table name for LDAPDirectory entities
pub const TABLE_NAME: &str = "ldap_directories";

/// Repository for LDAPDirectory entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct LDAPDirectoryRepository(
    backbone_orm::GenericCrudRepository<LDAPDirectory, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for LDAPDirectoryRepository {
    type Target = backbone_orm::GenericCrudRepository<LDAPDirectory, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl LDAPDirectoryRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl LDAPDirectoryRepository {
    /// Find ldap_directory by name (excludes soft-deleted)
    pub async fn find_by_name(&self, name: &str) -> Result<Option<LDAPDirectory>> {
        self.find_by_text_field("name", name).await
    }

    /// Check if ldap_directory exists by name (excludes soft-deleted)
    pub async fn exists_by_name(&self, name: &str) -> Result<bool> {
        self.exists_by_text_field("name", name).await
    }

}

backbone_core::impl_crud_repository!(LDAPDirectoryRepository, LDAPDirectory, soft_delete);
