//! Repository for DirectPermissionGrant entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<DirectPermissionGrant, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::DirectPermissionGrant;

/// Table name for DirectPermissionGrant entities
pub const TABLE_NAME: &str = "direct_permission_grants";

/// Repository for DirectPermissionGrant entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct DirectPermissionGrantRepository(
    backbone_orm::GenericCrudRepository<DirectPermissionGrant, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for DirectPermissionGrantRepository {
    type Target = backbone_orm::GenericCrudRepository<DirectPermissionGrant, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl DirectPermissionGrantRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(DirectPermissionGrantRepository, DirectPermissionGrant, soft_delete);
