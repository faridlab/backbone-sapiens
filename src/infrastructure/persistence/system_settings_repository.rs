//! Repository for SystemSettings entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<SystemSettings, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::SystemSettings;

/// Table name for SystemSettings entities
pub const TABLE_NAME: &str = "system_settings";

/// Repository for SystemSettings entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct SystemSettingsRepository(
    backbone_orm::GenericCrudRepository<SystemSettings, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for SystemSettingsRepository {
    type Target = backbone_orm::GenericCrudRepository<SystemSettings, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl SystemSettingsRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl SystemSettingsRepository {
    /// Find system_settings by key (excludes soft-deleted)
    pub async fn find_by_key(&self, key: &str) -> Result<Option<SystemSettings>> {
        self.find_by_text_field("key", key).await
    }

    /// Check if system_settings exists by key (excludes soft-deleted)
    pub async fn exists_by_key(&self, key: &str) -> Result<bool> {
        self.exists_by_text_field("key", key).await
    }

}

backbone_core::impl_crud_repository!(SystemSettingsRepository, SystemSettings, soft_delete);
