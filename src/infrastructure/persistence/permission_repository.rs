//! Repository for Permission entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<Permission, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::Permission;

/// Table name for Permission entities
pub const TABLE_NAME: &str = "permissions";

/// Repository for Permission entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct PermissionRepository(
    backbone_orm::GenericCrudRepository<Permission, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for PermissionRepository {
    type Target = backbone_orm::GenericCrudRepository<Permission, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl PermissionRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

impl PermissionRepository {
    /// Find permission by name (excludes soft-deleted)
    pub async fn find_by_name(&self, name: &str) -> Result<Option<Permission>> {
        self.find_by_text_field("name", name).await
    }

    /// Check if permission exists by name (excludes soft-deleted)
    pub async fn exists_by_name(&self, name: &str) -> Result<bool> {
        self.exists_by_text_field("name", name).await
    }

}

backbone_core::impl_crud_repository!(PermissionRepository, Permission, soft_delete);
