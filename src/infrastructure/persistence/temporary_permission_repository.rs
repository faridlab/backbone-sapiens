//! Repository for TemporaryPermission entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Thin newtype over `backbone_orm::GenericCrudRepository<TemporaryPermission, backbone_orm::SoftDelete>`.
//! All standard CRUD methods are available via `Deref`.

use anyhow::Result;
use sqlx::PgPool;

use crate::domain::entity::TemporaryPermission;

/// Table name for TemporaryPermission entities
pub const TABLE_NAME: &str = "temporary_permissions";

/// Repository for TemporaryPermission entities.
///
/// All standard CRUD, soft-delete, pagination, and bulk methods are
/// provided automatically via `Deref` to `backbone_orm::GenericCrudRepository`.
pub struct TemporaryPermissionRepository(
    backbone_orm::GenericCrudRepository<TemporaryPermission, backbone_orm::SoftDelete>,
);

impl std::ops::Deref for TemporaryPermissionRepository {
    type Target = backbone_orm::GenericCrudRepository<TemporaryPermission, backbone_orm::SoftDelete>;
    fn deref(&self) -> &Self::Target { &self.0 }
}

impl TemporaryPermissionRepository {
    /// Create a new repository instance.
    pub fn new(pool: PgPool) -> Self {
        Self(backbone_orm::GenericCrudRepository::new(pool, TABLE_NAME))
    }
}

backbone_core::impl_crud_repository!(TemporaryPermissionRepository, TemporaryPermission, soft_delete);
