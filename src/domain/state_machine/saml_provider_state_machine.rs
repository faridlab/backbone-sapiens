//! State machine for SAMLProvider workflow
//!
//! Generated by metaphor-schema

use serde::{Deserialize, Serialize};
use std::str::FromStr;

/// States for SAMLProvider workflow
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum SAMLProviderState {
    /// Initial state
    Draft,
    Active,
    /// Final state
    Inactive,
}

impl Default for SAMLProviderState {
    fn default() -> Self {
        Self::Draft
    }
}

impl std::fmt::Display for SAMLProviderState {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Draft => write!(f, "draft"),
            Self::Active => write!(f, "active"),
            Self::Inactive => write!(f, "inactive"),
        }
    }
}

impl FromStr for SAMLProviderState {
    type Err = StateMachineError;

    fn from_str(s: &str) -> Result<Self, Self::Err> {
        match s.to_lowercase().as_str() {
            "draft" => Ok(Self::Draft),
            "active" => Ok(Self::Active),
            "inactive" => Ok(Self::Inactive),
            _ => Err(StateMachineError::InvalidState(s.to_string())),
        }
    }
}

impl SAMLProviderState {
    /// Check if this is the initial state
    pub fn is_initial(&self) -> bool {
        matches!(self, Self::Draft)
    }

    /// Check if this is a final state
    pub fn is_final(&self) -> bool {
        matches!(self, Self::Inactive)
    }

    /// Get all possible states
    pub fn all() -> Vec<Self> {
        vec![
            Self::Draft,
            Self::Active,
            Self::Inactive,
        ]
    }
}

/// Transitions for SAMLProvider workflow
#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum SAMLProviderTransition {
    /// draft, inactive -> active
    Activate,
    /// active -> inactive
    Deactivate,
}

impl std::fmt::Display for SAMLProviderTransition {
    fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        match self {
            Self::Activate => write!(f, "activate"),
            Self::Deactivate => write!(f, "deactivate"),
        }
    }
}

impl FromStr for SAMLProviderTransition {
    type Err = StateMachineError;

    fn from_str(s: &str) -> Result<Self, Self::Err> {
        match s.to_lowercase().as_str() {
            "activate" => Ok(Self::Activate),
            "deactivate" => Ok(Self::Deactivate),
            _ => Err(StateMachineError::InvalidTransition(s.to_string())),
        }
    }
}

impl SAMLProviderTransition {
    /// Get the target state of this transition
    pub fn target_state(&self) -> SAMLProviderState {
        match self {
            Self::Activate => SAMLProviderState::Active,
            Self::Deactivate => SAMLProviderState::Inactive,
        }
    }

    /// Get all transitions
    pub fn all() -> Vec<Self> {
        vec![
            Self::Activate,
            Self::Deactivate,
        ]
    }

    /// Get allowed roles for this transition
    pub fn allowed_roles(&self) -> &'static [&'static str] {
        match self {
            Self::Activate => &["admin"],
            Self::Deactivate => &["admin"],
        }
    }
}

use super::StateMachineError;

/// State machine for SAMLProvider workflow
#[derive(Debug, Clone)]
pub struct SAMLProviderStateMachine {
    current_state: SAMLProviderState,
}

impl SAMLProviderStateMachine {
    /// Create a new state machine with initial state
    pub fn new() -> Self {
        Self {
            current_state: SAMLProviderState::default(),
        }
    }

    /// Create from an existing state
    pub fn from_state(state: SAMLProviderState) -> Self {
        Self { current_state: state }
    }

    /// Get the current state
    pub fn current_state(&self) -> SAMLProviderState {
        self.current_state
    }

    /// Check if a transition is allowed from the current state
    pub fn can_transition(&self, transition: SAMLProviderTransition) -> bool {
        match (self.current_state, transition) {
            (SAMLProviderState::Draft, SAMLProviderTransition::Activate) => true,
            (SAMLProviderState::Inactive, SAMLProviderTransition::Activate) => true,
            (SAMLProviderState::Active, SAMLProviderTransition::Deactivate) => true,
            _ => false,
        }
    }

    /// Check if a transition is allowed for a given role
    pub fn can_transition_with_role(&self, transition: SAMLProviderTransition, role: &str) -> bool {
        if !self.can_transition(transition) {
            return false;
        }

        let allowed_roles = transition.allowed_roles();
        if allowed_roles.is_empty() {
            return true; // No role restriction
        }

        allowed_roles.iter().any(|r| *r == role || *r == "*")
    }

    /// Apply a transition, returning the new state
    pub fn transition(&mut self, transition: SAMLProviderTransition) -> Result<SAMLProviderState, StateMachineError> {
        if !self.can_transition(transition) {
            return Err(StateMachineError::TransitionNotAllowed {
                transition: transition.to_string(),
                from: self.current_state.to_string(),
            });
        }

        self.current_state = transition.target_state();
        Ok(self.current_state)
    }

    /// Apply a transition with role check
    pub fn transition_with_role(&mut self, transition: SAMLProviderTransition, role: &str) -> Result<SAMLProviderState, StateMachineError> {
        // Check basic transition validity first
        if !self.can_transition(transition) {
            return Err(StateMachineError::TransitionNotAllowed {
                transition: transition.to_string(),
                from: self.current_state.to_string(),
            });
        }

        // Check role authorization
        if !self.can_transition_with_role(transition, role) {
            return Err(StateMachineError::RoleNotAuthorized {
                role: role.to_string(),
                transition: transition.to_string(),
            });
        }

        self.current_state = transition.target_state();
        Ok(self.current_state)
    }

    /// Get all available transitions from the current state
    pub fn available_transitions(&self) -> Vec<SAMLProviderTransition> {
        SAMLProviderTransition::all()
            .into_iter()
            .filter(|t| self.can_transition(*t))
            .collect()
    }

    /// Get all available transitions for a given role
    pub fn available_transitions_for_role(&self, role: &str) -> Vec<SAMLProviderTransition> {
        SAMLProviderTransition::all()
            .into_iter()
            .filter(|t| self.can_transition_with_role(*t, role))
            .collect()
    }

    /// Attempt to transition directly to a target state.
    ///
    /// Finds any transition that leads to the target state and applies it.
    /// Returns Err if no valid transition leads from current state to target.
    pub fn transition_to_state(&mut self, target: SAMLProviderState) -> Result<SAMLProviderState, StateMachineError> {
        let valid = SAMLProviderTransition::all().into_iter()
            .filter(|t| self.can_transition(*t))
            .find(|t| t.target_state() == target);
        match valid {
            Some(t) => self.transition(t),
            None => Err(StateMachineError::TransitionNotAllowed {
                transition: target.to_string(),
                from: self.current_state.to_string(),
            }),
        }
    }
}

impl Default for SAMLProviderStateMachine {
    fn default() -> Self {
        Self::new()
    }
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test_initial_state() {
        let sm = SAMLProviderStateMachine::new();
        assert_eq!(sm.current_state(), SAMLProviderState::Draft);
        assert!(sm.current_state().is_initial());
    }

    #[test]
    fn test_valid_transition() {
        let mut sm = SAMLProviderStateMachine::from_state(SAMLProviderState::Draft);
        assert!(sm.can_transition(SAMLProviderTransition::Activate));
        let result = sm.transition(SAMLProviderTransition::Activate);
        assert!(result.is_ok());
        assert_eq!(sm.current_state(), SAMLProviderState::Active);
    }

    #[test]
    fn test_invalid_transition() {
        let mut sm = SAMLProviderStateMachine::from_state(SAMLProviderState::Draft);
        // Deactivate is not valid from Draft state
        let result = sm.transition(SAMLProviderTransition::Deactivate);
        assert!(result.is_err());
    }

    #[test]
    fn test_state_parsing() {
        let state: SAMLProviderState = "draft".parse().unwrap();
        assert_eq!(state, SAMLProviderState::Draft);
    }

    #[test]
    fn test_available_transitions() {
        let sm = SAMLProviderStateMachine::new();
        let available = sm.available_transitions();
        // Should have at least some transitions available from initial state
        assert!(!available.is_empty() || sm.current_state().is_final());
    }
}
