//! Domain policy for UserOAuthLink aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::UserOAuthLink;

/// Domain policy for UserOAuthLink — enforces business invariants.
pub struct UserOAuthLinkDomainPolicy;

#[async_trait]
impl DomainPolicy<UserOAuthLink> for UserOAuthLinkDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &UserOAuthLink) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked user_o_auth_link cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}