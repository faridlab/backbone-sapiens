//! Domain policy for ImpersonationSession aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::ImpersonationSession;

/// Domain policy for ImpersonationSession — enforces business invariants.
pub struct ImpersonationSessionDomainPolicy;

#[async_trait]
impl DomainPolicy<ImpersonationSession> for ImpersonationSessionDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &ImpersonationSession) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked impersonation_session cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}