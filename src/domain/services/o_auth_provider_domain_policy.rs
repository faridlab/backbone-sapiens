//! Domain policy for OAuthProvider aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::OAuthProvider;

/// Domain policy for OAuthProvider — enforces business invariants.
pub struct OAuthProviderDomainPolicy;

#[async_trait]
impl DomainPolicy<OAuthProvider> for OAuthProviderDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &OAuthProvider) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked o_auth_provider cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}