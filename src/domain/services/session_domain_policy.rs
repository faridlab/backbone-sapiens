//! Domain policy for Session aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::Session;

/// Domain policy for Session — enforces business invariants.
pub struct SessionDomainPolicy;

#[async_trait]
impl DomainPolicy<Session> for SessionDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &Session) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked session cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}