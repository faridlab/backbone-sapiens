//! Domain policy for MFADevice aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::MFADevice;

/// Domain policy for MFADevice — enforces business invariants.
pub struct MFADeviceDomainPolicy;

#[async_trait]
impl DomainPolicy<MFADevice> for MFADeviceDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &MFADevice) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked mfa_device cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}