//! Domain policy for BackupCode aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::BackupCode;

/// Domain policy for BackupCode — enforces business invariants.
pub struct BackupCodeDomainPolicy;

#[async_trait]
impl DomainPolicy<BackupCode> for BackupCodeDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &BackupCode) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked backup_code cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}