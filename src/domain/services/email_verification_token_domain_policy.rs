//! Domain policy for EmailVerificationToken aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::EmailVerificationToken;

/// Domain policy for EmailVerificationToken — enforces business invariants.
pub struct EmailVerificationTokenDomainPolicy;

#[async_trait]
impl DomainPolicy<EmailVerificationToken> for EmailVerificationTokenDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &EmailVerificationToken) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked email_verification_token cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}