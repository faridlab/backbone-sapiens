//! Domain policy for DataExport aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::DataExport;

/// Domain policy for DataExport — enforces business invariants.
pub struct DataExportDomainPolicy;

#[async_trait]
impl DomainPolicy<DataExport> for DataExportDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &DataExport) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked data_export cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}