//! Domain policy for AuditLog aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::AuditLog;

/// Domain policy for AuditLog — enforces business invariants.
pub struct AuditLogDomainPolicy;

#[async_trait]
impl DomainPolicy<AuditLog> for AuditLogDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &AuditLog) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked audit_log cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}