//! Domain policy for NotificationPreference aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::NotificationPreference;

/// Domain policy for NotificationPreference — enforces business invariants.
pub struct NotificationPreferenceDomainPolicy;

#[async_trait]
impl DomainPolicy<NotificationPreference> for NotificationPreferenceDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &NotificationPreference) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked notification_preference cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}