//! Domain policy for SessionLimit aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::SessionLimit;

/// Domain policy for SessionLimit — enforces business invariants.
pub struct SessionLimitDomainPolicy;

#[async_trait]
impl DomainPolicy<SessionLimit> for SessionLimitDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &SessionLimit) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked session_limit cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}