//! Domain policy for Role aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::Role;

/// Domain policy for Role — enforces business invariants.
pub struct RoleDomainPolicy;

#[async_trait]
impl DomainPolicy<Role> for RoleDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &Role) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked role cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}