//! Domain policy for SecurityEvent aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::SecurityEvent;

/// Domain policy for SecurityEvent — enforces business invariants.
pub struct SecurityEventDomainPolicy;

#[async_trait]
impl DomainPolicy<SecurityEvent> for SecurityEventDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &SecurityEvent) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked security_event cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}