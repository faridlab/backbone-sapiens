//! Domain policy for Workflow aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::Workflow;

/// Domain policy for Workflow — enforces business invariants.
pub struct WorkflowDomainPolicy;

#[async_trait]
impl DomainPolicy<Workflow> for WorkflowDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &Workflow) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked workflow cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}