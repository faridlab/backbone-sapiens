//! Domain policy for SAMLProvider aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::SAMLProvider;

/// Domain policy for SAMLProvider — enforces business invariants.
pub struct SAMLProviderDomainPolicy;

#[async_trait]
impl DomainPolicy<SAMLProvider> for SAMLProviderDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &SAMLProvider) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked saml_provider cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}