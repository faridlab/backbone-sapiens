//! Domain policy for Permission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::Permission;

/// Domain policy for Permission — enforces business invariants.
pub struct PermissionDomainPolicy;

#[async_trait]
impl DomainPolicy<Permission> for PermissionDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &Permission) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked permission cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}