//! Domain policy for PasswordResetToken aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::PasswordResetToken;

/// Domain policy for PasswordResetToken — enforces business invariants.
pub struct PasswordResetTokenDomainPolicy;

#[async_trait]
impl DomainPolicy<PasswordResetToken> for PasswordResetTokenDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &PasswordResetToken) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked password_reset_token cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}