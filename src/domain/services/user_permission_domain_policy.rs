//! Domain policy for UserPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::UserPermission;

/// Domain policy for UserPermission — enforces business invariants.
pub struct UserPermissionDomainPolicy;

#[async_trait]
impl DomainPolicy<UserPermission> for UserPermissionDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &UserPermission) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked user_permission cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}