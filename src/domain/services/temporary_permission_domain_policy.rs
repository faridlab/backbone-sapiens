//! Domain policy for TemporaryPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::TemporaryPermission;

/// Domain policy for TemporaryPermission — enforces business invariants.
pub struct TemporaryPermissionDomainPolicy;

#[async_trait]
impl DomainPolicy<TemporaryPermission> for TemporaryPermissionDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &TemporaryPermission) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked temporary_permission cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}