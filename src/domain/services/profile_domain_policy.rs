//! Domain policy for Profile aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::Profile;

/// Domain policy for Profile — enforces business invariants.
pub struct ProfileDomainPolicy;

#[async_trait]
impl DomainPolicy<Profile> for ProfileDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &Profile) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked profile cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}