//! Domain service for OAuthProvider aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This service contains domain business logic for OAuthProvider operations.
//! Add your custom business rules and domain operations here.

// ============================================================================
// IMPORTS - Add custom imports below
// ============================================================================

use anyhow::{Result, anyhow};

use crate::OAuthProvider;
// <<< CUSTOM IMPORTS START >>>

// <<< CUSTOM IMPORTS START >>>
// Add your custom imports here
// <<< CUSTOM IMPORTS END >>>

// ============================================================================
// DOMAIN SERVICE
// ============================================================================

/// Domain service for OAuthProvider business operations
///
/// This service contains:
/// - Business validation rules
/// - Domain operations that span multiple entities
/// - Complex business logic
///
/// Note: This is a stateless service. State should be in entities.
pub struct OAuthProviderDomainService;

impl OAuthProviderDomainService {
    /// Create a new domain service instance
    pub fn new() -> Self {
        Self
    }

    // =========================================================================
    // Generated Business Operations
    // =========================================================================

    /// Validate a o_auth_provider entity for business rules
    ///
    /// Override this method to add custom validation logic.
    pub fn validate(&self, entity: &OAuthProvider) -> Result<()> {
        // Default validation - check required fields
        if entity.display_name.is_empty() {
            return Err(anyhow!("display_name is required"));
        }
        if entity.client_id.is_empty() {
            return Err(anyhow!("client_id is required"));
        }
        if entity.client_secret.is_empty() {
            return Err(anyhow!("client_secret is required"));
        }
        if entity.redirect_uri.is_empty() {
            return Err(anyhow!("redirect_uri is required"));
        }
        if entity.authorization_url.is_empty() {
            return Err(anyhow!("authorization_url is required"));
        }
        if entity.token_url.is_empty() {
            return Err(anyhow!("token_url is required"));
        }
        if entity.user_info_url.is_empty() {
            return Err(anyhow!("user_info_url is required"));
        }
        Ok(())
    }

    /// Check if a o_auth_provider can be created
    ///
    /// Add custom business rules that must be satisfied before creation.
    pub fn can_create(&self, entity: &OAuthProvider) -> Result<bool> {
        self.validate(entity)?;
        Ok(true)
    }

    /// Check if a o_auth_provider can be updated
    pub fn can_update(&self, _current: &OAuthProvider, _updated: &OAuthProvider) -> Result<bool> {
        // Add update permission/business logic here
        Ok(true)
    }

    /// Check if a o_auth_provider can be deleted
    pub fn can_delete(&self, _entity: &OAuthProvider) -> Result<bool> {
        // Add deletion permission/business logic here
        Ok(true)
    }

    // =========================================================================
    // Custom Business Operations
    // =========================================================================
    // <<< CUSTOM OPERATIONS START >>>

    // <<< CUSTOM OPERATIONS START >>>
    // Add your custom domain operations here
    // Example:
    // pub fn calculate_something(&self, entity: &OAuthProvider) -> Result<i32> {
    //     // Business logic here
    //     Ok(0)
    // }
    // <<< CUSTOM OPERATIONS END >>>
}

impl Default for OAuthProviderDomainService {
    fn default() -> Self {
        Self::new()
    }
}