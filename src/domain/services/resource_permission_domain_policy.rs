//! Domain policy for ResourcePermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::ResourcePermission;

/// Domain policy for ResourcePermission — enforces business invariants.
pub struct ResourcePermissionDomainPolicy;

#[async_trait]
impl DomainPolicy<ResourcePermission> for ResourcePermissionDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &ResourcePermission) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked resource_permission cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}