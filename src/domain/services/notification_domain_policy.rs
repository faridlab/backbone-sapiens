//! Domain policy for Notification aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::Notification;

/// Domain policy for Notification — enforces business invariants.
pub struct NotificationDomainPolicy;

#[async_trait]
impl DomainPolicy<Notification> for NotificationDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &Notification) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked notification cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}