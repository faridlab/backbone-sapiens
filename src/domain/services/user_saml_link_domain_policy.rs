//! Domain policy for UserSAMLLink aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::UserSAMLLink;

/// Domain policy for UserSAMLLink — enforces business invariants.
pub struct UserSAMLLinkDomainPolicy;

#[async_trait]
impl DomainPolicy<UserSAMLLink> for UserSAMLLinkDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &UserSAMLLink) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked user_saml_link cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}