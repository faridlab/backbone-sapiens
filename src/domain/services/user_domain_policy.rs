//! Domain policy for User aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::User;

/// Domain policy for User — enforces business invariants.
pub struct UserDomainPolicy;

#[async_trait]
impl DomainPolicy<User> for UserDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &User) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked user cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}