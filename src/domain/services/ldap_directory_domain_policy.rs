//! Domain policy for LDAPDirectory aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::LDAPDirectory;

/// Domain policy for LDAPDirectory — enforces business invariants.
pub struct LDAPDirectoryDomainPolicy;

#[async_trait]
impl DomainPolicy<LDAPDirectory> for LDAPDirectoryDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &LDAPDirectory) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked ldap_directory cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}