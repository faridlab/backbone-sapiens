//! Domain policy for DeviceTrust aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! `DomainPolicy` enforces pure business invariants (state-based rules).
//! Identity-based rules live in `backbone_auth::ResourcePolicy`.

use async_trait::async_trait;
use backbone_core::{DomainPolicy, PolicyDecision};
use crate::domain::entity::DeviceTrust;

/// Domain policy for DeviceTrust — enforces business invariants.
pub struct DeviceTrustDomainPolicy;

#[async_trait]
impl DomainPolicy<DeviceTrust> for DeviceTrustDomainPolicy {
    // <<< CUSTOM
    // Add entity-specific invariant checks here.
    // Example:
    // async fn can_delete(&self, entity: &DeviceTrust) -> PolicyDecision {
    //     if entity.is_locked {
    //         Err("locked device_trust cannot be deleted".into())
    //     } else { Ok(true) }
    // }
    // END CUSTOM
}