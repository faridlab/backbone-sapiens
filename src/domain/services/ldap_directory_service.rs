//! Domain service for LDAPDirectory aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This service contains domain business logic for LDAPDirectory operations.
//! Add your custom business rules and domain operations here.

// ============================================================================
// IMPORTS - Add custom imports below
// ============================================================================

use anyhow::{Result, anyhow};

use crate::LDAPDirectory;
// <<< CUSTOM IMPORTS START >>>

// <<< CUSTOM IMPORTS START >>>
// Add your custom imports here
// <<< CUSTOM IMPORTS END >>>

// ============================================================================
// DOMAIN SERVICE
// ============================================================================

/// Domain service for LDAPDirectory business operations
///
/// This service contains:
/// - Business validation rules
/// - Domain operations that span multiple entities
/// - Complex business logic
///
/// Note: This is a stateless service. State should be in entities.
pub struct LDAPDirectoryDomainService;

impl LDAPDirectoryDomainService {
    /// Create a new domain service instance
    pub fn new() -> Self {
        Self
    }

    // =========================================================================
    // Generated Business Operations
    // =========================================================================

    /// Validate a ldap_directory entity for business rules
    ///
    /// Override this method to add custom validation logic.
    pub fn validate(&self, entity: &LDAPDirectory) -> Result<()> {
        // Default validation - check required fields
        if entity.name.is_empty() {
            return Err(anyhow!("name is required"));
        }
        if entity.display_name.is_empty() {
            return Err(anyhow!("display_name is required"));
        }
        if entity.host.is_empty() {
            return Err(anyhow!("host is required"));
        }
        if entity.bind_dn.is_empty() {
            return Err(anyhow!("bind_dn is required"));
        }
        if entity.bind_password.is_empty() {
            return Err(anyhow!("bind_password is required"));
        }
        if entity.search_base.is_empty() {
            return Err(anyhow!("search_base is required"));
        }
        if entity.search_filter.is_empty() {
            return Err(anyhow!("search_filter is required"));
        }
        Ok(())
    }

    /// Check if a ldap_directory can be created
    ///
    /// Add custom business rules that must be satisfied before creation.
    pub fn can_create(&self, entity: &LDAPDirectory) -> Result<bool> {
        self.validate(entity)?;
        Ok(true)
    }

    /// Check if a ldap_directory can be updated
    pub fn can_update(&self, _current: &LDAPDirectory, _updated: &LDAPDirectory) -> Result<bool> {
        // Add update permission/business logic here
        Ok(true)
    }

    /// Check if a ldap_directory can be deleted
    pub fn can_delete(&self, _entity: &LDAPDirectory) -> Result<bool> {
        // Add deletion permission/business logic here
        Ok(true)
    }

    // =========================================================================
    // Custom Business Operations
    // =========================================================================
    // <<< CUSTOM OPERATIONS START >>>

    // <<< CUSTOM OPERATIONS START >>>
    // Add your custom domain operations here
    // Example:
    // pub fn calculate_something(&self, entity: &LDAPDirectory) -> Result<i32> {
    //     // Business logic here
    //     Ok(0)
    // }
    // <<< CUSTOM OPERATIONS END >>>
}

impl Default for LDAPDirectoryDomainService {
    fn default() -> Self {
        Self::new()
    }
}