//! Repository trait for NotificationLog aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the NotificationLog aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{NotificationLog, NotificationChannel, NotificationLogStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type NotificationLogPaginationParams = DomainPaginationParams;
pub type NotificationLogPaginatedResult = DomainPaginatedResult<NotificationLog>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct NotificationLogFilter {
    pub notification_id: Option<Uuid>,
    pub template_id: Option<Uuid>,
    pub recipient_type: Option<String>,
    pub recipient_id: Option<Uuid>,
    pub channel: Option<NotificationChannel>,
    pub status: Option<NotificationLogStatus>,
    pub error_message: Option<String>,
    pub external_id: Option<String>,
}

impl NotificationLogFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.notification_id.is_some() || self.template_id.is_some() || self.recipient_type.is_some() || self.recipient_id.is_some() || self.channel.is_some() || self.status.is_some() || self.error_message.is_some() || self.external_id.is_some()
    }
}

/// Repository trait for NotificationLog aggregate
///
/// This trait defines the persistence contract for NotificationLog entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait NotificationLogRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new notification_log entity
    async fn save(&self, entity: &NotificationLog) -> Result<NotificationLog>;

    /// Find notification_log by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<NotificationLog>>;

    /// Find all notification_log entities
    async fn find_all(&self) -> Result<Vec<NotificationLog>>;

    /// Update notification_log by ID
    async fn update(&self, id: &str, entity: &NotificationLog) -> Result<Option<NotificationLog>>;

    /// Delete notification_log by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List notification_log with pagination
    async fn list(&self, params: NotificationLogPaginationParams) -> Result<NotificationLogPaginatedResult>;

    /// List notification_log with pagination and filters
    async fn list_with_filters(&self, params: NotificationLogPaginationParams, filters: NotificationLogFilter) -> Result<NotificationLogPaginatedResult>;

    /// Count all notification_log entities
    async fn count(&self) -> Result<u64>;

    /// Count notification_log entities matching filters
    async fn count_with_filters(&self, filters: NotificationLogFilter) -> Result<u64>;

    /// Check if notification_log exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete notification_log by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted notification_log by ID
    async fn restore(&self, id: &str) -> Result<Option<NotificationLog>>;

    /// List soft-deleted notification_log entities
    async fn list_deleted(&self, params: NotificationLogPaginationParams) -> Result<NotificationLogPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save notification_log entities
    async fn bulk_save(&self, entities: &[NotificationLog]) -> Result<Vec<NotificationLog>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
