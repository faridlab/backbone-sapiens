//! Repository trait for AnonymizationRecord aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the AnonymizationRecord aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{AnonymizationRecord, AnonymizationMethod, AnonymizationStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type AnonymizationRecordPaginationParams = DomainPaginationParams;
pub type AnonymizationRecordPaginatedResult = DomainPaginatedResult<AnonymizationRecord>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AnonymizationRecordFilter {
    pub user_id: Option<Uuid>,
    pub original_email: Option<String>,
    pub original_username: Option<String>,
    pub anonymized_by: Option<Uuid>,
    pub reason: Option<String>,
    pub method: Option<AnonymizationMethod>,
    pub status: Option<AnonymizationStatus>,
}

impl AnonymizationRecordFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.original_email.is_some() || self.original_username.is_some() || self.anonymized_by.is_some() || self.reason.is_some() || self.method.is_some() || self.status.is_some()
    }
}

/// Repository trait for AnonymizationRecord aggregate
///
/// This trait defines the persistence contract for AnonymizationRecord entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait AnonymizationRecordRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new anonymization_record entity
    async fn save(&self, entity: &AnonymizationRecord) -> Result<AnonymizationRecord>;

    /// Find anonymization_record by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<AnonymizationRecord>>;

    /// Find all anonymization_record entities
    async fn find_all(&self) -> Result<Vec<AnonymizationRecord>>;

    /// Update anonymization_record by ID
    async fn update(&self, id: &str, entity: &AnonymizationRecord) -> Result<Option<AnonymizationRecord>>;

    /// Delete anonymization_record by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List anonymization_record with pagination
    async fn list(&self, params: AnonymizationRecordPaginationParams) -> Result<AnonymizationRecordPaginatedResult>;

    /// List anonymization_record with pagination and filters
    async fn list_with_filters(&self, params: AnonymizationRecordPaginationParams, filters: AnonymizationRecordFilter) -> Result<AnonymizationRecordPaginatedResult>;

    /// Count all anonymization_record entities
    async fn count(&self) -> Result<u64>;

    /// Count anonymization_record entities matching filters
    async fn count_with_filters(&self, filters: AnonymizationRecordFilter) -> Result<u64>;

    /// Check if anonymization_record exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete anonymization_record by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted anonymization_record by ID
    async fn restore(&self, id: &str) -> Result<Option<AnonymizationRecord>>;

    /// List soft-deleted anonymization_record entities
    async fn list_deleted(&self, params: AnonymizationRecordPaginationParams) -> Result<AnonymizationRecordPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save anonymization_record entities
    async fn bulk_save(&self, entities: &[AnonymizationRecord]) -> Result<Vec<AnonymizationRecord>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
