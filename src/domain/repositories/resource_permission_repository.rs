//! Repository trait for ResourcePermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ResourcePermission aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{ResourcePermission, ResourcePermissionStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type ResourcePermissionPaginationParams = DomainPaginationParams;
pub type ResourcePermissionPaginatedResult = DomainPaginatedResult<ResourcePermission>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ResourcePermissionFilter {
    pub permission_id: Option<Uuid>,
    pub resource_type: Option<String>,
    pub resource_id: Option<String>,
    pub granted_to_user_id: Option<Uuid>,
    pub granted_to_role_id: Option<Uuid>,
    pub granted_by: Option<Uuid>,
    pub reason: Option<String>,
    pub status: Option<ResourcePermissionStatus>,
}

impl ResourcePermissionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.permission_id.is_some() || self.resource_type.is_some() || self.resource_id.is_some() || self.granted_to_user_id.is_some() || self.granted_to_role_id.is_some() || self.granted_by.is_some() || self.reason.is_some() || self.status.is_some()
    }
}

/// Repository trait for ResourcePermission aggregate
///
/// This trait defines the persistence contract for ResourcePermission entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ResourcePermissionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new resource_permission entity
    async fn save(&self, entity: &ResourcePermission) -> Result<ResourcePermission>;

    /// Find resource_permission by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ResourcePermission>>;

    /// Find all resource_permission entities
    async fn find_all(&self) -> Result<Vec<ResourcePermission>>;

    /// Update resource_permission by ID
    async fn update(&self, id: &str, entity: &ResourcePermission) -> Result<Option<ResourcePermission>>;

    /// Delete resource_permission by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List resource_permission with pagination
    async fn list(&self, params: ResourcePermissionPaginationParams) -> Result<ResourcePermissionPaginatedResult>;

    /// List resource_permission with pagination and filters
    async fn list_with_filters(&self, params: ResourcePermissionPaginationParams, filters: ResourcePermissionFilter) -> Result<ResourcePermissionPaginatedResult>;

    /// Count all resource_permission entities
    async fn count(&self) -> Result<u64>;

    /// Count resource_permission entities matching filters
    async fn count_with_filters(&self, filters: ResourcePermissionFilter) -> Result<u64>;

    /// Check if resource_permission exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete resource_permission by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted resource_permission by ID
    async fn restore(&self, id: &str) -> Result<Option<ResourcePermission>>;

    /// List soft-deleted resource_permission entities
    async fn list_deleted(&self, params: ResourcePermissionPaginationParams) -> Result<ResourcePermissionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save resource_permission entities
    async fn bulk_save(&self, entities: &[ResourcePermission]) -> Result<Vec<ResourcePermission>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
