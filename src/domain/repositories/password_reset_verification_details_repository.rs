//! Repository trait for PasswordResetVerificationDetails aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PasswordResetVerificationDetails aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::PasswordResetVerificationDetails;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PasswordResetVerificationDetailsPaginationParams = DomainPaginationParams;
pub type PasswordResetVerificationDetailsPaginatedResult = DomainPaginatedResult<PasswordResetVerificationDetails>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PasswordResetVerificationDetailsFilter {
    pub verification_method: Option<String>,
    pub device_fingerprint: Option<String>,
}

impl PasswordResetVerificationDetailsFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.verification_method.is_some() || self.device_fingerprint.is_some()
    }
}

/// Repository trait for PasswordResetVerificationDetails aggregate
///
/// This trait defines the persistence contract for PasswordResetVerificationDetails entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PasswordResetVerificationDetailsRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new password_reset_verification_details entity
    async fn save(&self, entity: &PasswordResetVerificationDetails) -> Result<PasswordResetVerificationDetails>;

    /// Find password_reset_verification_details by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PasswordResetVerificationDetails>>;

    /// Find all password_reset_verification_details entities
    async fn find_all(&self) -> Result<Vec<PasswordResetVerificationDetails>>;

    /// Update password_reset_verification_details by ID
    async fn update(&self, id: &str, entity: &PasswordResetVerificationDetails) -> Result<Option<PasswordResetVerificationDetails>>;

    /// Delete password_reset_verification_details by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List password_reset_verification_details with pagination
    async fn list(&self, params: PasswordResetVerificationDetailsPaginationParams) -> Result<PasswordResetVerificationDetailsPaginatedResult>;

    /// List password_reset_verification_details with pagination and filters
    async fn list_with_filters(&self, params: PasswordResetVerificationDetailsPaginationParams, filters: PasswordResetVerificationDetailsFilter) -> Result<PasswordResetVerificationDetailsPaginatedResult>;

    /// Count all password_reset_verification_details entities
    async fn count(&self) -> Result<u64>;

    /// Count password_reset_verification_details entities matching filters
    async fn count_with_filters(&self, filters: PasswordResetVerificationDetailsFilter) -> Result<u64>;

    /// Check if password_reset_verification_details exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete password_reset_verification_details by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted password_reset_verification_details by ID
    async fn restore(&self, id: &str) -> Result<Option<PasswordResetVerificationDetails>>;

    /// List soft-deleted password_reset_verification_details entities
    async fn list_deleted(&self, params: PasswordResetVerificationDetailsPaginationParams) -> Result<PasswordResetVerificationDetailsPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save password_reset_verification_details entities
    async fn bulk_save(&self, entities: &[PasswordResetVerificationDetails]) -> Result<Vec<PasswordResetVerificationDetails>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
