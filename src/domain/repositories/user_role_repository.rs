//! Repository trait for UserRole aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the UserRole aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::UserRole;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type UserRolePaginationParams = DomainPaginationParams;
pub type UserRolePaginatedResult = DomainPaginatedResult<UserRole>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UserRoleFilter {
    pub user_id: Option<Uuid>,
    pub role_id: Option<Uuid>,
    pub assigned_by: Option<Uuid>,
}

impl UserRoleFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.role_id.is_some() || self.assigned_by.is_some()
    }
}

/// Repository trait for UserRole aggregate
///
/// This trait defines the persistence contract for UserRole entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait UserRoleRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new user_role entity
    async fn save(&self, entity: &UserRole) -> Result<UserRole>;

    /// Find user_role by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<UserRole>>;

    /// Find all user_role entities
    async fn find_all(&self) -> Result<Vec<UserRole>>;

    /// Update user_role by ID
    async fn update(&self, id: &str, entity: &UserRole) -> Result<Option<UserRole>>;

    /// Delete user_role by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List user_role with pagination
    async fn list(&self, params: UserRolePaginationParams) -> Result<UserRolePaginatedResult>;

    /// List user_role with pagination and filters
    async fn list_with_filters(&self, params: UserRolePaginationParams, filters: UserRoleFilter) -> Result<UserRolePaginatedResult>;

    /// Count all user_role entities
    async fn count(&self) -> Result<u64>;

    /// Count user_role entities matching filters
    async fn count_with_filters(&self, filters: UserRoleFilter) -> Result<u64>;

    /// Check if user_role exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete user_role by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted user_role by ID
    async fn restore(&self, id: &str) -> Result<Option<UserRole>>;

    /// List soft-deleted user_role entities
    async fn list_deleted(&self, params: UserRolePaginationParams) -> Result<UserRolePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save user_role entities
    async fn bulk_save(&self, entities: &[UserRole]) -> Result<Vec<UserRole>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
