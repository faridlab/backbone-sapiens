//! Repository trait for DataExport aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the DataExport aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{DataExport, DataExportFormat, DataExportStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type DataExportPaginationParams = DomainPaginationParams;
pub type DataExportPaginatedResult = DomainPaginatedResult<DataExport>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DataExportFilter {
    pub user_id: Option<Uuid>,
    pub requested_by: Option<Uuid>,
    pub status: Option<DataExportStatus>,
    pub file_path: Option<String>,
    pub file_url: Option<String>,
    pub format: Option<DataExportFormat>,
    pub justification: Option<String>,
}

impl DataExportFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.requested_by.is_some() || self.status.is_some() || self.file_path.is_some() || self.file_url.is_some() || self.format.is_some() || self.justification.is_some()
    }
}

/// Repository trait for DataExport aggregate
///
/// This trait defines the persistence contract for DataExport entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait DataExportRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new data_export entity
    async fn save(&self, entity: &DataExport) -> Result<DataExport>;

    /// Find data_export by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<DataExport>>;

    /// Find all data_export entities
    async fn find_all(&self) -> Result<Vec<DataExport>>;

    /// Update data_export by ID
    async fn update(&self, id: &str, entity: &DataExport) -> Result<Option<DataExport>>;

    /// Delete data_export by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List data_export with pagination
    async fn list(&self, params: DataExportPaginationParams) -> Result<DataExportPaginatedResult>;

    /// List data_export with pagination and filters
    async fn list_with_filters(&self, params: DataExportPaginationParams, filters: DataExportFilter) -> Result<DataExportPaginatedResult>;

    /// Count all data_export entities
    async fn count(&self) -> Result<u64>;

    /// Count data_export entities matching filters
    async fn count_with_filters(&self, filters: DataExportFilter) -> Result<u64>;

    /// Check if data_export exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete data_export by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted data_export by ID
    async fn restore(&self, id: &str) -> Result<Option<DataExport>>;

    /// List soft-deleted data_export entities
    async fn list_deleted(&self, params: DataExportPaginationParams) -> Result<DataExportPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save data_export entities
    async fn bulk_save(&self, entities: &[DataExport]) -> Result<Vec<DataExport>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
