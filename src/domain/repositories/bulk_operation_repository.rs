//! Repository trait for BulkOperation aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the BulkOperation aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{BulkOperation, BulkOperationStatus, BulkOperationType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type BulkOperationPaginationParams = DomainPaginationParams;
pub type BulkOperationPaginatedResult = DomainPaginatedResult<BulkOperation>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BulkOperationFilter {
    pub operation_type: Option<BulkOperationType>,
    pub status: Option<BulkOperationStatus>,
    pub input_file_path: Option<String>,
    pub output_file_path: Option<String>,
    pub error_file_path: Option<String>,
    pub error_message: Option<String>,
}

impl BulkOperationFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.operation_type.is_some() || self.status.is_some() || self.input_file_path.is_some() || self.output_file_path.is_some() || self.error_file_path.is_some() || self.error_message.is_some()
    }
}

/// Repository trait for BulkOperation aggregate
///
/// This trait defines the persistence contract for BulkOperation entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait BulkOperationRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new bulk_operation entity
    async fn save(&self, entity: &BulkOperation) -> Result<BulkOperation>;

    /// Find bulk_operation by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<BulkOperation>>;

    /// Find all bulk_operation entities
    async fn find_all(&self) -> Result<Vec<BulkOperation>>;

    /// Update bulk_operation by ID
    async fn update(&self, id: &str, entity: &BulkOperation) -> Result<Option<BulkOperation>>;

    /// Delete bulk_operation by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List bulk_operation with pagination
    async fn list(&self, params: BulkOperationPaginationParams) -> Result<BulkOperationPaginatedResult>;

    /// List bulk_operation with pagination and filters
    async fn list_with_filters(&self, params: BulkOperationPaginationParams, filters: BulkOperationFilter) -> Result<BulkOperationPaginatedResult>;

    /// Count all bulk_operation entities
    async fn count(&self) -> Result<u64>;

    /// Count bulk_operation entities matching filters
    async fn count_with_filters(&self, filters: BulkOperationFilter) -> Result<u64>;

    /// Check if bulk_operation exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete bulk_operation by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted bulk_operation by ID
    async fn restore(&self, id: &str) -> Result<Option<BulkOperation>>;

    /// List soft-deleted bulk_operation entities
    async fn list_deleted(&self, params: BulkOperationPaginationParams) -> Result<BulkOperationPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save bulk_operation entities
    async fn bulk_save(&self, entities: &[BulkOperation]) -> Result<Vec<BulkOperation>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
