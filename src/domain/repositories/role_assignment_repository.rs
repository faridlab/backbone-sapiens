//! Repository trait for RoleAssignment aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the RoleAssignment aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{RoleAssignment, ConflictResolutionType, RoleAssignmentStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type RoleAssignmentPaginationParams = DomainPaginationParams;
pub type RoleAssignmentPaginatedResult = DomainPaginatedResult<RoleAssignment>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RoleAssignmentFilter {
    pub user_id: Option<Uuid>,
    pub role_id: Option<Uuid>,
    pub assigned_by: Option<Uuid>,
    pub reason: Option<String>,
    pub parent_assignment_id: Option<Uuid>,
    pub inherited_from: Option<Uuid>,
    pub conflict_resolution: Option<ConflictResolutionType>,
    pub has_conflicts: Option<bool>,
    pub status: Option<RoleAssignmentStatus>,
    pub warning_sent: Option<bool>,
    pub auto_renew: Option<bool>,
}

impl RoleAssignmentFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.role_id.is_some() || self.assigned_by.is_some() || self.reason.is_some() || self.parent_assignment_id.is_some() || self.inherited_from.is_some() || self.conflict_resolution.is_some() || self.has_conflicts.is_some() || self.status.is_some() || self.warning_sent.is_some() || self.auto_renew.is_some()
    }
}

/// Repository trait for RoleAssignment aggregate
///
/// This trait defines the persistence contract for RoleAssignment entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RoleAssignmentRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new role_assignment entity
    async fn save(&self, entity: &RoleAssignment) -> Result<RoleAssignment>;

    /// Find role_assignment by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<RoleAssignment>>;

    /// Find all role_assignment entities
    async fn find_all(&self) -> Result<Vec<RoleAssignment>>;

    /// Update role_assignment by ID
    async fn update(&self, id: &str, entity: &RoleAssignment) -> Result<Option<RoleAssignment>>;

    /// Delete role_assignment by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List role_assignment with pagination
    async fn list(&self, params: RoleAssignmentPaginationParams) -> Result<RoleAssignmentPaginatedResult>;

    /// List role_assignment with pagination and filters
    async fn list_with_filters(&self, params: RoleAssignmentPaginationParams, filters: RoleAssignmentFilter) -> Result<RoleAssignmentPaginatedResult>;

    /// Count all role_assignment entities
    async fn count(&self) -> Result<u64>;

    /// Count role_assignment entities matching filters
    async fn count_with_filters(&self, filters: RoleAssignmentFilter) -> Result<u64>;

    /// Check if role_assignment exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete role_assignment by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted role_assignment by ID
    async fn restore(&self, id: &str) -> Result<Option<RoleAssignment>>;

    /// List soft-deleted role_assignment entities
    async fn list_deleted(&self, params: RoleAssignmentPaginationParams) -> Result<RoleAssignmentPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save role_assignment entities
    async fn bulk_save(&self, entities: &[RoleAssignment]) -> Result<Vec<RoleAssignment>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
