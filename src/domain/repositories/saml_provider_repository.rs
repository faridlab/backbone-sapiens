//! Repository trait for SAMLProvider aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SAMLProvider aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{SAMLProvider, SAMLProviderStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type SAMLProviderPaginationParams = DomainPaginationParams;
pub type SAMLProviderPaginatedResult = DomainPaginatedResult<SAMLProvider>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SAMLProviderFilter {
    pub name: Option<String>,
    pub display_name: Option<String>,
    pub entity_id: Option<String>,
    pub sso_url: Option<String>,
    pub slo_url: Option<String>,
    pub certificate: Option<String>,
    pub acs_url: Option<String>,
    pub sls_url: Option<String>,
    pub name_id_format: Option<String>,
    pub is_active: Option<bool>,
    pub status: Option<SAMLProviderStatus>,
}

impl SAMLProviderFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.display_name.is_some() || self.entity_id.is_some() || self.sso_url.is_some() || self.slo_url.is_some() || self.certificate.is_some() || self.acs_url.is_some() || self.sls_url.is_some() || self.name_id_format.is_some() || self.is_active.is_some() || self.status.is_some()
    }
}

/// Repository trait for SAMLProvider aggregate
///
/// This trait defines the persistence contract for SAMLProvider entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SAMLProviderRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new saml_provider entity
    async fn save(&self, entity: &SAMLProvider) -> Result<SAMLProvider>;

    /// Find saml_provider by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SAMLProvider>>;

    /// Find all saml_provider entities
    async fn find_all(&self) -> Result<Vec<SAMLProvider>>;

    /// Update saml_provider by ID
    async fn update(&self, id: &str, entity: &SAMLProvider) -> Result<Option<SAMLProvider>>;

    /// Delete saml_provider by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List saml_provider with pagination
    async fn list(&self, params: SAMLProviderPaginationParams) -> Result<SAMLProviderPaginatedResult>;

    /// List saml_provider with pagination and filters
    async fn list_with_filters(&self, params: SAMLProviderPaginationParams, filters: SAMLProviderFilter) -> Result<SAMLProviderPaginatedResult>;

    /// Count all saml_provider entities
    async fn count(&self) -> Result<u64>;

    /// Count saml_provider entities matching filters
    async fn count_with_filters(&self, filters: SAMLProviderFilter) -> Result<u64>;

    /// Check if saml_provider exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete saml_provider by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted saml_provider by ID
    async fn restore(&self, id: &str) -> Result<Option<SAMLProvider>>;

    /// List soft-deleted saml_provider entities
    async fn list_deleted(&self, params: SAMLProviderPaginationParams) -> Result<SAMLProviderPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find saml_provider by name
    async fn find_by_name(&self, name: &str) -> Result<Option<SAMLProvider>>;

    /// Find saml_provider by entity_id
    async fn find_by_entity_id(&self, entity_id: &str) -> Result<Option<SAMLProvider>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if saml_provider exists by name
    async fn exists_by_name(&self, name: &str) -> Result<bool>;

    /// Check if saml_provider exists by entity_id
    async fn exists_by_entity_id(&self, entity_id: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save saml_provider entities
    async fn bulk_save(&self, entities: &[SAMLProvider]) -> Result<Vec<SAMLProvider>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
