//! Repository trait for PasswordRequirements aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PasswordRequirements aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::PasswordRequirements;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PasswordRequirementsPaginationParams = DomainPaginationParams;
pub type PasswordRequirementsPaginatedResult = DomainPaginatedResult<PasswordRequirements>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PasswordRequirementsFilter {
    pub require_uppercase: Option<bool>,
    pub require_lowercase: Option<bool>,
    pub require_digits: Option<bool>,
    pub require_special_characters: Option<bool>,
    pub require_no_common_passwords: Option<bool>,
    pub require_no_user_info: Option<bool>,
}

impl PasswordRequirementsFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.require_uppercase.is_some() || self.require_lowercase.is_some() || self.require_digits.is_some() || self.require_special_characters.is_some() || self.require_no_common_passwords.is_some() || self.require_no_user_info.is_some()
    }
}

/// Repository trait for PasswordRequirements aggregate
///
/// This trait defines the persistence contract for PasswordRequirements entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PasswordRequirementsRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new password_requirements entity
    async fn save(&self, entity: &PasswordRequirements) -> Result<PasswordRequirements>;

    /// Find password_requirements by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PasswordRequirements>>;

    /// Find all password_requirements entities
    async fn find_all(&self) -> Result<Vec<PasswordRequirements>>;

    /// Update password_requirements by ID
    async fn update(&self, id: &str, entity: &PasswordRequirements) -> Result<Option<PasswordRequirements>>;

    /// Delete password_requirements by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List password_requirements with pagination
    async fn list(&self, params: PasswordRequirementsPaginationParams) -> Result<PasswordRequirementsPaginatedResult>;

    /// List password_requirements with pagination and filters
    async fn list_with_filters(&self, params: PasswordRequirementsPaginationParams, filters: PasswordRequirementsFilter) -> Result<PasswordRequirementsPaginatedResult>;

    /// Count all password_requirements entities
    async fn count(&self) -> Result<u64>;

    /// Count password_requirements entities matching filters
    async fn count_with_filters(&self, filters: PasswordRequirementsFilter) -> Result<u64>;

    /// Check if password_requirements exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete password_requirements by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted password_requirements by ID
    async fn restore(&self, id: &str) -> Result<Option<PasswordRequirements>>;

    /// List soft-deleted password_requirements entities
    async fn list_deleted(&self, params: PasswordRequirementsPaginationParams) -> Result<PasswordRequirementsPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save password_requirements entities
    async fn bulk_save(&self, entities: &[PasswordRequirements]) -> Result<Vec<PasswordRequirements>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
