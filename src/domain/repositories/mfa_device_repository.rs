//! Repository trait for MFADevice aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MFADevice aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{MFADevice, EnrollmentMethod, MFADeviceStatus, MFADeviceType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type MFADevicePaginationParams = DomainPaginationParams;
pub type MFADevicePaginatedResult = DomainPaginatedResult<MFADevice>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MFADeviceFilter {
    pub user_id: Option<Uuid>,
    pub device_type: Option<MFADeviceType>,
    pub device_name: Option<String>,
    pub phone_number: Option<String>,
    pub email_address: Option<String>,
    pub totp_secret: Option<String>,
    pub secret: Option<String>,
    pub hardware_key_id: Option<String>,
    pub push_token: Option<String>,
    pub device_fingerprint: Option<String>,
    pub manufacturer: Option<String>,
    pub model: Option<String>,
    pub operating_system: Option<String>,
    pub app_version: Option<String>,
    pub is_primary: Option<bool>,
    pub is_backup: Option<bool>,
    pub requires_verification: Option<bool>,
    pub auto_approval_enabled: Option<bool>,
    pub enrolled_by: Option<Uuid>,
    pub enrollment_method: Option<EnrollmentMethod>,
    pub enrollment_ip: Option<String>,
    pub enrollment_user_agent: Option<String>,
    pub backup_codes_generated: Option<bool>,
    pub is_locked: Option<bool>,
    pub lock_reason: Option<String>,
    pub is_active: Option<bool>,
    pub status: Option<MFADeviceStatus>,
}

impl MFADeviceFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.device_type.is_some() || self.device_name.is_some() || self.phone_number.is_some() || self.email_address.is_some() || self.totp_secret.is_some() || self.secret.is_some() || self.hardware_key_id.is_some() || self.push_token.is_some() || self.device_fingerprint.is_some() || self.manufacturer.is_some() || self.model.is_some() || self.operating_system.is_some() || self.app_version.is_some() || self.is_primary.is_some() || self.is_backup.is_some() || self.requires_verification.is_some() || self.auto_approval_enabled.is_some() || self.enrolled_by.is_some() || self.enrollment_method.is_some() || self.enrollment_ip.is_some() || self.enrollment_user_agent.is_some() || self.backup_codes_generated.is_some() || self.is_locked.is_some() || self.lock_reason.is_some() || self.is_active.is_some() || self.status.is_some()
    }
}

/// Repository trait for MFADevice aggregate
///
/// This trait defines the persistence contract for MFADevice entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MFADeviceRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mfa_device entity
    async fn save(&self, entity: &MFADevice) -> Result<MFADevice>;

    /// Find mfa_device by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MFADevice>>;

    /// Find all mfa_device entities
    async fn find_all(&self) -> Result<Vec<MFADevice>>;

    /// Update mfa_device by ID
    async fn update(&self, id: &str, entity: &MFADevice) -> Result<Option<MFADevice>>;

    /// Delete mfa_device by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mfa_device with pagination
    async fn list(&self, params: MFADevicePaginationParams) -> Result<MFADevicePaginatedResult>;

    /// List mfa_device with pagination and filters
    async fn list_with_filters(&self, params: MFADevicePaginationParams, filters: MFADeviceFilter) -> Result<MFADevicePaginatedResult>;

    /// Count all mfa_device entities
    async fn count(&self) -> Result<u64>;

    /// Count mfa_device entities matching filters
    async fn count_with_filters(&self, filters: MFADeviceFilter) -> Result<u64>;

    /// Check if mfa_device exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mfa_device by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mfa_device by ID
    async fn restore(&self, id: &str) -> Result<Option<MFADevice>>;

    /// List soft-deleted mfa_device entities
    async fn list_deleted(&self, params: MFADevicePaginationParams) -> Result<MFADevicePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mfa_device entities
    async fn bulk_save(&self, entities: &[MFADevice]) -> Result<Vec<MFADevice>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
