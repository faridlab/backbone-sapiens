//! Repository trait for RolePermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the RolePermission aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::RolePermission;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type RolePermissionPaginationParams = DomainPaginationParams;
pub type RolePermissionPaginatedResult = DomainPaginatedResult<RolePermission>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct RolePermissionFilter {
    pub role_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
}

impl RolePermissionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.role_id.is_some() || self.permission_id.is_some()
    }
}

/// Repository trait for RolePermission aggregate
///
/// This trait defines the persistence contract for RolePermission entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait RolePermissionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new role_permission entity
    async fn save(&self, entity: &RolePermission) -> Result<RolePermission>;

    /// Find role_permission by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<RolePermission>>;

    /// Find all role_permission entities
    async fn find_all(&self) -> Result<Vec<RolePermission>>;

    /// Update role_permission by ID
    async fn update(&self, id: &str, entity: &RolePermission) -> Result<Option<RolePermission>>;

    /// Delete role_permission by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List role_permission with pagination
    async fn list(&self, params: RolePermissionPaginationParams) -> Result<RolePermissionPaginatedResult>;

    /// List role_permission with pagination and filters
    async fn list_with_filters(&self, params: RolePermissionPaginationParams, filters: RolePermissionFilter) -> Result<RolePermissionPaginatedResult>;

    /// Count all role_permission entities
    async fn count(&self) -> Result<u64>;

    /// Count role_permission entities matching filters
    async fn count_with_filters(&self, filters: RolePermissionFilter) -> Result<u64>;

    /// Check if role_permission exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete role_permission by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted role_permission by ID
    async fn restore(&self, id: &str) -> Result<Option<RolePermission>>;

    /// List soft-deleted role_permission entities
    async fn list_deleted(&self, params: RolePermissionPaginationParams) -> Result<RolePermissionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save role_permission entities
    async fn bulk_save(&self, entities: &[RolePermission]) -> Result<Vec<RolePermission>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
