//! Repository trait for PasswordResetToken aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PasswordResetToken aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::PasswordResetToken;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PasswordResetTokenPaginationParams = DomainPaginationParams;
pub type PasswordResetTokenPaginatedResult = DomainPaginatedResult<PasswordResetToken>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PasswordResetTokenFilter {
    pub user_id: Option<Uuid>,
    pub token_hash: Option<String>,
    pub is_used: Option<bool>,
}

impl PasswordResetTokenFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.token_hash.is_some() || self.is_used.is_some()
    }
}

/// Repository trait for PasswordResetToken aggregate
///
/// This trait defines the persistence contract for PasswordResetToken entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PasswordResetTokenRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new password_reset_token entity
    async fn save(&self, entity: &PasswordResetToken) -> Result<PasswordResetToken>;

    /// Find password_reset_token by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PasswordResetToken>>;

    /// Find all password_reset_token entities
    async fn find_all(&self) -> Result<Vec<PasswordResetToken>>;

    /// Update password_reset_token by ID
    async fn update(&self, id: &str, entity: &PasswordResetToken) -> Result<Option<PasswordResetToken>>;

    /// Delete password_reset_token by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List password_reset_token with pagination
    async fn list(&self, params: PasswordResetTokenPaginationParams) -> Result<PasswordResetTokenPaginatedResult>;

    /// List password_reset_token with pagination and filters
    async fn list_with_filters(&self, params: PasswordResetTokenPaginationParams, filters: PasswordResetTokenFilter) -> Result<PasswordResetTokenPaginatedResult>;

    /// Count all password_reset_token entities
    async fn count(&self) -> Result<u64>;

    /// Count password_reset_token entities matching filters
    async fn count_with_filters(&self, filters: PasswordResetTokenFilter) -> Result<u64>;

    /// Check if password_reset_token exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete password_reset_token by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted password_reset_token by ID
    async fn restore(&self, id: &str) -> Result<Option<PasswordResetToken>>;

    /// List soft-deleted password_reset_token entities
    async fn list_deleted(&self, params: PasswordResetTokenPaginationParams) -> Result<PasswordResetTokenPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find password_reset_token by token_hash
    async fn find_by_token_hash(&self, token_hash: &str) -> Result<Option<PasswordResetToken>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if password_reset_token exists by token_hash
    async fn exists_by_token_hash(&self, token_hash: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save password_reset_token entities
    async fn bulk_save(&self, entities: &[PasswordResetToken]) -> Result<Vec<PasswordResetToken>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
