//! Repository trait for PasswordPolicy aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PasswordPolicy aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::PasswordPolicy;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PasswordPolicyPaginationParams = DomainPaginationParams;
pub type PasswordPolicyPaginatedResult = DomainPaginatedResult<PasswordPolicy>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PasswordPolicyFilter {
    pub name: Option<String>,
    pub organization_id: Option<Uuid>,
    pub is_active: Option<bool>,
}

impl PasswordPolicyFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.organization_id.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for PasswordPolicy aggregate
///
/// This trait defines the persistence contract for PasswordPolicy entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PasswordPolicyRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new password_policy entity
    async fn save(&self, entity: &PasswordPolicy) -> Result<PasswordPolicy>;

    /// Find password_policy by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PasswordPolicy>>;

    /// Find all password_policy entities
    async fn find_all(&self) -> Result<Vec<PasswordPolicy>>;

    /// Update password_policy by ID
    async fn update(&self, id: &str, entity: &PasswordPolicy) -> Result<Option<PasswordPolicy>>;

    /// Delete password_policy by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List password_policy with pagination
    async fn list(&self, params: PasswordPolicyPaginationParams) -> Result<PasswordPolicyPaginatedResult>;

    /// List password_policy with pagination and filters
    async fn list_with_filters(&self, params: PasswordPolicyPaginationParams, filters: PasswordPolicyFilter) -> Result<PasswordPolicyPaginatedResult>;

    /// Count all password_policy entities
    async fn count(&self) -> Result<u64>;

    /// Count password_policy entities matching filters
    async fn count_with_filters(&self, filters: PasswordPolicyFilter) -> Result<u64>;

    /// Check if password_policy exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete password_policy by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted password_policy by ID
    async fn restore(&self, id: &str) -> Result<Option<PasswordPolicy>>;

    /// List soft-deleted password_policy entities
    async fn list_deleted(&self, params: PasswordPolicyPaginationParams) -> Result<PasswordPolicyPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save password_policy entities
    async fn bulk_save(&self, entities: &[PasswordPolicy]) -> Result<Vec<PasswordPolicy>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
