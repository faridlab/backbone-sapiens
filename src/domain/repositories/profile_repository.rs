//! Repository trait for Profile aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Profile aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{Profile, Gender};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProfilePaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ProfilePaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ProfilePaginatedResult {
    pub data: Vec<Profile>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ProfileFilter {
    pub first_name: Option<String>,
    pub middle_name: Option<String>,
    pub last_name: Option<String>,
    pub pob: Option<String>,
    pub gender: Option<Gender>,
}

impl ProfileFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.first_name.is_some() || self.middle_name.is_some() || self.last_name.is_some() || self.pob.is_some() || self.gender.is_some()
    }
}

/// Repository trait for Profile aggregate
///
/// This trait defines the persistence contract for Profile entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ProfileRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new profile entity
    async fn save(&self, entity: &Profile) -> Result<Profile>;

    /// Find profile by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Profile>>;

    /// Find all profile entities
    async fn find_all(&self) -> Result<Vec<Profile>>;

    /// Update profile by ID
    async fn update(&self, id: &str, entity: &Profile) -> Result<Option<Profile>>;

    /// Delete profile by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List profile with pagination
    async fn list(&self, params: ProfilePaginationParams) -> Result<ProfilePaginatedResult>;

    /// List profile with pagination and filters
    async fn list_with_filters(&self, params: ProfilePaginationParams, filters: ProfileFilter) -> Result<ProfilePaginatedResult>;

    /// Count all profile entities
    async fn count(&self) -> Result<u64>;

    /// Count profile entities matching filters
    async fn count_with_filters(&self, filters: ProfileFilter) -> Result<u64>;

    /// Check if profile exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete profile by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted profile by ID
    async fn restore(&self, id: &str) -> Result<Option<Profile>>;

    /// List soft-deleted profile entities
    async fn list_deleted(&self, params: ProfilePaginationParams) -> Result<ProfilePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save profile entities
    async fn bulk_save(&self, entities: &[Profile]) -> Result<Vec<Profile>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
