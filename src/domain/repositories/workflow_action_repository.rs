//! Repository trait for WorkflowAction aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the WorkflowAction aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{WorkflowAction, ActionType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type WorkflowActionPaginationParams = DomainPaginationParams;
pub type WorkflowActionPaginatedResult = DomainPaginatedResult<WorkflowAction>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct WorkflowActionFilter {
    pub workflow_definition_id: Option<Uuid>,
    pub action_type: Option<ActionType>,
    pub action_name: Option<String>,
    pub description: Option<String>,
    pub is_required: Option<bool>,
}

impl WorkflowActionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.workflow_definition_id.is_some() || self.action_type.is_some() || self.action_name.is_some() || self.description.is_some() || self.is_required.is_some()
    }
}

/// Repository trait for WorkflowAction aggregate
///
/// This trait defines the persistence contract for WorkflowAction entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait WorkflowActionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new workflow_action entity
    async fn save(&self, entity: &WorkflowAction) -> Result<WorkflowAction>;

    /// Find workflow_action by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<WorkflowAction>>;

    /// Find all workflow_action entities
    async fn find_all(&self) -> Result<Vec<WorkflowAction>>;

    /// Update workflow_action by ID
    async fn update(&self, id: &str, entity: &WorkflowAction) -> Result<Option<WorkflowAction>>;

    /// Delete workflow_action by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List workflow_action with pagination
    async fn list(&self, params: WorkflowActionPaginationParams) -> Result<WorkflowActionPaginatedResult>;

    /// List workflow_action with pagination and filters
    async fn list_with_filters(&self, params: WorkflowActionPaginationParams, filters: WorkflowActionFilter) -> Result<WorkflowActionPaginatedResult>;

    /// Count all workflow_action entities
    async fn count(&self) -> Result<u64>;

    /// Count workflow_action entities matching filters
    async fn count_with_filters(&self, filters: WorkflowActionFilter) -> Result<u64>;

    /// Check if workflow_action exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete workflow_action by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted workflow_action by ID
    async fn restore(&self, id: &str) -> Result<Option<WorkflowAction>>;

    /// List soft-deleted workflow_action entities
    async fn list_deleted(&self, params: WorkflowActionPaginationParams) -> Result<WorkflowActionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save workflow_action entities
    async fn bulk_save(&self, entities: &[WorkflowAction]) -> Result<Vec<WorkflowAction>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
