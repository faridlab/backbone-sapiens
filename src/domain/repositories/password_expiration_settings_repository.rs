//! Repository trait for PasswordExpirationSettings aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PasswordExpirationSettings aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::PasswordExpirationSettings;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PasswordExpirationSettingsPaginationParams = DomainPaginationParams;
pub type PasswordExpirationSettingsPaginatedResult = DomainPaginatedResult<PasswordExpirationSettings>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PasswordExpirationSettingsFilter {
}

impl PasswordExpirationSettingsFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        false
    }
}

/// Repository trait for PasswordExpirationSettings aggregate
///
/// This trait defines the persistence contract for PasswordExpirationSettings entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PasswordExpirationSettingsRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new password_expiration_settings entity
    async fn save(&self, entity: &PasswordExpirationSettings) -> Result<PasswordExpirationSettings>;

    /// Find password_expiration_settings by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PasswordExpirationSettings>>;

    /// Find all password_expiration_settings entities
    async fn find_all(&self) -> Result<Vec<PasswordExpirationSettings>>;

    /// Update password_expiration_settings by ID
    async fn update(&self, id: &str, entity: &PasswordExpirationSettings) -> Result<Option<PasswordExpirationSettings>>;

    /// Delete password_expiration_settings by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List password_expiration_settings with pagination
    async fn list(&self, params: PasswordExpirationSettingsPaginationParams) -> Result<PasswordExpirationSettingsPaginatedResult>;

    /// List password_expiration_settings with pagination and filters
    async fn list_with_filters(&self, params: PasswordExpirationSettingsPaginationParams, filters: PasswordExpirationSettingsFilter) -> Result<PasswordExpirationSettingsPaginatedResult>;

    /// Count all password_expiration_settings entities
    async fn count(&self) -> Result<u64>;

    /// Count password_expiration_settings entities matching filters
    async fn count_with_filters(&self, filters: PasswordExpirationSettingsFilter) -> Result<u64>;

    /// Check if password_expiration_settings exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete password_expiration_settings by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted password_expiration_settings by ID
    async fn restore(&self, id: &str) -> Result<Option<PasswordExpirationSettings>>;

    /// List soft-deleted password_expiration_settings entities
    async fn list_deleted(&self, params: PasswordExpirationSettingsPaginationParams) -> Result<PasswordExpirationSettingsPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save password_expiration_settings entities
    async fn bulk_save(&self, entities: &[PasswordExpirationSettings]) -> Result<Vec<PasswordExpirationSettings>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
