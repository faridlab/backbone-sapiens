//! Repository trait for SessionLimit aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SessionLimit aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::SessionLimit;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SessionLimitPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl SessionLimitPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct SessionLimitPaginatedResult {
    pub data: Vec<SessionLimit>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SessionLimitFilter {
    pub user_id: Option<Uuid>,
    pub enforce_limit: Option<bool>,
}

impl SessionLimitFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.enforce_limit.is_some()
    }
}

/// Repository trait for SessionLimit aggregate
///
/// This trait defines the persistence contract for SessionLimit entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SessionLimitRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new session_limit entity
    async fn save(&self, entity: &SessionLimit) -> Result<SessionLimit>;

    /// Find session_limit by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SessionLimit>>;

    /// Find all session_limit entities
    async fn find_all(&self) -> Result<Vec<SessionLimit>>;

    /// Update session_limit by ID
    async fn update(&self, id: &str, entity: &SessionLimit) -> Result<Option<SessionLimit>>;

    /// Delete session_limit by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List session_limit with pagination
    async fn list(&self, params: SessionLimitPaginationParams) -> Result<SessionLimitPaginatedResult>;

    /// List session_limit with pagination and filters
    async fn list_with_filters(&self, params: SessionLimitPaginationParams, filters: SessionLimitFilter) -> Result<SessionLimitPaginatedResult>;

    /// Count all session_limit entities
    async fn count(&self) -> Result<u64>;

    /// Count session_limit entities matching filters
    async fn count_with_filters(&self, filters: SessionLimitFilter) -> Result<u64>;

    /// Check if session_limit exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete session_limit by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted session_limit by ID
    async fn restore(&self, id: &str) -> Result<Option<SessionLimit>>;

    /// List soft-deleted session_limit entities
    async fn list_deleted(&self, params: SessionLimitPaginationParams) -> Result<SessionLimitPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find session_limit by user_id
    async fn find_by_user_id(&self, user_id: Uuid) -> Result<Option<SessionLimit>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if session_limit exists by user_id
    async fn exists_by_user_id(&self, user_id: Uuid) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save session_limit entities
    async fn bulk_save(&self, entities: &[SessionLimit]) -> Result<Vec<SessionLimit>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
