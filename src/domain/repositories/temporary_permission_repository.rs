//! Repository trait for TemporaryPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the TemporaryPermission aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{TemporaryPermission, TemporaryPermissionStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type TemporaryPermissionPaginationParams = DomainPaginationParams;
pub type TemporaryPermissionPaginatedResult = DomainPaginatedResult<TemporaryPermission>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct TemporaryPermissionFilter {
    pub user_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
    pub granted_by: Option<Uuid>,
    pub revoked_by: Option<Uuid>,
    pub reason: Option<String>,
    pub notified_before_expiry: Option<bool>,
    pub status: Option<TemporaryPermissionStatus>,
}

impl TemporaryPermissionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.permission_id.is_some() || self.granted_by.is_some() || self.revoked_by.is_some() || self.reason.is_some() || self.notified_before_expiry.is_some() || self.status.is_some()
    }
}

/// Repository trait for TemporaryPermission aggregate
///
/// This trait defines the persistence contract for TemporaryPermission entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait TemporaryPermissionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new temporary_permission entity
    async fn save(&self, entity: &TemporaryPermission) -> Result<TemporaryPermission>;

    /// Find temporary_permission by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<TemporaryPermission>>;

    /// Find all temporary_permission entities
    async fn find_all(&self) -> Result<Vec<TemporaryPermission>>;

    /// Update temporary_permission by ID
    async fn update(&self, id: &str, entity: &TemporaryPermission) -> Result<Option<TemporaryPermission>>;

    /// Delete temporary_permission by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List temporary_permission with pagination
    async fn list(&self, params: TemporaryPermissionPaginationParams) -> Result<TemporaryPermissionPaginatedResult>;

    /// List temporary_permission with pagination and filters
    async fn list_with_filters(&self, params: TemporaryPermissionPaginationParams, filters: TemporaryPermissionFilter) -> Result<TemporaryPermissionPaginatedResult>;

    /// Count all temporary_permission entities
    async fn count(&self) -> Result<u64>;

    /// Count temporary_permission entities matching filters
    async fn count_with_filters(&self, filters: TemporaryPermissionFilter) -> Result<u64>;

    /// Check if temporary_permission exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete temporary_permission by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted temporary_permission by ID
    async fn restore(&self, id: &str) -> Result<Option<TemporaryPermission>>;

    /// List soft-deleted temporary_permission entities
    async fn list_deleted(&self, params: TemporaryPermissionPaginationParams) -> Result<TemporaryPermissionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save temporary_permission entities
    async fn bulk_save(&self, entities: &[TemporaryPermission]) -> Result<Vec<TemporaryPermission>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
