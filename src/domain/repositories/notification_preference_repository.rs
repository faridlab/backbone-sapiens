//! Repository trait for NotificationPreference aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the NotificationPreference aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{NotificationPreference, DigestFrequency};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type NotificationPreferencePaginationParams = DomainPaginationParams;
pub type NotificationPreferencePaginatedResult = DomainPaginatedResult<NotificationPreference>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct NotificationPreferenceFilter {
    pub user_id: Option<Uuid>,
    pub notification_type: Option<String>,
    pub channel_enabled: Option<bool>,
    pub quiet_hours_start: Option<String>,
    pub quiet_hours_end: Option<String>,
    pub quiet_timezone: Option<String>,
    pub digest_enabled: Option<bool>,
    pub digest_frequency: Option<DigestFrequency>,
    pub enabled: Option<bool>,
}

impl NotificationPreferenceFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.notification_type.is_some() || self.channel_enabled.is_some() || self.quiet_hours_start.is_some() || self.quiet_hours_end.is_some() || self.quiet_timezone.is_some() || self.digest_enabled.is_some() || self.digest_frequency.is_some() || self.enabled.is_some()
    }
}

/// Repository trait for NotificationPreference aggregate
///
/// This trait defines the persistence contract for NotificationPreference entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait NotificationPreferenceRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new notification_preference entity
    async fn save(&self, entity: &NotificationPreference) -> Result<NotificationPreference>;

    /// Find notification_preference by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<NotificationPreference>>;

    /// Find all notification_preference entities
    async fn find_all(&self) -> Result<Vec<NotificationPreference>>;

    /// Update notification_preference by ID
    async fn update(&self, id: &str, entity: &NotificationPreference) -> Result<Option<NotificationPreference>>;

    /// Delete notification_preference by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List notification_preference with pagination
    async fn list(&self, params: NotificationPreferencePaginationParams) -> Result<NotificationPreferencePaginatedResult>;

    /// List notification_preference with pagination and filters
    async fn list_with_filters(&self, params: NotificationPreferencePaginationParams, filters: NotificationPreferenceFilter) -> Result<NotificationPreferencePaginatedResult>;

    /// Count all notification_preference entities
    async fn count(&self) -> Result<u64>;

    /// Count notification_preference entities matching filters
    async fn count_with_filters(&self, filters: NotificationPreferenceFilter) -> Result<u64>;

    /// Check if notification_preference exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete notification_preference by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted notification_preference by ID
    async fn restore(&self, id: &str) -> Result<Option<NotificationPreference>>;

    /// List soft-deleted notification_preference entities
    async fn list_deleted(&self, params: NotificationPreferencePaginationParams) -> Result<NotificationPreferencePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find notification_preference by user_id
    async fn find_by_user_id(&self, user_id: Uuid) -> Result<Option<NotificationPreference>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if notification_preference exists by user_id
    async fn exists_by_user_id(&self, user_id: Uuid) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save notification_preference entities
    async fn bulk_save(&self, entities: &[NotificationPreference]) -> Result<Vec<NotificationPreference>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
