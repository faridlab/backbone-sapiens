//! Repository trait for MFASession aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MFASession aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{MFASession, DeviceTrustStatus, MFASessionStatus, MFAVerificationMethod, MfaTrustLevel, NetworkQuality, PrimaryAuthMethod};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type MFASessionPaginationParams = DomainPaginationParams;
pub type MFASessionPaginatedResult = DomainPaginatedResult<MFASession>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MFASessionFilter {
    pub user_id: Option<Uuid>,
    pub device_id: Option<Uuid>,
    pub session_token: Option<String>,
    pub session_hash: Option<String>,
    pub primary_authentication_method: Option<PrimaryAuthMethod>,
    pub mfa_required: Option<bool>,
    pub trust_level: Option<MfaTrustLevel>,
    pub adaptive_authentication_triggered: Option<bool>,
    pub location_anomaly: Option<bool>,
    pub time_anomaly: Option<bool>,
    pub verification_method: Option<MFAVerificationMethod>,
    pub verification_success: Option<bool>,
    pub device_trust_status: Option<DeviceTrustStatus>,
    pub is_terminated: Option<bool>,
    pub termination_reason: Option<String>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub device_fingerprint: Option<String>,
    pub country: Option<String>,
    pub region: Option<String>,
    pub city: Option<String>,
    pub auto_renew_enabled: Option<bool>,
    pub network_quality: Option<NetworkQuality>,
    pub audit_log_required: Option<bool>,
    pub status: Option<MFASessionStatus>,
}

impl MFASessionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.device_id.is_some() || self.session_token.is_some() || self.session_hash.is_some() || self.primary_authentication_method.is_some() || self.mfa_required.is_some() || self.trust_level.is_some() || self.adaptive_authentication_triggered.is_some() || self.location_anomaly.is_some() || self.time_anomaly.is_some() || self.verification_method.is_some() || self.verification_success.is_some() || self.device_trust_status.is_some() || self.is_terminated.is_some() || self.termination_reason.is_some() || self.ip_address.is_some() || self.user_agent.is_some() || self.device_fingerprint.is_some() || self.country.is_some() || self.region.is_some() || self.city.is_some() || self.auto_renew_enabled.is_some() || self.network_quality.is_some() || self.audit_log_required.is_some() || self.status.is_some()
    }
}

/// Repository trait for MFASession aggregate
///
/// This trait defines the persistence contract for MFASession entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MFASessionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mfa_session entity
    async fn save(&self, entity: &MFASession) -> Result<MFASession>;

    /// Find mfa_session by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MFASession>>;

    /// Find all mfa_session entities
    async fn find_all(&self) -> Result<Vec<MFASession>>;

    /// Update mfa_session by ID
    async fn update(&self, id: &str, entity: &MFASession) -> Result<Option<MFASession>>;

    /// Delete mfa_session by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mfa_session with pagination
    async fn list(&self, params: MFASessionPaginationParams) -> Result<MFASessionPaginatedResult>;

    /// List mfa_session with pagination and filters
    async fn list_with_filters(&self, params: MFASessionPaginationParams, filters: MFASessionFilter) -> Result<MFASessionPaginatedResult>;

    /// Count all mfa_session entities
    async fn count(&self) -> Result<u64>;

    /// Count mfa_session entities matching filters
    async fn count_with_filters(&self, filters: MFASessionFilter) -> Result<u64>;

    /// Check if mfa_session exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mfa_session by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mfa_session by ID
    async fn restore(&self, id: &str) -> Result<Option<MFASession>>;

    /// List soft-deleted mfa_session entities
    async fn list_deleted(&self, params: MFASessionPaginationParams) -> Result<MFASessionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find mfa_session by session_token
    async fn find_by_session_token(&self, session_token: &str) -> Result<Option<MFASession>>;

    /// Find mfa_session by session_hash
    async fn find_by_session_hash(&self, session_hash: &str) -> Result<Option<MFASession>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if mfa_session exists by session_token
    async fn exists_by_session_token(&self, session_token: &str) -> Result<bool>;

    /// Check if mfa_session exists by session_hash
    async fn exists_by_session_hash(&self, session_hash: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mfa_session entities
    async fn bulk_save(&self, entities: &[MFASession]) -> Result<Vec<MFASession>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
