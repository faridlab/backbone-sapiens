//! Repository trait for PasswordCreationContext aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PasswordCreationContext aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PasswordCreationContext, PasswordCreationMethod};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PasswordCreationContextPaginationParams = DomainPaginationParams;
pub type PasswordCreationContextPaginatedResult = DomainPaginatedResult<PasswordCreationContext>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PasswordCreationContextFilter {
    pub user_id: Option<Uuid>,
    pub created_via: Option<PasswordCreationMethod>,
    pub user_agent: Option<String>,
    pub reason: Option<String>,
}

impl PasswordCreationContextFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.created_via.is_some() || self.user_agent.is_some() || self.reason.is_some()
    }
}

/// Repository trait for PasswordCreationContext aggregate
///
/// This trait defines the persistence contract for PasswordCreationContext entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PasswordCreationContextRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new password_creation_context entity
    async fn save(&self, entity: &PasswordCreationContext) -> Result<PasswordCreationContext>;

    /// Find password_creation_context by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PasswordCreationContext>>;

    /// Find all password_creation_context entities
    async fn find_all(&self) -> Result<Vec<PasswordCreationContext>>;

    /// Update password_creation_context by ID
    async fn update(&self, id: &str, entity: &PasswordCreationContext) -> Result<Option<PasswordCreationContext>>;

    /// Delete password_creation_context by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List password_creation_context with pagination
    async fn list(&self, params: PasswordCreationContextPaginationParams) -> Result<PasswordCreationContextPaginatedResult>;

    /// List password_creation_context with pagination and filters
    async fn list_with_filters(&self, params: PasswordCreationContextPaginationParams, filters: PasswordCreationContextFilter) -> Result<PasswordCreationContextPaginatedResult>;

    /// Count all password_creation_context entities
    async fn count(&self) -> Result<u64>;

    /// Count password_creation_context entities matching filters
    async fn count_with_filters(&self, filters: PasswordCreationContextFilter) -> Result<u64>;

    /// Check if password_creation_context exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete password_creation_context by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted password_creation_context by ID
    async fn restore(&self, id: &str) -> Result<Option<PasswordCreationContext>>;

    /// List soft-deleted password_creation_context entities
    async fn list_deleted(&self, params: PasswordCreationContextPaginationParams) -> Result<PasswordCreationContextPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save password_creation_context entities
    async fn bulk_save(&self, entities: &[PasswordCreationContext]) -> Result<Vec<PasswordCreationContext>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
