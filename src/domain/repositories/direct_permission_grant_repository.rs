//! Repository trait for DirectPermissionGrant aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the DirectPermissionGrant aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{DirectPermissionGrant, DirectPermissionGrantStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type DirectPermissionGrantPaginationParams = DomainPaginationParams;
pub type DirectPermissionGrantPaginatedResult = DomainPaginatedResult<DirectPermissionGrant>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DirectPermissionGrantFilter {
    pub user_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
    pub granted_by: Option<Uuid>,
    pub reason: Option<String>,
    pub status: Option<DirectPermissionGrantStatus>,
    pub revoked_by: Option<Uuid>,
    pub revocation_reason: Option<String>,
}

impl DirectPermissionGrantFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.permission_id.is_some() || self.granted_by.is_some() || self.reason.is_some() || self.status.is_some() || self.revoked_by.is_some() || self.revocation_reason.is_some()
    }
}

/// Repository trait for DirectPermissionGrant aggregate
///
/// This trait defines the persistence contract for DirectPermissionGrant entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait DirectPermissionGrantRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new direct_permission_grant entity
    async fn save(&self, entity: &DirectPermissionGrant) -> Result<DirectPermissionGrant>;

    /// Find direct_permission_grant by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<DirectPermissionGrant>>;

    /// Find all direct_permission_grant entities
    async fn find_all(&self) -> Result<Vec<DirectPermissionGrant>>;

    /// Update direct_permission_grant by ID
    async fn update(&self, id: &str, entity: &DirectPermissionGrant) -> Result<Option<DirectPermissionGrant>>;

    /// Delete direct_permission_grant by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List direct_permission_grant with pagination
    async fn list(&self, params: DirectPermissionGrantPaginationParams) -> Result<DirectPermissionGrantPaginatedResult>;

    /// List direct_permission_grant with pagination and filters
    async fn list_with_filters(&self, params: DirectPermissionGrantPaginationParams, filters: DirectPermissionGrantFilter) -> Result<DirectPermissionGrantPaginatedResult>;

    /// Count all direct_permission_grant entities
    async fn count(&self) -> Result<u64>;

    /// Count direct_permission_grant entities matching filters
    async fn count_with_filters(&self, filters: DirectPermissionGrantFilter) -> Result<u64>;

    /// Check if direct_permission_grant exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete direct_permission_grant by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted direct_permission_grant by ID
    async fn restore(&self, id: &str) -> Result<Option<DirectPermissionGrant>>;

    /// List soft-deleted direct_permission_grant entities
    async fn list_deleted(&self, params: DirectPermissionGrantPaginationParams) -> Result<DirectPermissionGrantPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save direct_permission_grant entities
    async fn bulk_save(&self, entities: &[DirectPermissionGrant]) -> Result<Vec<DirectPermissionGrant>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
