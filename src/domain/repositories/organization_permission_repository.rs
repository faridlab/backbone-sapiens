//! Repository trait for OrganizationPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the OrganizationPermission aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::OrganizationPermission;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type OrganizationPermissionPaginationParams = DomainPaginationParams;
pub type OrganizationPermissionPaginatedResult = DomainPaginatedResult<OrganizationPermission>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct OrganizationPermissionFilter {
    pub organization_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
    pub permission_name: Option<String>,
    pub resource_id: Option<Uuid>,
    pub resource_type: Option<String>,
    pub role_id: Option<Uuid>,
    pub granted_by: Option<Uuid>,
    pub reason: Option<String>,
    pub is_active: Option<bool>,
}

impl OrganizationPermissionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.organization_id.is_some() || self.permission_id.is_some() || self.permission_name.is_some() || self.resource_id.is_some() || self.resource_type.is_some() || self.role_id.is_some() || self.granted_by.is_some() || self.reason.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for OrganizationPermission aggregate
///
/// This trait defines the persistence contract for OrganizationPermission entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait OrganizationPermissionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new organization_permission entity
    async fn save(&self, entity: &OrganizationPermission) -> Result<OrganizationPermission>;

    /// Find organization_permission by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<OrganizationPermission>>;

    /// Find all organization_permission entities
    async fn find_all(&self) -> Result<Vec<OrganizationPermission>>;

    /// Update organization_permission by ID
    async fn update(&self, id: &str, entity: &OrganizationPermission) -> Result<Option<OrganizationPermission>>;

    /// Delete organization_permission by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List organization_permission with pagination
    async fn list(&self, params: OrganizationPermissionPaginationParams) -> Result<OrganizationPermissionPaginatedResult>;

    /// List organization_permission with pagination and filters
    async fn list_with_filters(&self, params: OrganizationPermissionPaginationParams, filters: OrganizationPermissionFilter) -> Result<OrganizationPermissionPaginatedResult>;

    /// Count all organization_permission entities
    async fn count(&self) -> Result<u64>;

    /// Count organization_permission entities matching filters
    async fn count_with_filters(&self, filters: OrganizationPermissionFilter) -> Result<u64>;

    /// Check if organization_permission exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete organization_permission by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted organization_permission by ID
    async fn restore(&self, id: &str) -> Result<Option<OrganizationPermission>>;

    /// List soft-deleted organization_permission entities
    async fn list_deleted(&self, params: OrganizationPermissionPaginationParams) -> Result<OrganizationPermissionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save organization_permission entities
    async fn bulk_save(&self, entities: &[OrganizationPermission]) -> Result<Vec<OrganizationPermission>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
