//! Repository trait for BackupCode aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the BackupCode aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::BackupCode;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type BackupCodePaginationParams = DomainPaginationParams;
pub type BackupCodePaginatedResult = DomainPaginatedResult<BackupCode>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BackupCodeFilter {
    pub user_id: Option<Uuid>,
    pub batch_id: Option<String>,
    pub code_hash: Option<String>,
    pub is_used: Option<bool>,
    pub used_user_agent: Option<String>,
}

impl BackupCodeFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.batch_id.is_some() || self.code_hash.is_some() || self.is_used.is_some() || self.used_user_agent.is_some()
    }
}

/// Repository trait for BackupCode aggregate
///
/// This trait defines the persistence contract for BackupCode entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait BackupCodeRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new backup_code entity
    async fn save(&self, entity: &BackupCode) -> Result<BackupCode>;

    /// Find backup_code by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<BackupCode>>;

    /// Find all backup_code entities
    async fn find_all(&self) -> Result<Vec<BackupCode>>;

    /// Update backup_code by ID
    async fn update(&self, id: &str, entity: &BackupCode) -> Result<Option<BackupCode>>;

    /// Delete backup_code by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List backup_code with pagination
    async fn list(&self, params: BackupCodePaginationParams) -> Result<BackupCodePaginatedResult>;

    /// List backup_code with pagination and filters
    async fn list_with_filters(&self, params: BackupCodePaginationParams, filters: BackupCodeFilter) -> Result<BackupCodePaginatedResult>;

    /// Count all backup_code entities
    async fn count(&self) -> Result<u64>;

    /// Count backup_code entities matching filters
    async fn count_with_filters(&self, filters: BackupCodeFilter) -> Result<u64>;

    /// Check if backup_code exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete backup_code by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted backup_code by ID
    async fn restore(&self, id: &str) -> Result<Option<BackupCode>>;

    /// List soft-deleted backup_code entities
    async fn list_deleted(&self, params: BackupCodePaginationParams) -> Result<BackupCodePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save backup_code entities
    async fn bulk_save(&self, entities: &[BackupCode]) -> Result<Vec<BackupCode>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
