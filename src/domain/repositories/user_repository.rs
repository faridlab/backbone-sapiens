//! Repository trait for User aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the User aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{User, UserStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type UserPaginationParams = DomainPaginationParams;
pub type UserPaginatedResult = DomainPaginatedResult<User>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UserFilter {
    pub username: Option<String>,
    pub email: Option<String>,
    pub password_hash: Option<String>,
    pub status: Option<UserStatus>,
    pub email_verified: Option<bool>,
}

impl UserFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.username.is_some() || self.email.is_some() || self.password_hash.is_some() || self.status.is_some() || self.email_verified.is_some()
    }
}

/// Repository trait for User aggregate
///
/// This trait defines the persistence contract for User entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait UserRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new user entity
    async fn save(&self, entity: &User) -> Result<User>;

    /// Find user by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<User>>;

    /// Find all user entities
    async fn find_all(&self) -> Result<Vec<User>>;

    /// Update user by ID
    async fn update(&self, id: &str, entity: &User) -> Result<Option<User>>;

    /// Delete user by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List user with pagination
    async fn list(&self, params: UserPaginationParams) -> Result<UserPaginatedResult>;

    /// List user with pagination and filters
    async fn list_with_filters(&self, params: UserPaginationParams, filters: UserFilter) -> Result<UserPaginatedResult>;

    /// Count all user entities
    async fn count(&self) -> Result<u64>;

    /// Count user entities matching filters
    async fn count_with_filters(&self, filters: UserFilter) -> Result<u64>;

    /// Check if user exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete user by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted user by ID
    async fn restore(&self, id: &str) -> Result<Option<User>>;

    /// List soft-deleted user entities
    async fn list_deleted(&self, params: UserPaginationParams) -> Result<UserPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find user by username
    async fn find_by_username(&self, username: &str) -> Result<Option<User>>;

    /// Find user by email
    async fn find_by_email(&self, email: &str) -> Result<Option<User>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if user exists by username
    async fn exists_by_username(&self, username: &str) -> Result<bool>;

    /// Check if user exists by email
    async fn exists_by_email(&self, email: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save user entities
    async fn bulk_save(&self, entities: &[User]) -> Result<Vec<User>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
