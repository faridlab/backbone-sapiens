//! Repository trait for Workflow aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Workflow aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Workflow, WorkflowStatus, WorkflowType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type WorkflowPaginationParams = DomainPaginationParams;
pub type WorkflowPaginatedResult = DomainPaginatedResult<Workflow>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct WorkflowFilter {
    pub workflow_type: Option<WorkflowType>,
    pub status: Option<WorkflowStatus>,
    pub initiator_id: Option<Uuid>,
    pub target_user_id: Option<Uuid>,
    pub title: Option<String>,
    pub description: Option<String>,
    pub error_message: Option<String>,
}

impl WorkflowFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.workflow_type.is_some() || self.status.is_some() || self.initiator_id.is_some() || self.target_user_id.is_some() || self.title.is_some() || self.description.is_some() || self.error_message.is_some()
    }
}

/// Repository trait for Workflow aggregate
///
/// This trait defines the persistence contract for Workflow entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait WorkflowRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new workflow entity
    async fn save(&self, entity: &Workflow) -> Result<Workflow>;

    /// Find workflow by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Workflow>>;

    /// Find all workflow entities
    async fn find_all(&self) -> Result<Vec<Workflow>>;

    /// Update workflow by ID
    async fn update(&self, id: &str, entity: &Workflow) -> Result<Option<Workflow>>;

    /// Delete workflow by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List workflow with pagination
    async fn list(&self, params: WorkflowPaginationParams) -> Result<WorkflowPaginatedResult>;

    /// List workflow with pagination and filters
    async fn list_with_filters(&self, params: WorkflowPaginationParams, filters: WorkflowFilter) -> Result<WorkflowPaginatedResult>;

    /// Count all workflow entities
    async fn count(&self) -> Result<u64>;

    /// Count workflow entities matching filters
    async fn count_with_filters(&self, filters: WorkflowFilter) -> Result<u64>;

    /// Check if workflow exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete workflow by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted workflow by ID
    async fn restore(&self, id: &str) -> Result<Option<Workflow>>;

    /// List soft-deleted workflow entities
    async fn list_deleted(&self, params: WorkflowPaginationParams) -> Result<WorkflowPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save workflow entities
    async fn bulk_save(&self, entities: &[Workflow]) -> Result<Vec<Workflow>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
