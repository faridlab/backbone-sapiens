//! Repository trait for NotificationTemplate aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the NotificationTemplate aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{NotificationTemplate, NotificationChannel, NotificationType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type NotificationTemplatePaginationParams = DomainPaginationParams;
pub type NotificationTemplatePaginatedResult = DomainPaginatedResult<NotificationTemplate>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct NotificationTemplateFilter {
    pub name: Option<String>,
    pub notification_type: Option<NotificationType>,
    pub channel: Option<NotificationChannel>,
    pub subject_template: Option<String>,
    pub message_template: Option<String>,
    pub is_active: Option<bool>,
    pub language: Option<String>,
}

impl NotificationTemplateFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.notification_type.is_some() || self.channel.is_some() || self.subject_template.is_some() || self.message_template.is_some() || self.is_active.is_some() || self.language.is_some()
    }
}

/// Repository trait for NotificationTemplate aggregate
///
/// This trait defines the persistence contract for NotificationTemplate entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait NotificationTemplateRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new notification_template entity
    async fn save(&self, entity: &NotificationTemplate) -> Result<NotificationTemplate>;

    /// Find notification_template by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<NotificationTemplate>>;

    /// Find all notification_template entities
    async fn find_all(&self) -> Result<Vec<NotificationTemplate>>;

    /// Update notification_template by ID
    async fn update(&self, id: &str, entity: &NotificationTemplate) -> Result<Option<NotificationTemplate>>;

    /// Delete notification_template by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List notification_template with pagination
    async fn list(&self, params: NotificationTemplatePaginationParams) -> Result<NotificationTemplatePaginatedResult>;

    /// List notification_template with pagination and filters
    async fn list_with_filters(&self, params: NotificationTemplatePaginationParams, filters: NotificationTemplateFilter) -> Result<NotificationTemplatePaginatedResult>;

    /// Count all notification_template entities
    async fn count(&self) -> Result<u64>;

    /// Count notification_template entities matching filters
    async fn count_with_filters(&self, filters: NotificationTemplateFilter) -> Result<u64>;

    /// Check if notification_template exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete notification_template by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted notification_template by ID
    async fn restore(&self, id: &str) -> Result<Option<NotificationTemplate>>;

    /// List soft-deleted notification_template entities
    async fn list_deleted(&self, params: NotificationTemplatePaginationParams) -> Result<NotificationTemplatePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find notification_template by name
    async fn find_by_name(&self, name: &str) -> Result<Option<NotificationTemplate>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if notification_template exists by name
    async fn exists_by_name(&self, name: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save notification_template entities
    async fn bulk_save(&self, entities: &[NotificationTemplate]) -> Result<Vec<NotificationTemplate>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
