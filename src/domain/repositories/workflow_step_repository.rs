//! Repository trait for WorkflowStep aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the WorkflowStep aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{WorkflowStep, WorkflowStepStatus, WorkflowStepType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type WorkflowStepPaginationParams = DomainPaginationParams;
pub type WorkflowStepPaginatedResult = DomainPaginatedResult<WorkflowStep>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct WorkflowStepFilter {
    pub workflow_id: Option<Uuid>,
    pub step_type: Option<WorkflowStepType>,
    pub step_name: Option<String>,
    pub description: Option<String>,
    pub status: Option<WorkflowStepStatus>,
    pub error_message: Option<String>,
}

impl WorkflowStepFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.workflow_id.is_some() || self.step_type.is_some() || self.step_name.is_some() || self.description.is_some() || self.status.is_some() || self.error_message.is_some()
    }
}

/// Repository trait for WorkflowStep aggregate
///
/// This trait defines the persistence contract for WorkflowStep entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait WorkflowStepRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new workflow_step entity
    async fn save(&self, entity: &WorkflowStep) -> Result<WorkflowStep>;

    /// Find workflow_step by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<WorkflowStep>>;

    /// Find all workflow_step entities
    async fn find_all(&self) -> Result<Vec<WorkflowStep>>;

    /// Update workflow_step by ID
    async fn update(&self, id: &str, entity: &WorkflowStep) -> Result<Option<WorkflowStep>>;

    /// Delete workflow_step by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List workflow_step with pagination
    async fn list(&self, params: WorkflowStepPaginationParams) -> Result<WorkflowStepPaginatedResult>;

    /// List workflow_step with pagination and filters
    async fn list_with_filters(&self, params: WorkflowStepPaginationParams, filters: WorkflowStepFilter) -> Result<WorkflowStepPaginatedResult>;

    /// Count all workflow_step entities
    async fn count(&self) -> Result<u64>;

    /// Count workflow_step entities matching filters
    async fn count_with_filters(&self, filters: WorkflowStepFilter) -> Result<u64>;

    /// Check if workflow_step exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete workflow_step by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted workflow_step by ID
    async fn restore(&self, id: &str) -> Result<Option<WorkflowStep>>;

    /// List soft-deleted workflow_step entities
    async fn list_deleted(&self, params: WorkflowStepPaginationParams) -> Result<WorkflowStepPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save workflow_step entities
    async fn bulk_save(&self, entities: &[WorkflowStep]) -> Result<Vec<WorkflowStep>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
