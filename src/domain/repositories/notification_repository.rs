//! Repository trait for Notification aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Notification aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{Notification, NotificationChannel, NotificationPriority, NotificationType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type NotificationPaginationParams = DomainPaginationParams;
pub type NotificationPaginatedResult = DomainPaginatedResult<Notification>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct NotificationFilter {
    pub user_id: Option<Uuid>,
    pub notification_type: Option<NotificationType>,
    pub channel: Option<NotificationChannel>,
    pub title: Option<String>,
    pub message: Option<String>,
    pub is_read: Option<bool>,
    pub priority: Option<NotificationPriority>,
    pub action_text: Option<String>,
    pub category: Option<String>,
}

impl NotificationFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.notification_type.is_some() || self.channel.is_some() || self.title.is_some() || self.message.is_some() || self.is_read.is_some() || self.priority.is_some() || self.action_text.is_some() || self.category.is_some()
    }
}

/// Repository trait for Notification aggregate
///
/// This trait defines the persistence contract for Notification entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait NotificationRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new notification entity
    async fn save(&self, entity: &Notification) -> Result<Notification>;

    /// Find notification by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Notification>>;

    /// Find all notification entities
    async fn find_all(&self) -> Result<Vec<Notification>>;

    /// Update notification by ID
    async fn update(&self, id: &str, entity: &Notification) -> Result<Option<Notification>>;

    /// Delete notification by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List notification with pagination
    async fn list(&self, params: NotificationPaginationParams) -> Result<NotificationPaginatedResult>;

    /// List notification with pagination and filters
    async fn list_with_filters(&self, params: NotificationPaginationParams, filters: NotificationFilter) -> Result<NotificationPaginatedResult>;

    /// Count all notification entities
    async fn count(&self) -> Result<u64>;

    /// Count notification entities matching filters
    async fn count_with_filters(&self, filters: NotificationFilter) -> Result<u64>;

    /// Check if notification exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete notification by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted notification by ID
    async fn restore(&self, id: &str) -> Result<Option<Notification>>;

    /// List soft-deleted notification entities
    async fn list_deleted(&self, params: NotificationPaginationParams) -> Result<NotificationPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save notification entities
    async fn bulk_save(&self, entities: &[Notification]) -> Result<Vec<Notification>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
