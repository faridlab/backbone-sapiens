//! Repository trait for PermissionConflict aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PermissionConflict aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PermissionConflict, ConflictResolutionStatus, ConflictSeverity};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PermissionConflictPaginationParams = DomainPaginationParams;
pub type PermissionConflictPaginatedResult = DomainPaginatedResult<PermissionConflict>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PermissionConflictFilter {
    pub conflict_type: Option<String>,
    pub severity: Option<ConflictSeverity>,
    pub description: Option<String>,
    pub conflicting_entity_id: Option<Uuid>,
    pub resolution_status: Option<ConflictResolutionStatus>,
}

impl PermissionConflictFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.conflict_type.is_some() || self.severity.is_some() || self.description.is_some() || self.conflicting_entity_id.is_some() || self.resolution_status.is_some()
    }
}

/// Repository trait for PermissionConflict aggregate
///
/// This trait defines the persistence contract for PermissionConflict entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PermissionConflictRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new permission_conflict entity
    async fn save(&self, entity: &PermissionConflict) -> Result<PermissionConflict>;

    /// Find permission_conflict by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PermissionConflict>>;

    /// Find all permission_conflict entities
    async fn find_all(&self) -> Result<Vec<PermissionConflict>>;

    /// Update permission_conflict by ID
    async fn update(&self, id: &str, entity: &PermissionConflict) -> Result<Option<PermissionConflict>>;

    /// Delete permission_conflict by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List permission_conflict with pagination
    async fn list(&self, params: PermissionConflictPaginationParams) -> Result<PermissionConflictPaginatedResult>;

    /// List permission_conflict with pagination and filters
    async fn list_with_filters(&self, params: PermissionConflictPaginationParams, filters: PermissionConflictFilter) -> Result<PermissionConflictPaginatedResult>;

    /// Count all permission_conflict entities
    async fn count(&self) -> Result<u64>;

    /// Count permission_conflict entities matching filters
    async fn count_with_filters(&self, filters: PermissionConflictFilter) -> Result<u64>;

    /// Check if permission_conflict exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete permission_conflict by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted permission_conflict by ID
    async fn restore(&self, id: &str) -> Result<Option<PermissionConflict>>;

    /// List soft-deleted permission_conflict entities
    async fn list_deleted(&self, params: PermissionConflictPaginationParams) -> Result<PermissionConflictPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save permission_conflict entities
    async fn bulk_save(&self, entities: &[PermissionConflict]) -> Result<Vec<PermissionConflict>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
