//! Repository trait for OrganizationUser aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the OrganizationUser aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{OrganizationUser, OrganizationMembershipStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type OrganizationUserPaginationParams = DomainPaginationParams;
pub type OrganizationUserPaginatedResult = DomainPaginatedResult<OrganizationUser>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct OrganizationUserFilter {
    pub organization_id: Option<Uuid>,
    pub user_id: Option<Uuid>,
    pub role_id: Option<Uuid>,
    pub status: Option<OrganizationMembershipStatus>,
}

impl OrganizationUserFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.organization_id.is_some() || self.user_id.is_some() || self.role_id.is_some() || self.status.is_some()
    }
}

/// Repository trait for OrganizationUser aggregate
///
/// This trait defines the persistence contract for OrganizationUser entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait OrganizationUserRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new organization_user entity
    async fn save(&self, entity: &OrganizationUser) -> Result<OrganizationUser>;

    /// Find organization_user by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<OrganizationUser>>;

    /// Find all organization_user entities
    async fn find_all(&self) -> Result<Vec<OrganizationUser>>;

    /// Update organization_user by ID
    async fn update(&self, id: &str, entity: &OrganizationUser) -> Result<Option<OrganizationUser>>;

    /// Delete organization_user by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List organization_user with pagination
    async fn list(&self, params: OrganizationUserPaginationParams) -> Result<OrganizationUserPaginatedResult>;

    /// List organization_user with pagination and filters
    async fn list_with_filters(&self, params: OrganizationUserPaginationParams, filters: OrganizationUserFilter) -> Result<OrganizationUserPaginatedResult>;

    /// Count all organization_user entities
    async fn count(&self) -> Result<u64>;

    /// Count organization_user entities matching filters
    async fn count_with_filters(&self, filters: OrganizationUserFilter) -> Result<u64>;

    /// Check if organization_user exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete organization_user by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted organization_user by ID
    async fn restore(&self, id: &str) -> Result<Option<OrganizationUser>>;

    /// List soft-deleted organization_user entities
    async fn list_deleted(&self, params: OrganizationUserPaginationParams) -> Result<OrganizationUserPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save organization_user entities
    async fn bulk_save(&self, entities: &[OrganizationUser]) -> Result<Vec<OrganizationUser>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
