//! Repository trait for PasswordHistory aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PasswordHistory aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PasswordHistory, PasswordHistoryStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PasswordHistoryPaginationParams = DomainPaginationParams;
pub type PasswordHistoryPaginatedResult = DomainPaginatedResult<PasswordHistory>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PasswordHistoryFilter {
    pub user_id: Option<Uuid>,
    pub password_hash: Option<String>,
    pub status: Option<PasswordHistoryStatus>,
}

impl PasswordHistoryFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.password_hash.is_some() || self.status.is_some()
    }
}

/// Repository trait for PasswordHistory aggregate
///
/// This trait defines the persistence contract for PasswordHistory entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PasswordHistoryRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new password_history entity
    async fn save(&self, entity: &PasswordHistory) -> Result<PasswordHistory>;

    /// Find password_history by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PasswordHistory>>;

    /// Find all password_history entities
    async fn find_all(&self) -> Result<Vec<PasswordHistory>>;

    /// Update password_history by ID
    async fn update(&self, id: &str, entity: &PasswordHistory) -> Result<Option<PasswordHistory>>;

    /// Delete password_history by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List password_history with pagination
    async fn list(&self, params: PasswordHistoryPaginationParams) -> Result<PasswordHistoryPaginatedResult>;

    /// List password_history with pagination and filters
    async fn list_with_filters(&self, params: PasswordHistoryPaginationParams, filters: PasswordHistoryFilter) -> Result<PasswordHistoryPaginatedResult>;

    /// Count all password_history entities
    async fn count(&self) -> Result<u64>;

    /// Count password_history entities matching filters
    async fn count_with_filters(&self, filters: PasswordHistoryFilter) -> Result<u64>;

    /// Check if password_history exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete password_history by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted password_history by ID
    async fn restore(&self, id: &str) -> Result<Option<PasswordHistory>>;

    /// List soft-deleted password_history entities
    async fn list_deleted(&self, params: PasswordHistoryPaginationParams) -> Result<PasswordHistoryPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save password_history entities
    async fn bulk_save(&self, entities: &[PasswordHistory]) -> Result<Vec<PasswordHistory>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
