//! Repository trait for OrganizationRole aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the OrganizationRole aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::OrganizationRole;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type OrganizationRolePaginationParams = DomainPaginationParams;
pub type OrganizationRolePaginatedResult = DomainPaginatedResult<OrganizationRole>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct OrganizationRoleFilter {
    pub organization_id: Option<Uuid>,
    pub name: Option<String>,
    pub description: Option<String>,
    pub is_active: Option<bool>,
}

impl OrganizationRoleFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.organization_id.is_some() || self.name.is_some() || self.description.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for OrganizationRole aggregate
///
/// This trait defines the persistence contract for OrganizationRole entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait OrganizationRoleRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new organization_role entity
    async fn save(&self, entity: &OrganizationRole) -> Result<OrganizationRole>;

    /// Find organization_role by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<OrganizationRole>>;

    /// Find all organization_role entities
    async fn find_all(&self) -> Result<Vec<OrganizationRole>>;

    /// Update organization_role by ID
    async fn update(&self, id: &str, entity: &OrganizationRole) -> Result<Option<OrganizationRole>>;

    /// Delete organization_role by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List organization_role with pagination
    async fn list(&self, params: OrganizationRolePaginationParams) -> Result<OrganizationRolePaginatedResult>;

    /// List organization_role with pagination and filters
    async fn list_with_filters(&self, params: OrganizationRolePaginationParams, filters: OrganizationRoleFilter) -> Result<OrganizationRolePaginatedResult>;

    /// Count all organization_role entities
    async fn count(&self) -> Result<u64>;

    /// Count organization_role entities matching filters
    async fn count_with_filters(&self, filters: OrganizationRoleFilter) -> Result<u64>;

    /// Check if organization_role exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete organization_role by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted organization_role by ID
    async fn restore(&self, id: &str) -> Result<Option<OrganizationRole>>;

    /// List soft-deleted organization_role entities
    async fn list_deleted(&self, params: OrganizationRolePaginationParams) -> Result<OrganizationRolePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save organization_role entities
    async fn bulk_save(&self, entities: &[OrganizationRole]) -> Result<Vec<OrganizationRole>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
