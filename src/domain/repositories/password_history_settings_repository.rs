//! Repository trait for PasswordHistorySettings aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PasswordHistorySettings aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::PasswordHistorySettings;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PasswordHistorySettingsPaginationParams = DomainPaginationParams;
pub type PasswordHistorySettingsPaginatedResult = DomainPaginatedResult<PasswordHistorySettings>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PasswordHistorySettingsFilter {
    pub track_history: Option<bool>,
    pub prevent_reuse: Option<bool>,
    pub prevent_similar: Option<bool>,
}

impl PasswordHistorySettingsFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.track_history.is_some() || self.prevent_reuse.is_some() || self.prevent_similar.is_some()
    }
}

/// Repository trait for PasswordHistorySettings aggregate
///
/// This trait defines the persistence contract for PasswordHistorySettings entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PasswordHistorySettingsRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new password_history_settings entity
    async fn save(&self, entity: &PasswordHistorySettings) -> Result<PasswordHistorySettings>;

    /// Find password_history_settings by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PasswordHistorySettings>>;

    /// Find all password_history_settings entities
    async fn find_all(&self) -> Result<Vec<PasswordHistorySettings>>;

    /// Update password_history_settings by ID
    async fn update(&self, id: &str, entity: &PasswordHistorySettings) -> Result<Option<PasswordHistorySettings>>;

    /// Delete password_history_settings by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List password_history_settings with pagination
    async fn list(&self, params: PasswordHistorySettingsPaginationParams) -> Result<PasswordHistorySettingsPaginatedResult>;

    /// List password_history_settings with pagination and filters
    async fn list_with_filters(&self, params: PasswordHistorySettingsPaginationParams, filters: PasswordHistorySettingsFilter) -> Result<PasswordHistorySettingsPaginatedResult>;

    /// Count all password_history_settings entities
    async fn count(&self) -> Result<u64>;

    /// Count password_history_settings entities matching filters
    async fn count_with_filters(&self, filters: PasswordHistorySettingsFilter) -> Result<u64>;

    /// Check if password_history_settings exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete password_history_settings by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted password_history_settings by ID
    async fn restore(&self, id: &str) -> Result<Option<PasswordHistorySettings>>;

    /// List soft-deleted password_history_settings entities
    async fn list_deleted(&self, params: PasswordHistorySettingsPaginationParams) -> Result<PasswordHistorySettingsPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save password_history_settings entities
    async fn bulk_save(&self, entities: &[PasswordHistorySettings]) -> Result<Vec<PasswordHistorySettings>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
