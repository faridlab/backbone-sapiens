//! Repository trait for ImpersonationSession aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the ImpersonationSession aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{ImpersonationSession, ImpersonationSessionStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ImpersonationSessionPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl ImpersonationSessionPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct ImpersonationSessionPaginatedResult {
    pub data: Vec<ImpersonationSession>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct ImpersonationSessionFilter {
    pub admin_id: Option<Uuid>,
    pub target_user_id: Option<Uuid>,
    pub session_id: Option<Uuid>,
    pub reason: Option<String>,
    pub terminated_by: Option<Uuid>,
    pub termination_reason: Option<String>,
    pub status: Option<ImpersonationSessionStatus>,
}

impl ImpersonationSessionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.admin_id.is_some() || self.target_user_id.is_some() || self.session_id.is_some() || self.reason.is_some() || self.terminated_by.is_some() || self.termination_reason.is_some() || self.status.is_some()
    }
}

/// Repository trait for ImpersonationSession aggregate
///
/// This trait defines the persistence contract for ImpersonationSession entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait ImpersonationSessionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new impersonation_session entity
    async fn save(&self, entity: &ImpersonationSession) -> Result<ImpersonationSession>;

    /// Find impersonation_session by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<ImpersonationSession>>;

    /// Find all impersonation_session entities
    async fn find_all(&self) -> Result<Vec<ImpersonationSession>>;

    /// Update impersonation_session by ID
    async fn update(&self, id: &str, entity: &ImpersonationSession) -> Result<Option<ImpersonationSession>>;

    /// Delete impersonation_session by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List impersonation_session with pagination
    async fn list(&self, params: ImpersonationSessionPaginationParams) -> Result<ImpersonationSessionPaginatedResult>;

    /// List impersonation_session with pagination and filters
    async fn list_with_filters(&self, params: ImpersonationSessionPaginationParams, filters: ImpersonationSessionFilter) -> Result<ImpersonationSessionPaginatedResult>;

    /// Count all impersonation_session entities
    async fn count(&self) -> Result<u64>;

    /// Count impersonation_session entities matching filters
    async fn count_with_filters(&self, filters: ImpersonationSessionFilter) -> Result<u64>;

    /// Check if impersonation_session exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete impersonation_session by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted impersonation_session by ID
    async fn restore(&self, id: &str) -> Result<Option<ImpersonationSession>>;

    /// List soft-deleted impersonation_session entities
    async fn list_deleted(&self, params: ImpersonationSessionPaginationParams) -> Result<ImpersonationSessionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save impersonation_session entities
    async fn bulk_save(&self, entities: &[ImpersonationSession]) -> Result<Vec<ImpersonationSession>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
