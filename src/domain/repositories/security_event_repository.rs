//! Repository trait for SecurityEvent aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SecurityEvent aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{SecurityEvent, SecurityEventSeverity, SecurityEventType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type SecurityEventPaginationParams = DomainPaginationParams;
pub type SecurityEventPaginatedResult = DomainPaginatedResult<SecurityEvent>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SecurityEventFilter {
    pub user_id: Option<Uuid>,
    pub session_id: Option<Uuid>,
    pub event_type: Option<SecurityEventType>,
    pub severity: Option<SecurityEventSeverity>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub description: Option<String>,
    pub resolved: Option<bool>,
    pub resolved_by_user_id: Option<Uuid>,
    pub resolution_notes: Option<String>,
}

impl SecurityEventFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.session_id.is_some() || self.event_type.is_some() || self.severity.is_some() || self.ip_address.is_some() || self.user_agent.is_some() || self.description.is_some() || self.resolved.is_some() || self.resolved_by_user_id.is_some() || self.resolution_notes.is_some()
    }
}

/// Repository trait for SecurityEvent aggregate
///
/// This trait defines the persistence contract for SecurityEvent entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SecurityEventRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new security_event entity
    async fn save(&self, entity: &SecurityEvent) -> Result<SecurityEvent>;

    /// Find security_event by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SecurityEvent>>;

    /// Find all security_event entities
    async fn find_all(&self) -> Result<Vec<SecurityEvent>>;

    /// Update security_event by ID
    async fn update(&self, id: &str, entity: &SecurityEvent) -> Result<Option<SecurityEvent>>;

    /// Delete security_event by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List security_event with pagination
    async fn list(&self, params: SecurityEventPaginationParams) -> Result<SecurityEventPaginatedResult>;

    /// List security_event with pagination and filters
    async fn list_with_filters(&self, params: SecurityEventPaginationParams, filters: SecurityEventFilter) -> Result<SecurityEventPaginatedResult>;

    /// Count all security_event entities
    async fn count(&self) -> Result<u64>;

    /// Count security_event entities matching filters
    async fn count_with_filters(&self, filters: SecurityEventFilter) -> Result<u64>;

    /// Check if security_event exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete security_event by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted security_event by ID
    async fn restore(&self, id: &str) -> Result<Option<SecurityEvent>>;

    /// List soft-deleted security_event entities
    async fn list_deleted(&self, params: SecurityEventPaginationParams) -> Result<SecurityEventPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save security_event entities
    async fn bulk_save(&self, entities: &[SecurityEvent]) -> Result<Vec<SecurityEvent>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
