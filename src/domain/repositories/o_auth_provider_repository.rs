//! Repository trait for OAuthProvider aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the OAuthProvider aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{OAuthProvider, OAuthProviderType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type OAuthProviderPaginationParams = DomainPaginationParams;
pub type OAuthProviderPaginatedResult = DomainPaginatedResult<OAuthProvider>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct OAuthProviderFilter {
    pub provider_name: Option<OAuthProviderType>,
    pub display_name: Option<String>,
    pub client_id: Option<String>,
    pub client_secret: Option<String>,
    pub is_active: Option<bool>,
}

impl OAuthProviderFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.provider_name.is_some() || self.display_name.is_some() || self.client_id.is_some() || self.client_secret.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for OAuthProvider aggregate
///
/// This trait defines the persistence contract for OAuthProvider entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait OAuthProviderRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new o_auth_provider entity
    async fn save(&self, entity: &OAuthProvider) -> Result<OAuthProvider>;

    /// Find o_auth_provider by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<OAuthProvider>>;

    /// Find all o_auth_provider entities
    async fn find_all(&self) -> Result<Vec<OAuthProvider>>;

    /// Update o_auth_provider by ID
    async fn update(&self, id: &str, entity: &OAuthProvider) -> Result<Option<OAuthProvider>>;

    /// Delete o_auth_provider by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List o_auth_provider with pagination
    async fn list(&self, params: OAuthProviderPaginationParams) -> Result<OAuthProviderPaginatedResult>;

    /// List o_auth_provider with pagination and filters
    async fn list_with_filters(&self, params: OAuthProviderPaginationParams, filters: OAuthProviderFilter) -> Result<OAuthProviderPaginatedResult>;

    /// Count all o_auth_provider entities
    async fn count(&self) -> Result<u64>;

    /// Count o_auth_provider entities matching filters
    async fn count_with_filters(&self, filters: OAuthProviderFilter) -> Result<u64>;

    /// Check if o_auth_provider exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete o_auth_provider by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted o_auth_provider by ID
    async fn restore(&self, id: &str) -> Result<Option<OAuthProvider>>;

    /// List soft-deleted o_auth_provider entities
    async fn list_deleted(&self, params: OAuthProviderPaginationParams) -> Result<OAuthProviderPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find o_auth_provider by provider_name
    async fn find_by_provider_name(&self, provider_name: &OAuthProviderType) -> Result<Option<OAuthProvider>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if o_auth_provider exists by provider_name
    async fn exists_by_provider_name(&self, provider_name: &OAuthProviderType) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save o_auth_provider entities
    async fn bulk_save(&self, entities: &[OAuthProvider]) -> Result<Vec<OAuthProvider>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
