//! Repository trait for MFABackupCode aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the MFABackupCode aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{MFABackupCode, CompromiseDetectionMethod, GenerationMethod};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type MFABackupCodePaginationParams = DomainPaginationParams;
pub type MFABackupCodePaginatedResult = DomainPaginatedResult<MFABackupCode>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct MFABackupCodeFilter {
    pub user_id: Option<Uuid>,
    pub device_id: Option<Uuid>,
    pub batch_id: Option<Uuid>,
    pub code: Option<String>,
    pub code_hash: Option<String>,
    pub generated_by: Option<Uuid>,
    pub generation_method: Option<GenerationMethod>,
    pub generation_ip: Option<String>,
    pub algorithm_version: Option<String>,
    pub used_ip: Option<String>,
    pub used_user_agent: Option<String>,
    pub session_id: Option<Uuid>,
    pub is_consumed: Option<bool>,
    pub successful_verification: Option<bool>,
    pub is_revoked: Option<bool>,
    pub revoked_by: Option<Uuid>,
    pub revocation_reason: Option<String>,
    pub compromised: Option<bool>,
    pub compromise_detection_method: Option<CompromiseDetectionMethod>,
    pub created_with_expiry: Option<bool>,
    pub auto_renew_enabled: Option<bool>,
    pub renewal_reminder_sent: Option<bool>,
    pub creation_notification_sent: Option<bool>,
    pub expiry_warning_sent: Option<bool>,
    pub usage_notification_sent: Option<bool>,
    pub security_alert_sent: Option<bool>,
    pub gdpr_compliant: Option<bool>,
    pub audit_log_required: Option<bool>,
}

impl MFABackupCodeFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.device_id.is_some() || self.batch_id.is_some() || self.code.is_some() || self.code_hash.is_some() || self.generated_by.is_some() || self.generation_method.is_some() || self.generation_ip.is_some() || self.algorithm_version.is_some() || self.used_ip.is_some() || self.used_user_agent.is_some() || self.session_id.is_some() || self.is_consumed.is_some() || self.successful_verification.is_some() || self.is_revoked.is_some() || self.revoked_by.is_some() || self.revocation_reason.is_some() || self.compromised.is_some() || self.compromise_detection_method.is_some() || self.created_with_expiry.is_some() || self.auto_renew_enabled.is_some() || self.renewal_reminder_sent.is_some() || self.creation_notification_sent.is_some() || self.expiry_warning_sent.is_some() || self.usage_notification_sent.is_some() || self.security_alert_sent.is_some() || self.gdpr_compliant.is_some() || self.audit_log_required.is_some()
    }
}

/// Repository trait for MFABackupCode aggregate
///
/// This trait defines the persistence contract for MFABackupCode entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait MFABackupCodeRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new mfa_backup_code entity
    async fn save(&self, entity: &MFABackupCode) -> Result<MFABackupCode>;

    /// Find mfa_backup_code by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<MFABackupCode>>;

    /// Find all mfa_backup_code entities
    async fn find_all(&self) -> Result<Vec<MFABackupCode>>;

    /// Update mfa_backup_code by ID
    async fn update(&self, id: &str, entity: &MFABackupCode) -> Result<Option<MFABackupCode>>;

    /// Delete mfa_backup_code by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List mfa_backup_code with pagination
    async fn list(&self, params: MFABackupCodePaginationParams) -> Result<MFABackupCodePaginatedResult>;

    /// List mfa_backup_code with pagination and filters
    async fn list_with_filters(&self, params: MFABackupCodePaginationParams, filters: MFABackupCodeFilter) -> Result<MFABackupCodePaginatedResult>;

    /// Count all mfa_backup_code entities
    async fn count(&self) -> Result<u64>;

    /// Count mfa_backup_code entities matching filters
    async fn count_with_filters(&self, filters: MFABackupCodeFilter) -> Result<u64>;

    /// Check if mfa_backup_code exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete mfa_backup_code by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted mfa_backup_code by ID
    async fn restore(&self, id: &str) -> Result<Option<MFABackupCode>>;

    /// List soft-deleted mfa_backup_code entities
    async fn list_deleted(&self, params: MFABackupCodePaginationParams) -> Result<MFABackupCodePaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save mfa_backup_code entities
    async fn bulk_save(&self, entities: &[MFABackupCode]) -> Result<Vec<MFABackupCode>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
