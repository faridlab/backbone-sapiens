//! Repository trait for WorkflowActionExecution aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the WorkflowActionExecution aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{WorkflowActionExecution, WorkflowActionExecutionStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type WorkflowActionExecutionPaginationParams = DomainPaginationParams;
pub type WorkflowActionExecutionPaginatedResult = DomainPaginatedResult<WorkflowActionExecution>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct WorkflowActionExecutionFilter {
    pub workflow_execution_id: Option<Uuid>,
    pub workflow_action_id: Option<Uuid>,
    pub status: Option<WorkflowActionExecutionStatus>,
    pub error_message: Option<String>,
}

impl WorkflowActionExecutionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.workflow_execution_id.is_some() || self.workflow_action_id.is_some() || self.status.is_some() || self.error_message.is_some()
    }
}

/// Repository trait for WorkflowActionExecution aggregate
///
/// This trait defines the persistence contract for WorkflowActionExecution entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait WorkflowActionExecutionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new workflow_action_execution entity
    async fn save(&self, entity: &WorkflowActionExecution) -> Result<WorkflowActionExecution>;

    /// Find workflow_action_execution by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<WorkflowActionExecution>>;

    /// Find all workflow_action_execution entities
    async fn find_all(&self) -> Result<Vec<WorkflowActionExecution>>;

    /// Update workflow_action_execution by ID
    async fn update(&self, id: &str, entity: &WorkflowActionExecution) -> Result<Option<WorkflowActionExecution>>;

    /// Delete workflow_action_execution by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List workflow_action_execution with pagination
    async fn list(&self, params: WorkflowActionExecutionPaginationParams) -> Result<WorkflowActionExecutionPaginatedResult>;

    /// List workflow_action_execution with pagination and filters
    async fn list_with_filters(&self, params: WorkflowActionExecutionPaginationParams, filters: WorkflowActionExecutionFilter) -> Result<WorkflowActionExecutionPaginatedResult>;

    /// Count all workflow_action_execution entities
    async fn count(&self) -> Result<u64>;

    /// Count workflow_action_execution entities matching filters
    async fn count_with_filters(&self, filters: WorkflowActionExecutionFilter) -> Result<u64>;

    /// Check if workflow_action_execution exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete workflow_action_execution by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted workflow_action_execution by ID
    async fn restore(&self, id: &str) -> Result<Option<WorkflowActionExecution>>;

    /// List soft-deleted workflow_action_execution entities
    async fn list_deleted(&self, params: WorkflowActionExecutionPaginationParams) -> Result<WorkflowActionExecutionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save workflow_action_execution entities
    async fn bulk_save(&self, entities: &[WorkflowActionExecution]) -> Result<Vec<WorkflowActionExecution>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
