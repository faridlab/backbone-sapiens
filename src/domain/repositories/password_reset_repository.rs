//! Repository trait for PasswordReset aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PasswordReset aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PasswordReset, PasswordResetStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PasswordResetPaginationParams = DomainPaginationParams;
pub type PasswordResetPaginatedResult = DomainPaginatedResult<PasswordReset>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PasswordResetFilter {
    pub user_id: Option<Uuid>,
    pub token: Option<String>,
    pub token_hash: Option<String>,
    pub email: Option<String>,
    pub status: Option<PasswordResetStatus>,
    pub verification_details_id: Option<Uuid>,
    pub completion_details_id: Option<Uuid>,
    pub security_id: Option<Uuid>,
    pub device_fingerprint: Option<String>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
}

impl PasswordResetFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.token.is_some() || self.token_hash.is_some() || self.email.is_some() || self.status.is_some() || self.verification_details_id.is_some() || self.completion_details_id.is_some() || self.security_id.is_some() || self.device_fingerprint.is_some() || self.ip_address.is_some() || self.user_agent.is_some()
    }
}

/// Repository trait for PasswordReset aggregate
///
/// This trait defines the persistence contract for PasswordReset entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PasswordResetRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new password_reset entity
    async fn save(&self, entity: &PasswordReset) -> Result<PasswordReset>;

    /// Find password_reset by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PasswordReset>>;

    /// Find all password_reset entities
    async fn find_all(&self) -> Result<Vec<PasswordReset>>;

    /// Update password_reset by ID
    async fn update(&self, id: &str, entity: &PasswordReset) -> Result<Option<PasswordReset>>;

    /// Delete password_reset by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List password_reset with pagination
    async fn list(&self, params: PasswordResetPaginationParams) -> Result<PasswordResetPaginatedResult>;

    /// List password_reset with pagination and filters
    async fn list_with_filters(&self, params: PasswordResetPaginationParams, filters: PasswordResetFilter) -> Result<PasswordResetPaginatedResult>;

    /// Count all password_reset entities
    async fn count(&self) -> Result<u64>;

    /// Count password_reset entities matching filters
    async fn count_with_filters(&self, filters: PasswordResetFilter) -> Result<u64>;

    /// Check if password_reset exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete password_reset by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted password_reset by ID
    async fn restore(&self, id: &str) -> Result<Option<PasswordReset>>;

    /// List soft-deleted password_reset entities
    async fn list_deleted(&self, params: PasswordResetPaginationParams) -> Result<PasswordResetPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find password_reset by token
    async fn find_by_token(&self, token: &str) -> Result<Option<PasswordReset>>;

    /// Find password_reset by token_hash
    async fn find_by_token_hash(&self, token_hash: &str) -> Result<Option<PasswordReset>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if password_reset exists by token
    async fn exists_by_token(&self, token: &str) -> Result<bool>;

    /// Check if password_reset exists by token_hash
    async fn exists_by_token_hash(&self, token_hash: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save password_reset entities
    async fn bulk_save(&self, entities: &[PasswordReset]) -> Result<Vec<PasswordReset>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
