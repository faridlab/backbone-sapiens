//! Repository trait for WorkflowDefinition aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the WorkflowDefinition aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{WorkflowDefinition, TriggerType, WorkflowType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type WorkflowDefinitionPaginationParams = DomainPaginationParams;
pub type WorkflowDefinitionPaginatedResult = DomainPaginatedResult<WorkflowDefinition>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct WorkflowDefinitionFilter {
    pub name: Option<String>,
    pub workflow_type: Option<WorkflowType>,
    pub description: Option<String>,
    pub trigger_type: Option<TriggerType>,
    pub is_active: Option<bool>,
}

impl WorkflowDefinitionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.workflow_type.is_some() || self.description.is_some() || self.trigger_type.is_some() || self.is_active.is_some()
    }
}

/// Repository trait for WorkflowDefinition aggregate
///
/// This trait defines the persistence contract for WorkflowDefinition entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait WorkflowDefinitionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new workflow_definition entity
    async fn save(&self, entity: &WorkflowDefinition) -> Result<WorkflowDefinition>;

    /// Find workflow_definition by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<WorkflowDefinition>>;

    /// Find all workflow_definition entities
    async fn find_all(&self) -> Result<Vec<WorkflowDefinition>>;

    /// Update workflow_definition by ID
    async fn update(&self, id: &str, entity: &WorkflowDefinition) -> Result<Option<WorkflowDefinition>>;

    /// Delete workflow_definition by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List workflow_definition with pagination
    async fn list(&self, params: WorkflowDefinitionPaginationParams) -> Result<WorkflowDefinitionPaginatedResult>;

    /// List workflow_definition with pagination and filters
    async fn list_with_filters(&self, params: WorkflowDefinitionPaginationParams, filters: WorkflowDefinitionFilter) -> Result<WorkflowDefinitionPaginatedResult>;

    /// Count all workflow_definition entities
    async fn count(&self) -> Result<u64>;

    /// Count workflow_definition entities matching filters
    async fn count_with_filters(&self, filters: WorkflowDefinitionFilter) -> Result<u64>;

    /// Check if workflow_definition exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete workflow_definition by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted workflow_definition by ID
    async fn restore(&self, id: &str) -> Result<Option<WorkflowDefinition>>;

    /// List soft-deleted workflow_definition entities
    async fn list_deleted(&self, params: WorkflowDefinitionPaginationParams) -> Result<WorkflowDefinitionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find workflow_definition by name
    async fn find_by_name(&self, name: &str) -> Result<Option<WorkflowDefinition>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if workflow_definition exists by name
    async fn exists_by_name(&self, name: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save workflow_definition entities
    async fn bulk_save(&self, entities: &[WorkflowDefinition]) -> Result<Vec<WorkflowDefinition>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
