//! Repository trait for BulkOperationResult aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the BulkOperationResult aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{BulkOperationResult, BulkOperationResultStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type BulkOperationResultPaginationParams = DomainPaginationParams;
pub type BulkOperationResultPaginatedResult = DomainPaginatedResult<BulkOperationResult>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct BulkOperationResultFilter {
    pub bulk_operation_id: Option<Uuid>,
    pub record_type: Option<String>,
    pub status: Option<BulkOperationResultStatus>,
    pub error_message: Option<String>,
    pub warning_message: Option<String>,
}

impl BulkOperationResultFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.bulk_operation_id.is_some() || self.record_type.is_some() || self.status.is_some() || self.error_message.is_some() || self.warning_message.is_some()
    }
}

/// Repository trait for BulkOperationResult aggregate
///
/// This trait defines the persistence contract for BulkOperationResult entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait BulkOperationResultRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new bulk_operation_result entity
    async fn save(&self, entity: &BulkOperationResult) -> Result<BulkOperationResult>;

    /// Find bulk_operation_result by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<BulkOperationResult>>;

    /// Find all bulk_operation_result entities
    async fn find_all(&self) -> Result<Vec<BulkOperationResult>>;

    /// Update bulk_operation_result by ID
    async fn update(&self, id: &str, entity: &BulkOperationResult) -> Result<Option<BulkOperationResult>>;

    /// Delete bulk_operation_result by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List bulk_operation_result with pagination
    async fn list(&self, params: BulkOperationResultPaginationParams) -> Result<BulkOperationResultPaginatedResult>;

    /// List bulk_operation_result with pagination and filters
    async fn list_with_filters(&self, params: BulkOperationResultPaginationParams, filters: BulkOperationResultFilter) -> Result<BulkOperationResultPaginatedResult>;

    /// Count all bulk_operation_result entities
    async fn count(&self) -> Result<u64>;

    /// Count bulk_operation_result entities matching filters
    async fn count_with_filters(&self, filters: BulkOperationResultFilter) -> Result<u64>;

    /// Check if bulk_operation_result exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete bulk_operation_result by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted bulk_operation_result by ID
    async fn restore(&self, id: &str) -> Result<Option<BulkOperationResult>>;

    /// List soft-deleted bulk_operation_result entities
    async fn list_deleted(&self, params: BulkOperationResultPaginationParams) -> Result<BulkOperationResultPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save bulk_operation_result entities
    async fn bulk_save(&self, entities: &[BulkOperationResult]) -> Result<Vec<BulkOperationResult>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
