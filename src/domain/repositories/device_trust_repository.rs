//! Repository trait for DeviceTrust aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the DeviceTrust aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{DeviceTrust, DeviceTrustType, TrustLevel};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type DeviceTrustPaginationParams = DomainPaginationParams;
pub type DeviceTrustPaginatedResult = DomainPaginatedResult<DeviceTrust>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct DeviceTrustFilter {
    pub user_id: Option<Uuid>,
    pub device_fingerprint: Option<String>,
    pub device_name: Option<String>,
    pub device_type: Option<DeviceTrustType>,
    pub trust_level: Option<TrustLevel>,
    pub is_trusted: Option<bool>,
    pub requires_mfa: Option<bool>,
}

impl DeviceTrustFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.device_fingerprint.is_some() || self.device_name.is_some() || self.device_type.is_some() || self.trust_level.is_some() || self.is_trusted.is_some() || self.requires_mfa.is_some()
    }
}

/// Repository trait for DeviceTrust aggregate
///
/// This trait defines the persistence contract for DeviceTrust entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait DeviceTrustRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new device_trust entity
    async fn save(&self, entity: &DeviceTrust) -> Result<DeviceTrust>;

    /// Find device_trust by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<DeviceTrust>>;

    /// Find all device_trust entities
    async fn find_all(&self) -> Result<Vec<DeviceTrust>>;

    /// Update device_trust by ID
    async fn update(&self, id: &str, entity: &DeviceTrust) -> Result<Option<DeviceTrust>>;

    /// Delete device_trust by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List device_trust with pagination
    async fn list(&self, params: DeviceTrustPaginationParams) -> Result<DeviceTrustPaginatedResult>;

    /// List device_trust with pagination and filters
    async fn list_with_filters(&self, params: DeviceTrustPaginationParams, filters: DeviceTrustFilter) -> Result<DeviceTrustPaginatedResult>;

    /// Count all device_trust entities
    async fn count(&self) -> Result<u64>;

    /// Count device_trust entities matching filters
    async fn count_with_filters(&self, filters: DeviceTrustFilter) -> Result<u64>;

    /// Check if device_trust exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete device_trust by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted device_trust by ID
    async fn restore(&self, id: &str) -> Result<Option<DeviceTrust>>;

    /// List soft-deleted device_trust entities
    async fn list_deleted(&self, params: DeviceTrustPaginationParams) -> Result<DeviceTrustPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find device_trust by device_fingerprint
    async fn find_by_device_fingerprint(&self, device_fingerprint: &str) -> Result<Option<DeviceTrust>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if device_trust exists by device_fingerprint
    async fn exists_by_device_fingerprint(&self, device_fingerprint: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save device_trust entities
    async fn bulk_save(&self, entities: &[DeviceTrust]) -> Result<Vec<DeviceTrust>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
