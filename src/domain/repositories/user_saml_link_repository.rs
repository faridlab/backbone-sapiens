//! Repository trait for UserSAMLLink aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the UserSAMLLink aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{UserSAMLLink, UserSAMLLinkStatus};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UserSAMLLinkPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl UserSAMLLinkPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct UserSAMLLinkPaginatedResult {
    pub data: Vec<UserSAMLLink>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UserSAMLLinkFilter {
    pub user_id: Option<Uuid>,
    pub provider_id: Option<Uuid>,
    pub name_id: Option<String>,
    pub session_index: Option<String>,
    pub is_primary: Option<bool>,
    pub status: Option<UserSAMLLinkStatus>,
}

impl UserSAMLLinkFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.provider_id.is_some() || self.name_id.is_some() || self.session_index.is_some() || self.is_primary.is_some() || self.status.is_some()
    }
}

/// Repository trait for UserSAMLLink aggregate
///
/// This trait defines the persistence contract for UserSAMLLink entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait UserSAMLLinkRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new user_saml_link entity
    async fn save(&self, entity: &UserSAMLLink) -> Result<UserSAMLLink>;

    /// Find user_saml_link by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<UserSAMLLink>>;

    /// Find all user_saml_link entities
    async fn find_all(&self) -> Result<Vec<UserSAMLLink>>;

    /// Update user_saml_link by ID
    async fn update(&self, id: &str, entity: &UserSAMLLink) -> Result<Option<UserSAMLLink>>;

    /// Delete user_saml_link by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List user_saml_link with pagination
    async fn list(&self, params: UserSAMLLinkPaginationParams) -> Result<UserSAMLLinkPaginatedResult>;

    /// List user_saml_link with pagination and filters
    async fn list_with_filters(&self, params: UserSAMLLinkPaginationParams, filters: UserSAMLLinkFilter) -> Result<UserSAMLLinkPaginatedResult>;

    /// Count all user_saml_link entities
    async fn count(&self) -> Result<u64>;

    /// Count user_saml_link entities matching filters
    async fn count_with_filters(&self, filters: UserSAMLLinkFilter) -> Result<u64>;

    /// Check if user_saml_link exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete user_saml_link by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted user_saml_link by ID
    async fn restore(&self, id: &str) -> Result<Option<UserSAMLLink>>;

    /// List soft-deleted user_saml_link entities
    async fn list_deleted(&self, params: UserSAMLLinkPaginationParams) -> Result<UserSAMLLinkPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save user_saml_link entities
    async fn bulk_save(&self, entities: &[UserSAMLLink]) -> Result<Vec<UserSAMLLink>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
