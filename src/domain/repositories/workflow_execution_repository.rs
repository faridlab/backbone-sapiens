//! Repository trait for WorkflowExecution aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the WorkflowExecution aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{WorkflowExecution, WorkflowExecutionStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type WorkflowExecutionPaginationParams = DomainPaginationParams;
pub type WorkflowExecutionPaginatedResult = DomainPaginatedResult<WorkflowExecution>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct WorkflowExecutionFilter {
    pub workflow_id: Option<Uuid>,
    pub workflow_definition_id: Option<Uuid>,
    pub status: Option<WorkflowExecutionStatus>,
    pub error_message: Option<String>,
}

impl WorkflowExecutionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.workflow_id.is_some() || self.workflow_definition_id.is_some() || self.status.is_some() || self.error_message.is_some()
    }
}

/// Repository trait for WorkflowExecution aggregate
///
/// This trait defines the persistence contract for WorkflowExecution entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait WorkflowExecutionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new workflow_execution entity
    async fn save(&self, entity: &WorkflowExecution) -> Result<WorkflowExecution>;

    /// Find workflow_execution by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<WorkflowExecution>>;

    /// Find all workflow_execution entities
    async fn find_all(&self) -> Result<Vec<WorkflowExecution>>;

    /// Update workflow_execution by ID
    async fn update(&self, id: &str, entity: &WorkflowExecution) -> Result<Option<WorkflowExecution>>;

    /// Delete workflow_execution by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List workflow_execution with pagination
    async fn list(&self, params: WorkflowExecutionPaginationParams) -> Result<WorkflowExecutionPaginatedResult>;

    /// List workflow_execution with pagination and filters
    async fn list_with_filters(&self, params: WorkflowExecutionPaginationParams, filters: WorkflowExecutionFilter) -> Result<WorkflowExecutionPaginatedResult>;

    /// Count all workflow_execution entities
    async fn count(&self) -> Result<u64>;

    /// Count workflow_execution entities matching filters
    async fn count_with_filters(&self, filters: WorkflowExecutionFilter) -> Result<u64>;

    /// Check if workflow_execution exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete workflow_execution by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted workflow_execution by ID
    async fn restore(&self, id: &str) -> Result<Option<WorkflowExecution>>;

    /// List soft-deleted workflow_execution entities
    async fn list_deleted(&self, params: WorkflowExecutionPaginationParams) -> Result<WorkflowExecutionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save workflow_execution entities
    async fn bulk_save(&self, entities: &[WorkflowExecution]) -> Result<Vec<WorkflowExecution>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
