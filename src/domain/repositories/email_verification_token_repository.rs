//! Repository trait for EmailVerificationToken aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the EmailVerificationToken aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{EmailVerificationToken, EmailVerificationTokenStatus, EmailVerificationType};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type EmailVerificationTokenPaginationParams = DomainPaginationParams;
pub type EmailVerificationTokenPaginatedResult = DomainPaginatedResult<EmailVerificationToken>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct EmailVerificationTokenFilter {
    pub user_id: Option<Uuid>,
    pub token: Option<String>,
    pub email: Option<String>,
    pub token_type: Option<EmailVerificationType>,
    pub old_email: Option<String>,
    pub status: Option<EmailVerificationTokenStatus>,
    pub user_agent: Option<String>,
}

impl EmailVerificationTokenFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.token.is_some() || self.email.is_some() || self.token_type.is_some() || self.old_email.is_some() || self.status.is_some() || self.user_agent.is_some()
    }
}

/// Repository trait for EmailVerificationToken aggregate
///
/// This trait defines the persistence contract for EmailVerificationToken entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait EmailVerificationTokenRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new email_verification_token entity
    async fn save(&self, entity: &EmailVerificationToken) -> Result<EmailVerificationToken>;

    /// Find email_verification_token by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<EmailVerificationToken>>;

    /// Find all email_verification_token entities
    async fn find_all(&self) -> Result<Vec<EmailVerificationToken>>;

    /// Update email_verification_token by ID
    async fn update(&self, id: &str, entity: &EmailVerificationToken) -> Result<Option<EmailVerificationToken>>;

    /// Delete email_verification_token by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List email_verification_token with pagination
    async fn list(&self, params: EmailVerificationTokenPaginationParams) -> Result<EmailVerificationTokenPaginatedResult>;

    /// List email_verification_token with pagination and filters
    async fn list_with_filters(&self, params: EmailVerificationTokenPaginationParams, filters: EmailVerificationTokenFilter) -> Result<EmailVerificationTokenPaginatedResult>;

    /// Count all email_verification_token entities
    async fn count(&self) -> Result<u64>;

    /// Count email_verification_token entities matching filters
    async fn count_with_filters(&self, filters: EmailVerificationTokenFilter) -> Result<u64>;

    /// Check if email_verification_token exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete email_verification_token by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted email_verification_token by ID
    async fn restore(&self, id: &str) -> Result<Option<EmailVerificationToken>>;

    /// List soft-deleted email_verification_token entities
    async fn list_deleted(&self, params: EmailVerificationTokenPaginationParams) -> Result<EmailVerificationTokenPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find email_verification_token by token
    async fn find_by_token(&self, token: &str) -> Result<Option<EmailVerificationToken>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if email_verification_token exists by token
    async fn exists_by_token(&self, token: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save email_verification_token entities
    async fn bulk_save(&self, entities: &[EmailVerificationToken]) -> Result<Vec<EmailVerificationToken>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
