//! Repository trait for UserSettings aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the UserSettings aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{UserSettings, Theme};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type UserSettingsPaginationParams = DomainPaginationParams;
pub type UserSettingsPaginatedResult = DomainPaginatedResult<UserSettings>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UserSettingsFilter {
    pub user_id: Option<Uuid>,
    pub theme: Option<Theme>,
    pub language: Option<String>,
    pub timezone: Option<String>,
    pub notifications_enabled: Option<bool>,
    pub email_notifications: Option<bool>,
    pub sms_notifications: Option<bool>,
    pub mfa_enabled: Option<bool>,
}

impl UserSettingsFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.theme.is_some() || self.language.is_some() || self.timezone.is_some() || self.notifications_enabled.is_some() || self.email_notifications.is_some() || self.sms_notifications.is_some() || self.mfa_enabled.is_some()
    }
}

/// Repository trait for UserSettings aggregate
///
/// This trait defines the persistence contract for UserSettings entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait UserSettingsRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new user_settings entity
    async fn save(&self, entity: &UserSettings) -> Result<UserSettings>;

    /// Find user_settings by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<UserSettings>>;

    /// Find all user_settings entities
    async fn find_all(&self) -> Result<Vec<UserSettings>>;

    /// Update user_settings by ID
    async fn update(&self, id: &str, entity: &UserSettings) -> Result<Option<UserSettings>>;

    /// Delete user_settings by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List user_settings with pagination
    async fn list(&self, params: UserSettingsPaginationParams) -> Result<UserSettingsPaginatedResult>;

    /// List user_settings with pagination and filters
    async fn list_with_filters(&self, params: UserSettingsPaginationParams, filters: UserSettingsFilter) -> Result<UserSettingsPaginatedResult>;

    /// Count all user_settings entities
    async fn count(&self) -> Result<u64>;

    /// Count user_settings entities matching filters
    async fn count_with_filters(&self, filters: UserSettingsFilter) -> Result<u64>;

    /// Check if user_settings exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete user_settings by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted user_settings by ID
    async fn restore(&self, id: &str) -> Result<Option<UserSettings>>;

    /// List soft-deleted user_settings entities
    async fn list_deleted(&self, params: UserSettingsPaginationParams) -> Result<UserSettingsPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save user_settings entities
    async fn bulk_save(&self, entities: &[UserSettings]) -> Result<Vec<UserSettings>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
