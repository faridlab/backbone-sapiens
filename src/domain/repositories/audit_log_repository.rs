//! Repository trait for AuditLog aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the AuditLog aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{AuditLog, AuditLogSeverity};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type AuditLogPaginationParams = DomainPaginationParams;
pub type AuditLogPaginatedResult = DomainPaginatedResult<AuditLog>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AuditLogFilter {
    pub user_id: Option<Uuid>,
    pub action: Option<String>,
    pub severity: Option<AuditLogSeverity>,
    pub user_agent: Option<String>,
    pub session_id: Option<Uuid>,
    pub resource_type: Option<String>,
    pub resource_id: Option<String>,
    pub archived: Option<bool>,
    pub archive_location: Option<String>,
}

impl AuditLogFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.action.is_some() || self.severity.is_some() || self.user_agent.is_some() || self.session_id.is_some() || self.resource_type.is_some() || self.resource_id.is_some() || self.archived.is_some() || self.archive_location.is_some()
    }
}

/// Repository trait for AuditLog aggregate
///
/// This trait defines the persistence contract for AuditLog entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait AuditLogRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new audit_log entity
    async fn save(&self, entity: &AuditLog) -> Result<AuditLog>;

    /// Find audit_log by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<AuditLog>>;

    /// Find all audit_log entities
    async fn find_all(&self) -> Result<Vec<AuditLog>>;

    /// Update audit_log by ID
    async fn update(&self, id: &str, entity: &AuditLog) -> Result<Option<AuditLog>>;

    /// Delete audit_log by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List audit_log with pagination
    async fn list(&self, params: AuditLogPaginationParams) -> Result<AuditLogPaginatedResult>;

    /// List audit_log with pagination and filters
    async fn list_with_filters(&self, params: AuditLogPaginationParams, filters: AuditLogFilter) -> Result<AuditLogPaginatedResult>;

    /// Count all audit_log entities
    async fn count(&self) -> Result<u64>;

    /// Count audit_log entities matching filters
    async fn count_with_filters(&self, filters: AuditLogFilter) -> Result<u64>;

    /// Check if audit_log exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete audit_log by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted audit_log by ID
    async fn restore(&self, id: &str) -> Result<Option<AuditLog>>;

    /// List soft-deleted audit_log entities
    async fn list_deleted(&self, params: AuditLogPaginationParams) -> Result<AuditLogPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save audit_log entities
    async fn bulk_save(&self, entities: &[AuditLog]) -> Result<Vec<AuditLog>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
