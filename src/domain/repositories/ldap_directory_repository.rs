//! Repository trait for LDAPDirectory aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the LDAPDirectory aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::{LDAPDirectory, LDAPDirectoryStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type LDAPDirectoryPaginationParams = DomainPaginationParams;
pub type LDAPDirectoryPaginatedResult = DomainPaginatedResult<LDAPDirectory>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct LDAPDirectoryFilter {
    pub name: Option<String>,
    pub display_name: Option<String>,
    pub host: Option<String>,
    pub use_ssl: Option<bool>,
    pub use_tls: Option<bool>,
    pub bind_dn: Option<String>,
    pub bind_password: Option<String>,
    pub search_base: Option<String>,
    pub search_filter: Option<String>,
    pub sync_enabled: Option<bool>,
    pub is_active: Option<bool>,
    pub status: Option<LDAPDirectoryStatus>,
}

impl LDAPDirectoryFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.display_name.is_some() || self.host.is_some() || self.use_ssl.is_some() || self.use_tls.is_some() || self.bind_dn.is_some() || self.bind_password.is_some() || self.search_base.is_some() || self.search_filter.is_some() || self.sync_enabled.is_some() || self.is_active.is_some() || self.status.is_some()
    }
}

/// Repository trait for LDAPDirectory aggregate
///
/// This trait defines the persistence contract for LDAPDirectory entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait LDAPDirectoryRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new ldap_directory entity
    async fn save(&self, entity: &LDAPDirectory) -> Result<LDAPDirectory>;

    /// Find ldap_directory by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<LDAPDirectory>>;

    /// Find all ldap_directory entities
    async fn find_all(&self) -> Result<Vec<LDAPDirectory>>;

    /// Update ldap_directory by ID
    async fn update(&self, id: &str, entity: &LDAPDirectory) -> Result<Option<LDAPDirectory>>;

    /// Delete ldap_directory by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List ldap_directory with pagination
    async fn list(&self, params: LDAPDirectoryPaginationParams) -> Result<LDAPDirectoryPaginatedResult>;

    /// List ldap_directory with pagination and filters
    async fn list_with_filters(&self, params: LDAPDirectoryPaginationParams, filters: LDAPDirectoryFilter) -> Result<LDAPDirectoryPaginatedResult>;

    /// Count all ldap_directory entities
    async fn count(&self) -> Result<u64>;

    /// Count ldap_directory entities matching filters
    async fn count_with_filters(&self, filters: LDAPDirectoryFilter) -> Result<u64>;

    /// Check if ldap_directory exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete ldap_directory by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted ldap_directory by ID
    async fn restore(&self, id: &str) -> Result<Option<LDAPDirectory>>;

    /// List soft-deleted ldap_directory entities
    async fn list_deleted(&self, params: LDAPDirectoryPaginationParams) -> Result<LDAPDirectoryPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find ldap_directory by name
    async fn find_by_name(&self, name: &str) -> Result<Option<LDAPDirectory>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if ldap_directory exists by name
    async fn exists_by_name(&self, name: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save ldap_directory entities
    async fn bulk_save(&self, entities: &[LDAPDirectory]) -> Result<Vec<LDAPDirectory>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
