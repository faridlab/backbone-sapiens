//! Repository trait for AnalyticsReport aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the AnalyticsReport aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{AnalyticsReport, AnalyticsReportType, ReportFormat, ReportStatus};
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type AnalyticsReportPaginationParams = DomainPaginationParams;
pub type AnalyticsReportPaginatedResult = DomainPaginatedResult<AnalyticsReport>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AnalyticsReportFilter {
    pub report_name: Option<String>,
    pub report_type: Option<AnalyticsReportType>,
    pub description: Option<String>,
    pub format: Option<ReportFormat>,
    pub generated_by: Option<Uuid>,
    pub file_path: Option<String>,
    pub status: Option<ReportStatus>,
}

impl AnalyticsReportFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.report_name.is_some() || self.report_type.is_some() || self.description.is_some() || self.format.is_some() || self.generated_by.is_some() || self.file_path.is_some() || self.status.is_some()
    }
}

/// Repository trait for AnalyticsReport aggregate
///
/// This trait defines the persistence contract for AnalyticsReport entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait AnalyticsReportRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new analytics_report entity
    async fn save(&self, entity: &AnalyticsReport) -> Result<AnalyticsReport>;

    /// Find analytics_report by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<AnalyticsReport>>;

    /// Find all analytics_report entities
    async fn find_all(&self) -> Result<Vec<AnalyticsReport>>;

    /// Update analytics_report by ID
    async fn update(&self, id: &str, entity: &AnalyticsReport) -> Result<Option<AnalyticsReport>>;

    /// Delete analytics_report by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List analytics_report with pagination
    async fn list(&self, params: AnalyticsReportPaginationParams) -> Result<AnalyticsReportPaginatedResult>;

    /// List analytics_report with pagination and filters
    async fn list_with_filters(&self, params: AnalyticsReportPaginationParams, filters: AnalyticsReportFilter) -> Result<AnalyticsReportPaginatedResult>;

    /// Count all analytics_report entities
    async fn count(&self) -> Result<u64>;

    /// Count analytics_report entities matching filters
    async fn count_with_filters(&self, filters: AnalyticsReportFilter) -> Result<u64>;

    /// Check if analytics_report exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete analytics_report by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted analytics_report by ID
    async fn restore(&self, id: &str) -> Result<Option<AnalyticsReport>>;

    /// List soft-deleted analytics_report entities
    async fn list_deleted(&self, params: AnalyticsReportPaginationParams) -> Result<AnalyticsReportPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save analytics_report entities
    async fn bulk_save(&self, entities: &[AnalyticsReport]) -> Result<Vec<AnalyticsReport>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
