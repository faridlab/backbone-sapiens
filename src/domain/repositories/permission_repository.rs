//! Repository trait for Permission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the Permission aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::Permission;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type PermissionPaginationParams = DomainPaginationParams;
pub type PermissionPaginatedResult = DomainPaginatedResult<Permission>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PermissionFilter {
    pub name: Option<String>,
    pub description: Option<String>,
    pub resource: Option<String>,
    pub action: Option<String>,
}

impl PermissionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.name.is_some() || self.description.is_some() || self.resource.is_some() || self.action.is_some()
    }
}

/// Repository trait for Permission aggregate
///
/// This trait defines the persistence contract for Permission entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PermissionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new permission entity
    async fn save(&self, entity: &Permission) -> Result<Permission>;

    /// Find permission by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<Permission>>;

    /// Find all permission entities
    async fn find_all(&self) -> Result<Vec<Permission>>;

    /// Update permission by ID
    async fn update(&self, id: &str, entity: &Permission) -> Result<Option<Permission>>;

    /// Delete permission by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List permission with pagination
    async fn list(&self, params: PermissionPaginationParams) -> Result<PermissionPaginatedResult>;

    /// List permission with pagination and filters
    async fn list_with_filters(&self, params: PermissionPaginationParams, filters: PermissionFilter) -> Result<PermissionPaginatedResult>;

    /// Count all permission entities
    async fn count(&self) -> Result<u64>;

    /// Count permission entities matching filters
    async fn count_with_filters(&self, filters: PermissionFilter) -> Result<u64>;

    /// Check if permission exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete permission by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted permission by ID
    async fn restore(&self, id: &str) -> Result<Option<Permission>>;

    /// List soft-deleted permission entities
    async fn list_deleted(&self, params: PermissionPaginationParams) -> Result<PermissionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find permission by name
    async fn find_by_name(&self, name: &str) -> Result<Option<Permission>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if permission exists by name
    async fn exists_by_name(&self, name: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save permission entities
    async fn bulk_save(&self, entities: &[Permission]) -> Result<Vec<Permission>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
