//! Repository trait for AnalyticsEvent aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the AnalyticsEvent aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{AnalyticsEvent, AnalyticsEventType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AnalyticsEventPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl AnalyticsEventPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct AnalyticsEventPaginatedResult {
    pub data: Vec<AnalyticsEvent>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct AnalyticsEventFilter {
    pub event_type: Option<AnalyticsEventType>,
    pub user_id: Option<Uuid>,
    pub session_id: Option<Uuid>,
    pub resource_type: Option<String>,
    pub resource_id: Option<String>,
    pub user_agent: Option<String>,
    pub processed: Option<bool>,
}

impl AnalyticsEventFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.event_type.is_some() || self.user_id.is_some() || self.session_id.is_some() || self.resource_type.is_some() || self.resource_id.is_some() || self.user_agent.is_some() || self.processed.is_some()
    }
}

/// Repository trait for AnalyticsEvent aggregate
///
/// This trait defines the persistence contract for AnalyticsEvent entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait AnalyticsEventRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new analytics_event entity
    async fn save(&self, entity: &AnalyticsEvent) -> Result<AnalyticsEvent>;

    /// Find analytics_event by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<AnalyticsEvent>>;

    /// Find all analytics_event entities
    async fn find_all(&self) -> Result<Vec<AnalyticsEvent>>;

    /// Update analytics_event by ID
    async fn update(&self, id: &str, entity: &AnalyticsEvent) -> Result<Option<AnalyticsEvent>>;

    /// Delete analytics_event by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List analytics_event with pagination
    async fn list(&self, params: AnalyticsEventPaginationParams) -> Result<AnalyticsEventPaginatedResult>;

    /// List analytics_event with pagination and filters
    async fn list_with_filters(&self, params: AnalyticsEventPaginationParams, filters: AnalyticsEventFilter) -> Result<AnalyticsEventPaginatedResult>;

    /// Count all analytics_event entities
    async fn count(&self) -> Result<u64>;

    /// Count analytics_event entities matching filters
    async fn count_with_filters(&self, filters: AnalyticsEventFilter) -> Result<u64>;

    /// Check if analytics_event exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete analytics_event by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted analytics_event by ID
    async fn restore(&self, id: &str) -> Result<Option<AnalyticsEvent>>;

    /// List soft-deleted analytics_event entities
    async fn list_deleted(&self, params: AnalyticsEventPaginationParams) -> Result<AnalyticsEventPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save analytics_event entities
    async fn bulk_save(&self, entities: &[AnalyticsEvent]) -> Result<Vec<AnalyticsEvent>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
