//! Repository trait for UserPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the UserPermission aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::UserPermission;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type UserPermissionPaginationParams = DomainPaginationParams;
pub type UserPermissionPaginatedResult = DomainPaginatedResult<UserPermission>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct UserPermissionFilter {
    pub user_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
    pub granted_by: Option<Uuid>,
    pub reason: Option<String>,
    pub resource_id: Option<String>,
    pub resource_type: Option<String>,
    pub is_active: Option<bool>,
    pub revoked_by: Option<Uuid>,
    pub revoked_reason: Option<String>,
}

impl UserPermissionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.user_id.is_some() || self.permission_id.is_some() || self.granted_by.is_some() || self.reason.is_some() || self.resource_id.is_some() || self.resource_type.is_some() || self.is_active.is_some() || self.revoked_by.is_some() || self.revoked_reason.is_some()
    }
}

/// Repository trait for UserPermission aggregate
///
/// This trait defines the persistence contract for UserPermission entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait UserPermissionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new user_permission entity
    async fn save(&self, entity: &UserPermission) -> Result<UserPermission>;

    /// Find user_permission by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<UserPermission>>;

    /// Find all user_permission entities
    async fn find_all(&self) -> Result<Vec<UserPermission>>;

    /// Update user_permission by ID
    async fn update(&self, id: &str, entity: &UserPermission) -> Result<Option<UserPermission>>;

    /// Delete user_permission by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List user_permission with pagination
    async fn list(&self, params: UserPermissionPaginationParams) -> Result<UserPermissionPaginatedResult>;

    /// List user_permission with pagination and filters
    async fn list_with_filters(&self, params: UserPermissionPaginationParams, filters: UserPermissionFilter) -> Result<UserPermissionPaginatedResult>;

    /// Count all user_permission entities
    async fn count(&self) -> Result<u64>;

    /// Count user_permission entities matching filters
    async fn count_with_filters(&self, filters: UserPermissionFilter) -> Result<u64>;

    /// Check if user_permission exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete user_permission by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted user_permission by ID
    async fn restore(&self, id: &str) -> Result<Option<UserPermission>>;

    /// List soft-deleted user_permission entities
    async fn list_deleted(&self, params: UserPermissionPaginationParams) -> Result<UserPermissionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save user_permission entities
    async fn bulk_save(&self, entities: &[UserPermission]) -> Result<Vec<UserPermission>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
