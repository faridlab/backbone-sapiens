//! Repository trait for SystemSettings aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the SystemSettings aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;

use crate::domain::entity::SystemSettings;
use backbone_core::repository::{DomainPaginationParams, DomainPaginatedResult};

pub type SystemSettingsPaginationParams = DomainPaginationParams;
pub type SystemSettingsPaginatedResult = DomainPaginatedResult<SystemSettings>;

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct SystemSettingsFilter {
    pub key: Option<String>,
    pub description: Option<String>,
}

impl SystemSettingsFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.key.is_some() || self.description.is_some()
    }
}

/// Repository trait for SystemSettings aggregate
///
/// This trait defines the persistence contract for SystemSettings entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait SystemSettingsRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new system_settings entity
    async fn save(&self, entity: &SystemSettings) -> Result<SystemSettings>;

    /// Find system_settings by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<SystemSettings>>;

    /// Find all system_settings entities
    async fn find_all(&self) -> Result<Vec<SystemSettings>>;

    /// Update system_settings by ID
    async fn update(&self, id: &str, entity: &SystemSettings) -> Result<Option<SystemSettings>>;

    /// Delete system_settings by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List system_settings with pagination
    async fn list(&self, params: SystemSettingsPaginationParams) -> Result<SystemSettingsPaginatedResult>;

    /// List system_settings with pagination and filters
    async fn list_with_filters(&self, params: SystemSettingsPaginationParams, filters: SystemSettingsFilter) -> Result<SystemSettingsPaginatedResult>;

    /// Count all system_settings entities
    async fn count(&self) -> Result<u64>;

    /// Count system_settings entities matching filters
    async fn count_with_filters(&self, filters: SystemSettingsFilter) -> Result<u64>;

    /// Check if system_settings exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete system_settings by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted system_settings by ID
    async fn restore(&self, id: &str) -> Result<Option<SystemSettings>>;

    /// List soft-deleted system_settings entities
    async fn list_deleted(&self, params: SystemSettingsPaginationParams) -> Result<SystemSettingsPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Query by Unique Fields
    // =========================================================================

    /// Find system_settings by key
    async fn find_by_key(&self, key: &str) -> Result<Option<SystemSettings>>;

    // =========================================================================
    // Existence Checks by Unique Fields
    // =========================================================================

    /// Check if system_settings exists by key
    async fn exists_by_key(&self, key: &str) -> Result<bool>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save system_settings entities
    async fn bulk_save(&self, entities: &[SystemSettings]) -> Result<Vec<SystemSettings>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
