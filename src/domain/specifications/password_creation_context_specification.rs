//! Specifications for PasswordCreationContext aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Entity-specific predicate structs implementing `backbone_core::Specification`.
//! Combinators (AND/OR/NOT) are available via the trait methods — no duplication.

use backbone_core::Specification;
use crate::domain::entity::PasswordCreationContext;

pub use backbone_core::{AndSpecification, OrSpecification, NotSpecification};

// ─── Entity-specific predicates ──────────────────────────────────────────────
//
// Add entity-specific predicate structs below.
// Each predicate implements `Specification<PasswordCreationContext>` and can be combined
// using `.and()`, `.or()`, `.not()` from the trait.
//
// Example:
//   let spec = ActiveSpec.and(VerifiedSpec);
//   if spec.is_satisfied_by(&entity) { ... }

// <<< CUSTOM
// Add predicate structs here. Example:
//
// pub struct ActivePasswordCreationContextSpec;
// impl Specification<PasswordCreationContext> for ActivePasswordCreationContextSpec {
//     fn is_satisfied_by(&self, e: &PasswordCreationContext) -> bool {
//         // e.is_active
//         todo!()
//     }
// }
// <<< CUSTOM SPECIFICATIONS START >>>
// Add your custom specifications here
// pub struct PasswordCreationContextHasValidEmailSpec;
// impl PasswordCreationContextSpecification for PasswordCreationContextHasValidEmailSpec {
//     fn is_satisfied_by(&self, entity: &PasswordCreationContext) -> bool {
//         entity.email.contains('@')
// END CUSTOM
// END CUSTOM