//! Specifications for OAuthProvider aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Entity-specific predicate structs implementing `backbone_core::Specification`.
//! Combinators (AND/OR/NOT) are available via the trait methods — no duplication.

use backbone_core::Specification;
use crate::domain::entity::OAuthProvider;

pub use backbone_core::{AndSpecification, OrSpecification, NotSpecification};

// ─── Entity-specific predicates ──────────────────────────────────────────────
//
// Add entity-specific predicate structs below.
// Each predicate implements `Specification<OAuthProvider>` and can be combined
// using `.and()`, `.or()`, `.not()` from the trait.
//
// Example:
//   let spec = ActiveSpec.and(VerifiedSpec);
//   if spec.is_satisfied_by(&entity) { ... }

// <<< CUSTOM
// Add predicate structs here. Example:
//
// pub struct ActiveOAuthProviderSpec;
// impl Specification<OAuthProvider> for ActiveOAuthProviderSpec {
//     fn is_satisfied_by(&self, e: &OAuthProvider) -> bool {
//         // e.is_active
//         todo!()
//     }
// }
// <<< CUSTOM SPECIFICATIONS START >>>
// Add your custom specifications here
// pub struct OAuthProviderHasValidEmailSpec;
// impl OAuthProviderSpecification for OAuthProviderHasValidEmailSpec {
//     fn is_satisfied_by(&self, entity: &OAuthProvider) -> bool {
//         entity.email.contains('@')
// END CUSTOM
// END CUSTOM