//! Specifications for EmailVerificationToken aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Entity-specific predicate structs implementing `backbone_core::Specification`.
//! Combinators (AND/OR/NOT) are available via the trait methods — no duplication.

use backbone_core::Specification;
use crate::domain::entity::EmailVerificationToken;

pub use backbone_core::{AndSpecification, OrSpecification, NotSpecification};

// ─── Entity-specific predicates ──────────────────────────────────────────────
//
// Add entity-specific predicate structs below.
// Each predicate implements `Specification<EmailVerificationToken>` and can be combined
// using `.and()`, `.or()`, `.not()` from the trait.
//
// Example:
//   let spec = ActiveSpec.and(VerifiedSpec);
//   if spec.is_satisfied_by(&entity) { ... }

// <<< CUSTOM
// Add predicate structs here. Example:
//
// pub struct ActiveEmailVerificationTokenSpec;
// impl Specification<EmailVerificationToken> for ActiveEmailVerificationTokenSpec {
//     fn is_satisfied_by(&self, e: &EmailVerificationToken) -> bool {
//         // e.is_active
//         todo!()
//     }
// }
// <<< CUSTOM SPECIFICATIONS START >>>
// Add your custom specifications here
// pub struct EmailVerificationTokenHasValidEmailSpec;
// impl EmailVerificationTokenSpecification for EmailVerificationTokenHasValidEmailSpec {
//     fn is_satisfied_by(&self, entity: &EmailVerificationToken) -> bool {
//         entity.email.contains('@')
// END CUSTOM
// END CUSTOM