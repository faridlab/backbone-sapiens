//! Computed Fields Module
//!
//! Generated by backbone-schema.
//!
//! Provides virtual/computed fields for entities based on their data and relationships.
//! These fields are not stored in the database but calculated on-demand.

use std::collections::HashMap;
use serde::{Deserialize, Serialize};
#[allow(unused_imports)]
use chrono::{DateTime, Utc, NaiveDate};

use crate::domain::entity::*;

/// Trait for entities that have computed fields
pub trait HasComputedFields {
    /// Get all computed field values
    fn computed_fields(&self) -> ComputedFieldValues;

    /// Get a specific computed field value
    fn computed_field(&self, name: &str) -> Option<serde_json::Value>;
}

/// Collection of computed field values
#[derive(Debug, Clone, Default, Serialize, Deserialize)]
pub struct ComputedFieldValues {
    pub fields: HashMap<String, serde_json::Value>,
}

impl ComputedFieldValues {
    pub fn new() -> Self {
        Self { fields: HashMap::new() }
    }

    pub fn set(&mut self, name: impl Into<String>, value: serde_json::Value) {
        self.fields.insert(name.into(), value);
    }

    pub fn get(&self, name: &str) -> Option<&serde_json::Value> {
        self.fields.get(name)
    }

    pub fn get_bool(&self, name: &str) -> Option<bool> {
        self.fields.get(name).and_then(|v| v.as_bool())
    }

    pub fn get_str(&self, name: &str) -> Option<&str> {
        self.fields.get(name).and_then(|v| v.as_str())
    }

    pub fn get_i64(&self, name: &str) -> Option<i64> {
        self.fields.get(name).and_then(|v| v.as_i64())
    }
}

/// Computed fields for AnalyticsEvent entity
impl HasComputedFields for AnalyticsEvent {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // event_age_minutes
        // WARNING: Expression references fields not on this entity: ["created_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("event_age_minutes", serde_json::json!(null));

        // metric_change_rate
        // WARNING: Expression references fields not on this entity: ["current_value", "previous_value", "time_period"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("metric_change_rate", serde_json::json!(null));

        // event_trend
        // WARNING: Expression references fields not on this entity: ["time_window"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("event_trend", serde_json::json!(null));

        // user_engagement_score
        // WARNING: Expression references fields not on this entity: ["activity_period"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("user_engagement_score", serde_json::json!(null));

        // system_health_score
        // NOTE: calculate_system_health_score() is a custom function requiring implementation - requires repository access
        // Implement via async computed field trait or cache
        fields.set("system_health_score", serde_json::json!(null));

        // report_completion_percentage
        // WARNING: Expression references fields not on this entity: ["report_status"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("report_completion_percentage", serde_json::json!(null));

        // active_users_count
        // WARNING: Expression references fields not on this entity: ["AnalyticsEvent", "created_at", "event_category"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("active_users_count", serde_json::json!(null));

        // events_per_hour
        // NOTE: count() requires database query - requires repository access
        // Implement via async computed field trait or cache
        fields.set("events_per_hour", serde_json::json!(null));

        // security_severity_level
        // WARNING: Expression references fields not on this entity: ["event_data"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("security_severity_level", serde_json::json!(null));

        // data_freshness_minutes
        // WARNING: Expression references fields not on this entity: ["last_updated_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("data_freshness_minutes", serde_json::json!(null));

        // anomaly_score
        // WARNING: Expression references fields not on this entity: ["current_metrics", "historical_patterns"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("anomaly_score", serde_json::json!(null));

        // conversion_rate
        // WARNING: Expression references fields not on this entity: ["funnel_steps", "time_period"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("conversion_rate", serde_json::json!(null));

        // performance_vs_benchmark
        // WARNING: Expression references fields not on this entity: ["benchmark_value", "current_value"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("performance_vs_benchmark", serde_json::json!(null));

        // user_retention_rate
        // WARNING: Expression references fields not on this entity: ["cohort", "time_period"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("user_retention_rate", serde_json::json!(null));

        // data_quality_score
        // WARNING: Expression references fields not on this entity: ["accuracy", "data_completeness", "timeliness"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("data_quality_score", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for AuditLog entity
impl HasComputedFields for AuditLog {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // age
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("age", serde_json::json!(null));

        // is_archive_candidate
        // WARNING: Expression references fields not on this entity: ["age"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_archive_candidate", serde_json::json!(null));

        // is_retention_expired
        // WARNING: Expression references fields not on this entity: ["age"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_retention_expired", serde_json::json!(null));

        // display_label
        // WARNING: Expression references fields not on this entity: ["format"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("display_label", serde_json::json!(null));

        // actor_display
        fields.set("actor_display", serde_json::json!(self.user_id.is_some()));

        // event_summary
        // Expression: actor_display + ' performed ' + action + (resource_type !...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("event_summary", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for BackupCode entity
impl HasComputedFields for BackupCode {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at < chrono::Utc::now()));

        // is_used
        fields.set("is_used", serde_json::json!(self.is_used));

        // is_active
        fields.set("is_active", serde_json::json!(!self.is_used && self.expires_at > chrono::Utc::now()));

        // age_days
        // WARNING: Expression references fields not on this entity: ["created_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("age_days", serde_json::json!(null));

        // time_until_expiry_hours
        fields.set("time_until_expiry_hours", serde_json::json!(self.expires_at > chrono::Utc::now()));

        // batch_completion_percentage
        // WARNING: Expression references fields not on this entity: ["total_count"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("batch_completion_percentage", serde_json::json!(null));

        // remaining_codes
        // WARNING: Expression references fields not on this entity: ["total_count", "used_count"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("remaining_codes", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for DataExport entity
impl HasComputedFields for DataExport {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_ready
        fields.set("is_ready", serde_json::json!(self.status == DataExportStatus::Completed && self.expires_at > chrono::Utc::now()));

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at <= chrono::Utc::now()));

        // is_processing
        fields.set("is_processing", serde_json::json!(self.status));

        // time_until_expiry
        fields.set("time_until_expiry", serde_json::json!(self.expires_at - chrono::Utc::now()));

        // expires_soon
        // WARNING: Expression references fields not on this entity: ["chrono"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("expires_soon", serde_json::json!(null));

        // file_exists
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("file_exists", serde_json::json!(null));

        // processing_duration
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("processing_duration", serde_json::json!(null));

        // retention_days
        // Expression: (expires_at - (completed_at ?? now())).days
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("retention_days", serde_json::json!(null));

        // file_extension
        fields.set("file_extension", serde_json::json!(self.format == DataExportFormat::Json));

        // mime_type
        fields.set("mime_type", serde_json::json!(self.format == DataExportFormat::Json));

        // download_filename
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("download_filename", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for DeviceTrust entity
impl HasComputedFields for DeviceTrust {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at.is_some() && self.expires_at < Some(chrono::Utc::now())));

        // is_active
        // WARNING: Expression references fields not on this entity: ["is_expired"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_active", serde_json::json!(null));

        // age_days
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("age_days", serde_json::json!(null));

        // days_since_last_use
        // NOTE: Expression requires DateTime arithmetic - requires repository access
        // Implement via async computed field trait or cache
        fields.set("days_since_last_use", serde_json::json!(null));

        // time_until_expiry_hours
        fields.set("time_until_expiry_hours", serde_json::json!(self.expires_at.is_some() && self.expires_at > Some(chrono::Utc::now())));

        // is_high_risk
        fields.set("is_high_risk", serde_json::json!(self.risk_score > 0.7));

        // requires_mfa_verification
        // WARNING: Expression references fields not on this entity: ["is_high_risk"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("requires_mfa_verification", serde_json::json!(null));

        // trust_status
        // WARNING: Expression references fields not on this entity: ["is_expired"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("trust_status", serde_json::json!(null));

        // usage_frequency_per_day
        // WARNING: Expression references fields not on this entity: ["age_days"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("usage_frequency_per_day", serde_json::json!(null));

        // is_recently_used
        // NOTE: Expression requires DateTime arithmetic - requires repository access
        // Implement via async computed field trait or cache
        fields.set("is_recently_used", serde_json::json!(null));

        // risk_category
        fields.set("risk_category", serde_json::json!(self.risk_score >= 0.8));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for EmailVerificationToken entity
impl HasComputedFields for EmailVerificationToken {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at < chrono::Utc::now() && self.verified_at.is_none()));

        // is_verified
        fields.set("is_verified", serde_json::json!(self.verified_at.is_some()));

        // is_active
        fields.set("is_active", serde_json::json!(self.verified_at.is_none() && self.expires_at > chrono::Utc::now()));

        // age_hours
        // WARNING: Expression references fields not on this entity: ["created_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("age_hours", serde_json::json!(null));

        // max_attempts_reached
        fields.set("max_attempts_reached", serde_json::json!(self.attempts >= self.max_attempts));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for ImpersonationSession entity
impl HasComputedFields for ImpersonationSession {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // expires_at
        // Expression: started_at + (max_duration_minutes as i64).minutes
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("expires_at", serde_json::json!(null));

        // is_active
        // Expression: ended_at == null && (started_at + (max_duration_minutes a...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("is_active", serde_json::json!(null));

        // is_expired
        // Expression: (started_at + (max_duration_minutes as i64).minutes) <= n...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("is_expired", serde_json::json!(null));

        // duration_minutes
        fields.set("duration_minutes", serde_json::json!(self.ended_at.is_some()));

        // remaining_minutes
        // NOTE: Expression requires string concatenation - requires repository access
        // Implement via async computed field trait or cache
        fields.set("remaining_minutes", serde_json::json!(null));

        // expires_soon
        // Expression: ended_at == null && ((started_at + (max_duration_minutes ...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("expires_soon", serde_json::json!(null));

        // is_admin_session
        // NOTE: Expression requires runtime context ($actor) - requires repository access
        // Implement via async computed field trait or cache
        fields.set("is_admin_session", serde_json::json!(null));

        // can_terminate
        // NOTE: Expression requires runtime context ($actor) - requires repository access
        // Implement via async computed field trait or cache
        fields.set("can_terminate", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for LDAPDirectory entity
impl HasComputedFields for LDAPDirectory {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_active
        fields.set("is_active", serde_json::json!(self.status == LDAPDirectoryStatus::Active));

        // connection_url
        // NOTE: Expression requires string concatenation - requires repository access
        // Implement via async computed field trait or cache
        fields.set("connection_url", serde_json::json!(null));

        // is_encrypted
        // WARNING: Expression references fields not on this entity: ["use_starttls"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_encrypted", serde_json::json!(null));

        // encryption_type
        // WARNING: Expression references fields not on this entity: ["use_starttls"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("encryption_type", serde_json::json!(null));

        // last_sync_age_minutes
        fields.set("last_sync_age_minutes", serde_json::json!(self.last_sync_at.is_some()));

        // needs_sync
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("needs_sync", serde_json::json!(null));

        // sync_overdue
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("sync_overdue", serde_json::json!(null));

        // last_sync_status
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("last_sync_status", serde_json::json!(null));

        // total_users_synced
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("total_users_synced", serde_json::json!(null));

        // total_users_failed
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("total_users_failed", serde_json::json!(null));

        // sync_success_rate
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("sync_success_rate", serde_json::json!(null));

        // is_healthy
        // WARNING: Expression references fields not on this entity: ["last_connection_test"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_healthy", serde_json::json!(null));

        // last_connection_test_age_minutes
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("last_connection_test_age_minutes", serde_json::json!(null));

        // display_name
        fields.set("display_name", serde_json::json!(true));

        // domain_name
        // WARNING: Expression references fields not on this entity: ["base_dn"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("domain_name", serde_json::json!(null));

        // estimated_user_count
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("estimated_user_count", serde_json::json!(null));

        // can_authenticate
        // WARNING: Expression references fields not on this entity: ["is_healthy"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("can_authenticate", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for MFADevice entity
impl HasComputedFields for MFADevice {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_verified
        fields.set("is_verified", serde_json::json!(self.verified_at.is_some()));

        // is_usable
        // WARNING: Expression references fields not on this entity: ["is_verified"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_usable", serde_json::json!(null));

        // days_since_last_use
        fields.set("days_since_last_use", serde_json::json!(self.last_used.is_some()));

        // is_stale
        // WARNING: Expression references fields not on this entity: ["days_since_last_use"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_stale", serde_json::json!(null));

        // age
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("age", serde_json::json!(null));

        // display_label
        fields.set("display_label", serde_json::json!(format!("{}{}{}{}", self.device_name.as_deref().unwrap_or(""), " (", format!("{:?}", self.device_type), ")")));

        // status_display
        fields.set("status_display", serde_json::json!(!self.is_active));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for Notification entity
impl HasComputedFields for Notification {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at.is_some() && self.expires_at < Some(chrono::Utc::now())));

        // is_read
        fields.set("is_read", serde_json::json!(self.read_at.is_some()));

        // is_sent
        fields.set("is_sent", serde_json::json!(self.sent_at.is_some()));

        // is_failed
        // WARNING: Expression references fields not on this entity: ["failed_at", "status"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_failed", serde_json::json!(null));

        // age_minutes
        // WARNING: Expression references fields not on this entity: ["created_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("age_minutes", serde_json::json!(null));

        // time_until_expiry_minutes
        fields.set("time_until_expiry_minutes", serde_json::json!(self.expires_at.is_some() && self.expires_at > Some(chrono::Utc::now())));

        // delivery_status
        // WARNING: Expression references fields not on this entity: ["is_expired", "is_failed", "is_sent"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("delivery_status", serde_json::json!(null));

        // is_high_priority
        fields.set("is_high_priority", serde_json::json!(self.priority));

        // is_actionable
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("is_actionable", serde_json::json!(null));

        // category
        // Expression: type in ['alert', 'error'] ? 'critical' : (type in ['succ...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("category", serde_json::json!(null));

        // time_since_read_minutes
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("time_since_read_minutes", serde_json::json!(null));

        // response_time_minutes
        fields.set("response_time_minutes", serde_json::json!(self.sent_at.is_some() && self.read_at.is_some()));

        // is_recent
        // WARNING: Expression references fields not on this entity: ["age_minutes"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_recent", serde_json::json!(null));

        // requires_attention
        // WARNING: Expression references fields not on this entity: ["is_expired", "is_high_priority"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("requires_attention", serde_json::json!(null));

        // visibility_status
        // WARNING: Expression references fields not on this entity: ["is_expired"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("visibility_status", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for NotificationPreference entity
impl HasComputedFields for NotificationPreference {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_active
        fields.set("is_active", serde_json::json!(self.enabled));

        // channels_enabled_count
        // WARNING: Expression references fields not on this entity: ["email_enabled", "in_app_enabled", "push_enabled", "sms_enabled"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("channels_enabled_count", serde_json::json!(null));

        // has_email_enabled
        // WARNING: Expression references fields not on this entity: ["email_enabled"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("has_email_enabled", serde_json::json!(null));

        // has_sms_enabled
        // WARNING: Expression references fields not on this entity: ["sms_enabled"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("has_sms_enabled", serde_json::json!(null));

        // has_push_enabled
        // WARNING: Expression references fields not on this entity: ["push_enabled"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("has_push_enabled", serde_json::json!(null));

        // has_in_app_enabled
        // WARNING: Expression references fields not on this entity: ["in_app_enabled"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("has_in_app_enabled", serde_json::json!(null));

        // is_immediate_digest
        fields.set("is_immediate_digest", serde_json::json!(self.digest_frequency == DigestFrequency::Immediate));

        // digest_hours
        fields.set("digest_hours", serde_json::json!(self.digest_frequency == DigestFrequency::Immediate));

        // should_send_immediate
        fields.set("should_send_immediate", serde_json::json!(self.enabled && self.digest_frequency == DigestFrequency::Immediate));

        // security_notifications_enabled
        // WARNING: Expression references fields not on this entity: ["notify_security_events"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("security_notifications_enabled", serde_json::json!(null));

        // system_notifications_enabled
        // WARNING: Expression references fields not on this entity: ["notify_system_updates"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("system_notifications_enabled", serde_json::json!(null));

        // marketing_notifications_enabled
        // WARNING: Expression references fields not on this entity: ["notify_marketing"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("marketing_notifications_enabled", serde_json::json!(null));

        // is_in_notification_window
        // Expression: quiet_hours_start == null || quiet_hours_end == null || (...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("is_in_notification_window", serde_json::json!(null));

        // next_digest_at
        fields.set("next_digest_at", serde_json::json!(self.digest_frequency == DigestFrequency::Immediate));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for OAuthProvider entity
impl HasComputedFields for OAuthProvider {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // linked_users_count
        // WARNING: Expression references fields not on this entity: ["user_oauth_links"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("linked_users_count", serde_json::json!(null));

        // has_active_users
        // WARNING: Expression references fields not on this entity: ["user_oauth_links"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("has_active_users", serde_json::json!(null));

        // configuration_score
        // NOTE: calculate_configuration_score() is a custom function requiring implementation - requires repository access
        // Implement via async computed field trait or cache
        fields.set("configuration_score", serde_json::json!(null));

        // display_status
        // WARNING: Expression references fields not on this entity: ["maintenance"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("display_status", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for PasswordResetToken entity
impl HasComputedFields for PasswordResetToken {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at <= chrono::Utc::now()));

        // is_valid
        fields.set("is_valid", serde_json::json!(!self.is_used && self.expires_at > chrono::Utc::now()));

        // time_until_expiry
        fields.set("time_until_expiry", serde_json::json!(self.expires_at > chrono::Utc::now()));

        // age
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("age", serde_json::json!(null));

        // status_display
        // WARNING: Expression references fields not on this entity: ["is_expired"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("status_display", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for Permission entity
impl HasComputedFields for Permission {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // role_count
        // WARNING: Expression references fields not on this entity: ["RolePermission", "permission_id"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("role_count", serde_json::json!(null));

        // direct_user_count
        // WARNING: Expression references fields not on this entity: ["UserPermission", "is_active", "permission_id"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("direct_user_count", serde_json::json!(null));

        // total_assignments
        // WARNING: Expression references fields not on this entity: ["direct_user_count", "role_count"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("total_assignments", serde_json::json!(null));

        // display_label
        // WARNING: Expression references fields not on this entity: ["role_count"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("display_label", serde_json::json!(null));

        // is_widely_used
        // WARNING: Expression references fields not on this entity: ["total_assignments"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_widely_used", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for Profile entity
impl HasComputedFields for Profile {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // full_name
        // Expression: [first_name, middle_name, last_name].filter(v => v != nul...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("full_name", serde_json::json!(null));

        // display_name
        fields.set("display_name", serde_json::json!(self.first_name));

        // initials
        // Expression: (first_name != null ? first_name[0].uppercase() : '') + (...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("initials", serde_json::json!(null));

        // age
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("age", serde_json::json!(null));

        // is_complete
        fields.set("is_complete", serde_json::json!(true && self.last_name.is_some() && self.phone_number.is_some()));

        // completeness_percent
        // Expression: ( (first_name != null ? 1 : 0) + (last_name != null ? 1 :...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("completeness_percent", serde_json::json!(null));

        // has_avatar
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("has_avatar", serde_json::json!(null));

        // age_category
        // WARNING: Expression references fields not on this entity: ["age"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("age_category", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for ResourcePermission entity
impl HasComputedFields for ResourcePermission {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_active
        fields.set("is_active", serde_json::json!(self.status == ResourcePermissionStatus::Active && self.expires_at.is_none() || self.expires_at > Some(chrono::Utc::now())));

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at.is_some() && self.expires_at <= Some(chrono::Utc::now()) && !(self.status == ResourcePermissionStatus::Revoked)));

        // time_until_expiry
        fields.set("time_until_expiry", serde_json::json!(self.expires_at.is_some()));

        // expires_soon
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("expires_soon", serde_json::json!(null));

        // duration_days
        fields.set("duration_days", serde_json::json!(self.expires_at.is_some()));

        // remaining_days
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("remaining_days", serde_json::json!(null));

        // granted_target
        fields.set("granted_target", serde_json::json!(self.granted_to_user_id.is_some()));

        // resource_key
        fields.set("resource_key", serde_json::json!(format!("{}{}{}", &self.resource_type, ":", format!("{}", self.resource_id))));

        // can_revoke
        // NOTE: Expression requires runtime context ($actor) - requires repository access
        // Implement via async computed field trait or cache
        fields.set("can_revoke", serde_json::json!(null));

        // is_role_grant
        fields.set("is_role_grant", serde_json::json!(self.granted_to_role_id.is_some()));

        // is_user_grant
        fields.set("is_user_grant", serde_json::json!(self.granted_to_user_id.is_some()));

        // max_allowed_duration
        // Expression: { 'financial_record': 30, 'personal_data': 30, 'credentia...
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("max_allowed_duration", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for Role entity
impl HasComputedFields for Role {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // user_count
        // WARNING: Expression references fields not on this entity: ["UserRole", "role_id"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("user_count", serde_json::json!(null));

        // permission_count
        // Expression: permissions.count
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("permission_count", serde_json::json!(null));

        // is_system_role
        fields.set("is_system_role", serde_json::json!(self.name));

        // display_label
        // WARNING: Expression references fields not on this entity: ["user_count"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("display_label", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for SAMLProvider entity
impl HasComputedFields for SAMLProvider {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_active
        fields.set("is_active", serde_json::json!(self.status == SAMLProviderStatus::Active));

        // has_metadata_url
        // WARNING: Expression references fields not on this entity: ["metadata_url"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("has_metadata_url", serde_json::json!(null));

        // has_slo
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("has_slo", serde_json::json!(null));

        // wants_encrypted_assertions
        // WARNING: Expression references fields not on this entity: ["encrypt_assertions"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("wants_encrypted_assertions", serde_json::json!(null));

        // certificate_expires_at
        // NOTE: parse_certificate_expiry() is a custom function requiring implementation - requires repository access
        // Implement via async computed field trait or cache
        fields.set("certificate_expires_at", serde_json::json!(null));

        // certificate_expires_soon
        // WARNING: Expression references fields not on this entity: ["certificate_expires_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("certificate_expires_soon", serde_json::json!(null));

        // is_certificate_expired
        // WARNING: Expression references fields not on this entity: ["certificate_expires_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_certificate_expired", serde_json::json!(null));

        // days_until_certificate_expiry
        // WARNING: Expression references fields not on this entity: ["certificate_expires_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("days_until_certificate_expiry", serde_json::json!(null));

        // authn_context_classes_count
        // WARNING: Expression references fields not on this entity: ["authn_context_classes"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("authn_context_classes_count", serde_json::json!(null));

        // is_configured
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("is_configured", serde_json::json!(null));

        // display_name
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("display_name", serde_json::json!(null));

        // metadata_freshness
        // WARNING: Expression references fields not on this entity: ["metadata_last_refreshed"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("metadata_freshness", serde_json::json!(null));

        // needs_metadata_refresh
        // WARNING: Expression references fields not on this entity: ["metadata_last_refreshed", "metadata_url"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("needs_metadata_refresh", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for SecurityEvent entity
impl HasComputedFields for SecurityEvent {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_high_priority
        fields.set("is_high_priority", serde_json::json!(self.severity));

        // requires_immediate_attention
        fields.set("requires_immediate_attention", serde_json::json!(self.severity == SecurityEventSeverity::Critical));

        // is_stale
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("is_stale", serde_json::json!(null));

        // age_days
        // WARNING: Expression references fields not on this entity: ["created_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("age_days", serde_json::json!(null));

        // resolution_duration
        fields.set("resolution_duration", serde_json::json!(self.resolved_at.is_some()));

        // should_notify
        // WARNING: Expression references fields not on this entity: ["is_high_priority"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("should_notify", serde_json::json!(null));

        // risk_score
        fields.set("risk_score", serde_json::json!(self.severity == SecurityEventSeverity::Critical));

        // needs_attention
        // WARNING: Expression references fields not on this entity: ["age_days"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("needs_attention", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for Session entity
impl HasComputedFields for Session {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at <= chrono::Utc::now()));

        // is_valid
        fields.set("is_valid", serde_json::json!(self.is_active && self.expires_at > chrono::Utc::now()));

        // time_until_expiry
        fields.set("time_until_expiry", serde_json::json!(self.expires_at - chrono::Utc::now()));

        // age
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("age", serde_json::json!(null));

        // is_recently_active
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("is_recently_active", serde_json::json!(null));

        // status_label
        fields.set("status_label", serde_json::json!(!self.is_active));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for SessionLimit entity
impl HasComputedFields for SessionLimit {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // effective_limit
        // Expression: max_sessions_per_device ?? max_sessions
        // NOTE: Computed via AsyncComputedFields trait
        fields.set("effective_limit", serde_json::json!(null));

        // current_count
        fields.set("current_count", serde_json::json!(self.current_session_count));

        // can_create_session
        fields.set("can_create_session", serde_json::json!(!self.enforce_limit || self.current_session_count < self.max_sessions));

        // excess_sessions
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("excess_sessions", serde_json::json!(null));

        // utilization_percent
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("utilization_percent", serde_json::json!(null));

        // is_near_limit
        // NOTE: Contains repository-dependent function - requires repository access
        // Implement via async computed field trait or cache
        fields.set("is_near_limit", serde_json::json!(null));

        // time_since_revoke
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("time_since_revoke", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for TemporaryPermission entity
impl HasComputedFields for TemporaryPermission {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_active
        // WARNING: Expression references fields not on this entity: ["effective_from"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_active", serde_json::json!(null));

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at <= chrono::Utc::now() && self.revoked_at.is_none()));

        // time_until_expiry
        fields.set("time_until_expiry", serde_json::json!(self.expires_at - chrono::Utc::now()));

        // expires_soon
        fields.set("expires_soon", serde_json::json!(self.expires_at <= chrono::Utc::now() + chrono::Duration::hours(24) && self.revoked_at.is_none()));

        // duration_days
        fields.set("duration_days", serde_json::json!((self.expires_at - self.granted_at).num_days()));

        // remaining_days
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("remaining_days", serde_json::json!(null));

        // was_notified
        fields.set("was_notified", serde_json::json!(self.notified_before_expiry));

        // should_notify_expiry
        fields.set("should_notify_expiry", serde_json::json!(self.revoked_at.is_none() && self.expires_at <= chrono::Utc::now() + chrono::Duration::hours(24) && !self.notified_before_expiry));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for User entity
impl HasComputedFields for User {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // full_name
        // WARNING: Expression references fields not on this entity: ["profile"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("full_name", serde_json::json!(null));

        // display_name
        fields.set("display_name", serde_json::json!(self.username));

        // is_admin
        // WARNING: Expression references fields not on this entity: ["name", "roles"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_admin", serde_json::json!(null));

        // is_verified
        fields.set("is_verified", serde_json::json!(self.email_verified && self.status == UserStatus::Active));

        // is_locked
        fields.set("is_locked", serde_json::json!(self.locked_until.is_some() && self.locked_until > Some(chrono::Utc::now())));

        // active_session_count
        // WARNING: Expression references fields not on this entity: ["expires_at", "is_active", "sessions"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("active_session_count", serde_json::json!(null));

        // last_activity
        // WARNING: Expression references fields not on this entity: ["last_activity", "sessions"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("last_activity", serde_json::json!(null));

        // effective_permission_count
        // NOTE: Expression requires string concatenation - requires repository access
        // Implement via async computed field trait or cache
        fields.set("effective_permission_count", serde_json::json!(null));

        // mfa_enabled
        // WARNING: Expression references fields not on this entity: ["user_settings"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("mfa_enabled", serde_json::json!(null));

        // profile_picture_url
        // WARNING: Expression references fields not on this entity: ["profile"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("profile_picture_url", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for UserOAuthLink entity
impl HasComputedFields for UserOAuthLink {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_token_expired
        fields.set("is_token_expired", serde_json::json!(self.token_expires_at.is_some() && self.token_expires_at < Some(chrono::Utc::now())));

        // needs_token_refresh
        fields.set("needs_token_refresh", serde_json::json!(self.token_expires_at.is_some() && self.token_expires_at < Some(chrono::Utc::now() + chrono::Duration::hours(1))));

        // age_days
        // WARNING: Expression references fields not on this entity: ["created_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("age_days", serde_json::json!(null));

        // last_sync_age_hours
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("last_sync_age_hours", serde_json::json!(null));

        // provider_display_name
        // WARNING: Expression references fields not on this entity: ["oauth_provider"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("provider_display_name", serde_json::json!(null));

        // provider_icon_url
        // WARNING: Expression references fields not on this entity: ["oauth_provider"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("provider_icon_url", serde_json::json!(null));

        // is_primary_auth
        // WARNING: Expression references fields not on this entity: ["UserOAuthLink", "user"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_primary_auth", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for UserPermission entity
impl HasComputedFields for UserPermission {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_expired
        fields.set("is_expired", serde_json::json!(self.expires_at.is_some() && self.expires_at <= Some(chrono::Utc::now())));

        // is_valid
        fields.set("is_valid", serde_json::json!(self.is_active && self.expires_at.is_none() || self.expires_at > Some(chrono::Utc::now())));

        // time_until_expiry
        fields.set("time_until_expiry", serde_json::json!(self.expires_at.is_some()));

        // age
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("age", serde_json::json!(null));

        // is_temporary
        fields.set("is_temporary", serde_json::json!(self.expires_at.is_some()));

        // display_label
        // WARNING: Expression references fields not on this entity: ["is_temporary", "permission"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("display_label", serde_json::json!(null));

        // scope_description
        fields.set("scope_description", serde_json::json!(self.resource_type.is_some()));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for UserSAMLLink entity
impl HasComputedFields for UserSAMLLink {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // is_active
        fields.set("is_active", serde_json::json!(self.status == UserSAMLLinkStatus::Active));

        // provider_name
        // WARNING: Expression references fields not on this entity: ["SAMLProvider"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("provider_name", serde_json::json!(null));

        // provider_entity_id
        // WARNING: Expression references fields not on this entity: ["SAMLProvider"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("provider_entity_id", serde_json::json!(null));

        // linked_date
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("linked_date", serde_json::json!(null));

        // link_age_days
        // NOTE: Expression requires JsonValue field access - requires repository access
        // Implement via async computed field trait or cache
        fields.set("link_age_days", serde_json::json!(null));

        // last_used_days_ago
        // WARNING: Expression references fields not on this entity: ["last_used_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("last_used_days_ago", serde_json::json!(null));

        // is_recently_used
        // WARNING: Expression references fields not on this entity: ["last_used_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_recently_used", serde_json::json!(null));

        // has_email
        // WARNING: Expression references fields not on this entity: ["saml_email"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("has_email", serde_json::json!(null));

        // has_attributes
        // WARNING: Expression references fields not on this entity: ["saml_attributes"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("has_attributes", serde_json::json!(null));

        // display_name
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("display_name", serde_json::json!(null));

        // can_login_via_saml
        // WARNING: Expression references fields not on this entity: ["SAMLProvider"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("can_login_via_saml", serde_json::json!(null));

        // needs_sync
        // WARNING: Expression references fields not on this entity: ["last_synced_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("needs_sync", serde_json::json!(null));

        // sync_age_hours
        // WARNING: Expression references fields not on this entity: ["last_synced_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("sync_age_hours", serde_json::json!(null));

        // is_primary_auth
        // WARNING: Expression references fields not on this entity: ["user"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_primary_auth", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

/// Computed fields for Workflow entity
impl HasComputedFields for Workflow {
    fn computed_fields(&self) -> ComputedFieldValues {
        let mut fields = ComputedFieldValues::new();

        // workflow_duration_minutes
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("workflow_duration_minutes", serde_json::json!(null));

        // execution_duration_minutes
        // NOTE: Expression requires external data - requires repository access
        // Implement via async computed field trait or cache
        fields.set("execution_duration_minutes", serde_json::json!(null));

        // step_duration_minutes
        fields.set("step_duration_minutes", serde_json::json!(self.started_at.is_some() && self.completed_at.is_some()));

        // time_until_timeout_minutes
        // WARNING: Expression references fields not on this entity: ["timeout_minutes"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("time_until_timeout_minutes", serde_json::json!(null));

        // workflow_progress_percentage
        fields.set("workflow_progress_percentage", serde_json::json!(self.total_steps > 0));

        // execution_progress_percentage
        // WARNING: Expression references fields not on this entity: ["total_actions"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("execution_progress_percentage", serde_json::json!(null));

        // is_workflow_overdue
        // WARNING: Expression references fields not on this entity: ["time_until_timeout_minutes"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_workflow_overdue", serde_json::json!(null));

        // is_execution_overdue
        // WARNING: Expression references fields not on this entity: ["time_until_timeout_minutes"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_execution_overdue", serde_json::json!(null));

        // workflow_success_rate
        // WARNING: Expression references fields not on this entity: ["time_period"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("workflow_success_rate", serde_json::json!(null));

        // average_execution_time
        // NOTE: calculate_average_execution_time() is a custom function requiring implementation - requires repository access
        // Implement via async computed field trait or cache
        fields.set("average_execution_time", serde_json::json!(null));

        // workflow_health_status
        // WARNING: Expression references fields not on this entity: ["workflow_success_rate"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("workflow_health_status", serde_json::json!(null));

        // is_workflow_active
        // WARNING: Expression references fields not on this entity: ["last_execution_at"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("is_workflow_active", serde_json::json!(null));

        // has_workflow_bottleneck
        // WARNING: Expression references fields not on this entity: ["average_execution_time", "expected_duration"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("has_workflow_bottleneck", serde_json::json!(null));

        // workflow_completion_rate
        // NOTE: calculate_workflow_completion_rate() is a custom function requiring implementation - requires repository access
        // Implement via async computed field trait or cache
        fields.set("workflow_completion_rate", serde_json::json!(null));

        // workflow_priority_level
        fields.set("workflow_priority_level", serde_json::json!(self.workflow_type));

        // next_scheduled_execution_at
        // WARNING: Expression references fields not on this entity: ["trigger_config"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("next_scheduled_execution_at", serde_json::json!(null));

        // workflow_complexity_score
        // WARNING: Expression references fields not on this entity: ["conditional_logic_count", "integration_count"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("workflow_complexity_score", serde_json::json!(null));

        // automation_value_score
        // WARNING: Expression references fields not on this entity: ["execution_frequency", "manual_effort", "time_saved_per_execution"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("automation_value_score", serde_json::json!(null));

        // action_execution_efficiency
        // WARNING: Expression references fields not on this entity: ["duration_ms", "expected_duration_ms", "success_count"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("action_execution_efficiency", serde_json::json!(null));

        // workflow_retry_rate
        // WARNING: Expression references fields not on this entity: ["time_period", "workflow_id"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("workflow_retry_rate", serde_json::json!(null));

        // workflow_error_rate
        // WARNING: Expression references fields not on this entity: ["time_period", "workflow_id"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("workflow_error_rate", serde_json::json!(null));

        // user_workflow_participation
        // WARNING: Expression references fields not on this entity: ["time_period", "user_id"]
        // These may be on a related entity or junction table.
        // Implement via AsyncComputedFields trait or repository query.
        fields.set("user_workflow_participation", serde_json::json!(null));

        fields
    }

    fn computed_field(&self, name: &str) -> Option<serde_json::Value> {
        self.computed_fields().fields.get(name).cloned()
    }
}

#[cfg(test)]
mod tests {
    #[allow(unused_imports)]
    use super::*;

    #[test]
    fn test_analytics_event_computed_fields() {
        // TODO: Add test implementation for AnalyticsEvent
    }

    #[test]
    fn test_audit_log_computed_fields() {
        // TODO: Add test implementation for AuditLog
    }

    #[test]
    fn test_backup_code_computed_fields() {
        // TODO: Add test implementation for BackupCode
    }

    #[test]
    fn test_data_export_computed_fields() {
        // TODO: Add test implementation for DataExport
    }

    #[test]
    fn test_device_trust_computed_fields() {
        // TODO: Add test implementation for DeviceTrust
    }

    #[test]
    fn test_email_verification_token_computed_fields() {
        // TODO: Add test implementation for EmailVerificationToken
    }

    #[test]
    fn test_impersonation_session_computed_fields() {
        // TODO: Add test implementation for ImpersonationSession
    }

    #[test]
    fn test_l_d_a_p_directory_computed_fields() {
        // TODO: Add test implementation for LDAPDirectory
    }

    #[test]
    fn test_m_f_a_device_computed_fields() {
        // TODO: Add test implementation for MFADevice
    }

    #[test]
    fn test_notification_computed_fields() {
        // TODO: Add test implementation for Notification
    }

    #[test]
    fn test_notification_preference_computed_fields() {
        // TODO: Add test implementation for NotificationPreference
    }

    #[test]
    fn test_o_auth_provider_computed_fields() {
        // TODO: Add test implementation for OAuthProvider
    }

    #[test]
    fn test_password_reset_token_computed_fields() {
        // TODO: Add test implementation for PasswordResetToken
    }

    #[test]
    fn test_permission_computed_fields() {
        // TODO: Add test implementation for Permission
    }

    #[test]
    fn test_profile_computed_fields() {
        // TODO: Add test implementation for Profile
    }

    #[test]
    fn test_resource_permission_computed_fields() {
        // TODO: Add test implementation for ResourcePermission
    }

    #[test]
    fn test_role_computed_fields() {
        // TODO: Add test implementation for Role
    }

    #[test]
    fn test_s_a_m_l_provider_computed_fields() {
        // TODO: Add test implementation for SAMLProvider
    }

    #[test]
    fn test_security_event_computed_fields() {
        // TODO: Add test implementation for SecurityEvent
    }

    #[test]
    fn test_session_computed_fields() {
        // TODO: Add test implementation for Session
    }

    #[test]
    fn test_session_limit_computed_fields() {
        // TODO: Add test implementation for SessionLimit
    }

    #[test]
    fn test_temporary_permission_computed_fields() {
        // TODO: Add test implementation for TemporaryPermission
    }

    #[test]
    fn test_user_computed_fields() {
        // TODO: Add test implementation for User
    }

    #[test]
    fn test_user_o_auth_link_computed_fields() {
        // TODO: Add test implementation for UserOAuthLink
    }

    #[test]
    fn test_user_permission_computed_fields() {
        // TODO: Add test implementation for UserPermission
    }

    #[test]
    fn test_user_s_a_m_l_link_computed_fields() {
        // TODO: Add test implementation for UserSAMLLink
    }

    #[test]
    fn test_workflow_computed_fields() {
        // TODO: Add test implementation for Workflow
    }

}
