//! Domain events for UserSettings entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::UserSettings;

/// Domain events for UserSettings — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<UserSettings>)` variant.
pub type UserSettingsEvent = CrudEvent<UserSettings>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for UserSettings events.
pub type UserSettingsEventPublisher = Arc<dyn CrudEventPublisher<UserSettings> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopUserSettingsEventPublisher = NoOpCrudEventPublisher<UserSettings>;
