//! Domain events for UserRole entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::UserRole;

/// Domain events for UserRole — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<UserRole>)` variant.
pub type UserRoleEvent = CrudEvent<UserRole>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for UserRole events.
pub type UserRoleEventPublisher = Arc<dyn CrudEventPublisher<UserRole> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopUserRoleEventPublisher = NoOpCrudEventPublisher<UserRole>;
