//! Domain events for PermissionConflict entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PermissionConflict;

/// Domain events for PermissionConflict — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PermissionConflict>)` variant.
pub type PermissionConflictEvent = CrudEvent<PermissionConflict>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PermissionConflict events.
pub type PermissionConflictEventPublisher = Arc<dyn CrudEventPublisher<PermissionConflict> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPermissionConflictEventPublisher = NoOpCrudEventPublisher<PermissionConflict>;
