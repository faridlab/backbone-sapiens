//! Domain events for TemporaryPermission entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::TemporaryPermission;

/// Domain events for TemporaryPermission — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<TemporaryPermission>)` variant.
pub type TemporaryPermissionEvent = CrudEvent<TemporaryPermission>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for TemporaryPermission events.
pub type TemporaryPermissionEventPublisher = Arc<dyn CrudEventPublisher<TemporaryPermission> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopTemporaryPermissionEventPublisher = NoOpCrudEventPublisher<TemporaryPermission>;
