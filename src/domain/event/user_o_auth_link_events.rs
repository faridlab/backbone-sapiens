//! Domain events for UserOAuthLink entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::UserOAuthLink;

/// Domain events for UserOAuthLink — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<UserOAuthLink>)` variant.
pub type UserOAuthLinkEvent = CrudEvent<UserOAuthLink>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for UserOAuthLink events.
pub type UserOAuthLinkEventPublisher = Arc<dyn CrudEventPublisher<UserOAuthLink> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopUserOAuthLinkEventPublisher = NoOpCrudEventPublisher<UserOAuthLink>;
