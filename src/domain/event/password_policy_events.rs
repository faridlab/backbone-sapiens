//! Domain events for PasswordPolicy entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PasswordPolicy;

/// Domain events for PasswordPolicy — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PasswordPolicy>)` variant.
pub type PasswordPolicyEvent = CrudEvent<PasswordPolicy>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PasswordPolicy events.
pub type PasswordPolicyEventPublisher = Arc<dyn CrudEventPublisher<PasswordPolicy> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPasswordPolicyEventPublisher = NoOpCrudEventPublisher<PasswordPolicy>;
