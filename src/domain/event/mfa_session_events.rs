//! Domain events for MFASession entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MFASession;

/// Domain events for MFASession — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MFASession>)` variant.
pub type MFASessionEvent = CrudEvent<MFASession>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MFASession events.
pub type MFASessionEventPublisher = Arc<dyn CrudEventPublisher<MFASession> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMFASessionEventPublisher = NoOpCrudEventPublisher<MFASession>;
