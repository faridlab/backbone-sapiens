//! Domain events for User entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::User;

/// Domain events for User — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<User>)` variant.
pub type UserEvent = CrudEvent<User>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for User events.
pub type UserEventPublisher = Arc<dyn CrudEventPublisher<User> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopUserEventPublisher = NoOpCrudEventPublisher<User>;
