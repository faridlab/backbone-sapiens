//! Domain events for PasswordExpirationSettings entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PasswordExpirationSettings;

/// Domain events for PasswordExpirationSettings — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PasswordExpirationSettings>)` variant.
pub type PasswordExpirationSettingsEvent = CrudEvent<PasswordExpirationSettings>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PasswordExpirationSettings events.
pub type PasswordExpirationSettingsEventPublisher = Arc<dyn CrudEventPublisher<PasswordExpirationSettings> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPasswordExpirationSettingsEventPublisher = NoOpCrudEventPublisher<PasswordExpirationSettings>;
