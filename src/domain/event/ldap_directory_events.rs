//! Domain events for LDAPDirectory entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::LDAPDirectory;

/// Domain events for LDAPDirectory — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<LDAPDirectory>)` variant.
pub type LDAPDirectoryEvent = CrudEvent<LDAPDirectory>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for LDAPDirectory events.
pub type LDAPDirectoryEventPublisher = Arc<dyn CrudEventPublisher<LDAPDirectory> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopLDAPDirectoryEventPublisher = NoOpCrudEventPublisher<LDAPDirectory>;
