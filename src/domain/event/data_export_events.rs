//! Domain events for DataExport entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::DataExport;

/// Domain events for DataExport — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<DataExport>)` variant.
pub type DataExportEvent = CrudEvent<DataExport>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for DataExport events.
pub type DataExportEventPublisher = Arc<dyn CrudEventPublisher<DataExport> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopDataExportEventPublisher = NoOpCrudEventPublisher<DataExport>;
