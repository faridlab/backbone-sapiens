//! Domain events for SAMLProvider entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::SAMLProvider;

/// Domain events for SAMLProvider — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<SAMLProvider>)` variant.
pub type SAMLProviderEvent = CrudEvent<SAMLProvider>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for SAMLProvider events.
pub type SAMLProviderEventPublisher = Arc<dyn CrudEventPublisher<SAMLProvider> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSAMLProviderEventPublisher = NoOpCrudEventPublisher<SAMLProvider>;
