//! Domain events for WorkflowStep entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::WorkflowStep;

/// Domain events for WorkflowStep — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<WorkflowStep>)` variant.
pub type WorkflowStepEvent = CrudEvent<WorkflowStep>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for WorkflowStep events.
pub type WorkflowStepEventPublisher = Arc<dyn CrudEventPublisher<WorkflowStep> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopWorkflowStepEventPublisher = NoOpCrudEventPublisher<WorkflowStep>;
