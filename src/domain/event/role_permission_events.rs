//! Domain events for RolePermission entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::RolePermission;

/// Domain events for RolePermission — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<RolePermission>)` variant.
pub type RolePermissionEvent = CrudEvent<RolePermission>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for RolePermission events.
pub type RolePermissionEventPublisher = Arc<dyn CrudEventPublisher<RolePermission> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRolePermissionEventPublisher = NoOpCrudEventPublisher<RolePermission>;
