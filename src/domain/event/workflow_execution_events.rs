//! Domain events for WorkflowExecution entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::WorkflowExecution;

/// Domain events for WorkflowExecution — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<WorkflowExecution>)` variant.
pub type WorkflowExecutionEvent = CrudEvent<WorkflowExecution>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for WorkflowExecution events.
pub type WorkflowExecutionEventPublisher = Arc<dyn CrudEventPublisher<WorkflowExecution> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopWorkflowExecutionEventPublisher = NoOpCrudEventPublisher<WorkflowExecution>;
