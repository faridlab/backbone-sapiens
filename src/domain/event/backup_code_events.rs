//! Domain events for BackupCode entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::BackupCode;

/// Domain events for BackupCode — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<BackupCode>)` variant.
pub type BackupCodeEvent = CrudEvent<BackupCode>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for BackupCode events.
pub type BackupCodeEventPublisher = Arc<dyn CrudEventPublisher<BackupCode> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopBackupCodeEventPublisher = NoOpCrudEventPublisher<BackupCode>;
