//! Domain events for AuditLog entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::AuditLog;

/// Domain events for AuditLog — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<AuditLog>)` variant.
pub type AuditLogEvent = CrudEvent<AuditLog>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for AuditLog events.
pub type AuditLogEventPublisher = Arc<dyn CrudEventPublisher<AuditLog> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopAuditLogEventPublisher = NoOpCrudEventPublisher<AuditLog>;
