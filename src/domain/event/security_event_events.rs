//! Domain events for SecurityEvent entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::SecurityEvent;

/// Domain events for SecurityEvent — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<SecurityEvent>)` variant.
pub type SecurityEventEvent = CrudEvent<SecurityEvent>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for SecurityEvent events.
pub type SecurityEventEventPublisher = Arc<dyn CrudEventPublisher<SecurityEvent> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSecurityEventEventPublisher = NoOpCrudEventPublisher<SecurityEvent>;
