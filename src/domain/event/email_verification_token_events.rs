//! Domain events for EmailVerificationToken entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::EmailVerificationToken;

/// Domain events for EmailVerificationToken — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<EmailVerificationToken>)` variant.
pub type EmailVerificationTokenEvent = CrudEvent<EmailVerificationToken>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for EmailVerificationToken events.
pub type EmailVerificationTokenEventPublisher = Arc<dyn CrudEventPublisher<EmailVerificationToken> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopEmailVerificationTokenEventPublisher = NoOpCrudEventPublisher<EmailVerificationToken>;
