//! Domain events for WorkflowAction entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::WorkflowAction;

/// Domain events for WorkflowAction — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<WorkflowAction>)` variant.
pub type WorkflowActionEvent = CrudEvent<WorkflowAction>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for WorkflowAction events.
pub type WorkflowActionEventPublisher = Arc<dyn CrudEventPublisher<WorkflowAction> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopWorkflowActionEventPublisher = NoOpCrudEventPublisher<WorkflowAction>;
