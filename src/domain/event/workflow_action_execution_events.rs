//! Domain events for WorkflowActionExecution entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::WorkflowActionExecution;

/// Domain events for WorkflowActionExecution — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<WorkflowActionExecution>)` variant.
pub type WorkflowActionExecutionEvent = CrudEvent<WorkflowActionExecution>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for WorkflowActionExecution events.
pub type WorkflowActionExecutionEventPublisher = Arc<dyn CrudEventPublisher<WorkflowActionExecution> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopWorkflowActionExecutionEventPublisher = NoOpCrudEventPublisher<WorkflowActionExecution>;
