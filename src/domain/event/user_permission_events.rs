//! Domain events for UserPermission entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::UserPermission;

/// Domain events for UserPermission — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<UserPermission>)` variant.
pub type UserPermissionEvent = CrudEvent<UserPermission>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for UserPermission events.
pub type UserPermissionEventPublisher = Arc<dyn CrudEventPublisher<UserPermission> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopUserPermissionEventPublisher = NoOpCrudEventPublisher<UserPermission>;
