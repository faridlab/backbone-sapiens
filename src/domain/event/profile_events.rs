//! Domain events for Profile entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Profile;

/// Domain events for Profile — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Profile>)` variant.
pub type ProfileEvent = CrudEvent<Profile>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Profile events.
pub type ProfileEventPublisher = Arc<dyn CrudEventPublisher<Profile> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopProfileEventPublisher = NoOpCrudEventPublisher<Profile>;
