//! Domain events for Role entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Role;

/// Domain events for Role — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Role>)` variant.
pub type RoleEvent = CrudEvent<Role>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Role events.
pub type RoleEventPublisher = Arc<dyn CrudEventPublisher<Role> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopRoleEventPublisher = NoOpCrudEventPublisher<Role>;
