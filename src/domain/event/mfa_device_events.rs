//! Domain events for MFADevice entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::MFADevice;

/// Domain events for MFADevice — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<MFADevice>)` variant.
pub type MFADeviceEvent = CrudEvent<MFADevice>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for MFADevice events.
pub type MFADeviceEventPublisher = Arc<dyn CrudEventPublisher<MFADevice> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopMFADeviceEventPublisher = NoOpCrudEventPublisher<MFADevice>;
