//! Domain events for OAuthProvider entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::OAuthProvider;

/// Domain events for OAuthProvider — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<OAuthProvider>)` variant.
pub type OAuthProviderEvent = CrudEvent<OAuthProvider>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for OAuthProvider events.
pub type OAuthProviderEventPublisher = Arc<dyn CrudEventPublisher<OAuthProvider> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopOAuthProviderEventPublisher = NoOpCrudEventPublisher<OAuthProvider>;
