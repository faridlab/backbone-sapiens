//! Domain events for DirectPermissionGrant entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::DirectPermissionGrant;

/// Domain events for DirectPermissionGrant — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<DirectPermissionGrant>)` variant.
pub type DirectPermissionGrantEvent = CrudEvent<DirectPermissionGrant>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for DirectPermissionGrant events.
pub type DirectPermissionGrantEventPublisher = Arc<dyn CrudEventPublisher<DirectPermissionGrant> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopDirectPermissionGrantEventPublisher = NoOpCrudEventPublisher<DirectPermissionGrant>;
