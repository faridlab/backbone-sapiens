//! Domain events for WorkflowDefinition entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::WorkflowDefinition;

/// Domain events for WorkflowDefinition — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<WorkflowDefinition>)` variant.
pub type WorkflowDefinitionEvent = CrudEvent<WorkflowDefinition>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for WorkflowDefinition events.
pub type WorkflowDefinitionEventPublisher = Arc<dyn CrudEventPublisher<WorkflowDefinition> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopWorkflowDefinitionEventPublisher = NoOpCrudEventPublisher<WorkflowDefinition>;
