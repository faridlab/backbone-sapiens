//! Domain events for PasswordRequirements entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PasswordRequirements;

/// Domain events for PasswordRequirements — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PasswordRequirements>)` variant.
pub type PasswordRequirementsEvent = CrudEvent<PasswordRequirements>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PasswordRequirements events.
pub type PasswordRequirementsEventPublisher = Arc<dyn CrudEventPublisher<PasswordRequirements> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPasswordRequirementsEventPublisher = NoOpCrudEventPublisher<PasswordRequirements>;
