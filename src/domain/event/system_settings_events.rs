//! Domain events for SystemSettings entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::SystemSettings;

/// Domain events for SystemSettings — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<SystemSettings>)` variant.
pub type SystemSettingsEvent = CrudEvent<SystemSettings>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for SystemSettings events.
pub type SystemSettingsEventPublisher = Arc<dyn CrudEventPublisher<SystemSettings> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSystemSettingsEventPublisher = NoOpCrudEventPublisher<SystemSettings>;
