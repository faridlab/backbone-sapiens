//! Domain events for ResourcePermission entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::ResourcePermission;

/// Domain events for ResourcePermission — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<ResourcePermission>)` variant.
pub type ResourcePermissionEvent = CrudEvent<ResourcePermission>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for ResourcePermission events.
pub type ResourcePermissionEventPublisher = Arc<dyn CrudEventPublisher<ResourcePermission> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopResourcePermissionEventPublisher = NoOpCrudEventPublisher<ResourcePermission>;
