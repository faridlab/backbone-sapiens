//! Domain events for SessionLimit entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::SessionLimit;

/// Domain events for SessionLimit — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<SessionLimit>)` variant.
pub type SessionLimitEvent = CrudEvent<SessionLimit>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for SessionLimit events.
pub type SessionLimitEventPublisher = Arc<dyn CrudEventPublisher<SessionLimit> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSessionLimitEventPublisher = NoOpCrudEventPublisher<SessionLimit>;
