//! Domain events for PasswordCreationContext entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::PasswordCreationContext;

/// Domain events for PasswordCreationContext — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<PasswordCreationContext>)` variant.
pub type PasswordCreationContextEvent = CrudEvent<PasswordCreationContext>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for PasswordCreationContext events.
pub type PasswordCreationContextEventPublisher = Arc<dyn CrudEventPublisher<PasswordCreationContext> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPasswordCreationContextEventPublisher = NoOpCrudEventPublisher<PasswordCreationContext>;
