//! Domain events for Notification entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Notification;

/// Domain events for Notification — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Notification>)` variant.
pub type NotificationEvent = CrudEvent<Notification>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Notification events.
pub type NotificationEventPublisher = Arc<dyn CrudEventPublisher<Notification> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopNotificationEventPublisher = NoOpCrudEventPublisher<Notification>;
