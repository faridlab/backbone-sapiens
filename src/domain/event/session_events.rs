//! Domain events for Session entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Session;

/// Domain events for Session — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Session>)` variant.
pub type SessionEvent = CrudEvent<Session>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Session events.
pub type SessionEventPublisher = Arc<dyn CrudEventPublisher<Session> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopSessionEventPublisher = NoOpCrudEventPublisher<Session>;
