//! Domain events for AnonymizationRecord entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::AnonymizationRecord;

/// Domain events for AnonymizationRecord — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<AnonymizationRecord>)` variant.
pub type AnonymizationRecordEvent = CrudEvent<AnonymizationRecord>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for AnonymizationRecord events.
pub type AnonymizationRecordEventPublisher = Arc<dyn CrudEventPublisher<AnonymizationRecord> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopAnonymizationRecordEventPublisher = NoOpCrudEventPublisher<AnonymizationRecord>;
