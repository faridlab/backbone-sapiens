//! Domain events for Permission entity
//!
//! Generated by backbone-schema. Do not edit manually.

use backbone_messaging::CrudEvent;
use crate::domain::entity::Permission;

/// Domain events for Permission — type alias over the generic CrudEvent.
///
/// To add entity-specific events, replace this alias with a wrapper
/// enum containing a `Crud(CrudEvent<Permission>)` variant.
pub type PermissionEvent = CrudEvent<Permission>;

// <<< CUSTOM
// END CUSTOM

use backbone_messaging::{CrudEventPublisher, NoOpCrudEventPublisher};
use std::sync::Arc;

/// Publisher alias for Permission events.
pub type PermissionEventPublisher = Arc<dyn CrudEventPublisher<Permission> + Send + Sync>;

/// No-op publisher — discards events silently (useful in tests).
pub type NoopPermissionEventPublisher = NoOpCrudEventPublisher<Permission>;
