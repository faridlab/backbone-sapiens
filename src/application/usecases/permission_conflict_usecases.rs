//! Use case type aliases for PermissionConflict aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Each alias delegates to the matching generic use case in backbone-core.
//! Custom pre/post hooks go in `UseCaseHooks` implementations (// <<< CUSTOM).

use backbone_core::{
    CreateUseCase, UpdateUseCase, GetUseCase, DeleteUseCase, ListUseCase,
};

use crate::presentation::dto::{CreatePermissionConflictDto, UpdatePermissionConflictDto};
use crate::application::service::PermissionConflictService;
use crate::domain::entity::PermissionConflict;

// ─── Use case type aliases ───────────────────────────────────────────────────

pub type CreatePermissionConflictUseCase = CreateUseCase<PermissionConflict, CreatePermissionConflictDto, PermissionConflictService>;
pub type UpdatePermissionConflictUseCase = UpdateUseCase<PermissionConflict, UpdatePermissionConflictDto, PermissionConflictService>;
pub type GetPermissionConflictUseCase    = GetUseCase<PermissionConflict, PermissionConflictService>;
pub type DeletePermissionConflictUseCase = DeleteUseCase<PermissionConflict, PermissionConflictService>;
pub type ListPermissionConflictUseCase   = ListUseCase<PermissionConflict, PermissionConflictService>;

// <<< CUSTOM
// END CUSTOM
