//! Commands for PasswordResetToken aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordResetToken;
use crate::infrastructure::persistence::PasswordResetTokenRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordResetToken operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordResetTokenCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_reset_token
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordResetTokenCommand {
    pub user_id: Uuid,
    pub token_hash: String,
    pub expires_at: DateTime<Utc>,
    pub is_used: bool,
    pub used_at: Option<DateTime<Utc>>,
    pub ip_address: Option<String>,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordResetTokenCommand
pub struct CreatePasswordResetTokenHandler<R: PasswordResetTokenRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetTokenRepository> CreatePasswordResetTokenHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetTokenRepository + 'static> CommandHandler<CreatePasswordResetTokenCommand> for CreatePasswordResetTokenHandler<R> {
    type Output = PasswordResetToken;

    async fn handle(&self, cmd: CreatePasswordResetTokenCommand) -> Result<Self::Output> {
        let entity = PasswordResetToken::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .token_hash(cmd.token_hash)
            .expires_at(cmd.expires_at)
            .is_used(cmd.is_used)
            .used_at(cmd.used_at)
            .ip_address(cmd.ip_address)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_reset_token
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordResetTokenCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub token_hash: Option<String>,
    pub expires_at: Option<DateTime<Utc>>,
    pub is_used: Option<bool>,
    pub used_at: Option<DateTime<Utc>>,
    pub ip_address: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordResetTokenCommand
pub struct UpdatePasswordResetTokenHandler<R: PasswordResetTokenRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetTokenRepository> UpdatePasswordResetTokenHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetTokenRepository + 'static> CommandHandler<UpdatePasswordResetTokenCommand> for UpdatePasswordResetTokenHandler<R> {
    type Output = Option<PasswordResetToken>;

    async fn handle(&self, cmd: UpdatePasswordResetTokenCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.token_hash {
            entity.token_hash = value;
        }
        if let Some(value) = cmd.expires_at {
            entity.expires_at = value;
        }
        if let Some(value) = cmd.is_used {
            entity.is_used = value;
        }
        entity.used_at = cmd.used_at;
        entity.ip_address = cmd.ip_address;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_reset_token
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordResetTokenCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordResetTokenCommand
pub struct DeletePasswordResetTokenHandler<R: PasswordResetTokenRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetTokenRepository> DeletePasswordResetTokenHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetTokenRepository + 'static> CommandHandler<DeletePasswordResetTokenCommand> for DeletePasswordResetTokenHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordResetTokenCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordResetTokenCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>