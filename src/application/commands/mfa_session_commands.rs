//! Commands for MFASession aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::MFASession;
use crate::infrastructure::persistence::MFASessionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for MFASession operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait MFASessionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new mfa_session
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateMFASessionCommand {
    pub user_id: Uuid,
    pub device_id: Uuid,
    pub session_token: String,
    pub session_hash: String,
    pub primary_authentication_method: PrimaryAuthMethod,
    pub mfa_required: bool,
    pub mfa_enforced_at: Option<DateTime<Utc>>,
    pub risk_score: i32,
    pub trust_level: MfaTrustLevel,
    pub adaptive_authentication_triggered: bool,
    pub additional_factors_required: i32,
    pub ip_risk_score: Option<i32>,
    pub device_risk_score: Option<i32>,
    pub location_anomaly: bool,
    pub time_anomaly: bool,
    pub verification_method: MFAVerificationMethod,
    pub verification_initiated_at: DateTime<Utc>,
    pub verification_completed_at: Option<DateTime<Utc>>,
    pub verification_attempts: i32,
    pub max_attempts_allowed: i32,
    pub verification_success: Option<bool>,
    pub verification_time_ms: Option<i32>,
    pub device_trust_status: DeviceTrustStatus,
    pub is_terminated: bool,
    pub terminated_at: Option<DateTime<Utc>>,
    pub termination_reason: Option<String>,
    pub ip_address: String,
    pub user_agent: Option<String>,
    pub device_fingerprint: Option<String>,
    pub country: Option<String>,
    pub region: Option<String>,
    pub city: Option<String>,
    pub latitude: Option<f64>,
    pub longitude: Option<f64>,
    pub expires_at: DateTime<Utc>,
    pub max_duration_minutes: i32,
    pub idle_timeout_minutes: i32,
    pub auto_renew_enabled: bool,
    pub last_activity_at: DateTime<Utc>,
    pub extension_count: i32,
    pub initiation_time_ms: i32,
    pub total_session_time_ms: Option<i32>,
    pub device_performance_score: Option<i32>,
    pub network_quality: NetworkQuality,
    pub audit_log_required: bool,
    pub data_retention_hours: i32,
    pub status: MFASessionStatus,
    pub metadata: serde_json::Value,
}

/// Handler for CreateMFASessionCommand
pub struct CreateMFASessionHandler<R: MFASessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: MFASessionRepository> CreateMFASessionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: MFASessionRepository + 'static> CommandHandler<CreateMFASessionCommand> for CreateMFASessionHandler<R> {
    type Output = MFASession;

    async fn handle(&self, cmd: CreateMFASessionCommand) -> Result<Self::Output> {
        let entity = MFASession::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .device_id(cmd.device_id)
            .session_token(cmd.session_token)
            .session_hash(cmd.session_hash)
            .primary_authentication_method(cmd.primary_authentication_method)
            .mfa_required(cmd.mfa_required)
            .mfa_enforced_at(cmd.mfa_enforced_at)
            .risk_score(cmd.risk_score)
            .trust_level(cmd.trust_level)
            .adaptive_authentication_triggered(cmd.adaptive_authentication_triggered)
            .additional_factors_required(cmd.additional_factors_required)
            .ip_risk_score(cmd.ip_risk_score)
            .device_risk_score(cmd.device_risk_score)
            .location_anomaly(cmd.location_anomaly)
            .time_anomaly(cmd.time_anomaly)
            .verification_method(cmd.verification_method)
            .verification_initiated_at(cmd.verification_initiated_at)
            .verification_completed_at(cmd.verification_completed_at)
            .verification_attempts(cmd.verification_attempts)
            .max_attempts_allowed(cmd.max_attempts_allowed)
            .verification_success(cmd.verification_success)
            .verification_time_ms(cmd.verification_time_ms)
            .device_trust_status(cmd.device_trust_status)
            .is_terminated(cmd.is_terminated)
            .terminated_at(cmd.terminated_at)
            .termination_reason(cmd.termination_reason)
            .ip_address(cmd.ip_address)
            .user_agent(cmd.user_agent)
            .device_fingerprint(cmd.device_fingerprint)
            .country(cmd.country)
            .region(cmd.region)
            .city(cmd.city)
            .latitude(cmd.latitude)
            .longitude(cmd.longitude)
            .expires_at(cmd.expires_at)
            .max_duration_minutes(cmd.max_duration_minutes)
            .idle_timeout_minutes(cmd.idle_timeout_minutes)
            .auto_renew_enabled(cmd.auto_renew_enabled)
            .last_activity_at(cmd.last_activity_at)
            .extension_count(cmd.extension_count)
            .initiation_time_ms(cmd.initiation_time_ms)
            .total_session_time_ms(cmd.total_session_time_ms)
            .device_performance_score(cmd.device_performance_score)
            .network_quality(cmd.network_quality)
            .audit_log_required(cmd.audit_log_required)
            .data_retention_hours(cmd.data_retention_hours)
            .status(cmd.status)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing mfa_session
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateMFASessionCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub device_id: Option<Uuid>,
    pub session_token: Option<String>,
    pub session_hash: Option<String>,
    pub primary_authentication_method: Option<PrimaryAuthMethod>,
    pub mfa_required: Option<bool>,
    pub mfa_enforced_at: Option<DateTime<Utc>>,
    pub risk_score: Option<i32>,
    pub trust_level: Option<MfaTrustLevel>,
    pub adaptive_authentication_triggered: Option<bool>,
    pub additional_factors_required: Option<i32>,
    pub ip_risk_score: Option<i32>,
    pub device_risk_score: Option<i32>,
    pub location_anomaly: Option<bool>,
    pub time_anomaly: Option<bool>,
    pub verification_method: Option<MFAVerificationMethod>,
    pub verification_initiated_at: Option<DateTime<Utc>>,
    pub verification_completed_at: Option<DateTime<Utc>>,
    pub verification_attempts: Option<i32>,
    pub max_attempts_allowed: Option<i32>,
    pub verification_success: Option<bool>,
    pub verification_time_ms: Option<i32>,
    pub device_trust_status: Option<DeviceTrustStatus>,
    pub is_terminated: Option<bool>,
    pub terminated_at: Option<DateTime<Utc>>,
    pub termination_reason: Option<String>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub device_fingerprint: Option<String>,
    pub country: Option<String>,
    pub region: Option<String>,
    pub city: Option<String>,
    pub latitude: Option<f64>,
    pub longitude: Option<f64>,
    pub expires_at: Option<DateTime<Utc>>,
    pub max_duration_minutes: Option<i32>,
    pub idle_timeout_minutes: Option<i32>,
    pub auto_renew_enabled: Option<bool>,
    pub last_activity_at: Option<DateTime<Utc>>,
    pub extension_count: Option<i32>,
    pub initiation_time_ms: Option<i32>,
    pub total_session_time_ms: Option<i32>,
    pub device_performance_score: Option<i32>,
    pub network_quality: Option<NetworkQuality>,
    pub audit_log_required: Option<bool>,
    pub data_retention_hours: Option<i32>,
    pub status: Option<MFASessionStatus>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateMFASessionCommand
pub struct UpdateMFASessionHandler<R: MFASessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: MFASessionRepository> UpdateMFASessionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: MFASessionRepository + 'static> CommandHandler<UpdateMFASessionCommand> for UpdateMFASessionHandler<R> {
    type Output = Option<MFASession>;

    async fn handle(&self, cmd: UpdateMFASessionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.device_id {
            entity.device_id = value;
        }
        if let Some(value) = cmd.session_token {
            entity.session_token = value;
        }
        if let Some(value) = cmd.session_hash {
            entity.session_hash = value;
        }
        if let Some(value) = cmd.primary_authentication_method {
            entity.primary_authentication_method = value;
        }
        if let Some(value) = cmd.mfa_required {
            entity.mfa_required = value;
        }
        entity.mfa_enforced_at = cmd.mfa_enforced_at;
        if let Some(value) = cmd.risk_score {
            entity.risk_score = value;
        }
        if let Some(value) = cmd.trust_level {
            entity.trust_level = value;
        }
        if let Some(value) = cmd.adaptive_authentication_triggered {
            entity.adaptive_authentication_triggered = value;
        }
        if let Some(value) = cmd.additional_factors_required {
            entity.additional_factors_required = value;
        }
        entity.ip_risk_score = cmd.ip_risk_score;
        entity.device_risk_score = cmd.device_risk_score;
        if let Some(value) = cmd.location_anomaly {
            entity.location_anomaly = value;
        }
        if let Some(value) = cmd.time_anomaly {
            entity.time_anomaly = value;
        }
        if let Some(value) = cmd.verification_method {
            entity.verification_method = value;
        }
        if let Some(value) = cmd.verification_initiated_at {
            entity.verification_initiated_at = value;
        }
        entity.verification_completed_at = cmd.verification_completed_at;
        if let Some(value) = cmd.verification_attempts {
            entity.verification_attempts = value;
        }
        if let Some(value) = cmd.max_attempts_allowed {
            entity.max_attempts_allowed = value;
        }
        entity.verification_success = cmd.verification_success;
        entity.verification_time_ms = cmd.verification_time_ms;
        if let Some(value) = cmd.device_trust_status {
            entity.device_trust_status = value;
        }
        if let Some(value) = cmd.is_terminated {
            entity.is_terminated = value;
        }
        entity.terminated_at = cmd.terminated_at;
        entity.termination_reason = cmd.termination_reason;
        if let Some(value) = cmd.ip_address {
            entity.ip_address = value;
        }
        entity.user_agent = cmd.user_agent;
        entity.device_fingerprint = cmd.device_fingerprint;
        entity.country = cmd.country;
        entity.region = cmd.region;
        entity.city = cmd.city;
        entity.latitude = cmd.latitude;
        entity.longitude = cmd.longitude;
        if let Some(value) = cmd.expires_at {
            entity.expires_at = value;
        }
        if let Some(value) = cmd.max_duration_minutes {
            entity.max_duration_minutes = value;
        }
        if let Some(value) = cmd.idle_timeout_minutes {
            entity.idle_timeout_minutes = value;
        }
        if let Some(value) = cmd.auto_renew_enabled {
            entity.auto_renew_enabled = value;
        }
        if let Some(value) = cmd.last_activity_at {
            entity.last_activity_at = value;
        }
        if let Some(value) = cmd.extension_count {
            entity.extension_count = value;
        }
        if let Some(value) = cmd.initiation_time_ms {
            entity.initiation_time_ms = value;
        }
        entity.total_session_time_ms = cmd.total_session_time_ms;
        entity.device_performance_score = cmd.device_performance_score;
        if let Some(value) = cmd.network_quality {
            entity.network_quality = value;
        }
        if let Some(value) = cmd.audit_log_required {
            entity.audit_log_required = value;
        }
        if let Some(value) = cmd.data_retention_hours {
            entity.data_retention_hours = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a mfa_session
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteMFASessionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteMFASessionCommand
pub struct DeleteMFASessionHandler<R: MFASessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: MFASessionRepository> DeleteMFASessionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: MFASessionRepository + 'static> CommandHandler<DeleteMFASessionCommand> for DeleteMFASessionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteMFASessionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateMFASessionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>