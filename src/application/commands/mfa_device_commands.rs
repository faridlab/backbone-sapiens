//! Commands for MFADevice aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::MFADevice;
use crate::infrastructure::persistence::MFADeviceRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for MFADevice operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait MFADeviceCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new mfa_device
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateMFADeviceCommand {
    pub user_id: Uuid,
    pub device_type: MFADeviceType,
    pub device_name: Option<String>,
    pub phone_number: Option<String>,
    pub email_address: Option<String>,
    pub totp_secret: Option<String>,
    pub secret: Option<String>,
    pub hardware_key_id: Option<String>,
    pub push_token: Option<String>,
    pub device_fingerprint: Option<String>,
    pub manufacturer: Option<String>,
    pub model: Option<String>,
    pub operating_system: Option<String>,
    pub app_version: Option<String>,
    pub is_primary: bool,
    pub is_backup: bool,
    pub requires_verification: bool,
    pub auto_approval_enabled: bool,
    pub trusted_duration_hours: i32,
    pub enrolled_at: DateTime<Utc>,
    pub enrolled_by: Option<Uuid>,
    pub enrollment_method: EnrollmentMethod,
    pub enrollment_ip: String,
    pub enrollment_user_agent: Option<String>,
    pub verification_attempts: i32,
    pub backup_codes_generated: bool,
    pub verified_at: Option<DateTime<Utc>>,
    pub last_used: Option<DateTime<Utc>>,
    pub last_used_at: Option<DateTime<Utc>>,
    pub usage_count: i32,
    pub successful_verifications: i32,
    pub failed_verifications: i32,
    pub is_locked: bool,
    pub locked_at: Option<DateTime<Utc>>,
    pub locked_until: Option<DateTime<Utc>>,
    pub lock_reason: Option<String>,
    pub risk_score: i32,
    pub is_active: bool,
    pub status: MFADeviceStatus,
    pub backup_codes_data: Option<serde_json::Value>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateMFADeviceCommand
pub struct CreateMFADeviceHandler<R: MFADeviceRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: MFADeviceRepository> CreateMFADeviceHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: MFADeviceRepository + 'static> CommandHandler<CreateMFADeviceCommand> for CreateMFADeviceHandler<R> {
    type Output = MFADevice;

    async fn handle(&self, cmd: CreateMFADeviceCommand) -> Result<Self::Output> {
        let entity = MFADevice::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .device_type(cmd.device_type)
            .device_name(cmd.device_name)
            .phone_number(cmd.phone_number)
            .email_address(cmd.email_address)
            .totp_secret(cmd.totp_secret)
            .secret(cmd.secret)
            .hardware_key_id(cmd.hardware_key_id)
            .push_token(cmd.push_token)
            .device_fingerprint(cmd.device_fingerprint)
            .manufacturer(cmd.manufacturer)
            .model(cmd.model)
            .operating_system(cmd.operating_system)
            .app_version(cmd.app_version)
            .is_primary(cmd.is_primary)
            .is_backup(cmd.is_backup)
            .requires_verification(cmd.requires_verification)
            .auto_approval_enabled(cmd.auto_approval_enabled)
            .trusted_duration_hours(cmd.trusted_duration_hours)
            .enrolled_at(cmd.enrolled_at)
            .enrolled_by(cmd.enrolled_by)
            .enrollment_method(cmd.enrollment_method)
            .enrollment_ip(cmd.enrollment_ip)
            .enrollment_user_agent(cmd.enrollment_user_agent)
            .verification_attempts(cmd.verification_attempts)
            .backup_codes_generated(cmd.backup_codes_generated)
            .verified_at(cmd.verified_at)
            .last_used(cmd.last_used)
            .last_used_at(cmd.last_used_at)
            .usage_count(cmd.usage_count)
            .successful_verifications(cmd.successful_verifications)
            .failed_verifications(cmd.failed_verifications)
            .is_locked(cmd.is_locked)
            .locked_at(cmd.locked_at)
            .locked_until(cmd.locked_until)
            .lock_reason(cmd.lock_reason)
            .risk_score(cmd.risk_score)
            .is_active(cmd.is_active)
            .status(cmd.status)
            .backup_codes_data(cmd.backup_codes_data)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing mfa_device
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateMFADeviceCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub device_type: Option<MFADeviceType>,
    pub device_name: Option<String>,
    pub phone_number: Option<String>,
    pub email_address: Option<String>,
    pub totp_secret: Option<String>,
    pub secret: Option<String>,
    pub hardware_key_id: Option<String>,
    pub push_token: Option<String>,
    pub device_fingerprint: Option<String>,
    pub manufacturer: Option<String>,
    pub model: Option<String>,
    pub operating_system: Option<String>,
    pub app_version: Option<String>,
    pub is_primary: Option<bool>,
    pub is_backup: Option<bool>,
    pub requires_verification: Option<bool>,
    pub auto_approval_enabled: Option<bool>,
    pub trusted_duration_hours: Option<i32>,
    pub enrolled_at: Option<DateTime<Utc>>,
    pub enrolled_by: Option<Uuid>,
    pub enrollment_method: Option<EnrollmentMethod>,
    pub enrollment_ip: Option<String>,
    pub enrollment_user_agent: Option<String>,
    pub verification_attempts: Option<i32>,
    pub backup_codes_generated: Option<bool>,
    pub verified_at: Option<DateTime<Utc>>,
    pub last_used: Option<DateTime<Utc>>,
    pub last_used_at: Option<DateTime<Utc>>,
    pub usage_count: Option<i32>,
    pub successful_verifications: Option<i32>,
    pub failed_verifications: Option<i32>,
    pub is_locked: Option<bool>,
    pub locked_at: Option<DateTime<Utc>>,
    pub locked_until: Option<DateTime<Utc>>,
    pub lock_reason: Option<String>,
    pub risk_score: Option<i32>,
    pub is_active: Option<bool>,
    pub status: Option<MFADeviceStatus>,
    pub backup_codes_data: Option<serde_json::Value>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateMFADeviceCommand
pub struct UpdateMFADeviceHandler<R: MFADeviceRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: MFADeviceRepository> UpdateMFADeviceHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: MFADeviceRepository + 'static> CommandHandler<UpdateMFADeviceCommand> for UpdateMFADeviceHandler<R> {
    type Output = Option<MFADevice>;

    async fn handle(&self, cmd: UpdateMFADeviceCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.device_type {
            entity.device_type = value;
        }
        entity.device_name = cmd.device_name;
        entity.phone_number = cmd.phone_number;
        entity.email_address = cmd.email_address;
        entity.totp_secret = cmd.totp_secret;
        entity.secret = cmd.secret;
        entity.hardware_key_id = cmd.hardware_key_id;
        entity.push_token = cmd.push_token;
        entity.device_fingerprint = cmd.device_fingerprint;
        entity.manufacturer = cmd.manufacturer;
        entity.model = cmd.model;
        entity.operating_system = cmd.operating_system;
        entity.app_version = cmd.app_version;
        if let Some(value) = cmd.is_primary {
            entity.is_primary = value;
        }
        if let Some(value) = cmd.is_backup {
            entity.is_backup = value;
        }
        if let Some(value) = cmd.requires_verification {
            entity.requires_verification = value;
        }
        if let Some(value) = cmd.auto_approval_enabled {
            entity.auto_approval_enabled = value;
        }
        if let Some(value) = cmd.trusted_duration_hours {
            entity.trusted_duration_hours = value;
        }
        if let Some(value) = cmd.enrolled_at {
            entity.enrolled_at = value;
        }
        entity.enrolled_by = cmd.enrolled_by;
        if let Some(value) = cmd.enrollment_method {
            entity.enrollment_method = value;
        }
        if let Some(value) = cmd.enrollment_ip {
            entity.enrollment_ip = value;
        }
        entity.enrollment_user_agent = cmd.enrollment_user_agent;
        if let Some(value) = cmd.verification_attempts {
            entity.verification_attempts = value;
        }
        if let Some(value) = cmd.backup_codes_generated {
            entity.backup_codes_generated = value;
        }
        entity.verified_at = cmd.verified_at;
        entity.last_used = cmd.last_used;
        entity.last_used_at = cmd.last_used_at;
        if let Some(value) = cmd.usage_count {
            entity.usage_count = value;
        }
        if let Some(value) = cmd.successful_verifications {
            entity.successful_verifications = value;
        }
        if let Some(value) = cmd.failed_verifications {
            entity.failed_verifications = value;
        }
        if let Some(value) = cmd.is_locked {
            entity.is_locked = value;
        }
        entity.locked_at = cmd.locked_at;
        entity.locked_until = cmd.locked_until;
        entity.lock_reason = cmd.lock_reason;
        if let Some(value) = cmd.risk_score {
            entity.risk_score = value;
        }
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        entity.backup_codes_data = cmd.backup_codes_data;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a mfa_device
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteMFADeviceCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteMFADeviceCommand
pub struct DeleteMFADeviceHandler<R: MFADeviceRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: MFADeviceRepository> DeleteMFADeviceHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: MFADeviceRepository + 'static> CommandHandler<DeleteMFADeviceCommand> for DeleteMFADeviceHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteMFADeviceCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateMFADeviceCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>