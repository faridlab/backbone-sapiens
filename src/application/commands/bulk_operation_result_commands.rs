//! Commands for BulkOperationResult aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::BulkOperationResult;
use crate::infrastructure::persistence::BulkOperationResultRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for BulkOperationResult operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait BulkOperationResultCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new bulk_operation_result
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateBulkOperationResultCommand {
    pub bulk_operation_id: Uuid,
    pub row_number: i32,
    pub record_type: String,
    pub status: BulkOperationResultStatus,
    pub input_data: Option<serde_json::Value>,
    pub output_data: Option<serde_json::Value>,
    pub error_message: Option<String>,
    pub warning_message: Option<String>,
    pub processed_at: Option<DateTime<Utc>>,
    pub processing_duration_ms: Option<i32>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateBulkOperationResultCommand
pub struct CreateBulkOperationResultHandler<R: BulkOperationResultRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BulkOperationResultRepository> CreateBulkOperationResultHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BulkOperationResultRepository + 'static> CommandHandler<CreateBulkOperationResultCommand> for CreateBulkOperationResultHandler<R> {
    type Output = BulkOperationResult;

    async fn handle(&self, cmd: CreateBulkOperationResultCommand) -> Result<Self::Output> {
        let entity = BulkOperationResult::builder()
            .id(Uuid::new_v4().to_string())
            .bulk_operation_id(cmd.bulk_operation_id)
            .row_number(cmd.row_number)
            .record_type(cmd.record_type)
            .status(cmd.status)
            .input_data(cmd.input_data)
            .output_data(cmd.output_data)
            .error_message(cmd.error_message)
            .warning_message(cmd.warning_message)
            .processed_at(cmd.processed_at)
            .processing_duration_ms(cmd.processing_duration_ms)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing bulk_operation_result
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateBulkOperationResultCommand {
    pub id: String,
    pub bulk_operation_id: Option<Uuid>,
    pub row_number: Option<i32>,
    pub record_type: Option<String>,
    pub status: Option<BulkOperationResultStatus>,
    pub input_data: Option<serde_json::Value>,
    pub output_data: Option<serde_json::Value>,
    pub error_message: Option<String>,
    pub warning_message: Option<String>,
    pub processed_at: Option<DateTime<Utc>>,
    pub processing_duration_ms: Option<i32>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateBulkOperationResultCommand
pub struct UpdateBulkOperationResultHandler<R: BulkOperationResultRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BulkOperationResultRepository> UpdateBulkOperationResultHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BulkOperationResultRepository + 'static> CommandHandler<UpdateBulkOperationResultCommand> for UpdateBulkOperationResultHandler<R> {
    type Output = Option<BulkOperationResult>;

    async fn handle(&self, cmd: UpdateBulkOperationResultCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.bulk_operation_id {
            entity.bulk_operation_id = value;
        }
        if let Some(value) = cmd.row_number {
            entity.row_number = value;
        }
        if let Some(value) = cmd.record_type {
            entity.record_type = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        entity.input_data = cmd.input_data;
        entity.output_data = cmd.output_data;
        entity.error_message = cmd.error_message;
        entity.warning_message = cmd.warning_message;
        entity.processed_at = cmd.processed_at;
        entity.processing_duration_ms = cmd.processing_duration_ms;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a bulk_operation_result
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteBulkOperationResultCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteBulkOperationResultCommand
pub struct DeleteBulkOperationResultHandler<R: BulkOperationResultRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BulkOperationResultRepository> DeleteBulkOperationResultHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BulkOperationResultRepository + 'static> CommandHandler<DeleteBulkOperationResultCommand> for DeleteBulkOperationResultHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteBulkOperationResultCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateBulkOperationResultCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>