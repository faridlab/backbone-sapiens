//! Commands for UserPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::UserPermission;
use crate::infrastructure::persistence::UserPermissionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for UserPermission operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait UserPermissionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new user_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateUserPermissionCommand {
    pub user_id: Uuid,
    pub permission_id: Uuid,
    pub granted_at: DateTime<Utc>,
    pub granted_by: Uuid,
    pub reason: String,
    pub expires_at: Option<DateTime<Utc>>,
    pub resource_id: Option<String>,
    pub resource_type: Option<String>,
    pub is_active: bool,
    pub revoked_at: Option<DateTime<Utc>>,
    pub revoked_by: Option<Uuid>,
    pub revoked_reason: Option<String>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateUserPermissionCommand
pub struct CreateUserPermissionHandler<R: UserPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserPermissionRepository> CreateUserPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserPermissionRepository + 'static> CommandHandler<CreateUserPermissionCommand> for CreateUserPermissionHandler<R> {
    type Output = UserPermission;

    async fn handle(&self, cmd: CreateUserPermissionCommand) -> Result<Self::Output> {
        let entity = UserPermission::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .permission_id(cmd.permission_id)
            .granted_at(cmd.granted_at)
            .granted_by(cmd.granted_by)
            .reason(cmd.reason)
            .expires_at(cmd.expires_at)
            .resource_id(cmd.resource_id)
            .resource_type(cmd.resource_type)
            .is_active(cmd.is_active)
            .revoked_at(cmd.revoked_at)
            .revoked_by(cmd.revoked_by)
            .revoked_reason(cmd.revoked_reason)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing user_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateUserPermissionCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
    pub granted_at: Option<DateTime<Utc>>,
    pub granted_by: Option<Uuid>,
    pub reason: Option<String>,
    pub expires_at: Option<DateTime<Utc>>,
    pub resource_id: Option<String>,
    pub resource_type: Option<String>,
    pub is_active: Option<bool>,
    pub revoked_at: Option<DateTime<Utc>>,
    pub revoked_by: Option<Uuid>,
    pub revoked_reason: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateUserPermissionCommand
pub struct UpdateUserPermissionHandler<R: UserPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserPermissionRepository> UpdateUserPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserPermissionRepository + 'static> CommandHandler<UpdateUserPermissionCommand> for UpdateUserPermissionHandler<R> {
    type Output = Option<UserPermission>;

    async fn handle(&self, cmd: UpdateUserPermissionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.permission_id {
            entity.permission_id = value;
        }
        if let Some(value) = cmd.granted_at {
            entity.granted_at = value;
        }
        if let Some(value) = cmd.granted_by {
            entity.granted_by = value;
        }
        if let Some(value) = cmd.reason {
            entity.reason = value;
        }
        entity.expires_at = cmd.expires_at;
        entity.resource_id = cmd.resource_id;
        entity.resource_type = cmd.resource_type;
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        entity.revoked_at = cmd.revoked_at;
        entity.revoked_by = cmd.revoked_by;
        entity.revoked_reason = cmd.revoked_reason;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a user_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteUserPermissionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteUserPermissionCommand
pub struct DeleteUserPermissionHandler<R: UserPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserPermissionRepository> DeleteUserPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserPermissionRepository + 'static> CommandHandler<DeleteUserPermissionCommand> for DeleteUserPermissionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteUserPermissionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateUserPermissionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>