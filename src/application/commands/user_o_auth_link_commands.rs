//! Commands for UserOAuthLink aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::UserOAuthLink;
use crate::infrastructure::persistence::UserOAuthLinkRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for UserOAuthLink operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait UserOAuthLinkCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new user_o_auth_link
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateUserOAuthLinkCommand {
    pub user_id: Uuid,
    pub oauth_provider_id: Uuid,
    pub provider_user_id: String,
    pub provider_email: String,
    pub provider_username: Option<String>,
    pub is_active: bool,
    pub access_token: Option<String>,
    pub refresh_token: Option<String>,
    pub token_expires_at: Option<DateTime<Utc>>,
    pub is_primary: bool,
    pub link_status: UserOAuthLinkStatus,
    pub last_synced: Option<DateTime<Utc>>,
    pub sync_enabled: bool,
    pub metadata: serde_json::Value,
}

/// Handler for CreateUserOAuthLinkCommand
pub struct CreateUserOAuthLinkHandler<R: UserOAuthLinkRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserOAuthLinkRepository> CreateUserOAuthLinkHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserOAuthLinkRepository + 'static> CommandHandler<CreateUserOAuthLinkCommand> for CreateUserOAuthLinkHandler<R> {
    type Output = UserOAuthLink;

    async fn handle(&self, cmd: CreateUserOAuthLinkCommand) -> Result<Self::Output> {
        let entity = UserOAuthLink::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .oauth_provider_id(cmd.oauth_provider_id)
            .provider_user_id(cmd.provider_user_id)
            .provider_email(cmd.provider_email)
            .provider_username(cmd.provider_username)
            .is_active(cmd.is_active)
            .access_token(cmd.access_token)
            .refresh_token(cmd.refresh_token)
            .token_expires_at(cmd.token_expires_at)
            .is_primary(cmd.is_primary)
            .link_status(cmd.link_status)
            .last_synced(cmd.last_synced)
            .sync_enabled(cmd.sync_enabled)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing user_o_auth_link
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateUserOAuthLinkCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub oauth_provider_id: Option<Uuid>,
    pub provider_user_id: Option<String>,
    pub provider_email: Option<String>,
    pub provider_username: Option<String>,
    pub is_active: Option<bool>,
    pub access_token: Option<String>,
    pub refresh_token: Option<String>,
    pub token_expires_at: Option<DateTime<Utc>>,
    pub is_primary: Option<bool>,
    pub link_status: Option<UserOAuthLinkStatus>,
    pub last_synced: Option<DateTime<Utc>>,
    pub sync_enabled: Option<bool>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateUserOAuthLinkCommand
pub struct UpdateUserOAuthLinkHandler<R: UserOAuthLinkRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserOAuthLinkRepository> UpdateUserOAuthLinkHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserOAuthLinkRepository + 'static> CommandHandler<UpdateUserOAuthLinkCommand> for UpdateUserOAuthLinkHandler<R> {
    type Output = Option<UserOAuthLink>;

    async fn handle(&self, cmd: UpdateUserOAuthLinkCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.oauth_provider_id {
            entity.oauth_provider_id = value;
        }
        if let Some(value) = cmd.provider_user_id {
            entity.provider_user_id = value;
        }
        if let Some(value) = cmd.provider_email {
            entity.provider_email = value;
        }
        entity.provider_username = cmd.provider_username;
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        entity.access_token = cmd.access_token;
        entity.refresh_token = cmd.refresh_token;
        entity.token_expires_at = cmd.token_expires_at;
        if let Some(value) = cmd.is_primary {
            entity.is_primary = value;
        }
        if let Some(value) = cmd.link_status {
            entity.link_status = value;
        }
        entity.last_synced = cmd.last_synced;
        if let Some(value) = cmd.sync_enabled {
            entity.sync_enabled = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a user_o_auth_link
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteUserOAuthLinkCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteUserOAuthLinkCommand
pub struct DeleteUserOAuthLinkHandler<R: UserOAuthLinkRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserOAuthLinkRepository> DeleteUserOAuthLinkHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserOAuthLinkRepository + 'static> CommandHandler<DeleteUserOAuthLinkCommand> for DeleteUserOAuthLinkHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteUserOAuthLinkCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateUserOAuthLinkCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>