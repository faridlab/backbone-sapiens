//! Commands for PasswordExpirationSettings aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordExpirationSettings;
use crate::infrastructure::persistence::PasswordExpirationSettingsRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordExpirationSettings operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordExpirationSettingsCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_expiration_settings
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordExpirationSettingsCommand {
    pub max_age_days: i32,
    pub warning_days: i32,
    pub grace_logins: i32,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordExpirationSettingsCommand
pub struct CreatePasswordExpirationSettingsHandler<R: PasswordExpirationSettingsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordExpirationSettingsRepository> CreatePasswordExpirationSettingsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordExpirationSettingsRepository + 'static> CommandHandler<CreatePasswordExpirationSettingsCommand> for CreatePasswordExpirationSettingsHandler<R> {
    type Output = PasswordExpirationSettings;

    async fn handle(&self, cmd: CreatePasswordExpirationSettingsCommand) -> Result<Self::Output> {
        let entity = PasswordExpirationSettings::builder()
            .id(Uuid::new_v4().to_string())
            .max_age_days(cmd.max_age_days)
            .warning_days(cmd.warning_days)
            .grace_logins(cmd.grace_logins)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_expiration_settings
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordExpirationSettingsCommand {
    pub id: String,
    pub max_age_days: Option<i32>,
    pub warning_days: Option<i32>,
    pub grace_logins: Option<i32>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordExpirationSettingsCommand
pub struct UpdatePasswordExpirationSettingsHandler<R: PasswordExpirationSettingsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordExpirationSettingsRepository> UpdatePasswordExpirationSettingsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordExpirationSettingsRepository + 'static> CommandHandler<UpdatePasswordExpirationSettingsCommand> for UpdatePasswordExpirationSettingsHandler<R> {
    type Output = Option<PasswordExpirationSettings>;

    async fn handle(&self, cmd: UpdatePasswordExpirationSettingsCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.max_age_days {
            entity.max_age_days = value;
        }
        if let Some(value) = cmd.warning_days {
            entity.warning_days = value;
        }
        if let Some(value) = cmd.grace_logins {
            entity.grace_logins = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_expiration_settings
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordExpirationSettingsCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordExpirationSettingsCommand
pub struct DeletePasswordExpirationSettingsHandler<R: PasswordExpirationSettingsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordExpirationSettingsRepository> DeletePasswordExpirationSettingsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordExpirationSettingsRepository + 'static> CommandHandler<DeletePasswordExpirationSettingsCommand> for DeletePasswordExpirationSettingsHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordExpirationSettingsCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordExpirationSettingsCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>