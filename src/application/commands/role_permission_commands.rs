//! Commands for RolePermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::RolePermission;
use crate::infrastructure::persistence::RolePermissionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for RolePermission operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait RolePermissionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new role_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateRolePermissionCommand {
    pub role_id: Uuid,
    pub permission_id: Uuid,
    pub granted_at: DateTime<Utc>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateRolePermissionCommand
pub struct CreateRolePermissionHandler<R: RolePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RolePermissionRepository> CreateRolePermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RolePermissionRepository + 'static> CommandHandler<CreateRolePermissionCommand> for CreateRolePermissionHandler<R> {
    type Output = RolePermission;

    async fn handle(&self, cmd: CreateRolePermissionCommand) -> Result<Self::Output> {
        let entity = RolePermission::builder()
            .id(Uuid::new_v4().to_string())
            .role_id(cmd.role_id)
            .permission_id(cmd.permission_id)
            .granted_at(cmd.granted_at)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing role_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateRolePermissionCommand {
    pub id: String,
    pub role_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
    pub granted_at: Option<DateTime<Utc>>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateRolePermissionCommand
pub struct UpdateRolePermissionHandler<R: RolePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RolePermissionRepository> UpdateRolePermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RolePermissionRepository + 'static> CommandHandler<UpdateRolePermissionCommand> for UpdateRolePermissionHandler<R> {
    type Output = Option<RolePermission>;

    async fn handle(&self, cmd: UpdateRolePermissionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.role_id {
            entity.role_id = value;
        }
        if let Some(value) = cmd.permission_id {
            entity.permission_id = value;
        }
        if let Some(value) = cmd.granted_at {
            entity.granted_at = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a role_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteRolePermissionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteRolePermissionCommand
pub struct DeleteRolePermissionHandler<R: RolePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RolePermissionRepository> DeleteRolePermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RolePermissionRepository + 'static> CommandHandler<DeleteRolePermissionCommand> for DeleteRolePermissionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteRolePermissionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateRolePermissionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>