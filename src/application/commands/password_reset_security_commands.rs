//! Commands for PasswordResetSecurity aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordResetSecurity;
use crate::infrastructure::persistence::PasswordResetSecurityRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordResetSecurity operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordResetSecurityCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_reset_security
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordResetSecurityCommand {
    pub max_attempts: i32,
    pub attempts_remaining: i32,
    pub last_attempt_at: Option<DateTime<Utc>>,
    pub expires_at: DateTime<Utc>,
    pub suspicious_activity: bool,
    pub risk_score: i32,
    pub security_flags: serde_json::Value,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordResetSecurityCommand
pub struct CreatePasswordResetSecurityHandler<R: PasswordResetSecurityRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetSecurityRepository> CreatePasswordResetSecurityHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetSecurityRepository + 'static> CommandHandler<CreatePasswordResetSecurityCommand> for CreatePasswordResetSecurityHandler<R> {
    type Output = PasswordResetSecurity;

    async fn handle(&self, cmd: CreatePasswordResetSecurityCommand) -> Result<Self::Output> {
        let entity = PasswordResetSecurity::builder()
            .id(Uuid::new_v4().to_string())
            .max_attempts(cmd.max_attempts)
            .attempts_remaining(cmd.attempts_remaining)
            .last_attempt_at(cmd.last_attempt_at)
            .expires_at(cmd.expires_at)
            .suspicious_activity(cmd.suspicious_activity)
            .risk_score(cmd.risk_score)
            .security_flags(cmd.security_flags)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_reset_security
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordResetSecurityCommand {
    pub id: String,
    pub max_attempts: Option<i32>,
    pub attempts_remaining: Option<i32>,
    pub last_attempt_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub suspicious_activity: Option<bool>,
    pub risk_score: Option<i32>,
    pub security_flags: Option<serde_json::Value>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordResetSecurityCommand
pub struct UpdatePasswordResetSecurityHandler<R: PasswordResetSecurityRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetSecurityRepository> UpdatePasswordResetSecurityHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetSecurityRepository + 'static> CommandHandler<UpdatePasswordResetSecurityCommand> for UpdatePasswordResetSecurityHandler<R> {
    type Output = Option<PasswordResetSecurity>;

    async fn handle(&self, cmd: UpdatePasswordResetSecurityCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.max_attempts {
            entity.max_attempts = value;
        }
        if let Some(value) = cmd.attempts_remaining {
            entity.attempts_remaining = value;
        }
        entity.last_attempt_at = cmd.last_attempt_at;
        if let Some(value) = cmd.expires_at {
            entity.expires_at = value;
        }
        if let Some(value) = cmd.suspicious_activity {
            entity.suspicious_activity = value;
        }
        if let Some(value) = cmd.risk_score {
            entity.risk_score = value;
        }
        if let Some(value) = cmd.security_flags {
            entity.security_flags = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_reset_security
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordResetSecurityCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordResetSecurityCommand
pub struct DeletePasswordResetSecurityHandler<R: PasswordResetSecurityRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetSecurityRepository> DeletePasswordResetSecurityHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetSecurityRepository + 'static> CommandHandler<DeletePasswordResetSecurityCommand> for DeletePasswordResetSecurityHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordResetSecurityCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordResetSecurityCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>