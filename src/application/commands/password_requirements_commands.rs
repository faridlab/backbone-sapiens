//! Commands for PasswordRequirements aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordRequirements;
use crate::infrastructure::persistence::PasswordRequirementsRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordRequirements operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordRequirementsCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_requirements
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordRequirementsCommand {
    pub min_length: i32,
    pub max_length: i32,
    pub require_uppercase: bool,
    pub require_lowercase: bool,
    pub require_digits: bool,
    pub require_special_characters: bool,
    pub require_no_common_passwords: bool,
    pub require_no_user_info: bool,
    pub min_entropy_bits: i32,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordRequirementsCommand
pub struct CreatePasswordRequirementsHandler<R: PasswordRequirementsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordRequirementsRepository> CreatePasswordRequirementsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordRequirementsRepository + 'static> CommandHandler<CreatePasswordRequirementsCommand> for CreatePasswordRequirementsHandler<R> {
    type Output = PasswordRequirements;

    async fn handle(&self, cmd: CreatePasswordRequirementsCommand) -> Result<Self::Output> {
        let entity = PasswordRequirements::builder()
            .id(Uuid::new_v4().to_string())
            .min_length(cmd.min_length)
            .max_length(cmd.max_length)
            .require_uppercase(cmd.require_uppercase)
            .require_lowercase(cmd.require_lowercase)
            .require_digits(cmd.require_digits)
            .require_special_characters(cmd.require_special_characters)
            .require_no_common_passwords(cmd.require_no_common_passwords)
            .require_no_user_info(cmd.require_no_user_info)
            .min_entropy_bits(cmd.min_entropy_bits)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_requirements
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordRequirementsCommand {
    pub id: String,
    pub min_length: Option<i32>,
    pub max_length: Option<i32>,
    pub require_uppercase: Option<bool>,
    pub require_lowercase: Option<bool>,
    pub require_digits: Option<bool>,
    pub require_special_characters: Option<bool>,
    pub require_no_common_passwords: Option<bool>,
    pub require_no_user_info: Option<bool>,
    pub min_entropy_bits: Option<i32>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordRequirementsCommand
pub struct UpdatePasswordRequirementsHandler<R: PasswordRequirementsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordRequirementsRepository> UpdatePasswordRequirementsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordRequirementsRepository + 'static> CommandHandler<UpdatePasswordRequirementsCommand> for UpdatePasswordRequirementsHandler<R> {
    type Output = Option<PasswordRequirements>;

    async fn handle(&self, cmd: UpdatePasswordRequirementsCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.min_length {
            entity.min_length = value;
        }
        if let Some(value) = cmd.max_length {
            entity.max_length = value;
        }
        if let Some(value) = cmd.require_uppercase {
            entity.require_uppercase = value;
        }
        if let Some(value) = cmd.require_lowercase {
            entity.require_lowercase = value;
        }
        if let Some(value) = cmd.require_digits {
            entity.require_digits = value;
        }
        if let Some(value) = cmd.require_special_characters {
            entity.require_special_characters = value;
        }
        if let Some(value) = cmd.require_no_common_passwords {
            entity.require_no_common_passwords = value;
        }
        if let Some(value) = cmd.require_no_user_info {
            entity.require_no_user_info = value;
        }
        if let Some(value) = cmd.min_entropy_bits {
            entity.min_entropy_bits = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_requirements
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordRequirementsCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordRequirementsCommand
pub struct DeletePasswordRequirementsHandler<R: PasswordRequirementsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordRequirementsRepository> DeletePasswordRequirementsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordRequirementsRepository + 'static> CommandHandler<DeletePasswordRequirementsCommand> for DeletePasswordRequirementsHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordRequirementsCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordRequirementsCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>