//! Commands for TemporaryPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::TemporaryPermission;
use crate::infrastructure::persistence::TemporaryPermissionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for TemporaryPermission operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait TemporaryPermissionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new temporary_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateTemporaryPermissionCommand {
    pub user_id: Uuid,
    pub permission_id: Uuid,
    pub granted_by: Uuid,
    pub granted_at: DateTime<Utc>,
    pub expires_at: DateTime<Utc>,
    pub revoked_at: Option<DateTime<Utc>>,
    pub revoked_by: Option<Uuid>,
    pub reason: Option<String>,
    pub notified_before_expiry: bool,
    pub status: TemporaryPermissionStatus,
    pub metadata: serde_json::Value,
}

/// Handler for CreateTemporaryPermissionCommand
pub struct CreateTemporaryPermissionHandler<R: TemporaryPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: TemporaryPermissionRepository> CreateTemporaryPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: TemporaryPermissionRepository + 'static> CommandHandler<CreateTemporaryPermissionCommand> for CreateTemporaryPermissionHandler<R> {
    type Output = TemporaryPermission;

    async fn handle(&self, cmd: CreateTemporaryPermissionCommand) -> Result<Self::Output> {
        let entity = TemporaryPermission::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .permission_id(cmd.permission_id)
            .granted_by(cmd.granted_by)
            .granted_at(cmd.granted_at)
            .expires_at(cmd.expires_at)
            .revoked_at(cmd.revoked_at)
            .revoked_by(cmd.revoked_by)
            .reason(cmd.reason)
            .notified_before_expiry(cmd.notified_before_expiry)
            .status(cmd.status)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing temporary_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateTemporaryPermissionCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
    pub granted_by: Option<Uuid>,
    pub granted_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub revoked_at: Option<DateTime<Utc>>,
    pub revoked_by: Option<Uuid>,
    pub reason: Option<String>,
    pub notified_before_expiry: Option<bool>,
    pub status: Option<TemporaryPermissionStatus>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateTemporaryPermissionCommand
pub struct UpdateTemporaryPermissionHandler<R: TemporaryPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: TemporaryPermissionRepository> UpdateTemporaryPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: TemporaryPermissionRepository + 'static> CommandHandler<UpdateTemporaryPermissionCommand> for UpdateTemporaryPermissionHandler<R> {
    type Output = Option<TemporaryPermission>;

    async fn handle(&self, cmd: UpdateTemporaryPermissionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.permission_id {
            entity.permission_id = value;
        }
        if let Some(value) = cmd.granted_by {
            entity.granted_by = value;
        }
        if let Some(value) = cmd.granted_at {
            entity.granted_at = value;
        }
        if let Some(value) = cmd.expires_at {
            entity.expires_at = value;
        }
        entity.revoked_at = cmd.revoked_at;
        entity.revoked_by = cmd.revoked_by;
        entity.reason = cmd.reason;
        if let Some(value) = cmd.notified_before_expiry {
            entity.notified_before_expiry = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a temporary_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteTemporaryPermissionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteTemporaryPermissionCommand
pub struct DeleteTemporaryPermissionHandler<R: TemporaryPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: TemporaryPermissionRepository> DeleteTemporaryPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: TemporaryPermissionRepository + 'static> CommandHandler<DeleteTemporaryPermissionCommand> for DeleteTemporaryPermissionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteTemporaryPermissionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateTemporaryPermissionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>