//! Commands for RoleAssignment aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::RoleAssignment;
use crate::infrastructure::persistence::RoleAssignmentRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for RoleAssignment operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait RoleAssignmentCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new role_assignment
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateRoleAssignmentCommand {
    pub user_id: Uuid,
    pub role_id: Uuid,
    pub assigned_by: Uuid,
    pub assigned_at: DateTime<Utc>,
    pub expires_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub priority: i32,
    pub scope: Option<serde_json::Value>,
    pub conditions: Option<serde_json::Value>,
    pub parent_assignment_id: Option<Uuid>,
    pub inheritance_level: i32,
    pub inherited_from: Option<Uuid>,
    pub conflicts_with: Vec<Uuid>,
    pub conflict_resolution: ConflictResolutionType,
    pub has_conflicts: bool,
    pub conflict_types: Vec<String>,
    pub detected_conflicts: Option<serde_json::Value>,
    pub last_used_at: Option<DateTime<Utc>>,
    pub usage_count: i32,
    pub modification_history: Option<serde_json::Value>,
    pub status: RoleAssignmentStatus,
    pub warning_sent: bool,
    pub auto_renew: bool,
    pub renewal_period_days: i32,
    pub metadata: serde_json::Value,
}

/// Handler for CreateRoleAssignmentCommand
pub struct CreateRoleAssignmentHandler<R: RoleAssignmentRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RoleAssignmentRepository> CreateRoleAssignmentHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RoleAssignmentRepository + 'static> CommandHandler<CreateRoleAssignmentCommand> for CreateRoleAssignmentHandler<R> {
    type Output = RoleAssignment;

    async fn handle(&self, cmd: CreateRoleAssignmentCommand) -> Result<Self::Output> {
        let entity = RoleAssignment::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .role_id(cmd.role_id)
            .assigned_by(cmd.assigned_by)
            .assigned_at(cmd.assigned_at)
            .expires_at(cmd.expires_at)
            .reason(cmd.reason)
            .priority(cmd.priority)
            .scope(cmd.scope)
            .conditions(cmd.conditions)
            .parent_assignment_id(cmd.parent_assignment_id)
            .inheritance_level(cmd.inheritance_level)
            .inherited_from(cmd.inherited_from)
            .conflicts_with(cmd.conflicts_with)
            .conflict_resolution(cmd.conflict_resolution)
            .has_conflicts(cmd.has_conflicts)
            .conflict_types(cmd.conflict_types)
            .detected_conflicts(cmd.detected_conflicts)
            .last_used_at(cmd.last_used_at)
            .usage_count(cmd.usage_count)
            .modification_history(cmd.modification_history)
            .status(cmd.status)
            .warning_sent(cmd.warning_sent)
            .auto_renew(cmd.auto_renew)
            .renewal_period_days(cmd.renewal_period_days)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing role_assignment
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateRoleAssignmentCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub role_id: Option<Uuid>,
    pub assigned_by: Option<Uuid>,
    pub assigned_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub priority: Option<i32>,
    pub scope: Option<serde_json::Value>,
    pub conditions: Option<serde_json::Value>,
    pub parent_assignment_id: Option<Uuid>,
    pub inheritance_level: Option<i32>,
    pub inherited_from: Option<Uuid>,
    pub conflicts_with: Option<Vec<Uuid>>,
    pub conflict_resolution: Option<ConflictResolutionType>,
    pub has_conflicts: Option<bool>,
    pub conflict_types: Option<Vec<String>>,
    pub detected_conflicts: Option<serde_json::Value>,
    pub last_used_at: Option<DateTime<Utc>>,
    pub usage_count: Option<i32>,
    pub modification_history: Option<serde_json::Value>,
    pub status: Option<RoleAssignmentStatus>,
    pub warning_sent: Option<bool>,
    pub auto_renew: Option<bool>,
    pub renewal_period_days: Option<i32>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateRoleAssignmentCommand
pub struct UpdateRoleAssignmentHandler<R: RoleAssignmentRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RoleAssignmentRepository> UpdateRoleAssignmentHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RoleAssignmentRepository + 'static> CommandHandler<UpdateRoleAssignmentCommand> for UpdateRoleAssignmentHandler<R> {
    type Output = Option<RoleAssignment>;

    async fn handle(&self, cmd: UpdateRoleAssignmentCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.role_id {
            entity.role_id = value;
        }
        if let Some(value) = cmd.assigned_by {
            entity.assigned_by = value;
        }
        if let Some(value) = cmd.assigned_at {
            entity.assigned_at = value;
        }
        entity.expires_at = cmd.expires_at;
        entity.reason = cmd.reason;
        if let Some(value) = cmd.priority {
            entity.priority = value;
        }
        entity.scope = cmd.scope;
        entity.conditions = cmd.conditions;
        entity.parent_assignment_id = cmd.parent_assignment_id;
        if let Some(value) = cmd.inheritance_level {
            entity.inheritance_level = value;
        }
        entity.inherited_from = cmd.inherited_from;
        if let Some(value) = cmd.conflicts_with {
            entity.conflicts_with = value;
        }
        if let Some(value) = cmd.conflict_resolution {
            entity.conflict_resolution = value;
        }
        if let Some(value) = cmd.has_conflicts {
            entity.has_conflicts = value;
        }
        if let Some(value) = cmd.conflict_types {
            entity.conflict_types = value;
        }
        entity.detected_conflicts = cmd.detected_conflicts;
        entity.last_used_at = cmd.last_used_at;
        if let Some(value) = cmd.usage_count {
            entity.usage_count = value;
        }
        entity.modification_history = cmd.modification_history;
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.warning_sent {
            entity.warning_sent = value;
        }
        if let Some(value) = cmd.auto_renew {
            entity.auto_renew = value;
        }
        if let Some(value) = cmd.renewal_period_days {
            entity.renewal_period_days = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a role_assignment
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteRoleAssignmentCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteRoleAssignmentCommand
pub struct DeleteRoleAssignmentHandler<R: RoleAssignmentRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RoleAssignmentRepository> DeleteRoleAssignmentHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RoleAssignmentRepository + 'static> CommandHandler<DeleteRoleAssignmentCommand> for DeleteRoleAssignmentHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteRoleAssignmentCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateRoleAssignmentCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>