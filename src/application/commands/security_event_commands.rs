//! Commands for SecurityEvent aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::SecurityEvent;
use crate::infrastructure::persistence::SecurityEventRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for SecurityEvent operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait SecurityEventCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new security_event
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateSecurityEventCommand {
    pub user_id: Option<Uuid>,
    pub session_id: Option<Uuid>,
    pub event_type: SecurityEventType,
    pub severity: SecurityEventSeverity,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub description: Option<String>,
    pub details: Option<serde_json::Value>,
    pub resolved: bool,
    pub resolved_at: Option<DateTime<Utc>>,
    pub resolved_by_user_id: Option<Uuid>,
    pub resolution_notes: Option<String>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateSecurityEventCommand
pub struct CreateSecurityEventHandler<R: SecurityEventRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SecurityEventRepository> CreateSecurityEventHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SecurityEventRepository + 'static> CommandHandler<CreateSecurityEventCommand> for CreateSecurityEventHandler<R> {
    type Output = SecurityEvent;

    async fn handle(&self, cmd: CreateSecurityEventCommand) -> Result<Self::Output> {
        let entity = SecurityEvent::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .session_id(cmd.session_id)
            .event_type(cmd.event_type)
            .severity(cmd.severity)
            .ip_address(cmd.ip_address)
            .user_agent(cmd.user_agent)
            .description(cmd.description)
            .details(cmd.details)
            .resolved(cmd.resolved)
            .resolved_at(cmd.resolved_at)
            .resolved_by_user_id(cmd.resolved_by_user_id)
            .resolution_notes(cmd.resolution_notes)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing security_event
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateSecurityEventCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub session_id: Option<Uuid>,
    pub event_type: Option<SecurityEventType>,
    pub severity: Option<SecurityEventSeverity>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub description: Option<String>,
    pub details: Option<serde_json::Value>,
    pub resolved: Option<bool>,
    pub resolved_at: Option<DateTime<Utc>>,
    pub resolved_by_user_id: Option<Uuid>,
    pub resolution_notes: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateSecurityEventCommand
pub struct UpdateSecurityEventHandler<R: SecurityEventRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SecurityEventRepository> UpdateSecurityEventHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SecurityEventRepository + 'static> CommandHandler<UpdateSecurityEventCommand> for UpdateSecurityEventHandler<R> {
    type Output = Option<SecurityEvent>;

    async fn handle(&self, cmd: UpdateSecurityEventCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        entity.user_id = cmd.user_id;
        entity.session_id = cmd.session_id;
        if let Some(value) = cmd.event_type {
            entity.event_type = value;
        }
        if let Some(value) = cmd.severity {
            entity.severity = value;
        }
        entity.ip_address = cmd.ip_address;
        entity.user_agent = cmd.user_agent;
        entity.description = cmd.description;
        entity.details = cmd.details;
        if let Some(value) = cmd.resolved {
            entity.resolved = value;
        }
        entity.resolved_at = cmd.resolved_at;
        entity.resolved_by_user_id = cmd.resolved_by_user_id;
        entity.resolution_notes = cmd.resolution_notes;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a security_event
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteSecurityEventCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteSecurityEventCommand
pub struct DeleteSecurityEventHandler<R: SecurityEventRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SecurityEventRepository> DeleteSecurityEventHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SecurityEventRepository + 'static> CommandHandler<DeleteSecurityEventCommand> for DeleteSecurityEventHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteSecurityEventCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateSecurityEventCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>