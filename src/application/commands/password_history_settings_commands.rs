//! Commands for PasswordHistorySettings aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordHistorySettings;
use crate::infrastructure::persistence::PasswordHistorySettingsRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordHistorySettings operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordHistorySettingsCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_history_settings
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordHistorySettingsCommand {
    pub track_history: bool,
    pub history_count: i32,
    pub prevent_reuse: bool,
    pub prevent_similar: bool,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordHistorySettingsCommand
pub struct CreatePasswordHistorySettingsHandler<R: PasswordHistorySettingsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordHistorySettingsRepository> CreatePasswordHistorySettingsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordHistorySettingsRepository + 'static> CommandHandler<CreatePasswordHistorySettingsCommand> for CreatePasswordHistorySettingsHandler<R> {
    type Output = PasswordHistorySettings;

    async fn handle(&self, cmd: CreatePasswordHistorySettingsCommand) -> Result<Self::Output> {
        let entity = PasswordHistorySettings::builder()
            .id(Uuid::new_v4().to_string())
            .track_history(cmd.track_history)
            .history_count(cmd.history_count)
            .prevent_reuse(cmd.prevent_reuse)
            .prevent_similar(cmd.prevent_similar)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_history_settings
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordHistorySettingsCommand {
    pub id: String,
    pub track_history: Option<bool>,
    pub history_count: Option<i32>,
    pub prevent_reuse: Option<bool>,
    pub prevent_similar: Option<bool>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordHistorySettingsCommand
pub struct UpdatePasswordHistorySettingsHandler<R: PasswordHistorySettingsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordHistorySettingsRepository> UpdatePasswordHistorySettingsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordHistorySettingsRepository + 'static> CommandHandler<UpdatePasswordHistorySettingsCommand> for UpdatePasswordHistorySettingsHandler<R> {
    type Output = Option<PasswordHistorySettings>;

    async fn handle(&self, cmd: UpdatePasswordHistorySettingsCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.track_history {
            entity.track_history = value;
        }
        if let Some(value) = cmd.history_count {
            entity.history_count = value;
        }
        if let Some(value) = cmd.prevent_reuse {
            entity.prevent_reuse = value;
        }
        if let Some(value) = cmd.prevent_similar {
            entity.prevent_similar = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_history_settings
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordHistorySettingsCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordHistorySettingsCommand
pub struct DeletePasswordHistorySettingsHandler<R: PasswordHistorySettingsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordHistorySettingsRepository> DeletePasswordHistorySettingsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordHistorySettingsRepository + 'static> CommandHandler<DeletePasswordHistorySettingsCommand> for DeletePasswordHistorySettingsHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordHistorySettingsCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordHistorySettingsCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>