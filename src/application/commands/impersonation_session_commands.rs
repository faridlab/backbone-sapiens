//! Commands for ImpersonationSession aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::ImpersonationSession;
use crate::infrastructure::persistence::ImpersonationSessionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for ImpersonationSession operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait ImpersonationSessionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new impersonation_session
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateImpersonationSessionCommand {
    pub admin_id: Uuid,
    pub target_user_id: Uuid,
    pub session_id: Uuid,
    pub started_at: DateTime<Utc>,
    pub ended_at: Option<DateTime<Utc>>,
    pub max_duration_minutes: i32,
    pub reason: String,
    pub actions_performed: i32,
    pub terminated_by: Option<Uuid>,
    pub termination_reason: Option<String>,
    pub status: ImpersonationSessionStatus,
    pub metadata: serde_json::Value,
}

/// Handler for CreateImpersonationSessionCommand
pub struct CreateImpersonationSessionHandler<R: ImpersonationSessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ImpersonationSessionRepository> CreateImpersonationSessionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ImpersonationSessionRepository + 'static> CommandHandler<CreateImpersonationSessionCommand> for CreateImpersonationSessionHandler<R> {
    type Output = ImpersonationSession;

    async fn handle(&self, cmd: CreateImpersonationSessionCommand) -> Result<Self::Output> {
        let entity = ImpersonationSession::builder()
            .id(Uuid::new_v4().to_string())
            .admin_id(cmd.admin_id)
            .target_user_id(cmd.target_user_id)
            .session_id(cmd.session_id)
            .started_at(cmd.started_at)
            .ended_at(cmd.ended_at)
            .max_duration_minutes(cmd.max_duration_minutes)
            .reason(cmd.reason)
            .actions_performed(cmd.actions_performed)
            .terminated_by(cmd.terminated_by)
            .termination_reason(cmd.termination_reason)
            .status(cmd.status)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing impersonation_session
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateImpersonationSessionCommand {
    pub id: String,
    pub admin_id: Option<Uuid>,
    pub target_user_id: Option<Uuid>,
    pub session_id: Option<Uuid>,
    pub started_at: Option<DateTime<Utc>>,
    pub ended_at: Option<DateTime<Utc>>,
    pub max_duration_minutes: Option<i32>,
    pub reason: Option<String>,
    pub actions_performed: Option<i32>,
    pub terminated_by: Option<Uuid>,
    pub termination_reason: Option<String>,
    pub status: Option<ImpersonationSessionStatus>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateImpersonationSessionCommand
pub struct UpdateImpersonationSessionHandler<R: ImpersonationSessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ImpersonationSessionRepository> UpdateImpersonationSessionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ImpersonationSessionRepository + 'static> CommandHandler<UpdateImpersonationSessionCommand> for UpdateImpersonationSessionHandler<R> {
    type Output = Option<ImpersonationSession>;

    async fn handle(&self, cmd: UpdateImpersonationSessionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.admin_id {
            entity.admin_id = value;
        }
        if let Some(value) = cmd.target_user_id {
            entity.target_user_id = value;
        }
        if let Some(value) = cmd.session_id {
            entity.session_id = value;
        }
        if let Some(value) = cmd.started_at {
            entity.started_at = value;
        }
        entity.ended_at = cmd.ended_at;
        if let Some(value) = cmd.max_duration_minutes {
            entity.max_duration_minutes = value;
        }
        if let Some(value) = cmd.reason {
            entity.reason = value;
        }
        if let Some(value) = cmd.actions_performed {
            entity.actions_performed = value;
        }
        entity.terminated_by = cmd.terminated_by;
        entity.termination_reason = cmd.termination_reason;
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a impersonation_session
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteImpersonationSessionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteImpersonationSessionCommand
pub struct DeleteImpersonationSessionHandler<R: ImpersonationSessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ImpersonationSessionRepository> DeleteImpersonationSessionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ImpersonationSessionRepository + 'static> CommandHandler<DeleteImpersonationSessionCommand> for DeleteImpersonationSessionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteImpersonationSessionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateImpersonationSessionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>