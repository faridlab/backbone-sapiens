//! Commands for DeviceTrust aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::DeviceTrust;
use crate::infrastructure::persistence::DeviceTrustRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for DeviceTrust operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait DeviceTrustCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new device_trust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateDeviceTrustCommand {
    pub user_id: Uuid,
    pub device_fingerprint: String,
    pub device_name: String,
    pub device_type: DeviceTrustType,
    pub trust_level: TrustLevel,
    pub is_trusted: bool,
    pub trusted_at: DateTime<Utc>,
    pub expires_at: Option<DateTime<Utc>>,
    pub last_used_at: DateTime<Utc>,
    pub usage_count: i32,
    pub ip_address: Option<String>,
    pub location: Option<serde_json::Value>,
    pub risk_score: f64,
    pub requires_mfa: bool,
    pub metadata: serde_json::Value,
}

/// Handler for CreateDeviceTrustCommand
pub struct CreateDeviceTrustHandler<R: DeviceTrustRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DeviceTrustRepository> CreateDeviceTrustHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DeviceTrustRepository + 'static> CommandHandler<CreateDeviceTrustCommand> for CreateDeviceTrustHandler<R> {
    type Output = DeviceTrust;

    async fn handle(&self, cmd: CreateDeviceTrustCommand) -> Result<Self::Output> {
        let entity = DeviceTrust::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .device_fingerprint(cmd.device_fingerprint)
            .device_name(cmd.device_name)
            .device_type(cmd.device_type)
            .trust_level(cmd.trust_level)
            .is_trusted(cmd.is_trusted)
            .trusted_at(cmd.trusted_at)
            .expires_at(cmd.expires_at)
            .last_used_at(cmd.last_used_at)
            .usage_count(cmd.usage_count)
            .ip_address(cmd.ip_address)
            .location(cmd.location)
            .risk_score(cmd.risk_score)
            .requires_mfa(cmd.requires_mfa)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing device_trust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateDeviceTrustCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub device_fingerprint: Option<String>,
    pub device_name: Option<String>,
    pub device_type: Option<DeviceTrustType>,
    pub trust_level: Option<TrustLevel>,
    pub is_trusted: Option<bool>,
    pub trusted_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub last_used_at: Option<DateTime<Utc>>,
    pub usage_count: Option<i32>,
    pub ip_address: Option<String>,
    pub location: Option<serde_json::Value>,
    pub risk_score: Option<f64>,
    pub requires_mfa: Option<bool>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateDeviceTrustCommand
pub struct UpdateDeviceTrustHandler<R: DeviceTrustRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DeviceTrustRepository> UpdateDeviceTrustHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DeviceTrustRepository + 'static> CommandHandler<UpdateDeviceTrustCommand> for UpdateDeviceTrustHandler<R> {
    type Output = Option<DeviceTrust>;

    async fn handle(&self, cmd: UpdateDeviceTrustCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.device_fingerprint {
            entity.device_fingerprint = value;
        }
        if let Some(value) = cmd.device_name {
            entity.device_name = value;
        }
        if let Some(value) = cmd.device_type {
            entity.device_type = value;
        }
        if let Some(value) = cmd.trust_level {
            entity.trust_level = value;
        }
        if let Some(value) = cmd.is_trusted {
            entity.is_trusted = value;
        }
        if let Some(value) = cmd.trusted_at {
            entity.trusted_at = value;
        }
        entity.expires_at = cmd.expires_at;
        if let Some(value) = cmd.last_used_at {
            entity.last_used_at = value;
        }
        if let Some(value) = cmd.usage_count {
            entity.usage_count = value;
        }
        entity.ip_address = cmd.ip_address;
        entity.location = cmd.location;
        if let Some(value) = cmd.risk_score {
            entity.risk_score = value;
        }
        if let Some(value) = cmd.requires_mfa {
            entity.requires_mfa = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a device_trust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteDeviceTrustCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteDeviceTrustCommand
pub struct DeleteDeviceTrustHandler<R: DeviceTrustRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DeviceTrustRepository> DeleteDeviceTrustHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DeviceTrustRepository + 'static> CommandHandler<DeleteDeviceTrustCommand> for DeleteDeviceTrustHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteDeviceTrustCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateDeviceTrustCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>