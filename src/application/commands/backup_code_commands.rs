//! Commands for BackupCode aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::BackupCode;
use crate::infrastructure::persistence::BackupCodeRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for BackupCode operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait BackupCodeCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new backup_code
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateBackupCodeCommand {
    pub user_id: Uuid,
    pub batch_id: String,
    pub code_hash: String,
    pub code_index: i32,
    pub is_used: bool,
    pub used_at: Option<DateTime<Utc>>,
    pub used_ip: Option<String>,
    pub used_user_agent: Option<String>,
    pub expires_at: DateTime<Utc>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateBackupCodeCommand
pub struct CreateBackupCodeHandler<R: BackupCodeRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BackupCodeRepository> CreateBackupCodeHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BackupCodeRepository + 'static> CommandHandler<CreateBackupCodeCommand> for CreateBackupCodeHandler<R> {
    type Output = BackupCode;

    async fn handle(&self, cmd: CreateBackupCodeCommand) -> Result<Self::Output> {
        let entity = BackupCode::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .batch_id(cmd.batch_id)
            .code_hash(cmd.code_hash)
            .code_index(cmd.code_index)
            .is_used(cmd.is_used)
            .used_at(cmd.used_at)
            .used_ip(cmd.used_ip)
            .used_user_agent(cmd.used_user_agent)
            .expires_at(cmd.expires_at)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing backup_code
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateBackupCodeCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub batch_id: Option<String>,
    pub code_hash: Option<String>,
    pub code_index: Option<i32>,
    pub is_used: Option<bool>,
    pub used_at: Option<DateTime<Utc>>,
    pub used_ip: Option<String>,
    pub used_user_agent: Option<String>,
    pub expires_at: Option<DateTime<Utc>>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateBackupCodeCommand
pub struct UpdateBackupCodeHandler<R: BackupCodeRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BackupCodeRepository> UpdateBackupCodeHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BackupCodeRepository + 'static> CommandHandler<UpdateBackupCodeCommand> for UpdateBackupCodeHandler<R> {
    type Output = Option<BackupCode>;

    async fn handle(&self, cmd: UpdateBackupCodeCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.batch_id {
            entity.batch_id = value;
        }
        if let Some(value) = cmd.code_hash {
            entity.code_hash = value;
        }
        if let Some(value) = cmd.code_index {
            entity.code_index = value;
        }
        if let Some(value) = cmd.is_used {
            entity.is_used = value;
        }
        entity.used_at = cmd.used_at;
        entity.used_ip = cmd.used_ip;
        entity.used_user_agent = cmd.used_user_agent;
        if let Some(value) = cmd.expires_at {
            entity.expires_at = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a backup_code
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteBackupCodeCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteBackupCodeCommand
pub struct DeleteBackupCodeHandler<R: BackupCodeRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BackupCodeRepository> DeleteBackupCodeHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BackupCodeRepository + 'static> CommandHandler<DeleteBackupCodeCommand> for DeleteBackupCodeHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteBackupCodeCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateBackupCodeCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>