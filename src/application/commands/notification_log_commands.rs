//! Commands for NotificationLog aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::NotificationLog;
use crate::infrastructure::persistence::NotificationLogRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for NotificationLog operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait NotificationLogCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new notification_log
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateNotificationLogCommand {
    pub notification_id: Option<Uuid>,
    pub template_id: Option<Uuid>,
    pub recipient_type: String,
    pub recipient_id: Option<Uuid>,
    pub channel: NotificationChannel,
    pub status: NotificationLogStatus,
    pub sent_at: Option<DateTime<Utc>>,
    pub delivered_at: Option<DateTime<Utc>>,
    pub failed_at: Option<DateTime<Utc>>,
    pub error_message: Option<String>,
    pub external_id: Option<String>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateNotificationLogCommand
pub struct CreateNotificationLogHandler<R: NotificationLogRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationLogRepository> CreateNotificationLogHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationLogRepository + 'static> CommandHandler<CreateNotificationLogCommand> for CreateNotificationLogHandler<R> {
    type Output = NotificationLog;

    async fn handle(&self, cmd: CreateNotificationLogCommand) -> Result<Self::Output> {
        let entity = NotificationLog::builder()
            .id(Uuid::new_v4().to_string())
            .notification_id(cmd.notification_id)
            .template_id(cmd.template_id)
            .recipient_type(cmd.recipient_type)
            .recipient_id(cmd.recipient_id)
            .channel(cmd.channel)
            .status(cmd.status)
            .sent_at(cmd.sent_at)
            .delivered_at(cmd.delivered_at)
            .failed_at(cmd.failed_at)
            .error_message(cmd.error_message)
            .external_id(cmd.external_id)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing notification_log
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateNotificationLogCommand {
    pub id: String,
    pub notification_id: Option<Uuid>,
    pub template_id: Option<Uuid>,
    pub recipient_type: Option<String>,
    pub recipient_id: Option<Uuid>,
    pub channel: Option<NotificationChannel>,
    pub status: Option<NotificationLogStatus>,
    pub sent_at: Option<DateTime<Utc>>,
    pub delivered_at: Option<DateTime<Utc>>,
    pub failed_at: Option<DateTime<Utc>>,
    pub error_message: Option<String>,
    pub external_id: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateNotificationLogCommand
pub struct UpdateNotificationLogHandler<R: NotificationLogRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationLogRepository> UpdateNotificationLogHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationLogRepository + 'static> CommandHandler<UpdateNotificationLogCommand> for UpdateNotificationLogHandler<R> {
    type Output = Option<NotificationLog>;

    async fn handle(&self, cmd: UpdateNotificationLogCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        entity.notification_id = cmd.notification_id;
        entity.template_id = cmd.template_id;
        if let Some(value) = cmd.recipient_type {
            entity.recipient_type = value;
        }
        entity.recipient_id = cmd.recipient_id;
        if let Some(value) = cmd.channel {
            entity.channel = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        entity.sent_at = cmd.sent_at;
        entity.delivered_at = cmd.delivered_at;
        entity.failed_at = cmd.failed_at;
        entity.error_message = cmd.error_message;
        entity.external_id = cmd.external_id;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a notification_log
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteNotificationLogCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteNotificationLogCommand
pub struct DeleteNotificationLogHandler<R: NotificationLogRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationLogRepository> DeleteNotificationLogHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationLogRepository + 'static> CommandHandler<DeleteNotificationLogCommand> for DeleteNotificationLogHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteNotificationLogCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateNotificationLogCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>