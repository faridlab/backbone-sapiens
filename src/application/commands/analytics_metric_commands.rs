//! Commands for AnalyticsMetric aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::AnalyticsMetric;
use crate::infrastructure::persistence::AnalyticsMetricRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for AnalyticsMetric operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait AnalyticsMetricCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new analytics_metric
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateAnalyticsMetricCommand {
    pub metric_name: String,
    pub metric_type: AnalyticsMetricType,
    pub value: f64,
    pub unit: Option<String>,
    pub dimensions: Option<serde_json::Value>,
    pub period_start: DateTime<Utc>,
    pub period_end: DateTime<Utc>,
    pub aggregation_level: AggregationLevel,
    pub organization_id: Option<Uuid>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateAnalyticsMetricCommand
pub struct CreateAnalyticsMetricHandler<R: AnalyticsMetricRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsMetricRepository> CreateAnalyticsMetricHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsMetricRepository + 'static> CommandHandler<CreateAnalyticsMetricCommand> for CreateAnalyticsMetricHandler<R> {
    type Output = AnalyticsMetric;

    async fn handle(&self, cmd: CreateAnalyticsMetricCommand) -> Result<Self::Output> {
        let entity = AnalyticsMetric::builder()
            .id(Uuid::new_v4().to_string())
            .metric_name(cmd.metric_name)
            .metric_type(cmd.metric_type)
            .value(cmd.value)
            .unit(cmd.unit)
            .dimensions(cmd.dimensions)
            .period_start(cmd.period_start)
            .period_end(cmd.period_end)
            .aggregation_level(cmd.aggregation_level)
            .organization_id(cmd.organization_id)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing analytics_metric
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateAnalyticsMetricCommand {
    pub id: String,
    pub metric_name: Option<String>,
    pub metric_type: Option<AnalyticsMetricType>,
    pub value: Option<f64>,
    pub unit: Option<String>,
    pub dimensions: Option<serde_json::Value>,
    pub period_start: Option<DateTime<Utc>>,
    pub period_end: Option<DateTime<Utc>>,
    pub aggregation_level: Option<AggregationLevel>,
    pub organization_id: Option<Uuid>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateAnalyticsMetricCommand
pub struct UpdateAnalyticsMetricHandler<R: AnalyticsMetricRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsMetricRepository> UpdateAnalyticsMetricHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsMetricRepository + 'static> CommandHandler<UpdateAnalyticsMetricCommand> for UpdateAnalyticsMetricHandler<R> {
    type Output = Option<AnalyticsMetric>;

    async fn handle(&self, cmd: UpdateAnalyticsMetricCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.metric_name {
            entity.metric_name = value;
        }
        if let Some(value) = cmd.metric_type {
            entity.metric_type = value;
        }
        if let Some(value) = cmd.value {
            entity.value = value;
        }
        entity.unit = cmd.unit;
        entity.dimensions = cmd.dimensions;
        if let Some(value) = cmd.period_start {
            entity.period_start = value;
        }
        if let Some(value) = cmd.period_end {
            entity.period_end = value;
        }
        if let Some(value) = cmd.aggregation_level {
            entity.aggregation_level = value;
        }
        entity.organization_id = cmd.organization_id;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a analytics_metric
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteAnalyticsMetricCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteAnalyticsMetricCommand
pub struct DeleteAnalyticsMetricHandler<R: AnalyticsMetricRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsMetricRepository> DeleteAnalyticsMetricHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsMetricRepository + 'static> CommandHandler<DeleteAnalyticsMetricCommand> for DeleteAnalyticsMetricHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteAnalyticsMetricCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateAnalyticsMetricCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>