//! Commands for PasswordReset aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordReset;
use crate::infrastructure::persistence::PasswordResetRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordReset operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordResetCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_reset
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordResetCommand {
    pub user_id: Uuid,
    pub token: String,
    pub token_hash: String,
    pub email: String,
    pub status: PasswordResetStatus,
    pub max_attempts: i32,
    pub attempts_remaining: i32,
    pub last_attempt_at: Option<DateTime<Utc>>,
    pub request_metadata: serde_json::Value,
    pub verification_details_id: Option<Uuid>,
    pub completion_details_id: Option<Uuid>,
    pub security_id: Option<Uuid>,
    pub device_fingerprint: Option<String>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub verified_at: Option<DateTime<Utc>>,
    pub used_at: Option<DateTime<Utc>>,
    pub expires_at: DateTime<Utc>,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordResetCommand
pub struct CreatePasswordResetHandler<R: PasswordResetRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetRepository> CreatePasswordResetHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetRepository + 'static> CommandHandler<CreatePasswordResetCommand> for CreatePasswordResetHandler<R> {
    type Output = PasswordReset;

    async fn handle(&self, cmd: CreatePasswordResetCommand) -> Result<Self::Output> {
        let entity = PasswordReset::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .token(cmd.token)
            .token_hash(cmd.token_hash)
            .email(cmd.email)
            .status(cmd.status)
            .max_attempts(cmd.max_attempts)
            .attempts_remaining(cmd.attempts_remaining)
            .last_attempt_at(cmd.last_attempt_at)
            .request_metadata(cmd.request_metadata)
            .verification_details_id(cmd.verification_details_id)
            .completion_details_id(cmd.completion_details_id)
            .security_id(cmd.security_id)
            .device_fingerprint(cmd.device_fingerprint)
            .ip_address(cmd.ip_address)
            .user_agent(cmd.user_agent)
            .verified_at(cmd.verified_at)
            .used_at(cmd.used_at)
            .expires_at(cmd.expires_at)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_reset
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordResetCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub token: Option<String>,
    pub token_hash: Option<String>,
    pub email: Option<String>,
    pub status: Option<PasswordResetStatus>,
    pub max_attempts: Option<i32>,
    pub attempts_remaining: Option<i32>,
    pub last_attempt_at: Option<DateTime<Utc>>,
    pub request_metadata: Option<serde_json::Value>,
    pub verification_details_id: Option<Uuid>,
    pub completion_details_id: Option<Uuid>,
    pub security_id: Option<Uuid>,
    pub device_fingerprint: Option<String>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub verified_at: Option<DateTime<Utc>>,
    pub used_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordResetCommand
pub struct UpdatePasswordResetHandler<R: PasswordResetRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetRepository> UpdatePasswordResetHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetRepository + 'static> CommandHandler<UpdatePasswordResetCommand> for UpdatePasswordResetHandler<R> {
    type Output = Option<PasswordReset>;

    async fn handle(&self, cmd: UpdatePasswordResetCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.token {
            entity.token = value;
        }
        if let Some(value) = cmd.token_hash {
            entity.token_hash = value;
        }
        if let Some(value) = cmd.email {
            entity.email = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.max_attempts {
            entity.max_attempts = value;
        }
        if let Some(value) = cmd.attempts_remaining {
            entity.attempts_remaining = value;
        }
        entity.last_attempt_at = cmd.last_attempt_at;
        if let Some(value) = cmd.request_metadata {
            entity.request_metadata = value;
        }
        entity.verification_details_id = cmd.verification_details_id;
        entity.completion_details_id = cmd.completion_details_id;
        entity.security_id = cmd.security_id;
        entity.device_fingerprint = cmd.device_fingerprint;
        entity.ip_address = cmd.ip_address;
        entity.user_agent = cmd.user_agent;
        entity.verified_at = cmd.verified_at;
        entity.used_at = cmd.used_at;
        if let Some(value) = cmd.expires_at {
            entity.expires_at = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_reset
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordResetCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordResetCommand
pub struct DeletePasswordResetHandler<R: PasswordResetRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetRepository> DeletePasswordResetHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetRepository + 'static> CommandHandler<DeletePasswordResetCommand> for DeletePasswordResetHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordResetCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordResetCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>