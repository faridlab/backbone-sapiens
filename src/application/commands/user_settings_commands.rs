//! Commands for UserSettings aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::UserSettings;
use crate::infrastructure::persistence::UserSettingsRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for UserSettings operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait UserSettingsCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new user_settings
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateUserSettingsCommand {
    pub user_id: Uuid,
    pub theme: Theme,
    pub language: String,
    pub timezone: String,
    pub notifications_enabled: bool,
    pub email_notifications: bool,
    pub sms_notifications: bool,
    pub mfa_enabled: bool,
    pub custom_settings: Option<serde_json::Value>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateUserSettingsCommand
pub struct CreateUserSettingsHandler<R: UserSettingsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserSettingsRepository> CreateUserSettingsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserSettingsRepository + 'static> CommandHandler<CreateUserSettingsCommand> for CreateUserSettingsHandler<R> {
    type Output = UserSettings;

    async fn handle(&self, cmd: CreateUserSettingsCommand) -> Result<Self::Output> {
        let entity = UserSettings::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .theme(cmd.theme)
            .language(cmd.language)
            .timezone(cmd.timezone)
            .notifications_enabled(cmd.notifications_enabled)
            .email_notifications(cmd.email_notifications)
            .sms_notifications(cmd.sms_notifications)
            .mfa_enabled(cmd.mfa_enabled)
            .custom_settings(cmd.custom_settings)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing user_settings
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateUserSettingsCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub theme: Option<Theme>,
    pub language: Option<String>,
    pub timezone: Option<String>,
    pub notifications_enabled: Option<bool>,
    pub email_notifications: Option<bool>,
    pub sms_notifications: Option<bool>,
    pub mfa_enabled: Option<bool>,
    pub custom_settings: Option<serde_json::Value>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateUserSettingsCommand
pub struct UpdateUserSettingsHandler<R: UserSettingsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserSettingsRepository> UpdateUserSettingsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserSettingsRepository + 'static> CommandHandler<UpdateUserSettingsCommand> for UpdateUserSettingsHandler<R> {
    type Output = Option<UserSettings>;

    async fn handle(&self, cmd: UpdateUserSettingsCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.theme {
            entity.theme = value;
        }
        if let Some(value) = cmd.language {
            entity.language = value;
        }
        if let Some(value) = cmd.timezone {
            entity.timezone = value;
        }
        if let Some(value) = cmd.notifications_enabled {
            entity.notifications_enabled = value;
        }
        if let Some(value) = cmd.email_notifications {
            entity.email_notifications = value;
        }
        if let Some(value) = cmd.sms_notifications {
            entity.sms_notifications = value;
        }
        if let Some(value) = cmd.mfa_enabled {
            entity.mfa_enabled = value;
        }
        entity.custom_settings = cmd.custom_settings;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a user_settings
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteUserSettingsCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteUserSettingsCommand
pub struct DeleteUserSettingsHandler<R: UserSettingsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserSettingsRepository> DeleteUserSettingsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserSettingsRepository + 'static> CommandHandler<DeleteUserSettingsCommand> for DeleteUserSettingsHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteUserSettingsCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateUserSettingsCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>