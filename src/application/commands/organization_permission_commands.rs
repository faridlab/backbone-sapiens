//! Commands for OrganizationPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::OrganizationPermission;
use crate::infrastructure::persistence::OrganizationPermissionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for OrganizationPermission operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait OrganizationPermissionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new organization_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateOrganizationPermissionCommand {
    pub organization_id: Uuid,
    pub permission_id: Uuid,
    pub permission_name: Option<String>,
    pub resource_id: Option<Uuid>,
    pub resource_type: Option<String>,
    pub role_id: Option<Uuid>,
    pub granted_by: Uuid,
    pub granted_at: DateTime<Utc>,
    pub expires_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub is_active: bool,
    pub metadata: serde_json::Value,
}

/// Handler for CreateOrganizationPermissionCommand
pub struct CreateOrganizationPermissionHandler<R: OrganizationPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OrganizationPermissionRepository> CreateOrganizationPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OrganizationPermissionRepository + 'static> CommandHandler<CreateOrganizationPermissionCommand> for CreateOrganizationPermissionHandler<R> {
    type Output = OrganizationPermission;

    async fn handle(&self, cmd: CreateOrganizationPermissionCommand) -> Result<Self::Output> {
        let entity = OrganizationPermission::builder()
            .id(Uuid::new_v4().to_string())
            .organization_id(cmd.organization_id)
            .permission_id(cmd.permission_id)
            .permission_name(cmd.permission_name)
            .resource_id(cmd.resource_id)
            .resource_type(cmd.resource_type)
            .role_id(cmd.role_id)
            .granted_by(cmd.granted_by)
            .granted_at(cmd.granted_at)
            .expires_at(cmd.expires_at)
            .reason(cmd.reason)
            .is_active(cmd.is_active)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing organization_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateOrganizationPermissionCommand {
    pub id: String,
    pub organization_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
    pub permission_name: Option<String>,
    pub resource_id: Option<Uuid>,
    pub resource_type: Option<String>,
    pub role_id: Option<Uuid>,
    pub granted_by: Option<Uuid>,
    pub granted_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub is_active: Option<bool>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateOrganizationPermissionCommand
pub struct UpdateOrganizationPermissionHandler<R: OrganizationPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OrganizationPermissionRepository> UpdateOrganizationPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OrganizationPermissionRepository + 'static> CommandHandler<UpdateOrganizationPermissionCommand> for UpdateOrganizationPermissionHandler<R> {
    type Output = Option<OrganizationPermission>;

    async fn handle(&self, cmd: UpdateOrganizationPermissionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.organization_id {
            entity.organization_id = value;
        }
        if let Some(value) = cmd.permission_id {
            entity.permission_id = value;
        }
        entity.permission_name = cmd.permission_name;
        entity.resource_id = cmd.resource_id;
        entity.resource_type = cmd.resource_type;
        entity.role_id = cmd.role_id;
        if let Some(value) = cmd.granted_by {
            entity.granted_by = value;
        }
        if let Some(value) = cmd.granted_at {
            entity.granted_at = value;
        }
        entity.expires_at = cmd.expires_at;
        entity.reason = cmd.reason;
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a organization_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteOrganizationPermissionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteOrganizationPermissionCommand
pub struct DeleteOrganizationPermissionHandler<R: OrganizationPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OrganizationPermissionRepository> DeleteOrganizationPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OrganizationPermissionRepository + 'static> CommandHandler<DeleteOrganizationPermissionCommand> for DeleteOrganizationPermissionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteOrganizationPermissionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateOrganizationPermissionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>