//! Commands for Workflow aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::Workflow;
use crate::infrastructure::persistence::WorkflowRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for Workflow operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait WorkflowCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new workflow
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateWorkflowCommand {
    pub workflow_type: WorkflowType,
    pub status: WorkflowStatus,
    pub initiator_id: Uuid,
    pub target_user_id: Option<Uuid>,
    pub title: String,
    pub description: Option<String>,
    pub context: Option<serde_json::Value>,
    pub current_step: i32,
    pub total_steps: i32,
    pub progress_percentage: f64,
    pub started_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub failed_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub retry_count: i32,
    pub max_retries: i32,
    pub error_message: Option<String>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateWorkflowCommand
pub struct CreateWorkflowHandler<R: WorkflowRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowRepository> CreateWorkflowHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowRepository + 'static> CommandHandler<CreateWorkflowCommand> for CreateWorkflowHandler<R> {
    type Output = Workflow;

    async fn handle(&self, cmd: CreateWorkflowCommand) -> Result<Self::Output> {
        let entity = Workflow::builder()
            .id(Uuid::new_v4().to_string())
            .workflow_type(cmd.workflow_type)
            .status(cmd.status)
            .initiator_id(cmd.initiator_id)
            .target_user_id(cmd.target_user_id)
            .title(cmd.title)
            .description(cmd.description)
            .context(cmd.context)
            .current_step(cmd.current_step)
            .total_steps(cmd.total_steps)
            .progress_percentage(cmd.progress_percentage)
            .started_at(cmd.started_at)
            .completed_at(cmd.completed_at)
            .failed_at(cmd.failed_at)
            .expires_at(cmd.expires_at)
            .retry_count(cmd.retry_count)
            .max_retries(cmd.max_retries)
            .error_message(cmd.error_message)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing workflow
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateWorkflowCommand {
    pub id: String,
    pub workflow_type: Option<WorkflowType>,
    pub status: Option<WorkflowStatus>,
    pub initiator_id: Option<Uuid>,
    pub target_user_id: Option<Uuid>,
    pub title: Option<String>,
    pub description: Option<String>,
    pub context: Option<serde_json::Value>,
    pub current_step: Option<i32>,
    pub total_steps: Option<i32>,
    pub progress_percentage: Option<f64>,
    pub started_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub failed_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub retry_count: Option<i32>,
    pub max_retries: Option<i32>,
    pub error_message: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateWorkflowCommand
pub struct UpdateWorkflowHandler<R: WorkflowRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowRepository> UpdateWorkflowHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowRepository + 'static> CommandHandler<UpdateWorkflowCommand> for UpdateWorkflowHandler<R> {
    type Output = Option<Workflow>;

    async fn handle(&self, cmd: UpdateWorkflowCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.workflow_type {
            entity.workflow_type = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.initiator_id {
            entity.initiator_id = value;
        }
        entity.target_user_id = cmd.target_user_id;
        if let Some(value) = cmd.title {
            entity.title = value;
        }
        entity.description = cmd.description;
        entity.context = cmd.context;
        if let Some(value) = cmd.current_step {
            entity.current_step = value;
        }
        if let Some(value) = cmd.total_steps {
            entity.total_steps = value;
        }
        if let Some(value) = cmd.progress_percentage {
            entity.progress_percentage = value;
        }
        entity.started_at = cmd.started_at;
        entity.completed_at = cmd.completed_at;
        entity.failed_at = cmd.failed_at;
        entity.expires_at = cmd.expires_at;
        if let Some(value) = cmd.retry_count {
            entity.retry_count = value;
        }
        if let Some(value) = cmd.max_retries {
            entity.max_retries = value;
        }
        entity.error_message = cmd.error_message;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a workflow
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteWorkflowCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteWorkflowCommand
pub struct DeleteWorkflowHandler<R: WorkflowRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowRepository> DeleteWorkflowHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowRepository + 'static> CommandHandler<DeleteWorkflowCommand> for DeleteWorkflowHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteWorkflowCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateWorkflowCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>