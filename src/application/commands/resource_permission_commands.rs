//! Commands for ResourcePermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::ResourcePermission;
use crate::infrastructure::persistence::ResourcePermissionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for ResourcePermission operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait ResourcePermissionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new resource_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateResourcePermissionCommand {
    pub permission_id: Uuid,
    pub resource_type: String,
    pub resource_id: String,
    pub granted_to_user_id: Option<Uuid>,
    pub granted_to_role_id: Option<Uuid>,
    pub granted_by: Uuid,
    pub granted_at: DateTime<Utc>,
    pub expires_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub status: ResourcePermissionStatus,
    pub metadata: serde_json::Value,
}

/// Handler for CreateResourcePermissionCommand
pub struct CreateResourcePermissionHandler<R: ResourcePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ResourcePermissionRepository> CreateResourcePermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ResourcePermissionRepository + 'static> CommandHandler<CreateResourcePermissionCommand> for CreateResourcePermissionHandler<R> {
    type Output = ResourcePermission;

    async fn handle(&self, cmd: CreateResourcePermissionCommand) -> Result<Self::Output> {
        let entity = ResourcePermission::builder()
            .id(Uuid::new_v4().to_string())
            .permission_id(cmd.permission_id)
            .resource_type(cmd.resource_type)
            .resource_id(cmd.resource_id)
            .granted_to_user_id(cmd.granted_to_user_id)
            .granted_to_role_id(cmd.granted_to_role_id)
            .granted_by(cmd.granted_by)
            .granted_at(cmd.granted_at)
            .expires_at(cmd.expires_at)
            .reason(cmd.reason)
            .status(cmd.status)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing resource_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateResourcePermissionCommand {
    pub id: String,
    pub permission_id: Option<Uuid>,
    pub resource_type: Option<String>,
    pub resource_id: Option<String>,
    pub granted_to_user_id: Option<Uuid>,
    pub granted_to_role_id: Option<Uuid>,
    pub granted_by: Option<Uuid>,
    pub granted_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub status: Option<ResourcePermissionStatus>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateResourcePermissionCommand
pub struct UpdateResourcePermissionHandler<R: ResourcePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ResourcePermissionRepository> UpdateResourcePermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ResourcePermissionRepository + 'static> CommandHandler<UpdateResourcePermissionCommand> for UpdateResourcePermissionHandler<R> {
    type Output = Option<ResourcePermission>;

    async fn handle(&self, cmd: UpdateResourcePermissionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.permission_id {
            entity.permission_id = value;
        }
        if let Some(value) = cmd.resource_type {
            entity.resource_type = value;
        }
        if let Some(value) = cmd.resource_id {
            entity.resource_id = value;
        }
        entity.granted_to_user_id = cmd.granted_to_user_id;
        entity.granted_to_role_id = cmd.granted_to_role_id;
        if let Some(value) = cmd.granted_by {
            entity.granted_by = value;
        }
        if let Some(value) = cmd.granted_at {
            entity.granted_at = value;
        }
        entity.expires_at = cmd.expires_at;
        entity.reason = cmd.reason;
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a resource_permission
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteResourcePermissionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteResourcePermissionCommand
pub struct DeleteResourcePermissionHandler<R: ResourcePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ResourcePermissionRepository> DeleteResourcePermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ResourcePermissionRepository + 'static> CommandHandler<DeleteResourcePermissionCommand> for DeleteResourcePermissionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteResourcePermissionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateResourcePermissionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>