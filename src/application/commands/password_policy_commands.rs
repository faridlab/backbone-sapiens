//! Commands for PasswordPolicy aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordPolicy;
use crate::infrastructure::persistence::PasswordPolicyRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordPolicy operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordPolicyCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_policy
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordPolicyCommand {
    pub name: String,
    pub organization_id: Option<Uuid>,
    pub is_active: bool,
    pub password_requirements: serde_json::Value,
    pub password_history: serde_json::Value,
    pub reset_settings: serde_json::Value,
    pub expiry_settings: serde_json::Value,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordPolicyCommand
pub struct CreatePasswordPolicyHandler<R: PasswordPolicyRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordPolicyRepository> CreatePasswordPolicyHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordPolicyRepository + 'static> CommandHandler<CreatePasswordPolicyCommand> for CreatePasswordPolicyHandler<R> {
    type Output = PasswordPolicy;

    async fn handle(&self, cmd: CreatePasswordPolicyCommand) -> Result<Self::Output> {
        let entity = PasswordPolicy::builder()
            .id(Uuid::new_v4().to_string())
            .name(cmd.name)
            .organization_id(cmd.organization_id)
            .is_active(cmd.is_active)
            .password_requirements(cmd.password_requirements)
            .password_history(cmd.password_history)
            .reset_settings(cmd.reset_settings)
            .expiry_settings(cmd.expiry_settings)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_policy
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordPolicyCommand {
    pub id: String,
    pub name: Option<String>,
    pub organization_id: Option<Uuid>,
    pub is_active: Option<bool>,
    pub password_requirements: Option<serde_json::Value>,
    pub password_history: Option<serde_json::Value>,
    pub reset_settings: Option<serde_json::Value>,
    pub expiry_settings: Option<serde_json::Value>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordPolicyCommand
pub struct UpdatePasswordPolicyHandler<R: PasswordPolicyRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordPolicyRepository> UpdatePasswordPolicyHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordPolicyRepository + 'static> CommandHandler<UpdatePasswordPolicyCommand> for UpdatePasswordPolicyHandler<R> {
    type Output = Option<PasswordPolicy>;

    async fn handle(&self, cmd: UpdatePasswordPolicyCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.name {
            entity.name = value;
        }
        entity.organization_id = cmd.organization_id;
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        if let Some(value) = cmd.password_requirements {
            entity.password_requirements = value;
        }
        if let Some(value) = cmd.password_history {
            entity.password_history = value;
        }
        if let Some(value) = cmd.reset_settings {
            entity.reset_settings = value;
        }
        if let Some(value) = cmd.expiry_settings {
            entity.expiry_settings = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_policy
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordPolicyCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordPolicyCommand
pub struct DeletePasswordPolicyHandler<R: PasswordPolicyRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordPolicyRepository> DeletePasswordPolicyHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordPolicyRepository + 'static> CommandHandler<DeletePasswordPolicyCommand> for DeletePasswordPolicyHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordPolicyCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordPolicyCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>