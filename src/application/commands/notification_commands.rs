//! Commands for Notification aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::Notification;
use crate::infrastructure::persistence::NotificationRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for Notification operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait NotificationCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new notification
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateNotificationCommand {
    pub user_id: Uuid,
    pub notification_type: NotificationType,
    pub channel: NotificationChannel,
    pub title: String,
    pub message: String,
    pub data: Option<serde_json::Value>,
    pub is_read: bool,
    pub read_at: Option<DateTime<Utc>>,
    pub sent_at: Option<DateTime<Utc>>,
    pub delivered_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub priority: NotificationPriority,
    pub action_url: Option<String>,
    pub action_text: Option<String>,
    pub category: Option<String>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateNotificationCommand
pub struct CreateNotificationHandler<R: NotificationRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationRepository> CreateNotificationHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationRepository + 'static> CommandHandler<CreateNotificationCommand> for CreateNotificationHandler<R> {
    type Output = Notification;

    async fn handle(&self, cmd: CreateNotificationCommand) -> Result<Self::Output> {
        let entity = Notification::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .notification_type(cmd.notification_type)
            .channel(cmd.channel)
            .title(cmd.title)
            .message(cmd.message)
            .data(cmd.data)
            .is_read(cmd.is_read)
            .read_at(cmd.read_at)
            .sent_at(cmd.sent_at)
            .delivered_at(cmd.delivered_at)
            .expires_at(cmd.expires_at)
            .priority(cmd.priority)
            .action_url(cmd.action_url)
            .action_text(cmd.action_text)
            .category(cmd.category)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing notification
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateNotificationCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub notification_type: Option<NotificationType>,
    pub channel: Option<NotificationChannel>,
    pub title: Option<String>,
    pub message: Option<String>,
    pub data: Option<serde_json::Value>,
    pub is_read: Option<bool>,
    pub read_at: Option<DateTime<Utc>>,
    pub sent_at: Option<DateTime<Utc>>,
    pub delivered_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub priority: Option<NotificationPriority>,
    pub action_url: Option<String>,
    pub action_text: Option<String>,
    pub category: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateNotificationCommand
pub struct UpdateNotificationHandler<R: NotificationRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationRepository> UpdateNotificationHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationRepository + 'static> CommandHandler<UpdateNotificationCommand> for UpdateNotificationHandler<R> {
    type Output = Option<Notification>;

    async fn handle(&self, cmd: UpdateNotificationCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.notification_type {
            entity.notification_type = value;
        }
        if let Some(value) = cmd.channel {
            entity.channel = value;
        }
        if let Some(value) = cmd.title {
            entity.title = value;
        }
        if let Some(value) = cmd.message {
            entity.message = value;
        }
        entity.data = cmd.data;
        if let Some(value) = cmd.is_read {
            entity.is_read = value;
        }
        entity.read_at = cmd.read_at;
        entity.sent_at = cmd.sent_at;
        entity.delivered_at = cmd.delivered_at;
        entity.expires_at = cmd.expires_at;
        if let Some(value) = cmd.priority {
            entity.priority = value;
        }
        entity.action_url = cmd.action_url;
        entity.action_text = cmd.action_text;
        entity.category = cmd.category;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a notification
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteNotificationCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteNotificationCommand
pub struct DeleteNotificationHandler<R: NotificationRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationRepository> DeleteNotificationHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationRepository + 'static> CommandHandler<DeleteNotificationCommand> for DeleteNotificationHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteNotificationCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateNotificationCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>