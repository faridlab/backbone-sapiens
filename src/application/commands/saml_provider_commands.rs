//! Commands for SAMLProvider aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::SAMLProvider;
use crate::infrastructure::persistence::SAMLProviderRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for SAMLProvider operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait SAMLProviderCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new saml_provider
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateSAMLProviderCommand {
    pub name: String,
    pub display_name: String,
    pub entity_id: String,
    pub sso_url: String,
    pub slo_url: Option<String>,
    pub certificate: String,
    pub acs_url: String,
    pub sls_url: Option<String>,
    pub name_id_format: String,
    pub attribute_mapping: serde_json::Value,
    pub is_active: bool,
    pub status: SAMLProviderStatus,
    pub metadata: serde_json::Value,
}

/// Handler for CreateSAMLProviderCommand
pub struct CreateSAMLProviderHandler<R: SAMLProviderRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SAMLProviderRepository> CreateSAMLProviderHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SAMLProviderRepository + 'static> CommandHandler<CreateSAMLProviderCommand> for CreateSAMLProviderHandler<R> {
    type Output = SAMLProvider;

    async fn handle(&self, cmd: CreateSAMLProviderCommand) -> Result<Self::Output> {
        let entity = SAMLProvider::builder()
            .id(Uuid::new_v4().to_string())
            .name(cmd.name)
            .display_name(cmd.display_name)
            .entity_id(cmd.entity_id)
            .sso_url(cmd.sso_url)
            .slo_url(cmd.slo_url)
            .certificate(cmd.certificate)
            .acs_url(cmd.acs_url)
            .sls_url(cmd.sls_url)
            .name_id_format(cmd.name_id_format)
            .attribute_mapping(cmd.attribute_mapping)
            .is_active(cmd.is_active)
            .status(cmd.status)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing saml_provider
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateSAMLProviderCommand {
    pub id: String,
    pub name: Option<String>,
    pub display_name: Option<String>,
    pub entity_id: Option<String>,
    pub sso_url: Option<String>,
    pub slo_url: Option<String>,
    pub certificate: Option<String>,
    pub acs_url: Option<String>,
    pub sls_url: Option<String>,
    pub name_id_format: Option<String>,
    pub attribute_mapping: Option<serde_json::Value>,
    pub is_active: Option<bool>,
    pub status: Option<SAMLProviderStatus>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateSAMLProviderCommand
pub struct UpdateSAMLProviderHandler<R: SAMLProviderRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SAMLProviderRepository> UpdateSAMLProviderHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SAMLProviderRepository + 'static> CommandHandler<UpdateSAMLProviderCommand> for UpdateSAMLProviderHandler<R> {
    type Output = Option<SAMLProvider>;

    async fn handle(&self, cmd: UpdateSAMLProviderCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.name {
            entity.name = value;
        }
        if let Some(value) = cmd.display_name {
            entity.display_name = value;
        }
        if let Some(value) = cmd.entity_id {
            entity.entity_id = value;
        }
        if let Some(value) = cmd.sso_url {
            entity.sso_url = value;
        }
        entity.slo_url = cmd.slo_url;
        if let Some(value) = cmd.certificate {
            entity.certificate = value;
        }
        if let Some(value) = cmd.acs_url {
            entity.acs_url = value;
        }
        entity.sls_url = cmd.sls_url;
        if let Some(value) = cmd.name_id_format {
            entity.name_id_format = value;
        }
        if let Some(value) = cmd.attribute_mapping {
            entity.attribute_mapping = value;
        }
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a saml_provider
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteSAMLProviderCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteSAMLProviderCommand
pub struct DeleteSAMLProviderHandler<R: SAMLProviderRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SAMLProviderRepository> DeleteSAMLProviderHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SAMLProviderRepository + 'static> CommandHandler<DeleteSAMLProviderCommand> for DeleteSAMLProviderHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteSAMLProviderCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateSAMLProviderCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>