//! Commands for PasswordHistory aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordHistory;
use crate::infrastructure::persistence::PasswordHistoryRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordHistory operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordHistoryCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_history
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordHistoryCommand {
    pub user_id: Uuid,
    pub password_hash: String,
    pub password_strength: serde_json::Value,
    pub password_metadata: serde_json::Value,
    pub creation_context: serde_json::Value,
    pub status: PasswordHistoryStatus,
    pub usage: serde_json::Value,
    pub expiry: Option<serde_json::Value>,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordHistoryCommand
pub struct CreatePasswordHistoryHandler<R: PasswordHistoryRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordHistoryRepository> CreatePasswordHistoryHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordHistoryRepository + 'static> CommandHandler<CreatePasswordHistoryCommand> for CreatePasswordHistoryHandler<R> {
    type Output = PasswordHistory;

    async fn handle(&self, cmd: CreatePasswordHistoryCommand) -> Result<Self::Output> {
        let entity = PasswordHistory::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .password_hash(cmd.password_hash)
            .password_strength(cmd.password_strength)
            .password_metadata(cmd.password_metadata)
            .creation_context(cmd.creation_context)
            .status(cmd.status)
            .usage(cmd.usage)
            .expiry(cmd.expiry)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_history
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordHistoryCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub password_hash: Option<String>,
    pub password_strength: Option<serde_json::Value>,
    pub password_metadata: Option<serde_json::Value>,
    pub creation_context: Option<serde_json::Value>,
    pub status: Option<PasswordHistoryStatus>,
    pub usage: Option<serde_json::Value>,
    pub expiry: Option<serde_json::Value>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordHistoryCommand
pub struct UpdatePasswordHistoryHandler<R: PasswordHistoryRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordHistoryRepository> UpdatePasswordHistoryHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordHistoryRepository + 'static> CommandHandler<UpdatePasswordHistoryCommand> for UpdatePasswordHistoryHandler<R> {
    type Output = Option<PasswordHistory>;

    async fn handle(&self, cmd: UpdatePasswordHistoryCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.password_hash {
            entity.password_hash = value;
        }
        if let Some(value) = cmd.password_strength {
            entity.password_strength = value;
        }
        if let Some(value) = cmd.password_metadata {
            entity.password_metadata = value;
        }
        if let Some(value) = cmd.creation_context {
            entity.creation_context = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.usage {
            entity.usage = value;
        }
        entity.expiry = cmd.expiry;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_history
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordHistoryCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordHistoryCommand
pub struct DeletePasswordHistoryHandler<R: PasswordHistoryRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordHistoryRepository> DeletePasswordHistoryHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordHistoryRepository + 'static> CommandHandler<DeletePasswordHistoryCommand> for DeletePasswordHistoryHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordHistoryCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordHistoryCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>