//! Commands for BulkOperation aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::BulkOperation;
use crate::infrastructure::persistence::BulkOperationRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for BulkOperation operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait BulkOperationCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new bulk_operation
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateBulkOperationCommand {
    pub operation_type: BulkOperationType,
    pub status: BulkOperationStatus,
    pub total_records: i32,
    pub processed_records: i32,
    pub successful_records: i32,
    pub failed_records: i32,
    pub skipped_records: i32,
    pub input_file_path: Option<String>,
    pub output_file_path: Option<String>,
    pub error_file_path: Option<String>,
    pub parameters: Option<serde_json::Value>,
    pub progress_percentage: f64,
    pub started_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub estimated_completion: Option<DateTime<Utc>>,
    pub error_message: Option<String>,
    pub retry_count: i32,
    pub max_retries: i32,
    pub metadata: serde_json::Value,
}

/// Handler for CreateBulkOperationCommand
pub struct CreateBulkOperationHandler<R: BulkOperationRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BulkOperationRepository> CreateBulkOperationHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BulkOperationRepository + 'static> CommandHandler<CreateBulkOperationCommand> for CreateBulkOperationHandler<R> {
    type Output = BulkOperation;

    async fn handle(&self, cmd: CreateBulkOperationCommand) -> Result<Self::Output> {
        let entity = BulkOperation::builder()
            .id(Uuid::new_v4().to_string())
            .operation_type(cmd.operation_type)
            .status(cmd.status)
            .total_records(cmd.total_records)
            .processed_records(cmd.processed_records)
            .successful_records(cmd.successful_records)
            .failed_records(cmd.failed_records)
            .skipped_records(cmd.skipped_records)
            .input_file_path(cmd.input_file_path)
            .output_file_path(cmd.output_file_path)
            .error_file_path(cmd.error_file_path)
            .parameters(cmd.parameters)
            .progress_percentage(cmd.progress_percentage)
            .started_at(cmd.started_at)
            .completed_at(cmd.completed_at)
            .estimated_completion(cmd.estimated_completion)
            .error_message(cmd.error_message)
            .retry_count(cmd.retry_count)
            .max_retries(cmd.max_retries)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing bulk_operation
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateBulkOperationCommand {
    pub id: String,
    pub operation_type: Option<BulkOperationType>,
    pub status: Option<BulkOperationStatus>,
    pub total_records: Option<i32>,
    pub processed_records: Option<i32>,
    pub successful_records: Option<i32>,
    pub failed_records: Option<i32>,
    pub skipped_records: Option<i32>,
    pub input_file_path: Option<String>,
    pub output_file_path: Option<String>,
    pub error_file_path: Option<String>,
    pub parameters: Option<serde_json::Value>,
    pub progress_percentage: Option<f64>,
    pub started_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub estimated_completion: Option<DateTime<Utc>>,
    pub error_message: Option<String>,
    pub retry_count: Option<i32>,
    pub max_retries: Option<i32>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateBulkOperationCommand
pub struct UpdateBulkOperationHandler<R: BulkOperationRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BulkOperationRepository> UpdateBulkOperationHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BulkOperationRepository + 'static> CommandHandler<UpdateBulkOperationCommand> for UpdateBulkOperationHandler<R> {
    type Output = Option<BulkOperation>;

    async fn handle(&self, cmd: UpdateBulkOperationCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.operation_type {
            entity.operation_type = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.total_records {
            entity.total_records = value;
        }
        if let Some(value) = cmd.processed_records {
            entity.processed_records = value;
        }
        if let Some(value) = cmd.successful_records {
            entity.successful_records = value;
        }
        if let Some(value) = cmd.failed_records {
            entity.failed_records = value;
        }
        if let Some(value) = cmd.skipped_records {
            entity.skipped_records = value;
        }
        entity.input_file_path = cmd.input_file_path;
        entity.output_file_path = cmd.output_file_path;
        entity.error_file_path = cmd.error_file_path;
        entity.parameters = cmd.parameters;
        if let Some(value) = cmd.progress_percentage {
            entity.progress_percentage = value;
        }
        entity.started_at = cmd.started_at;
        entity.completed_at = cmd.completed_at;
        entity.estimated_completion = cmd.estimated_completion;
        entity.error_message = cmd.error_message;
        if let Some(value) = cmd.retry_count {
            entity.retry_count = value;
        }
        if let Some(value) = cmd.max_retries {
            entity.max_retries = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a bulk_operation
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteBulkOperationCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteBulkOperationCommand
pub struct DeleteBulkOperationHandler<R: BulkOperationRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BulkOperationRepository> DeleteBulkOperationHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BulkOperationRepository + 'static> CommandHandler<DeleteBulkOperationCommand> for DeleteBulkOperationHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteBulkOperationCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateBulkOperationCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>