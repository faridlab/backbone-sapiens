//! Commands for PermissionConflict aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PermissionConflict;
use crate::infrastructure::persistence::PermissionConflictRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PermissionConflict operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PermissionConflictCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new permission_conflict
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePermissionConflictCommand {
    pub conflict_type: String,
    pub severity: ConflictSeverity,
    pub description: Option<String>,
    pub conflicting_entity_id: Uuid,
    pub resolution_status: ConflictResolutionStatus,
    pub resolved_at: Option<DateTime<Utc>>,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePermissionConflictCommand
pub struct CreatePermissionConflictHandler<R: PermissionConflictRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PermissionConflictRepository> CreatePermissionConflictHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PermissionConflictRepository + 'static> CommandHandler<CreatePermissionConflictCommand> for CreatePermissionConflictHandler<R> {
    type Output = PermissionConflict;

    async fn handle(&self, cmd: CreatePermissionConflictCommand) -> Result<Self::Output> {
        let entity = PermissionConflict::builder()
            .id(Uuid::new_v4().to_string())
            .conflict_type(cmd.conflict_type)
            .severity(cmd.severity)
            .description(cmd.description)
            .conflicting_entity_id(cmd.conflicting_entity_id)
            .resolution_status(cmd.resolution_status)
            .resolved_at(cmd.resolved_at)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing permission_conflict
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePermissionConflictCommand {
    pub id: String,
    pub conflict_type: Option<String>,
    pub severity: Option<ConflictSeverity>,
    pub description: Option<String>,
    pub conflicting_entity_id: Option<Uuid>,
    pub resolution_status: Option<ConflictResolutionStatus>,
    pub resolved_at: Option<DateTime<Utc>>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePermissionConflictCommand
pub struct UpdatePermissionConflictHandler<R: PermissionConflictRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PermissionConflictRepository> UpdatePermissionConflictHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PermissionConflictRepository + 'static> CommandHandler<UpdatePermissionConflictCommand> for UpdatePermissionConflictHandler<R> {
    type Output = Option<PermissionConflict>;

    async fn handle(&self, cmd: UpdatePermissionConflictCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.conflict_type {
            entity.conflict_type = value;
        }
        if let Some(value) = cmd.severity {
            entity.severity = value;
        }
        entity.description = cmd.description;
        if let Some(value) = cmd.conflicting_entity_id {
            entity.conflicting_entity_id = value;
        }
        if let Some(value) = cmd.resolution_status {
            entity.resolution_status = value;
        }
        entity.resolved_at = cmd.resolved_at;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a permission_conflict
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePermissionConflictCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePermissionConflictCommand
pub struct DeletePermissionConflictHandler<R: PermissionConflictRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PermissionConflictRepository> DeletePermissionConflictHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PermissionConflictRepository + 'static> CommandHandler<DeletePermissionConflictCommand> for DeletePermissionConflictHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePermissionConflictCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePermissionConflictCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>