//! Commands for DataExport aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::DataExport;
use crate::infrastructure::persistence::DataExportRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for DataExport operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait DataExportCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new data_export
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateDataExportCommand {
    pub user_id: Uuid,
    pub requested_by: Uuid,
    pub requested_at: DateTime<Utc>,
    pub status: DataExportStatus,
    pub file_path: Option<String>,
    pub file_url: Option<String>,
    pub expires_at: DateTime<Utc>,
    pub completed_at: Option<DateTime<Utc>>,
    pub record_count: Option<i32>,
    pub format: DataExportFormat,
    pub justification: Option<String>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateDataExportCommand
pub struct CreateDataExportHandler<R: DataExportRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DataExportRepository> CreateDataExportHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DataExportRepository + 'static> CommandHandler<CreateDataExportCommand> for CreateDataExportHandler<R> {
    type Output = DataExport;

    async fn handle(&self, cmd: CreateDataExportCommand) -> Result<Self::Output> {
        let entity = DataExport::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .requested_by(cmd.requested_by)
            .requested_at(cmd.requested_at)
            .status(cmd.status)
            .file_path(cmd.file_path)
            .file_url(cmd.file_url)
            .expires_at(cmd.expires_at)
            .completed_at(cmd.completed_at)
            .record_count(cmd.record_count)
            .format(cmd.format)
            .justification(cmd.justification)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing data_export
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateDataExportCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub requested_by: Option<Uuid>,
    pub requested_at: Option<DateTime<Utc>>,
    pub status: Option<DataExportStatus>,
    pub file_path: Option<String>,
    pub file_url: Option<String>,
    pub expires_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub record_count: Option<i32>,
    pub format: Option<DataExportFormat>,
    pub justification: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateDataExportCommand
pub struct UpdateDataExportHandler<R: DataExportRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DataExportRepository> UpdateDataExportHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DataExportRepository + 'static> CommandHandler<UpdateDataExportCommand> for UpdateDataExportHandler<R> {
    type Output = Option<DataExport>;

    async fn handle(&self, cmd: UpdateDataExportCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.requested_by {
            entity.requested_by = value;
        }
        if let Some(value) = cmd.requested_at {
            entity.requested_at = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        entity.file_path = cmd.file_path;
        entity.file_url = cmd.file_url;
        if let Some(value) = cmd.expires_at {
            entity.expires_at = value;
        }
        entity.completed_at = cmd.completed_at;
        entity.record_count = cmd.record_count;
        if let Some(value) = cmd.format {
            entity.format = value;
        }
        entity.justification = cmd.justification;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a data_export
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteDataExportCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteDataExportCommand
pub struct DeleteDataExportHandler<R: DataExportRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DataExportRepository> DeleteDataExportHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DataExportRepository + 'static> CommandHandler<DeleteDataExportCommand> for DeleteDataExportHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteDataExportCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateDataExportCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>