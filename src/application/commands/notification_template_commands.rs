//! Commands for NotificationTemplate aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::NotificationTemplate;
use crate::infrastructure::persistence::NotificationTemplateRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for NotificationTemplate operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait NotificationTemplateCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new notification_template
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateNotificationTemplateCommand {
    pub name: String,
    pub notification_type: NotificationType,
    pub channel: NotificationChannel,
    pub subject_template: Option<String>,
    pub message_template: String,
    pub variables: Option<serde_json::Value>,
    pub is_active: bool,
    pub language: String,
    pub metadata: serde_json::Value,
}

/// Handler for CreateNotificationTemplateCommand
pub struct CreateNotificationTemplateHandler<R: NotificationTemplateRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationTemplateRepository> CreateNotificationTemplateHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationTemplateRepository + 'static> CommandHandler<CreateNotificationTemplateCommand> for CreateNotificationTemplateHandler<R> {
    type Output = NotificationTemplate;

    async fn handle(&self, cmd: CreateNotificationTemplateCommand) -> Result<Self::Output> {
        let entity = NotificationTemplate::builder()
            .id(Uuid::new_v4().to_string())
            .name(cmd.name)
            .notification_type(cmd.notification_type)
            .channel(cmd.channel)
            .subject_template(cmd.subject_template)
            .message_template(cmd.message_template)
            .variables(cmd.variables)
            .is_active(cmd.is_active)
            .language(cmd.language)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing notification_template
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateNotificationTemplateCommand {
    pub id: String,
    pub name: Option<String>,
    pub notification_type: Option<NotificationType>,
    pub channel: Option<NotificationChannel>,
    pub subject_template: Option<String>,
    pub message_template: Option<String>,
    pub variables: Option<serde_json::Value>,
    pub is_active: Option<bool>,
    pub language: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateNotificationTemplateCommand
pub struct UpdateNotificationTemplateHandler<R: NotificationTemplateRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationTemplateRepository> UpdateNotificationTemplateHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationTemplateRepository + 'static> CommandHandler<UpdateNotificationTemplateCommand> for UpdateNotificationTemplateHandler<R> {
    type Output = Option<NotificationTemplate>;

    async fn handle(&self, cmd: UpdateNotificationTemplateCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.name {
            entity.name = value;
        }
        if let Some(value) = cmd.notification_type {
            entity.notification_type = value;
        }
        if let Some(value) = cmd.channel {
            entity.channel = value;
        }
        entity.subject_template = cmd.subject_template;
        if let Some(value) = cmd.message_template {
            entity.message_template = value;
        }
        entity.variables = cmd.variables;
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        if let Some(value) = cmd.language {
            entity.language = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a notification_template
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteNotificationTemplateCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteNotificationTemplateCommand
pub struct DeleteNotificationTemplateHandler<R: NotificationTemplateRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationTemplateRepository> DeleteNotificationTemplateHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationTemplateRepository + 'static> CommandHandler<DeleteNotificationTemplateCommand> for DeleteNotificationTemplateHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteNotificationTemplateCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateNotificationTemplateCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>