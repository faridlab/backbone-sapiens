//! Commands for UserRole aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::UserRole;
use crate::infrastructure::persistence::UserRoleRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for UserRole operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait UserRoleCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new user_role
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateUserRoleCommand {
    pub user_id: Uuid,
    pub role_id: Uuid,
    pub assigned_at: DateTime<Utc>,
    pub assigned_by: Option<Uuid>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateUserRoleCommand
pub struct CreateUserRoleHandler<R: UserRoleRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserRoleRepository> CreateUserRoleHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserRoleRepository + 'static> CommandHandler<CreateUserRoleCommand> for CreateUserRoleHandler<R> {
    type Output = UserRole;

    async fn handle(&self, cmd: CreateUserRoleCommand) -> Result<Self::Output> {
        let entity = UserRole::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .role_id(cmd.role_id)
            .assigned_at(cmd.assigned_at)
            .assigned_by(cmd.assigned_by)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing user_role
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateUserRoleCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub role_id: Option<Uuid>,
    pub assigned_at: Option<DateTime<Utc>>,
    pub assigned_by: Option<Uuid>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateUserRoleCommand
pub struct UpdateUserRoleHandler<R: UserRoleRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserRoleRepository> UpdateUserRoleHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserRoleRepository + 'static> CommandHandler<UpdateUserRoleCommand> for UpdateUserRoleHandler<R> {
    type Output = Option<UserRole>;

    async fn handle(&self, cmd: UpdateUserRoleCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.role_id {
            entity.role_id = value;
        }
        if let Some(value) = cmd.assigned_at {
            entity.assigned_at = value;
        }
        entity.assigned_by = cmd.assigned_by;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a user_role
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteUserRoleCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteUserRoleCommand
pub struct DeleteUserRoleHandler<R: UserRoleRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserRoleRepository> DeleteUserRoleHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserRoleRepository + 'static> CommandHandler<DeleteUserRoleCommand> for DeleteUserRoleHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteUserRoleCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateUserRoleCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>