//! Commands for WorkflowExecution aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::WorkflowExecution;
use crate::infrastructure::persistence::WorkflowExecutionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for WorkflowExecution operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait WorkflowExecutionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new workflow_execution
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateWorkflowExecutionCommand {
    pub workflow_id: Uuid,
    pub workflow_definition_id: Uuid,
    pub status: WorkflowExecutionStatus,
    pub current_action: i32,
    pub total_actions: i32,
    pub progress_percentage: f64,
    pub timeout_minutes: Option<i32>,
    pub started_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub failed_at: Option<DateTime<Utc>>,
    pub cancelled_at: Option<DateTime<Utc>>,
    pub error_message: Option<String>,
    pub execution_context: Option<serde_json::Value>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateWorkflowExecutionCommand
pub struct CreateWorkflowExecutionHandler<R: WorkflowExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowExecutionRepository> CreateWorkflowExecutionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowExecutionRepository + 'static> CommandHandler<CreateWorkflowExecutionCommand> for CreateWorkflowExecutionHandler<R> {
    type Output = WorkflowExecution;

    async fn handle(&self, cmd: CreateWorkflowExecutionCommand) -> Result<Self::Output> {
        let entity = WorkflowExecution::builder()
            .id(Uuid::new_v4().to_string())
            .workflow_id(cmd.workflow_id)
            .workflow_definition_id(cmd.workflow_definition_id)
            .status(cmd.status)
            .current_action(cmd.current_action)
            .total_actions(cmd.total_actions)
            .progress_percentage(cmd.progress_percentage)
            .timeout_minutes(cmd.timeout_minutes)
            .started_at(cmd.started_at)
            .completed_at(cmd.completed_at)
            .failed_at(cmd.failed_at)
            .cancelled_at(cmd.cancelled_at)
            .error_message(cmd.error_message)
            .execution_context(cmd.execution_context)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing workflow_execution
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateWorkflowExecutionCommand {
    pub id: String,
    pub workflow_id: Option<Uuid>,
    pub workflow_definition_id: Option<Uuid>,
    pub status: Option<WorkflowExecutionStatus>,
    pub current_action: Option<i32>,
    pub total_actions: Option<i32>,
    pub progress_percentage: Option<f64>,
    pub timeout_minutes: Option<i32>,
    pub started_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub failed_at: Option<DateTime<Utc>>,
    pub cancelled_at: Option<DateTime<Utc>>,
    pub error_message: Option<String>,
    pub execution_context: Option<serde_json::Value>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateWorkflowExecutionCommand
pub struct UpdateWorkflowExecutionHandler<R: WorkflowExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowExecutionRepository> UpdateWorkflowExecutionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowExecutionRepository + 'static> CommandHandler<UpdateWorkflowExecutionCommand> for UpdateWorkflowExecutionHandler<R> {
    type Output = Option<WorkflowExecution>;

    async fn handle(&self, cmd: UpdateWorkflowExecutionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.workflow_id {
            entity.workflow_id = value;
        }
        if let Some(value) = cmd.workflow_definition_id {
            entity.workflow_definition_id = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.current_action {
            entity.current_action = value;
        }
        if let Some(value) = cmd.total_actions {
            entity.total_actions = value;
        }
        if let Some(value) = cmd.progress_percentage {
            entity.progress_percentage = value;
        }
        entity.timeout_minutes = cmd.timeout_minutes;
        entity.started_at = cmd.started_at;
        entity.completed_at = cmd.completed_at;
        entity.failed_at = cmd.failed_at;
        entity.cancelled_at = cmd.cancelled_at;
        entity.error_message = cmd.error_message;
        entity.execution_context = cmd.execution_context;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a workflow_execution
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteWorkflowExecutionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteWorkflowExecutionCommand
pub struct DeleteWorkflowExecutionHandler<R: WorkflowExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowExecutionRepository> DeleteWorkflowExecutionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowExecutionRepository + 'static> CommandHandler<DeleteWorkflowExecutionCommand> for DeleteWorkflowExecutionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteWorkflowExecutionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateWorkflowExecutionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>