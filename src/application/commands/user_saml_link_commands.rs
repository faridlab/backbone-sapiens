//! Commands for UserSAMLLink aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::UserSAMLLink;
use crate::infrastructure::persistence::UserSAMLLinkRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for UserSAMLLink operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait UserSAMLLinkCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new user_saml_link
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateUserSAMLLinkCommand {
    pub user_id: Uuid,
    pub provider_id: Uuid,
    pub name_id: String,
    pub session_index: Option<String>,
    pub first_login_at: DateTime<Utc>,
    pub last_login_at: DateTime<Utc>,
    pub attributes: serde_json::Value,
    pub is_primary: bool,
    pub status: UserSAMLLinkStatus,
    pub metadata: serde_json::Value,
}

/// Handler for CreateUserSAMLLinkCommand
pub struct CreateUserSAMLLinkHandler<R: UserSAMLLinkRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserSAMLLinkRepository> CreateUserSAMLLinkHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserSAMLLinkRepository + 'static> CommandHandler<CreateUserSAMLLinkCommand> for CreateUserSAMLLinkHandler<R> {
    type Output = UserSAMLLink;

    async fn handle(&self, cmd: CreateUserSAMLLinkCommand) -> Result<Self::Output> {
        let entity = UserSAMLLink::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .provider_id(cmd.provider_id)
            .name_id(cmd.name_id)
            .session_index(cmd.session_index)
            .first_login_at(cmd.first_login_at)
            .last_login_at(cmd.last_login_at)
            .attributes(cmd.attributes)
            .is_primary(cmd.is_primary)
            .status(cmd.status)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing user_saml_link
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateUserSAMLLinkCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub provider_id: Option<Uuid>,
    pub name_id: Option<String>,
    pub session_index: Option<String>,
    pub first_login_at: Option<DateTime<Utc>>,
    pub last_login_at: Option<DateTime<Utc>>,
    pub attributes: Option<serde_json::Value>,
    pub is_primary: Option<bool>,
    pub status: Option<UserSAMLLinkStatus>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateUserSAMLLinkCommand
pub struct UpdateUserSAMLLinkHandler<R: UserSAMLLinkRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserSAMLLinkRepository> UpdateUserSAMLLinkHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserSAMLLinkRepository + 'static> CommandHandler<UpdateUserSAMLLinkCommand> for UpdateUserSAMLLinkHandler<R> {
    type Output = Option<UserSAMLLink>;

    async fn handle(&self, cmd: UpdateUserSAMLLinkCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.provider_id {
            entity.provider_id = value;
        }
        if let Some(value) = cmd.name_id {
            entity.name_id = value;
        }
        entity.session_index = cmd.session_index;
        if let Some(value) = cmd.first_login_at {
            entity.first_login_at = value;
        }
        if let Some(value) = cmd.last_login_at {
            entity.last_login_at = value;
        }
        if let Some(value) = cmd.attributes {
            entity.attributes = value;
        }
        if let Some(value) = cmd.is_primary {
            entity.is_primary = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a user_saml_link
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteUserSAMLLinkCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteUserSAMLLinkCommand
pub struct DeleteUserSAMLLinkHandler<R: UserSAMLLinkRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserSAMLLinkRepository> DeleteUserSAMLLinkHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserSAMLLinkRepository + 'static> CommandHandler<DeleteUserSAMLLinkCommand> for DeleteUserSAMLLinkHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteUserSAMLLinkCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateUserSAMLLinkCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>