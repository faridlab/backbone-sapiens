//! Commands for AnalyticsEvent aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::AnalyticsEvent;
use crate::infrastructure::persistence::AnalyticsEventRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for AnalyticsEvent operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait AnalyticsEventCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new analytics_event
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateAnalyticsEventCommand {
    pub event_type: AnalyticsEventType,
    pub user_id: Option<Uuid>,
    pub session_id: Option<Uuid>,
    pub resource_type: Option<String>,
    pub resource_id: Option<String>,
    pub properties: Option<serde_json::Value>,
    pub timestamp: DateTime<Utc>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub geographic_data: Option<serde_json::Value>,
    pub processed: bool,
    pub metadata: serde_json::Value,
}

/// Handler for CreateAnalyticsEventCommand
pub struct CreateAnalyticsEventHandler<R: AnalyticsEventRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsEventRepository> CreateAnalyticsEventHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsEventRepository + 'static> CommandHandler<CreateAnalyticsEventCommand> for CreateAnalyticsEventHandler<R> {
    type Output = AnalyticsEvent;

    async fn handle(&self, cmd: CreateAnalyticsEventCommand) -> Result<Self::Output> {
        let entity = AnalyticsEvent::builder()
            .id(Uuid::new_v4().to_string())
            .event_type(cmd.event_type)
            .user_id(cmd.user_id)
            .session_id(cmd.session_id)
            .resource_type(cmd.resource_type)
            .resource_id(cmd.resource_id)
            .properties(cmd.properties)
            .timestamp(cmd.timestamp)
            .ip_address(cmd.ip_address)
            .user_agent(cmd.user_agent)
            .geographic_data(cmd.geographic_data)
            .processed(cmd.processed)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing analytics_event
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateAnalyticsEventCommand {
    pub id: String,
    pub event_type: Option<AnalyticsEventType>,
    pub user_id: Option<Uuid>,
    pub session_id: Option<Uuid>,
    pub resource_type: Option<String>,
    pub resource_id: Option<String>,
    pub properties: Option<serde_json::Value>,
    pub timestamp: Option<DateTime<Utc>>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub geographic_data: Option<serde_json::Value>,
    pub processed: Option<bool>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateAnalyticsEventCommand
pub struct UpdateAnalyticsEventHandler<R: AnalyticsEventRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsEventRepository> UpdateAnalyticsEventHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsEventRepository + 'static> CommandHandler<UpdateAnalyticsEventCommand> for UpdateAnalyticsEventHandler<R> {
    type Output = Option<AnalyticsEvent>;

    async fn handle(&self, cmd: UpdateAnalyticsEventCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.event_type {
            entity.event_type = value;
        }
        entity.user_id = cmd.user_id;
        entity.session_id = cmd.session_id;
        entity.resource_type = cmd.resource_type;
        entity.resource_id = cmd.resource_id;
        entity.properties = cmd.properties;
        if let Some(value) = cmd.timestamp {
            entity.timestamp = value;
        }
        entity.ip_address = cmd.ip_address;
        entity.user_agent = cmd.user_agent;
        entity.geographic_data = cmd.geographic_data;
        if let Some(value) = cmd.processed {
            entity.processed = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a analytics_event
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteAnalyticsEventCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteAnalyticsEventCommand
pub struct DeleteAnalyticsEventHandler<R: AnalyticsEventRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsEventRepository> DeleteAnalyticsEventHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsEventRepository + 'static> CommandHandler<DeleteAnalyticsEventCommand> for DeleteAnalyticsEventHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteAnalyticsEventCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateAnalyticsEventCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>