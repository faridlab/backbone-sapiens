//! Commands for WorkflowDefinition aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::WorkflowDefinition;
use crate::infrastructure::persistence::WorkflowDefinitionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for WorkflowDefinition operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait WorkflowDefinitionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new workflow_definition
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateWorkflowDefinitionCommand {
    pub name: String,
    pub workflow_type: WorkflowType,
    pub description: Option<String>,
    pub trigger_type: TriggerType,
    pub trigger_config: Option<serde_json::Value>,
    pub is_active: bool,
    pub timeout_minutes: Option<i32>,
    pub max_retries: i32,
    pub metadata: serde_json::Value,
}

/// Handler for CreateWorkflowDefinitionCommand
pub struct CreateWorkflowDefinitionHandler<R: WorkflowDefinitionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowDefinitionRepository> CreateWorkflowDefinitionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowDefinitionRepository + 'static> CommandHandler<CreateWorkflowDefinitionCommand> for CreateWorkflowDefinitionHandler<R> {
    type Output = WorkflowDefinition;

    async fn handle(&self, cmd: CreateWorkflowDefinitionCommand) -> Result<Self::Output> {
        let entity = WorkflowDefinition::builder()
            .id(Uuid::new_v4().to_string())
            .name(cmd.name)
            .workflow_type(cmd.workflow_type)
            .description(cmd.description)
            .trigger_type(cmd.trigger_type)
            .trigger_config(cmd.trigger_config)
            .is_active(cmd.is_active)
            .timeout_minutes(cmd.timeout_minutes)
            .max_retries(cmd.max_retries)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing workflow_definition
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateWorkflowDefinitionCommand {
    pub id: String,
    pub name: Option<String>,
    pub workflow_type: Option<WorkflowType>,
    pub description: Option<String>,
    pub trigger_type: Option<TriggerType>,
    pub trigger_config: Option<serde_json::Value>,
    pub is_active: Option<bool>,
    pub timeout_minutes: Option<i32>,
    pub max_retries: Option<i32>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateWorkflowDefinitionCommand
pub struct UpdateWorkflowDefinitionHandler<R: WorkflowDefinitionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowDefinitionRepository> UpdateWorkflowDefinitionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowDefinitionRepository + 'static> CommandHandler<UpdateWorkflowDefinitionCommand> for UpdateWorkflowDefinitionHandler<R> {
    type Output = Option<WorkflowDefinition>;

    async fn handle(&self, cmd: UpdateWorkflowDefinitionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.name {
            entity.name = value;
        }
        if let Some(value) = cmd.workflow_type {
            entity.workflow_type = value;
        }
        entity.description = cmd.description;
        if let Some(value) = cmd.trigger_type {
            entity.trigger_type = value;
        }
        entity.trigger_config = cmd.trigger_config;
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        entity.timeout_minutes = cmd.timeout_minutes;
        if let Some(value) = cmd.max_retries {
            entity.max_retries = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a workflow_definition
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteWorkflowDefinitionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteWorkflowDefinitionCommand
pub struct DeleteWorkflowDefinitionHandler<R: WorkflowDefinitionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowDefinitionRepository> DeleteWorkflowDefinitionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowDefinitionRepository + 'static> CommandHandler<DeleteWorkflowDefinitionCommand> for DeleteWorkflowDefinitionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteWorkflowDefinitionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateWorkflowDefinitionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>