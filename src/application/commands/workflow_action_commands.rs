//! Commands for WorkflowAction aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::WorkflowAction;
use crate::infrastructure::persistence::WorkflowActionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for WorkflowAction operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait WorkflowActionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new workflow_action
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateWorkflowActionCommand {
    pub workflow_definition_id: Uuid,
    pub action_type: ActionType,
    pub action_name: String,
    pub description: Option<String>,
    pub step_order: i32,
    pub parameters: Option<serde_json::Value>,
    pub conditions: Option<serde_json::Value>,
    pub is_required: bool,
    pub timeout_minutes: Option<i32>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateWorkflowActionCommand
pub struct CreateWorkflowActionHandler<R: WorkflowActionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowActionRepository> CreateWorkflowActionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowActionRepository + 'static> CommandHandler<CreateWorkflowActionCommand> for CreateWorkflowActionHandler<R> {
    type Output = WorkflowAction;

    async fn handle(&self, cmd: CreateWorkflowActionCommand) -> Result<Self::Output> {
        let entity = WorkflowAction::builder()
            .id(Uuid::new_v4().to_string())
            .workflow_definition_id(cmd.workflow_definition_id)
            .action_type(cmd.action_type)
            .action_name(cmd.action_name)
            .description(cmd.description)
            .step_order(cmd.step_order)
            .parameters(cmd.parameters)
            .conditions(cmd.conditions)
            .is_required(cmd.is_required)
            .timeout_minutes(cmd.timeout_minutes)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing workflow_action
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateWorkflowActionCommand {
    pub id: String,
    pub workflow_definition_id: Option<Uuid>,
    pub action_type: Option<ActionType>,
    pub action_name: Option<String>,
    pub description: Option<String>,
    pub step_order: Option<i32>,
    pub parameters: Option<serde_json::Value>,
    pub conditions: Option<serde_json::Value>,
    pub is_required: Option<bool>,
    pub timeout_minutes: Option<i32>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateWorkflowActionCommand
pub struct UpdateWorkflowActionHandler<R: WorkflowActionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowActionRepository> UpdateWorkflowActionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowActionRepository + 'static> CommandHandler<UpdateWorkflowActionCommand> for UpdateWorkflowActionHandler<R> {
    type Output = Option<WorkflowAction>;

    async fn handle(&self, cmd: UpdateWorkflowActionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.workflow_definition_id {
            entity.workflow_definition_id = value;
        }
        if let Some(value) = cmd.action_type {
            entity.action_type = value;
        }
        if let Some(value) = cmd.action_name {
            entity.action_name = value;
        }
        entity.description = cmd.description;
        if let Some(value) = cmd.step_order {
            entity.step_order = value;
        }
        entity.parameters = cmd.parameters;
        entity.conditions = cmd.conditions;
        if let Some(value) = cmd.is_required {
            entity.is_required = value;
        }
        entity.timeout_minutes = cmd.timeout_minutes;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a workflow_action
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteWorkflowActionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteWorkflowActionCommand
pub struct DeleteWorkflowActionHandler<R: WorkflowActionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowActionRepository> DeleteWorkflowActionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowActionRepository + 'static> CommandHandler<DeleteWorkflowActionCommand> for DeleteWorkflowActionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteWorkflowActionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateWorkflowActionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>