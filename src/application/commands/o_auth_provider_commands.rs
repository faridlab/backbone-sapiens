//! Commands for OAuthProvider aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::OAuthProvider;
use crate::infrastructure::persistence::OAuthProviderRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for OAuthProvider operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait OAuthProviderCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new o_auth_provider
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateOAuthProviderCommand {
    pub provider_name: OAuthProviderType,
    pub display_name: String,
    pub client_id: String,
    pub client_secret: String,
    pub redirect_uri: String,
    pub scopes: Vec<String>,
    pub authorization_url: String,
    pub token_url: String,
    pub user_info_url: String,
    pub is_active: bool,
    pub metadata: serde_json::Value,
}

/// Handler for CreateOAuthProviderCommand
pub struct CreateOAuthProviderHandler<R: OAuthProviderRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OAuthProviderRepository> CreateOAuthProviderHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OAuthProviderRepository + 'static> CommandHandler<CreateOAuthProviderCommand> for CreateOAuthProviderHandler<R> {
    type Output = OAuthProvider;

    async fn handle(&self, cmd: CreateOAuthProviderCommand) -> Result<Self::Output> {
        let entity = OAuthProvider::builder()
            .id(Uuid::new_v4().to_string())
            .provider_name(cmd.provider_name)
            .display_name(cmd.display_name)
            .client_id(cmd.client_id)
            .client_secret(cmd.client_secret)
            .redirect_uri(cmd.redirect_uri)
            .scopes(cmd.scopes)
            .authorization_url(cmd.authorization_url)
            .token_url(cmd.token_url)
            .user_info_url(cmd.user_info_url)
            .is_active(cmd.is_active)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing o_auth_provider
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateOAuthProviderCommand {
    pub id: String,
    pub provider_name: Option<OAuthProviderType>,
    pub display_name: Option<String>,
    pub client_id: Option<String>,
    pub client_secret: Option<String>,
    pub redirect_uri: Option<String>,
    pub scopes: Option<Vec<String>>,
    pub authorization_url: Option<String>,
    pub token_url: Option<String>,
    pub user_info_url: Option<String>,
    pub is_active: Option<bool>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateOAuthProviderCommand
pub struct UpdateOAuthProviderHandler<R: OAuthProviderRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OAuthProviderRepository> UpdateOAuthProviderHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OAuthProviderRepository + 'static> CommandHandler<UpdateOAuthProviderCommand> for UpdateOAuthProviderHandler<R> {
    type Output = Option<OAuthProvider>;

    async fn handle(&self, cmd: UpdateOAuthProviderCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.provider_name {
            entity.provider_name = value;
        }
        if let Some(value) = cmd.display_name {
            entity.display_name = value;
        }
        if let Some(value) = cmd.client_id {
            entity.client_id = value;
        }
        if let Some(value) = cmd.client_secret {
            entity.client_secret = value;
        }
        if let Some(value) = cmd.redirect_uri {
            entity.redirect_uri = value;
        }
        if let Some(value) = cmd.scopes {
            entity.scopes = value;
        }
        if let Some(value) = cmd.authorization_url {
            entity.authorization_url = value;
        }
        if let Some(value) = cmd.token_url {
            entity.token_url = value;
        }
        if let Some(value) = cmd.user_info_url {
            entity.user_info_url = value;
        }
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a o_auth_provider
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteOAuthProviderCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteOAuthProviderCommand
pub struct DeleteOAuthProviderHandler<R: OAuthProviderRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OAuthProviderRepository> DeleteOAuthProviderHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OAuthProviderRepository + 'static> CommandHandler<DeleteOAuthProviderCommand> for DeleteOAuthProviderHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteOAuthProviderCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateOAuthProviderCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>