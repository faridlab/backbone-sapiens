//! Commands for LDAPDirectory aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::LDAPDirectory;
use crate::infrastructure::persistence::LDAPDirectoryRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for LDAPDirectory operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait LDAPDirectoryCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new ldap_directory
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateLDAPDirectoryCommand {
    pub name: String,
    pub display_name: String,
    pub host: String,
    pub port: i32,
    pub use_ssl: bool,
    pub use_tls: bool,
    pub bind_dn: String,
    pub bind_password: String,
    pub search_base: String,
    pub search_filter: String,
    pub attribute_mapping: serde_json::Value,
    pub sync_enabled: bool,
    pub sync_interval_minutes: Option<i32>,
    pub last_sync_at: Option<DateTime<Utc>>,
    pub last_sync_result: Option<serde_json::Value>,
    pub is_active: bool,
    pub status: LDAPDirectoryStatus,
    pub metadata: serde_json::Value,
}

/// Handler for CreateLDAPDirectoryCommand
pub struct CreateLDAPDirectoryHandler<R: LDAPDirectoryRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: LDAPDirectoryRepository> CreateLDAPDirectoryHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: LDAPDirectoryRepository + 'static> CommandHandler<CreateLDAPDirectoryCommand> for CreateLDAPDirectoryHandler<R> {
    type Output = LDAPDirectory;

    async fn handle(&self, cmd: CreateLDAPDirectoryCommand) -> Result<Self::Output> {
        let entity = LDAPDirectory::builder()
            .id(Uuid::new_v4().to_string())
            .name(cmd.name)
            .display_name(cmd.display_name)
            .host(cmd.host)
            .port(cmd.port)
            .use_ssl(cmd.use_ssl)
            .use_tls(cmd.use_tls)
            .bind_dn(cmd.bind_dn)
            .bind_password(cmd.bind_password)
            .search_base(cmd.search_base)
            .search_filter(cmd.search_filter)
            .attribute_mapping(cmd.attribute_mapping)
            .sync_enabled(cmd.sync_enabled)
            .sync_interval_minutes(cmd.sync_interval_minutes)
            .last_sync_at(cmd.last_sync_at)
            .last_sync_result(cmd.last_sync_result)
            .is_active(cmd.is_active)
            .status(cmd.status)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing ldap_directory
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateLDAPDirectoryCommand {
    pub id: String,
    pub name: Option<String>,
    pub display_name: Option<String>,
    pub host: Option<String>,
    pub port: Option<i32>,
    pub use_ssl: Option<bool>,
    pub use_tls: Option<bool>,
    pub bind_dn: Option<String>,
    pub bind_password: Option<String>,
    pub search_base: Option<String>,
    pub search_filter: Option<String>,
    pub attribute_mapping: Option<serde_json::Value>,
    pub sync_enabled: Option<bool>,
    pub sync_interval_minutes: Option<i32>,
    pub last_sync_at: Option<DateTime<Utc>>,
    pub last_sync_result: Option<serde_json::Value>,
    pub is_active: Option<bool>,
    pub status: Option<LDAPDirectoryStatus>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateLDAPDirectoryCommand
pub struct UpdateLDAPDirectoryHandler<R: LDAPDirectoryRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: LDAPDirectoryRepository> UpdateLDAPDirectoryHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: LDAPDirectoryRepository + 'static> CommandHandler<UpdateLDAPDirectoryCommand> for UpdateLDAPDirectoryHandler<R> {
    type Output = Option<LDAPDirectory>;

    async fn handle(&self, cmd: UpdateLDAPDirectoryCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.name {
            entity.name = value;
        }
        if let Some(value) = cmd.display_name {
            entity.display_name = value;
        }
        if let Some(value) = cmd.host {
            entity.host = value;
        }
        if let Some(value) = cmd.port {
            entity.port = value;
        }
        if let Some(value) = cmd.use_ssl {
            entity.use_ssl = value;
        }
        if let Some(value) = cmd.use_tls {
            entity.use_tls = value;
        }
        if let Some(value) = cmd.bind_dn {
            entity.bind_dn = value;
        }
        if let Some(value) = cmd.bind_password {
            entity.bind_password = value;
        }
        if let Some(value) = cmd.search_base {
            entity.search_base = value;
        }
        if let Some(value) = cmd.search_filter {
            entity.search_filter = value;
        }
        if let Some(value) = cmd.attribute_mapping {
            entity.attribute_mapping = value;
        }
        if let Some(value) = cmd.sync_enabled {
            entity.sync_enabled = value;
        }
        entity.sync_interval_minutes = cmd.sync_interval_minutes;
        entity.last_sync_at = cmd.last_sync_at;
        entity.last_sync_result = cmd.last_sync_result;
        if let Some(value) = cmd.is_active {
            entity.is_active = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a ldap_directory
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteLDAPDirectoryCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteLDAPDirectoryCommand
pub struct DeleteLDAPDirectoryHandler<R: LDAPDirectoryRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: LDAPDirectoryRepository> DeleteLDAPDirectoryHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: LDAPDirectoryRepository + 'static> CommandHandler<DeleteLDAPDirectoryCommand> for DeleteLDAPDirectoryHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteLDAPDirectoryCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateLDAPDirectoryCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>