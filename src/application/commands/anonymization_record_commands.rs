//! Commands for AnonymizationRecord aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::AnonymizationRecord;
use crate::infrastructure::persistence::AnonymizationRecordRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for AnonymizationRecord operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait AnonymizationRecordCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new anonymization_record
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateAnonymizationRecordCommand {
    pub user_id: Uuid,
    pub original_email: String,
    pub original_username: String,
    pub anonymized_by: Uuid,
    pub anonymized_at: DateTime<Utc>,
    pub reason: String,
    pub method: AnonymizationMethod,
    pub retention_period_days: Option<i32>,
    pub metadata: serde_json::Value,
    pub status: AnonymizationStatus,
}

/// Handler for CreateAnonymizationRecordCommand
pub struct CreateAnonymizationRecordHandler<R: AnonymizationRecordRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnonymizationRecordRepository> CreateAnonymizationRecordHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnonymizationRecordRepository + 'static> CommandHandler<CreateAnonymizationRecordCommand> for CreateAnonymizationRecordHandler<R> {
    type Output = AnonymizationRecord;

    async fn handle(&self, cmd: CreateAnonymizationRecordCommand) -> Result<Self::Output> {
        let entity = AnonymizationRecord::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .original_email(cmd.original_email)
            .original_username(cmd.original_username)
            .anonymized_by(cmd.anonymized_by)
            .anonymized_at(cmd.anonymized_at)
            .reason(cmd.reason)
            .method(cmd.method)
            .retention_period_days(cmd.retention_period_days)
            .metadata(cmd.metadata)
            .status(cmd.status)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing anonymization_record
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateAnonymizationRecordCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub original_email: Option<String>,
    pub original_username: Option<String>,
    pub anonymized_by: Option<Uuid>,
    pub anonymized_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub method: Option<AnonymizationMethod>,
    pub retention_period_days: Option<i32>,
    pub metadata: Option<serde_json::Value>,
    pub status: Option<AnonymizationStatus>,
}

/// Handler for UpdateAnonymizationRecordCommand
pub struct UpdateAnonymizationRecordHandler<R: AnonymizationRecordRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnonymizationRecordRepository> UpdateAnonymizationRecordHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnonymizationRecordRepository + 'static> CommandHandler<UpdateAnonymizationRecordCommand> for UpdateAnonymizationRecordHandler<R> {
    type Output = Option<AnonymizationRecord>;

    async fn handle(&self, cmd: UpdateAnonymizationRecordCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.original_email {
            entity.original_email = value;
        }
        if let Some(value) = cmd.original_username {
            entity.original_username = value;
        }
        if let Some(value) = cmd.anonymized_by {
            entity.anonymized_by = value;
        }
        if let Some(value) = cmd.anonymized_at {
            entity.anonymized_at = value;
        }
        if let Some(value) = cmd.reason {
            entity.reason = value;
        }
        if let Some(value) = cmd.method {
            entity.method = value;
        }
        entity.retention_period_days = cmd.retention_period_days;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a anonymization_record
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteAnonymizationRecordCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteAnonymizationRecordCommand
pub struct DeleteAnonymizationRecordHandler<R: AnonymizationRecordRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnonymizationRecordRepository> DeleteAnonymizationRecordHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnonymizationRecordRepository + 'static> CommandHandler<DeleteAnonymizationRecordCommand> for DeleteAnonymizationRecordHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteAnonymizationRecordCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateAnonymizationRecordCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>