//! Commands for PasswordCreationContext aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordCreationContext;
use crate::infrastructure::persistence::PasswordCreationContextRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordCreationContext operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordCreationContextCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_creation_context
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordCreationContextCommand {
    pub user_id: Uuid,
    pub created_via: PasswordCreationMethod,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub reason: Option<String>,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordCreationContextCommand
pub struct CreatePasswordCreationContextHandler<R: PasswordCreationContextRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordCreationContextRepository> CreatePasswordCreationContextHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordCreationContextRepository + 'static> CommandHandler<CreatePasswordCreationContextCommand> for CreatePasswordCreationContextHandler<R> {
    type Output = PasswordCreationContext;

    async fn handle(&self, cmd: CreatePasswordCreationContextCommand) -> Result<Self::Output> {
        let entity = PasswordCreationContext::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .created_via(cmd.created_via)
            .ip_address(cmd.ip_address)
            .user_agent(cmd.user_agent)
            .reason(cmd.reason)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_creation_context
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordCreationContextCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub created_via: Option<PasswordCreationMethod>,
    pub ip_address: Option<String>,
    pub user_agent: Option<String>,
    pub reason: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordCreationContextCommand
pub struct UpdatePasswordCreationContextHandler<R: PasswordCreationContextRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordCreationContextRepository> UpdatePasswordCreationContextHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordCreationContextRepository + 'static> CommandHandler<UpdatePasswordCreationContextCommand> for UpdatePasswordCreationContextHandler<R> {
    type Output = Option<PasswordCreationContext>;

    async fn handle(&self, cmd: UpdatePasswordCreationContextCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.created_via {
            entity.created_via = value;
        }
        entity.ip_address = cmd.ip_address;
        entity.user_agent = cmd.user_agent;
        entity.reason = cmd.reason;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_creation_context
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordCreationContextCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordCreationContextCommand
pub struct DeletePasswordCreationContextHandler<R: PasswordCreationContextRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordCreationContextRepository> DeletePasswordCreationContextHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordCreationContextRepository + 'static> CommandHandler<DeletePasswordCreationContextCommand> for DeletePasswordCreationContextHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordCreationContextCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordCreationContextCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>