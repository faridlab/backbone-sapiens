//! Commands for WorkflowActionExecution aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::WorkflowActionExecution;
use crate::infrastructure::persistence::WorkflowActionExecutionRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for WorkflowActionExecution operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait WorkflowActionExecutionCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new workflow_action_execution
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateWorkflowActionExecutionCommand {
    pub workflow_execution_id: Uuid,
    pub workflow_action_id: Uuid,
    pub action_order: i32,
    pub status: WorkflowActionExecutionStatus,
    pub input_data: Option<serde_json::Value>,
    pub output_data: Option<serde_json::Value>,
    pub started_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub failed_at: Option<DateTime<Utc>>,
    pub duration_ms: Option<i32>,
    pub error_message: Option<String>,
    pub retry_count: i32,
    pub metadata: serde_json::Value,
}

/// Handler for CreateWorkflowActionExecutionCommand
pub struct CreateWorkflowActionExecutionHandler<R: WorkflowActionExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowActionExecutionRepository> CreateWorkflowActionExecutionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowActionExecutionRepository + 'static> CommandHandler<CreateWorkflowActionExecutionCommand> for CreateWorkflowActionExecutionHandler<R> {
    type Output = WorkflowActionExecution;

    async fn handle(&self, cmd: CreateWorkflowActionExecutionCommand) -> Result<Self::Output> {
        let entity = WorkflowActionExecution::builder()
            .id(Uuid::new_v4().to_string())
            .workflow_execution_id(cmd.workflow_execution_id)
            .workflow_action_id(cmd.workflow_action_id)
            .action_order(cmd.action_order)
            .status(cmd.status)
            .input_data(cmd.input_data)
            .output_data(cmd.output_data)
            .started_at(cmd.started_at)
            .completed_at(cmd.completed_at)
            .failed_at(cmd.failed_at)
            .duration_ms(cmd.duration_ms)
            .error_message(cmd.error_message)
            .retry_count(cmd.retry_count)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing workflow_action_execution
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateWorkflowActionExecutionCommand {
    pub id: String,
    pub workflow_execution_id: Option<Uuid>,
    pub workflow_action_id: Option<Uuid>,
    pub action_order: Option<i32>,
    pub status: Option<WorkflowActionExecutionStatus>,
    pub input_data: Option<serde_json::Value>,
    pub output_data: Option<serde_json::Value>,
    pub started_at: Option<DateTime<Utc>>,
    pub completed_at: Option<DateTime<Utc>>,
    pub failed_at: Option<DateTime<Utc>>,
    pub duration_ms: Option<i32>,
    pub error_message: Option<String>,
    pub retry_count: Option<i32>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateWorkflowActionExecutionCommand
pub struct UpdateWorkflowActionExecutionHandler<R: WorkflowActionExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowActionExecutionRepository> UpdateWorkflowActionExecutionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowActionExecutionRepository + 'static> CommandHandler<UpdateWorkflowActionExecutionCommand> for UpdateWorkflowActionExecutionHandler<R> {
    type Output = Option<WorkflowActionExecution>;

    async fn handle(&self, cmd: UpdateWorkflowActionExecutionCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.workflow_execution_id {
            entity.workflow_execution_id = value;
        }
        if let Some(value) = cmd.workflow_action_id {
            entity.workflow_action_id = value;
        }
        if let Some(value) = cmd.action_order {
            entity.action_order = value;
        }
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        entity.input_data = cmd.input_data;
        entity.output_data = cmd.output_data;
        entity.started_at = cmd.started_at;
        entity.completed_at = cmd.completed_at;
        entity.failed_at = cmd.failed_at;
        entity.duration_ms = cmd.duration_ms;
        entity.error_message = cmd.error_message;
        if let Some(value) = cmd.retry_count {
            entity.retry_count = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a workflow_action_execution
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteWorkflowActionExecutionCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteWorkflowActionExecutionCommand
pub struct DeleteWorkflowActionExecutionHandler<R: WorkflowActionExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowActionExecutionRepository> DeleteWorkflowActionExecutionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowActionExecutionRepository + 'static> CommandHandler<DeleteWorkflowActionExecutionCommand> for DeleteWorkflowActionExecutionHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteWorkflowActionExecutionCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateWorkflowActionExecutionCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>