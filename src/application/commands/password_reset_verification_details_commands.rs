//! Commands for PasswordResetVerificationDetails aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::PasswordResetVerificationDetails;
use crate::infrastructure::persistence::PasswordResetVerificationDetailsRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for PasswordResetVerificationDetails operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait PasswordResetVerificationDetailsCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new password_reset_verification_details
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreatePasswordResetVerificationDetailsCommand {
    pub verified_at: Option<DateTime<Utc>>,
    pub verification_ip: Option<String>,
    pub verification_attempts: i32,
    pub last_attempt_at: Option<DateTime<Utc>>,
    pub verification_method: Option<String>,
    pub device_fingerprint: Option<String>,
    pub geolocation: Option<serde_json::Value>,
    pub metadata: serde_json::Value,
}

/// Handler for CreatePasswordResetVerificationDetailsCommand
pub struct CreatePasswordResetVerificationDetailsHandler<R: PasswordResetVerificationDetailsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetVerificationDetailsRepository> CreatePasswordResetVerificationDetailsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetVerificationDetailsRepository + 'static> CommandHandler<CreatePasswordResetVerificationDetailsCommand> for CreatePasswordResetVerificationDetailsHandler<R> {
    type Output = PasswordResetVerificationDetails;

    async fn handle(&self, cmd: CreatePasswordResetVerificationDetailsCommand) -> Result<Self::Output> {
        let entity = PasswordResetVerificationDetails::builder()
            .id(Uuid::new_v4().to_string())
            .verified_at(cmd.verified_at)
            .verification_ip(cmd.verification_ip)
            .verification_attempts(cmd.verification_attempts)
            .last_attempt_at(cmd.last_attempt_at)
            .verification_method(cmd.verification_method)
            .device_fingerprint(cmd.device_fingerprint)
            .geolocation(cmd.geolocation)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing password_reset_verification_details
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdatePasswordResetVerificationDetailsCommand {
    pub id: String,
    pub verified_at: Option<DateTime<Utc>>,
    pub verification_ip: Option<String>,
    pub verification_attempts: Option<i32>,
    pub last_attempt_at: Option<DateTime<Utc>>,
    pub verification_method: Option<String>,
    pub device_fingerprint: Option<String>,
    pub geolocation: Option<serde_json::Value>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdatePasswordResetVerificationDetailsCommand
pub struct UpdatePasswordResetVerificationDetailsHandler<R: PasswordResetVerificationDetailsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetVerificationDetailsRepository> UpdatePasswordResetVerificationDetailsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetVerificationDetailsRepository + 'static> CommandHandler<UpdatePasswordResetVerificationDetailsCommand> for UpdatePasswordResetVerificationDetailsHandler<R> {
    type Output = Option<PasswordResetVerificationDetails>;

    async fn handle(&self, cmd: UpdatePasswordResetVerificationDetailsCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        entity.verified_at = cmd.verified_at;
        entity.verification_ip = cmd.verification_ip;
        if let Some(value) = cmd.verification_attempts {
            entity.verification_attempts = value;
        }
        entity.last_attempt_at = cmd.last_attempt_at;
        entity.verification_method = cmd.verification_method;
        entity.device_fingerprint = cmd.device_fingerprint;
        entity.geolocation = cmd.geolocation;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a password_reset_verification_details
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeletePasswordResetVerificationDetailsCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeletePasswordResetVerificationDetailsCommand
pub struct DeletePasswordResetVerificationDetailsHandler<R: PasswordResetVerificationDetailsRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetVerificationDetailsRepository> DeletePasswordResetVerificationDetailsHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetVerificationDetailsRepository + 'static> CommandHandler<DeletePasswordResetVerificationDetailsCommand> for DeletePasswordResetVerificationDetailsHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeletePasswordResetVerificationDetailsCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivatePasswordResetVerificationDetailsCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>