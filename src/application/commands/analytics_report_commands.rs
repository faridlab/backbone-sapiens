//! Commands for AnalyticsReport aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::AnalyticsReport;
use crate::infrastructure::persistence::AnalyticsReportRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for AnalyticsReport operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait AnalyticsReportCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new analytics_report
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateAnalyticsReportCommand {
    pub report_name: String,
    pub report_type: AnalyticsReportType,
    pub description: Option<String>,
    pub parameters: Option<serde_json::Value>,
    pub format: ReportFormat,
    pub generated_by: Uuid,
    pub generated_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub file_path: Option<String>,
    pub file_size: Option<i32>,
    pub status: ReportStatus,
    pub download_count: i32,
    pub metadata: serde_json::Value,
}

/// Handler for CreateAnalyticsReportCommand
pub struct CreateAnalyticsReportHandler<R: AnalyticsReportRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsReportRepository> CreateAnalyticsReportHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsReportRepository + 'static> CommandHandler<CreateAnalyticsReportCommand> for CreateAnalyticsReportHandler<R> {
    type Output = AnalyticsReport;

    async fn handle(&self, cmd: CreateAnalyticsReportCommand) -> Result<Self::Output> {
        let entity = AnalyticsReport::builder()
            .id(Uuid::new_v4().to_string())
            .report_name(cmd.report_name)
            .report_type(cmd.report_type)
            .description(cmd.description)
            .parameters(cmd.parameters)
            .format(cmd.format)
            .generated_by(cmd.generated_by)
            .generated_at(cmd.generated_at)
            .expires_at(cmd.expires_at)
            .file_path(cmd.file_path)
            .file_size(cmd.file_size)
            .status(cmd.status)
            .download_count(cmd.download_count)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing analytics_report
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateAnalyticsReportCommand {
    pub id: String,
    pub report_name: Option<String>,
    pub report_type: Option<AnalyticsReportType>,
    pub description: Option<String>,
    pub parameters: Option<serde_json::Value>,
    pub format: Option<ReportFormat>,
    pub generated_by: Option<Uuid>,
    pub generated_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub file_path: Option<String>,
    pub file_size: Option<i32>,
    pub status: Option<ReportStatus>,
    pub download_count: Option<i32>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateAnalyticsReportCommand
pub struct UpdateAnalyticsReportHandler<R: AnalyticsReportRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsReportRepository> UpdateAnalyticsReportHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsReportRepository + 'static> CommandHandler<UpdateAnalyticsReportCommand> for UpdateAnalyticsReportHandler<R> {
    type Output = Option<AnalyticsReport>;

    async fn handle(&self, cmd: UpdateAnalyticsReportCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.report_name {
            entity.report_name = value;
        }
        if let Some(value) = cmd.report_type {
            entity.report_type = value;
        }
        entity.description = cmd.description;
        entity.parameters = cmd.parameters;
        if let Some(value) = cmd.format {
            entity.format = value;
        }
        if let Some(value) = cmd.generated_by {
            entity.generated_by = value;
        }
        entity.generated_at = cmd.generated_at;
        entity.expires_at = cmd.expires_at;
        entity.file_path = cmd.file_path;
        entity.file_size = cmd.file_size;
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        if let Some(value) = cmd.download_count {
            entity.download_count = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a analytics_report
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteAnalyticsReportCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteAnalyticsReportCommand
pub struct DeleteAnalyticsReportHandler<R: AnalyticsReportRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsReportRepository> DeleteAnalyticsReportHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsReportRepository + 'static> CommandHandler<DeleteAnalyticsReportCommand> for DeleteAnalyticsReportHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteAnalyticsReportCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateAnalyticsReportCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>