//! Commands for NotificationPreference aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::NotificationPreference;
use crate::infrastructure::persistence::NotificationPreferenceRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for NotificationPreference operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait NotificationPreferenceCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new notification_preference
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateNotificationPreferenceCommand {
    pub user_id: Uuid,
    pub notification_type: String,
    pub channel_enabled: bool,
    pub channels: serde_json::Value,
    pub quiet_hours_start: Option<String>,
    pub quiet_hours_end: Option<String>,
    pub quiet_timezone: String,
    pub digest_enabled: bool,
    pub digest_frequency: DigestFrequency,
    pub enabled: bool,
    pub metadata: serde_json::Value,
}

/// Handler for CreateNotificationPreferenceCommand
pub struct CreateNotificationPreferenceHandler<R: NotificationPreferenceRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationPreferenceRepository> CreateNotificationPreferenceHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationPreferenceRepository + 'static> CommandHandler<CreateNotificationPreferenceCommand> for CreateNotificationPreferenceHandler<R> {
    type Output = NotificationPreference;

    async fn handle(&self, cmd: CreateNotificationPreferenceCommand) -> Result<Self::Output> {
        let entity = NotificationPreference::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .notification_type(cmd.notification_type)
            .channel_enabled(cmd.channel_enabled)
            .channels(cmd.channels)
            .quiet_hours_start(cmd.quiet_hours_start)
            .quiet_hours_end(cmd.quiet_hours_end)
            .quiet_timezone(cmd.quiet_timezone)
            .digest_enabled(cmd.digest_enabled)
            .digest_frequency(cmd.digest_frequency)
            .enabled(cmd.enabled)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing notification_preference
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateNotificationPreferenceCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub notification_type: Option<String>,
    pub channel_enabled: Option<bool>,
    pub channels: Option<serde_json::Value>,
    pub quiet_hours_start: Option<String>,
    pub quiet_hours_end: Option<String>,
    pub quiet_timezone: Option<String>,
    pub digest_enabled: Option<bool>,
    pub digest_frequency: Option<DigestFrequency>,
    pub enabled: Option<bool>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateNotificationPreferenceCommand
pub struct UpdateNotificationPreferenceHandler<R: NotificationPreferenceRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationPreferenceRepository> UpdateNotificationPreferenceHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationPreferenceRepository + 'static> CommandHandler<UpdateNotificationPreferenceCommand> for UpdateNotificationPreferenceHandler<R> {
    type Output = Option<NotificationPreference>;

    async fn handle(&self, cmd: UpdateNotificationPreferenceCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.notification_type {
            entity.notification_type = value;
        }
        if let Some(value) = cmd.channel_enabled {
            entity.channel_enabled = value;
        }
        if let Some(value) = cmd.channels {
            entity.channels = value;
        }
        entity.quiet_hours_start = cmd.quiet_hours_start;
        entity.quiet_hours_end = cmd.quiet_hours_end;
        if let Some(value) = cmd.quiet_timezone {
            entity.quiet_timezone = value;
        }
        if let Some(value) = cmd.digest_enabled {
            entity.digest_enabled = value;
        }
        if let Some(value) = cmd.digest_frequency {
            entity.digest_frequency = value;
        }
        if let Some(value) = cmd.enabled {
            entity.enabled = value;
        }
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a notification_preference
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteNotificationPreferenceCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteNotificationPreferenceCommand
pub struct DeleteNotificationPreferenceHandler<R: NotificationPreferenceRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationPreferenceRepository> DeleteNotificationPreferenceHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationPreferenceRepository + 'static> CommandHandler<DeleteNotificationPreferenceCommand> for DeleteNotificationPreferenceHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteNotificationPreferenceCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateNotificationPreferenceCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>