//! Commands for DirectPermissionGrant aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Commands represent intent to change state.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};
use uuid::Uuid;

use crate::domain::entity::DirectPermissionGrant;
use crate::infrastructure::persistence::DirectPermissionGrantRepository;
use super::CommandHandler;

// ============================================================================
// COMMAND TRAIT (DEPRECATED)
// ============================================================================

/// Command trait for DirectPermissionGrant operations
///
/// **DEPRECATED**: Use the generic `CommandHandler<C>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use CommandHandler<C> trait instead")]
#[async_trait]
pub trait DirectPermissionGrantCommand: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// CREATE COMMAND
// ============================================================================

/// Command to create a new direct_permission_grant
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct CreateDirectPermissionGrantCommand {
    pub user_id: Uuid,
    pub permission_id: Uuid,
    pub granted_by: Uuid,
    pub granted_at: DateTime<Utc>,
    pub expires_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub scope: Option<serde_json::Value>,
    pub conditions: Option<serde_json::Value>,
    pub last_used_at: Option<DateTime<Utc>>,
    pub usage_count: i32,
    pub access_log: Option<serde_json::Value>,
    pub status: DirectPermissionGrantStatus,
    pub revoked_at: Option<DateTime<Utc>>,
    pub revoked_by: Option<Uuid>,
    pub revocation_reason: Option<String>,
    pub metadata: serde_json::Value,
}

/// Handler for CreateDirectPermissionGrantCommand
pub struct CreateDirectPermissionGrantHandler<R: DirectPermissionGrantRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DirectPermissionGrantRepository> CreateDirectPermissionGrantHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DirectPermissionGrantRepository + 'static> CommandHandler<CreateDirectPermissionGrantCommand> for CreateDirectPermissionGrantHandler<R> {
    type Output = DirectPermissionGrant;

    async fn handle(&self, cmd: CreateDirectPermissionGrantCommand) -> Result<Self::Output> {
        let entity = DirectPermissionGrant::builder()
            .id(Uuid::new_v4().to_string())
            .user_id(cmd.user_id)
            .permission_id(cmd.permission_id)
            .granted_by(cmd.granted_by)
            .granted_at(cmd.granted_at)
            .expires_at(cmd.expires_at)
            .reason(cmd.reason)
            .scope(cmd.scope)
            .conditions(cmd.conditions)
            .last_used_at(cmd.last_used_at)
            .usage_count(cmd.usage_count)
            .access_log(cmd.access_log)
            .status(cmd.status)
            .revoked_at(cmd.revoked_at)
            .revoked_by(cmd.revoked_by)
            .revocation_reason(cmd.revocation_reason)
            .metadata(cmd.metadata)
            .build()?;

        self.repository.save(&entity).await
    }
}

// ============================================================================
// UPDATE COMMAND
// ============================================================================

/// Command to update an existing direct_permission_grant
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct UpdateDirectPermissionGrantCommand {
    pub id: String,
    pub user_id: Option<Uuid>,
    pub permission_id: Option<Uuid>,
    pub granted_by: Option<Uuid>,
    pub granted_at: Option<DateTime<Utc>>,
    pub expires_at: Option<DateTime<Utc>>,
    pub reason: Option<String>,
    pub scope: Option<serde_json::Value>,
    pub conditions: Option<serde_json::Value>,
    pub last_used_at: Option<DateTime<Utc>>,
    pub usage_count: Option<i32>,
    pub access_log: Option<serde_json::Value>,
    pub status: Option<DirectPermissionGrantStatus>,
    pub revoked_at: Option<DateTime<Utc>>,
    pub revoked_by: Option<Uuid>,
    pub revocation_reason: Option<String>,
    pub metadata: Option<serde_json::Value>,
}

/// Handler for UpdateDirectPermissionGrantCommand
pub struct UpdateDirectPermissionGrantHandler<R: DirectPermissionGrantRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DirectPermissionGrantRepository> UpdateDirectPermissionGrantHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DirectPermissionGrantRepository + 'static> CommandHandler<UpdateDirectPermissionGrantCommand> for UpdateDirectPermissionGrantHandler<R> {
    type Output = Option<DirectPermissionGrant>;

    async fn handle(&self, cmd: UpdateDirectPermissionGrantCommand) -> Result<Self::Output> {
        let existing = self.repository.find_by_id(&cmd.id).await?;

        let Some(mut entity) = existing else {
            return Ok(None);
        };

        // Apply updates
        if let Some(value) = cmd.user_id {
            entity.user_id = value;
        }
        if let Some(value) = cmd.permission_id {
            entity.permission_id = value;
        }
        if let Some(value) = cmd.granted_by {
            entity.granted_by = value;
        }
        if let Some(value) = cmd.granted_at {
            entity.granted_at = value;
        }
        entity.expires_at = cmd.expires_at;
        entity.reason = cmd.reason;
        entity.scope = cmd.scope;
        entity.conditions = cmd.conditions;
        entity.last_used_at = cmd.last_used_at;
        if let Some(value) = cmd.usage_count {
            entity.usage_count = value;
        }
        entity.access_log = cmd.access_log;
        if let Some(value) = cmd.status {
            entity.status = value;
        }
        entity.revoked_at = cmd.revoked_at;
        entity.revoked_by = cmd.revoked_by;
        entity.revocation_reason = cmd.revocation_reason;
        if let Some(value) = cmd.metadata {
            entity.metadata = value;
        }

        self.repository.update(&cmd.id, &entity).await
    }
}

// ============================================================================
// DELETE COMMAND
// ============================================================================

/// Command to delete a direct_permission_grant
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DeleteDirectPermissionGrantCommand {
    pub id: String,
    pub hard_delete: bool,
}

/// Handler for DeleteDirectPermissionGrantCommand
pub struct DeleteDirectPermissionGrantHandler<R: DirectPermissionGrantRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DirectPermissionGrantRepository> DeleteDirectPermissionGrantHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DirectPermissionGrantRepository + 'static> CommandHandler<DeleteDirectPermissionGrantCommand> for DeleteDirectPermissionGrantHandler<R> {
    type Output = bool;

    async fn handle(&self, cmd: DeleteDirectPermissionGrantCommand) -> Result<Self::Output> {
        if cmd.hard_delete {
            self.repository.delete(&cmd.id).await
        } else {
            self.repository.soft_delete(&cmd.id).await
        }
    }
}

// ============================================================================
// CUSTOM COMMANDS
// ============================================================================
// <<< CUSTOM COMMANDS START >>>

// <<< CUSTOM COMMANDS START >>>
// Add your custom commands here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct ActivateDirectPermissionGrantCommand {
//     pub id: String,
// }
// <<< CUSTOM COMMANDS END >>>