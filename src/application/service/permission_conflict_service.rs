//! Application service for PermissionConflict entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Type alias over `GenericCrudService` — all CRUD logic lives in backbone-core.
//! For custom behaviour, create `permission_conflict_service_custom.rs` (preserved on regen).

use backbone_core::GenericCrudService;

use crate::presentation::dto::{CreatePermissionConflictDto, UpdatePermissionConflictDto};
use crate::domain::entity::PermissionConflict;
use crate::infrastructure::persistence::PermissionConflictRepository;

/// Application service for PermissionConflict entities.
///
/// All 12 standard CRUD operations (list, create, get, update, patch,
/// soft_delete, restore, empty_trash, bulk_create, upsert, find_by_id,
/// list_deleted) are provided by `GenericCrudService`.
///
/// Custom extensions go in `permission_conflict_service_custom.rs`:
///
/// ```rust,ignore
/// pub struct PermissionConflictServiceCustom {
///     inner: Arc<PermissionConflictService>,
///     // add domain-specific dependencies here
/// }
/// ```
pub type PermissionConflictService = GenericCrudService<
    PermissionConflict,
    CreatePermissionConflictDto,
    UpdatePermissionConflictDto,
    PermissionConflictRepository,
>;

// <<< CUSTOM
// END CUSTOM
