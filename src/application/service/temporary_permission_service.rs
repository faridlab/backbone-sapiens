//! Application service for TemporaryPermission entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Type alias over `GenericCrudService` — all CRUD logic lives in backbone-core.
//! For custom behaviour, create `temporary_permission_service_custom.rs` (preserved on regen).

use backbone_core::GenericCrudService;

use crate::presentation::dto::{CreateTemporaryPermissionDto, UpdateTemporaryPermissionDto};
use crate::domain::entity::TemporaryPermission;
use crate::infrastructure::persistence::TemporaryPermissionRepository;

/// Application service for TemporaryPermission entities.
///
/// All 12 standard CRUD operations (list, create, get, update, patch,
/// soft_delete, restore, empty_trash, bulk_create, upsert, find_by_id,
/// list_deleted) are provided by `GenericCrudService`.
///
/// Custom extensions go in `temporary_permission_service_custom.rs`:
///
/// ```rust,ignore
/// pub struct TemporaryPermissionServiceCustom {
///     inner: Arc<TemporaryPermissionService>,
///     // add domain-specific dependencies here
/// }
/// ```
pub type TemporaryPermissionService = GenericCrudService<
    TemporaryPermission,
    CreateTemporaryPermissionDto,
    UpdateTemporaryPermissionDto,
    TemporaryPermissionRepository,
>;

// <<< CUSTOM
// END CUSTOM
