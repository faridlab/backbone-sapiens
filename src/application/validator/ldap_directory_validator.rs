//! Validator for LDAPDirectory entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<LDAPDirectory>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{NonNegative, RequiredString};
use crate::domain::entity::LDAPDirectory;

/// Validator type alias for LDAPDirectory entities.
pub type LDAPDirectoryValidator = EntityValidator<LDAPDirectory>;

/// Build a validator for LDAPDirectory with all schema-defined field rules.
pub fn ldap_directory_validator() -> LDAPDirectoryValidator {
    EntityValidator::new()
        .rule(RequiredString::new("name", |e: &LDAPDirectory| &e.name))
        .rule(RequiredString::new("display_name", |e: &LDAPDirectory| &e.display_name))
        .rule(RequiredString::new("host", |e: &LDAPDirectory| &e.host))
        .rule(RequiredString::new("bind_dn", |e: &LDAPDirectory| &e.bind_dn))
        .rule(RequiredString::new("bind_password", |e: &LDAPDirectory| &e.bind_password))
        .rule(RequiredString::new("search_base", |e: &LDAPDirectory| &e.search_base))
        .rule(RequiredString::new("search_filter", |e: &LDAPDirectory| &e.search_filter))
    // <<< CUSTOM RULES
    // hook rule: name_required (implement in CUSTOM zone)
    // hook rule: host_required (implement in CUSTOM zone)
    // hook rule: port_valid (implement in CUSTOM zone)
    // hook rule: base_dn_required (implement in CUSTOM zone)
    // hook rule: bind_dn_valid (implement in CUSTOM zone)
    // hook rule: user_search_filter_valid (implement in CUSTOM zone)
    // hook rule: use_tls_or_ssl (implement in CUSTOM zone)
    // hook rule: sync_interval_positive (implement in CUSTOM zone)
    // hook rule: max_sync_interval (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM