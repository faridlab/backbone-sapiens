//! Validator for Session entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<Session>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{OptionalNotBlank, RequiredString, RequiredUuid};
use crate::domain::entity::Session;

/// Validator type alias for Session entities.
pub type SessionValidator = EntityValidator<Session>;

/// Build a validator for Session with all schema-defined field rules.
pub fn session_validator() -> SessionValidator {
    EntityValidator::new()
        .rule(RequiredString::new("token_hash", |e: &Session| &e.token_hash))
        .rule(OptionalNotBlank::new("user_agent", |e: &Session| e.user_agent.as_deref()))
        .rule(OptionalNotBlank::new("device_fingerprint", |e: &Session| e.device_fingerprint.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: unique_token_hash (implement in CUSTOM zone)
    // hook rule: expires_in_future (implement in CUSTOM zone)
    // hook rule: expiry_after_creation (implement in CUSTOM zone)
    // hook rule: no_modify_revoked (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM