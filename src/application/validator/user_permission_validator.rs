//! Validator for UserPermission entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<UserPermission>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{NonNegative, OptionalNotBlank, RequiredString, RequiredUuid};
use crate::domain::entity::UserPermission;

/// Validator type alias for UserPermission entities.
pub type UserPermissionValidator = EntityValidator<UserPermission>;

/// Build a validator for UserPermission with all schema-defined field rules.
pub fn user_permission_validator() -> UserPermissionValidator {
    EntityValidator::new()
        .rule(RequiredString::new("reason", |e: &UserPermission| &e.reason))
        .rule(OptionalNotBlank::new("resource_id", |e: &UserPermission| e.resource_id.as_deref()))
        .rule(OptionalNotBlank::new("resource_type", |e: &UserPermission| e.resource_type.as_deref()))
        .rule(OptionalNotBlank::new("revoked_reason", |e: &UserPermission| e.revoked_reason.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: reason_required (implement in CUSTOM zone)
    // hook rule: expires_in_future (implement in CUSTOM zone)
    // hook rule: no_self_grant (implement in CUSTOM zone)
    // hook rule: no_duplicate_grant (implement in CUSTOM zone)
    // hook rule: resource_scope_consistency (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM