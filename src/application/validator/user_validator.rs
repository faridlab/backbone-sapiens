//! Validator for User entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<User>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{NonNegative, RequiredString};
use crate::domain::entity::User;

/// Validator type alias for User entities.
pub type UserValidator = EntityValidator<User>;

/// Build a validator for User with all schema-defined field rules.
pub fn user_validator() -> UserValidator {
    EntityValidator::new()
        .rule(RequiredString::new("username", |e: &User| &e.username))
        .rule(RequiredString::new("email", |e: &User| &e.email))
        .rule(RequiredString::new("password_hash", |e: &User| &e.password_hash))
    // <<< CUSTOM RULES
    // hook rule: username_format (implement in CUSTOM zone)
    // hook rule: username_lowercase_check (implement in CUSTOM zone)
    // hook rule: unique_email (implement in CUSTOM zone)
    // hook rule: unique_username (implement in CUSTOM zone)
    // hook rule: password_strength (implement in CUSTOM zone)
    // hook rule: account_not_locked (implement in CUSTOM zone)
    // hook rule: max_failed_attempts (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM