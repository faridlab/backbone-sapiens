//! Validator for Workflow entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<Workflow>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{NonNegative, OptionalNotBlank, RequiredString, RequiredUuid};
use crate::domain::entity::Workflow;

/// Validator type alias for Workflow entities.
pub type WorkflowValidator = EntityValidator<Workflow>;

/// Build a validator for Workflow with all schema-defined field rules.
pub fn workflow_validator() -> WorkflowValidator {
    EntityValidator::new()
        .rule(RequiredString::new("title", |e: &Workflow| &e.title))
        .rule(OptionalNotBlank::new("description", |e: &Workflow| e.description.as_deref()))
        .rule(OptionalNotBlank::new("error_message", |e: &Workflow| e.error_message.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: valid_workflow_type (implement in CUSTOM zone)
    // hook rule: valid_trigger_type (implement in CUSTOM zone)
    // hook rule: valid_status_transition (implement in CUSTOM zone)
    // hook rule: valid_execution_status_transition (implement in CUSTOM zone)
    // hook rule: valid_action_type (implement in CUSTOM zone)
    // hook rule: reasonable_timeout (implement in CUSTOM zone)
    // hook rule: valid_step_sequence (implement in CUSTOM zone)
    // hook rule: valid_action_order (implement in CUSTOM zone)
    // hook rule: valid_schedule (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM