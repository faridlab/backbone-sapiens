//! Validator for Profile entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<Profile>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{OptionalNotBlank, RequiredString};
use crate::domain::entity::Profile;

/// Validator type alias for Profile entities.
pub type ProfileValidator = EntityValidator<Profile>;

/// Build a validator for Profile with all schema-defined field rules.
pub fn profile_validator() -> ProfileValidator {
    EntityValidator::new()
        .rule(RequiredString::new("first_name", |e: &Profile| &e.first_name))
        .rule(OptionalNotBlank::new("middle_name", |e: &Profile| e.middle_name.as_deref()))
        .rule(OptionalNotBlank::new("last_name", |e: &Profile| e.last_name.as_deref()))
        .rule(OptionalNotBlank::new("pob", |e: &Profile| e.pob.as_deref()))
        .rule(OptionalNotBlank::new("profile_picture_url", |e: &Profile| e.profile_picture_url.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: first_name_required (implement in CUSTOM zone)
    // hook rule: phone_number_format (implement in CUSTOM zone)
    // hook rule: profile_picture_url_valid (implement in CUSTOM zone)
    // hook rule: user_exists (implement in CUSTOM zone)
    // hook rule: unique_profile_per_user (implement in CUSTOM zone)
    // hook rule: valid_gender (implement in CUSTOM zone)
    // hook rule: dob_in_past (implement in CUSTOM zone)
    // hook rule: reasonable_age (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM