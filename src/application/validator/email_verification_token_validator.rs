//! Validator for EmailVerificationToken entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<EmailVerificationToken>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{NonNegative, OptionalNotBlank, RequiredString, RequiredUuid};
use crate::domain::entity::EmailVerificationToken;

/// Validator type alias for EmailVerificationToken entities.
pub type EmailVerificationTokenValidator = EntityValidator<EmailVerificationToken>;

/// Build a validator for EmailVerificationToken with all schema-defined field rules.
pub fn email_verification_token_validator() -> EmailVerificationTokenValidator {
    EntityValidator::new()
        .rule(RequiredString::new("token", |e: &EmailVerificationToken| &e.token))
        .rule(RequiredString::new("email", |e: &EmailVerificationToken| &e.email))
        .rule(OptionalNotBlank::new("old_email", |e: &EmailVerificationToken| e.old_email.as_deref()))
        .rule(OptionalNotBlank::new("user_agent", |e: &EmailVerificationToken| e.user_agent.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: unique_pending_token (implement in CUSTOM zone)
    // hook rule: valid_email (implement in CUSTOM zone)
    // hook rule: valid_expiry (implement in CUSTOM zone)
    // hook rule: token_format (implement in CUSTOM zone)
    // hook rule: token_type_valid (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM