//! Validator for Role entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<Role>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{OptionalNotBlank, RequiredString};
use crate::domain::entity::Role;

/// Validator type alias for Role entities.
pub type RoleValidator = EntityValidator<Role>;

/// Build a validator for Role with all schema-defined field rules.
pub fn role_validator() -> RoleValidator {
    EntityValidator::new()
        .rule(RequiredString::new("name", |e: &Role| &e.name))
        .rule(OptionalNotBlank::new("description", |e: &Role| e.description.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: name_uppercase (implement in CUSTOM zone)
    // hook rule: unique_name (implement in CUSTOM zone)
    // hook rule: single_default_role (implement in CUSTOM zone)
    // hook rule: no_users_on_delete (implement in CUSTOM zone)
    // hook rule: reserved_names (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM