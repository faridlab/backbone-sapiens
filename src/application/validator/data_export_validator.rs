//! Validator for DataExport entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<DataExport>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{NonNegative, OptionalNotBlank, RequiredUuid};
use crate::domain::entity::DataExport;

/// Validator type alias for DataExport entities.
pub type DataExportValidator = EntityValidator<DataExport>;

/// Build a validator for DataExport with all schema-defined field rules.
pub fn data_export_validator() -> DataExportValidator {
    EntityValidator::new()
        .rule(OptionalNotBlank::new("file_path", |e: &DataExport| e.file_path.as_deref()))
        .rule(OptionalNotBlank::new("file_url", |e: &DataExport| e.file_url.as_deref()))
        .rule(OptionalNotBlank::new("justification", |e: &DataExport| e.justification.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: user_can_request_own_data (implement in CUSTOM zone)
    // hook rule: admin_requires_justification (implement in CUSTOM zone)
    // hook rule: export_format_valid (implement in CUSTOM zone)
    // hook rule: expires_after_completion (implement in CUSTOM zone)
    // hook rule: max_retention_30_days (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM