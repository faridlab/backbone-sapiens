//! Validator for TemporaryPermission entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<TemporaryPermission>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{OptionalNotBlank, RequiredUuid};
use crate::domain::entity::TemporaryPermission;

/// Validator type alias for TemporaryPermission entities.
pub type TemporaryPermissionValidator = EntityValidator<TemporaryPermission>;

/// Build a validator for TemporaryPermission with all schema-defined field rules.
pub fn temporary_permission_validator() -> TemporaryPermissionValidator {
    EntityValidator::new()
        .rule(OptionalNotBlank::new("reason", |e: &TemporaryPermission| e.reason.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: expires_after_granted (implement in CUSTOM zone)
    // hook rule: max_duration_30_days (implement in CUSTOM zone)
    // hook rule: reason_required (implement in CUSTOM zone)
    // hook rule: cannot_modify_revoked (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM