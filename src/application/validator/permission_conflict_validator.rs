//! Validator for PermissionConflict entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<PermissionConflict>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{OptionalNotBlank, RequiredString, RequiredUuid};
use crate::domain::entity::PermissionConflict;

/// Validator type alias for PermissionConflict entities.
pub type PermissionConflictValidator = EntityValidator<PermissionConflict>;

/// Build a validator for PermissionConflict with all schema-defined field rules.
pub fn permission_conflict_validator() -> PermissionConflictValidator {
    EntityValidator::new()
        .rule(RequiredString::new("conflict_type", |e: &PermissionConflict| &e.conflict_type))
        .rule(OptionalNotBlank::new("description", |e: &PermissionConflict| e.description.as_deref()))
    // <<< CUSTOM RULES
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM
