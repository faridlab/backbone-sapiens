//! Validator for DeviceTrust entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<DeviceTrust>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{NonNegative, RequiredString, RequiredUuid};
use crate::domain::entity::DeviceTrust;

/// Validator type alias for DeviceTrust entities.
pub type DeviceTrustValidator = EntityValidator<DeviceTrust>;

/// Build a validator for DeviceTrust with all schema-defined field rules.
pub fn device_trust_validator() -> DeviceTrustValidator {
    EntityValidator::new()
        .rule(RequiredString::new("device_fingerprint", |e: &DeviceTrust| &e.device_fingerprint))
        .rule(RequiredString::new("device_name", |e: &DeviceTrust| &e.device_name))
    // <<< CUSTOM RULES
    // hook rule: unique_device_per_user (implement in CUSTOM zone)
    // hook rule: unique_device_name_per_user (implement in CUSTOM zone)
    // hook rule: valid_risk_score (implement in CUSTOM zone)
    // hook rule: valid_trust_level (implement in CUSTOM zone)
    // hook rule: valid_device_type (implement in CUSTOM zone)
    // hook rule: reasonable_expiry (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM