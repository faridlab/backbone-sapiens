//! Validator for SecurityEvent entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<SecurityEvent>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{OptionalNotBlank};
use crate::domain::entity::SecurityEvent;

/// Validator type alias for SecurityEvent entities.
pub type SecurityEventValidator = EntityValidator<SecurityEvent>;

/// Build a validator for SecurityEvent with all schema-defined field rules.
pub fn security_event_validator() -> SecurityEventValidator {
    EntityValidator::new()
        .rule(OptionalNotBlank::new("ip_address", |e: &SecurityEvent| e.ip_address.as_deref()))
        .rule(OptionalNotBlank::new("user_agent", |e: &SecurityEvent| e.user_agent.as_deref()))
        .rule(OptionalNotBlank::new("description", |e: &SecurityEvent| e.description.as_deref()))
        .rule(OptionalNotBlank::new("resolution_notes", |e: &SecurityEvent| e.resolution_notes.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: event_type_required (implement in CUSTOM zone)
    // hook rule: severity_required (implement in CUSTOM zone)
    // hook rule: resolution_notes_for_resolve (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM