//! Validator for ResourcePermission entity
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Returns an `EntityValidator<ResourcePermission>` pre-loaded with schema-derived
//! field rules. Extend in the `// <<< CUSTOM` zone.

use backbone_core::{EntityValidator, ValidationErrors, ValidationError};
use backbone_core::{OptionalNotBlank, RequiredString, RequiredUuid};
use crate::domain::entity::ResourcePermission;

/// Validator type alias for ResourcePermission entities.
pub type ResourcePermissionValidator = EntityValidator<ResourcePermission>;

/// Build a validator for ResourcePermission with all schema-defined field rules.
pub fn resource_permission_validator() -> ResourcePermissionValidator {
    EntityValidator::new()
        .rule(RequiredString::new("resource_type", |e: &ResourcePermission| &e.resource_type))
        .rule(RequiredString::new("resource_id", |e: &ResourcePermission| &e.resource_id))
        .rule(OptionalNotBlank::new("reason", |e: &ResourcePermission| e.reason.as_deref()))
    // <<< CUSTOM RULES
    // hook rule: grant_to_user_or_role (implement in CUSTOM zone)
    // hook rule: resource_type_required (implement in CUSTOM zone)
    // hook rule: resource_id_required (implement in CUSTOM zone)
    // hook rule: expires_in_future (implement in CUSTOM zone)
    // hook rule: max_duration_by_type (implement in CUSTOM zone)
    // hook rule: no_duplicate_grants (implement in CUSTOM zone)
    // END CUSTOM RULES
}

// <<< CUSTOM
// END CUSTOM