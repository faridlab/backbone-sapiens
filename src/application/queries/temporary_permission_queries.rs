//! Queries for TemporaryPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::TemporaryPermission;
use crate::infrastructure::persistence::{TemporaryPermissionRepository, PaginationParams, PaginatedResult, TemporaryPermissionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for TemporaryPermission operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait TemporaryPermissionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a temporary_permission by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetTemporaryPermissionByIdQuery {
    pub id: String,
}

/// Handler for GetTemporaryPermissionByIdQuery
pub struct GetTemporaryPermissionByIdHandler<R: TemporaryPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: TemporaryPermissionRepository> GetTemporaryPermissionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: TemporaryPermissionRepository + 'static> QueryHandler<GetTemporaryPermissionByIdQuery> for GetTemporaryPermissionByIdHandler<R> {
    type Output = Option<TemporaryPermission>;

    async fn handle(&self, query: GetTemporaryPermissionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list temporary_permission entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListTemporaryPermissionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_permission_id: Option<Uuid>,
    pub filter_granted_by: Option<Uuid>,
    pub filter_revoked_by: Option<Uuid>,
    pub filter_reason: Option<String>,
    pub filter_notified_before_expiry: Option<bool>,
    pub filter_status: Option<TemporaryPermissionStatus>,
}

impl Default for ListTemporaryPermissionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_permission_id: None,
            filter_granted_by: None,
            filter_revoked_by: None,
            filter_reason: None,
            filter_notified_before_expiry: None,
            filter_status: None,
        }
    }
}

/// Handler for ListTemporaryPermissionQuery
pub struct ListTemporaryPermissionHandler<R: TemporaryPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: TemporaryPermissionRepository> ListTemporaryPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: TemporaryPermissionRepository + 'static> QueryHandler<ListTemporaryPermissionQuery> for ListTemporaryPermissionHandler<R> {
    type Output = PaginatedResult<TemporaryPermission>;

    async fn handle(&self, query: ListTemporaryPermissionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = TemporaryPermissionFilter {
            user_id: query.filter_user_id.clone(),
            permission_id: query.filter_permission_id.clone(),
            granted_by: query.filter_granted_by.clone(),
            revoked_by: query.filter_revoked_by.clone(),
            reason: query.filter_reason.clone(),
            notified_before_expiry: query.filter_notified_before_expiry.clone(),
            status: query.filter_status.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveTemporaryPermissionQuery;
// <<< CUSTOM QUERIES END >>>