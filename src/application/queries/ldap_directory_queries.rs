//! Queries for LDAPDirectory aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::LDAPDirectory;
use crate::infrastructure::persistence::{LDAPDirectoryRepository, PaginationParams, PaginatedResult, LDAPDirectoryFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for LDAPDirectory operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait LDAPDirectoryQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a ldap_directory by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetLDAPDirectoryByIdQuery {
    pub id: String,
}

/// Handler for GetLDAPDirectoryByIdQuery
pub struct GetLDAPDirectoryByIdHandler<R: LDAPDirectoryRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: LDAPDirectoryRepository> GetLDAPDirectoryByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: LDAPDirectoryRepository + 'static> QueryHandler<GetLDAPDirectoryByIdQuery> for GetLDAPDirectoryByIdHandler<R> {
    type Output = Option<LDAPDirectory>;

    async fn handle(&self, query: GetLDAPDirectoryByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list ldap_directory entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListLDAPDirectoryQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_name: Option<String>,
    pub filter_display_name: Option<String>,
    pub filter_host: Option<String>,
    pub filter_use_ssl: Option<bool>,
    pub filter_use_tls: Option<bool>,
    pub filter_bind_dn: Option<String>,
    pub filter_bind_password: Option<String>,
    pub filter_search_base: Option<String>,
    pub filter_search_filter: Option<String>,
    pub filter_sync_enabled: Option<bool>,
    pub filter_is_active: Option<bool>,
    pub filter_status: Option<LDAPDirectoryStatus>,
}

impl Default for ListLDAPDirectoryQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_name: None,
            filter_display_name: None,
            filter_host: None,
            filter_use_ssl: None,
            filter_use_tls: None,
            filter_bind_dn: None,
            filter_bind_password: None,
            filter_search_base: None,
            filter_search_filter: None,
            filter_sync_enabled: None,
            filter_is_active: None,
            filter_status: None,
        }
    }
}

/// Handler for ListLDAPDirectoryQuery
pub struct ListLDAPDirectoryHandler<R: LDAPDirectoryRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: LDAPDirectoryRepository> ListLDAPDirectoryHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: LDAPDirectoryRepository + 'static> QueryHandler<ListLDAPDirectoryQuery> for ListLDAPDirectoryHandler<R> {
    type Output = PaginatedResult<LDAPDirectory>;

    async fn handle(&self, query: ListLDAPDirectoryQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = LDAPDirectoryFilter {
            name: query.filter_name.clone(),
            display_name: query.filter_display_name.clone(),
            host: query.filter_host.clone(),
            use_ssl: query.filter_use_ssl.clone(),
            use_tls: query.filter_use_tls.clone(),
            bind_dn: query.filter_bind_dn.clone(),
            bind_password: query.filter_bind_password.clone(),
            search_base: query.filter_search_base.clone(),
            search_filter: query.filter_search_filter.clone(),
            sync_enabled: query.filter_sync_enabled.clone(),
            is_active: query.filter_is_active.clone(),
            status: query.filter_status.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find ldap_directory by name
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetLDAPDirectoryByNameQuery {
    pub name: String,
}

/// Handler for GetLDAPDirectoryByNameQuery
pub struct GetLDAPDirectoryByNameHandler<R: LDAPDirectoryRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: LDAPDirectoryRepository> GetLDAPDirectoryByNameHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: LDAPDirectoryRepository + 'static> QueryHandler<GetLDAPDirectoryByNameQuery> for GetLDAPDirectoryByNameHandler<R> {
    type Output = Option<LDAPDirectory>;

    async fn handle(&self, query: GetLDAPDirectoryByNameQuery) -> Result<Self::Output> {
        self.repository.find_by_name(&query.name).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveLDAPDirectoryQuery;
// <<< CUSTOM QUERIES END >>>