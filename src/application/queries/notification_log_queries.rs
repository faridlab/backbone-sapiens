//! Queries for NotificationLog aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::NotificationLog;
use crate::infrastructure::persistence::{NotificationLogRepository, PaginationParams, PaginatedResult, NotificationLogFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for NotificationLog operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait NotificationLogQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a notification_log by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetNotificationLogByIdQuery {
    pub id: String,
}

/// Handler for GetNotificationLogByIdQuery
pub struct GetNotificationLogByIdHandler<R: NotificationLogRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationLogRepository> GetNotificationLogByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationLogRepository + 'static> QueryHandler<GetNotificationLogByIdQuery> for GetNotificationLogByIdHandler<R> {
    type Output = Option<NotificationLog>;

    async fn handle(&self, query: GetNotificationLogByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list notification_log entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListNotificationLogQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_notification_id: Option<Uuid>,
    pub filter_template_id: Option<Uuid>,
    pub filter_recipient_type: Option<String>,
    pub filter_recipient_id: Option<Uuid>,
    pub filter_channel: Option<NotificationChannel>,
    pub filter_status: Option<NotificationLogStatus>,
    pub filter_error_message: Option<String>,
    pub filter_external_id: Option<String>,
}

impl Default for ListNotificationLogQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_notification_id: None,
            filter_template_id: None,
            filter_recipient_type: None,
            filter_recipient_id: None,
            filter_channel: None,
            filter_status: None,
            filter_error_message: None,
            filter_external_id: None,
        }
    }
}

/// Handler for ListNotificationLogQuery
pub struct ListNotificationLogHandler<R: NotificationLogRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: NotificationLogRepository> ListNotificationLogHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: NotificationLogRepository + 'static> QueryHandler<ListNotificationLogQuery> for ListNotificationLogHandler<R> {
    type Output = PaginatedResult<NotificationLog>;

    async fn handle(&self, query: ListNotificationLogQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = NotificationLogFilter {
            notification_id: query.filter_notification_id.clone(),
            template_id: query.filter_template_id.clone(),
            recipient_type: query.filter_recipient_type.clone(),
            recipient_id: query.filter_recipient_id.clone(),
            channel: query.filter_channel.clone(),
            status: query.filter_status.clone(),
            error_message: query.filter_error_message.clone(),
            external_id: query.filter_external_id.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveNotificationLogQuery;
// <<< CUSTOM QUERIES END >>>