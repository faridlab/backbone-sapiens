//! Queries for PasswordResetToken aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::PasswordResetToken;
use crate::infrastructure::persistence::{PasswordResetTokenRepository, PaginationParams, PaginatedResult, PasswordResetTokenFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for PasswordResetToken operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait PasswordResetTokenQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a password_reset_token by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPasswordResetTokenByIdQuery {
    pub id: String,
}

/// Handler for GetPasswordResetTokenByIdQuery
pub struct GetPasswordResetTokenByIdHandler<R: PasswordResetTokenRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetTokenRepository> GetPasswordResetTokenByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetTokenRepository + 'static> QueryHandler<GetPasswordResetTokenByIdQuery> for GetPasswordResetTokenByIdHandler<R> {
    type Output = Option<PasswordResetToken>;

    async fn handle(&self, query: GetPasswordResetTokenByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list password_reset_token entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListPasswordResetTokenQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_token_hash: Option<String>,
    pub filter_is_used: Option<bool>,
}

impl Default for ListPasswordResetTokenQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_token_hash: None,
            filter_is_used: None,
        }
    }
}

/// Handler for ListPasswordResetTokenQuery
pub struct ListPasswordResetTokenHandler<R: PasswordResetTokenRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetTokenRepository> ListPasswordResetTokenHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetTokenRepository + 'static> QueryHandler<ListPasswordResetTokenQuery> for ListPasswordResetTokenHandler<R> {
    type Output = PaginatedResult<PasswordResetToken>;

    async fn handle(&self, query: ListPasswordResetTokenQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = PasswordResetTokenFilter {
            user_id: query.filter_user_id.clone(),
            token_hash: query.filter_token_hash.clone(),
            is_used: query.filter_is_used.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find password_reset_token by token_hash
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPasswordResetTokenByTokenHashQuery {
    pub token_hash: String,
}

/// Handler for GetPasswordResetTokenByTokenHashQuery
pub struct GetPasswordResetTokenByTokenHashHandler<R: PasswordResetTokenRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetTokenRepository> GetPasswordResetTokenByTokenHashHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetTokenRepository + 'static> QueryHandler<GetPasswordResetTokenByTokenHashQuery> for GetPasswordResetTokenByTokenHashHandler<R> {
    type Output = Option<PasswordResetToken>;

    async fn handle(&self, query: GetPasswordResetTokenByTokenHashQuery) -> Result<Self::Output> {
        self.repository.find_by_token_hash(&query.token_hash).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActivePasswordResetTokenQuery;
// <<< CUSTOM QUERIES END >>>