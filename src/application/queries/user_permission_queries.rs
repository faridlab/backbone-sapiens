//! Queries for UserPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::UserPermission;
use crate::infrastructure::persistence::{UserPermissionRepository, PaginationParams, PaginatedResult, UserPermissionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for UserPermission operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait UserPermissionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a user_permission by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetUserPermissionByIdQuery {
    pub id: String,
}

/// Handler for GetUserPermissionByIdQuery
pub struct GetUserPermissionByIdHandler<R: UserPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserPermissionRepository> GetUserPermissionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserPermissionRepository + 'static> QueryHandler<GetUserPermissionByIdQuery> for GetUserPermissionByIdHandler<R> {
    type Output = Option<UserPermission>;

    async fn handle(&self, query: GetUserPermissionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list user_permission entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListUserPermissionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_permission_id: Option<Uuid>,
    pub filter_granted_by: Option<Uuid>,
    pub filter_reason: Option<String>,
    pub filter_resource_id: Option<String>,
    pub filter_resource_type: Option<String>,
    pub filter_is_active: Option<bool>,
    pub filter_revoked_by: Option<Uuid>,
    pub filter_revoked_reason: Option<String>,
}

impl Default for ListUserPermissionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_permission_id: None,
            filter_granted_by: None,
            filter_reason: None,
            filter_resource_id: None,
            filter_resource_type: None,
            filter_is_active: None,
            filter_revoked_by: None,
            filter_revoked_reason: None,
        }
    }
}

/// Handler for ListUserPermissionQuery
pub struct ListUserPermissionHandler<R: UserPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserPermissionRepository> ListUserPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserPermissionRepository + 'static> QueryHandler<ListUserPermissionQuery> for ListUserPermissionHandler<R> {
    type Output = PaginatedResult<UserPermission>;

    async fn handle(&self, query: ListUserPermissionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = UserPermissionFilter {
            user_id: query.filter_user_id.clone(),
            permission_id: query.filter_permission_id.clone(),
            granted_by: query.filter_granted_by.clone(),
            reason: query.filter_reason.clone(),
            resource_id: query.filter_resource_id.clone(),
            resource_type: query.filter_resource_type.clone(),
            is_active: query.filter_is_active.clone(),
            revoked_by: query.filter_revoked_by.clone(),
            revoked_reason: query.filter_revoked_reason.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveUserPermissionQuery;
// <<< CUSTOM QUERIES END >>>