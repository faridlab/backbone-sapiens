//! Queries for Permission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::Permission;
use crate::infrastructure::persistence::{PermissionRepository, PaginationParams, PaginatedResult, PermissionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for Permission operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait PermissionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a permission by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPermissionByIdQuery {
    pub id: String,
}

/// Handler for GetPermissionByIdQuery
pub struct GetPermissionByIdHandler<R: PermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PermissionRepository> GetPermissionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PermissionRepository + 'static> QueryHandler<GetPermissionByIdQuery> for GetPermissionByIdHandler<R> {
    type Output = Option<Permission>;

    async fn handle(&self, query: GetPermissionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list permission entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListPermissionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_name: Option<String>,
    pub filter_description: Option<String>,
    pub filter_resource: Option<String>,
    pub filter_action: Option<String>,
}

impl Default for ListPermissionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_name: None,
            filter_description: None,
            filter_resource: None,
            filter_action: None,
        }
    }
}

/// Handler for ListPermissionQuery
pub struct ListPermissionHandler<R: PermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PermissionRepository> ListPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PermissionRepository + 'static> QueryHandler<ListPermissionQuery> for ListPermissionHandler<R> {
    type Output = PaginatedResult<Permission>;

    async fn handle(&self, query: ListPermissionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = PermissionFilter {
            name: query.filter_name.clone(),
            description: query.filter_description.clone(),
            resource: query.filter_resource.clone(),
            action: query.filter_action.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find permission by name
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPermissionByNameQuery {
    pub name: String,
}

/// Handler for GetPermissionByNameQuery
pub struct GetPermissionByNameHandler<R: PermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PermissionRepository> GetPermissionByNameHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PermissionRepository + 'static> QueryHandler<GetPermissionByNameQuery> for GetPermissionByNameHandler<R> {
    type Output = Option<Permission>;

    async fn handle(&self, query: GetPermissionByNameQuery) -> Result<Self::Output> {
        self.repository.find_by_name(&query.name).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActivePermissionQuery;
// <<< CUSTOM QUERIES END >>>