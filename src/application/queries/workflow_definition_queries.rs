//! Queries for WorkflowDefinition aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::WorkflowDefinition;
use crate::infrastructure::persistence::{WorkflowDefinitionRepository, PaginationParams, PaginatedResult, WorkflowDefinitionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for WorkflowDefinition operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait WorkflowDefinitionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a workflow_definition by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetWorkflowDefinitionByIdQuery {
    pub id: String,
}

/// Handler for GetWorkflowDefinitionByIdQuery
pub struct GetWorkflowDefinitionByIdHandler<R: WorkflowDefinitionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowDefinitionRepository> GetWorkflowDefinitionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowDefinitionRepository + 'static> QueryHandler<GetWorkflowDefinitionByIdQuery> for GetWorkflowDefinitionByIdHandler<R> {
    type Output = Option<WorkflowDefinition>;

    async fn handle(&self, query: GetWorkflowDefinitionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list workflow_definition entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListWorkflowDefinitionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_name: Option<String>,
    pub filter_workflow_type: Option<WorkflowType>,
    pub filter_description: Option<String>,
    pub filter_trigger_type: Option<TriggerType>,
    pub filter_is_active: Option<bool>,
}

impl Default for ListWorkflowDefinitionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_name: None,
            filter_workflow_type: None,
            filter_description: None,
            filter_trigger_type: None,
            filter_is_active: None,
        }
    }
}

/// Handler for ListWorkflowDefinitionQuery
pub struct ListWorkflowDefinitionHandler<R: WorkflowDefinitionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowDefinitionRepository> ListWorkflowDefinitionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowDefinitionRepository + 'static> QueryHandler<ListWorkflowDefinitionQuery> for ListWorkflowDefinitionHandler<R> {
    type Output = PaginatedResult<WorkflowDefinition>;

    async fn handle(&self, query: ListWorkflowDefinitionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = WorkflowDefinitionFilter {
            name: query.filter_name.clone(),
            workflow_type: query.filter_workflow_type.clone(),
            description: query.filter_description.clone(),
            trigger_type: query.filter_trigger_type.clone(),
            is_active: query.filter_is_active.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find workflow_definition by name
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetWorkflowDefinitionByNameQuery {
    pub name: String,
}

/// Handler for GetWorkflowDefinitionByNameQuery
pub struct GetWorkflowDefinitionByNameHandler<R: WorkflowDefinitionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowDefinitionRepository> GetWorkflowDefinitionByNameHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowDefinitionRepository + 'static> QueryHandler<GetWorkflowDefinitionByNameQuery> for GetWorkflowDefinitionByNameHandler<R> {
    type Output = Option<WorkflowDefinition>;

    async fn handle(&self, query: GetWorkflowDefinitionByNameQuery) -> Result<Self::Output> {
        self.repository.find_by_name(&query.name).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveWorkflowDefinitionQuery;
// <<< CUSTOM QUERIES END >>>