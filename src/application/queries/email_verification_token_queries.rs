//! Queries for EmailVerificationToken aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::EmailVerificationToken;
use crate::infrastructure::persistence::{EmailVerificationTokenRepository, PaginationParams, PaginatedResult, EmailVerificationTokenFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for EmailVerificationToken operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait EmailVerificationTokenQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a email_verification_token by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetEmailVerificationTokenByIdQuery {
    pub id: String,
}

/// Handler for GetEmailVerificationTokenByIdQuery
pub struct GetEmailVerificationTokenByIdHandler<R: EmailVerificationTokenRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: EmailVerificationTokenRepository> GetEmailVerificationTokenByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: EmailVerificationTokenRepository + 'static> QueryHandler<GetEmailVerificationTokenByIdQuery> for GetEmailVerificationTokenByIdHandler<R> {
    type Output = Option<EmailVerificationToken>;

    async fn handle(&self, query: GetEmailVerificationTokenByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list email_verification_token entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListEmailVerificationTokenQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_token: Option<String>,
    pub filter_email: Option<String>,
    pub filter_token_type: Option<EmailVerificationType>,
    pub filter_old_email: Option<String>,
    pub filter_status: Option<EmailVerificationTokenStatus>,
    pub filter_user_agent: Option<String>,
}

impl Default for ListEmailVerificationTokenQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_token: None,
            filter_email: None,
            filter_token_type: None,
            filter_old_email: None,
            filter_status: None,
            filter_user_agent: None,
        }
    }
}

/// Handler for ListEmailVerificationTokenQuery
pub struct ListEmailVerificationTokenHandler<R: EmailVerificationTokenRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: EmailVerificationTokenRepository> ListEmailVerificationTokenHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: EmailVerificationTokenRepository + 'static> QueryHandler<ListEmailVerificationTokenQuery> for ListEmailVerificationTokenHandler<R> {
    type Output = PaginatedResult<EmailVerificationToken>;

    async fn handle(&self, query: ListEmailVerificationTokenQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = EmailVerificationTokenFilter {
            user_id: query.filter_user_id.clone(),
            token: query.filter_token.clone(),
            email: query.filter_email.clone(),
            token_type: query.filter_token_type.clone(),
            old_email: query.filter_old_email.clone(),
            status: query.filter_status.clone(),
            user_agent: query.filter_user_agent.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find email_verification_token by token
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetEmailVerificationTokenByTokenQuery {
    pub token: String,
}

/// Handler for GetEmailVerificationTokenByTokenQuery
pub struct GetEmailVerificationTokenByTokenHandler<R: EmailVerificationTokenRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: EmailVerificationTokenRepository> GetEmailVerificationTokenByTokenHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: EmailVerificationTokenRepository + 'static> QueryHandler<GetEmailVerificationTokenByTokenQuery> for GetEmailVerificationTokenByTokenHandler<R> {
    type Output = Option<EmailVerificationToken>;

    async fn handle(&self, query: GetEmailVerificationTokenByTokenQuery) -> Result<Self::Output> {
        self.repository.find_by_token(&query.token).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveEmailVerificationTokenQuery;
// <<< CUSTOM QUERIES END >>>