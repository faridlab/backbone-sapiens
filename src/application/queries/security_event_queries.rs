//! Queries for SecurityEvent aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::SecurityEvent;
use crate::infrastructure::persistence::{SecurityEventRepository, PaginationParams, PaginatedResult, SecurityEventFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for SecurityEvent operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait SecurityEventQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a security_event by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetSecurityEventByIdQuery {
    pub id: String,
}

/// Handler for GetSecurityEventByIdQuery
pub struct GetSecurityEventByIdHandler<R: SecurityEventRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SecurityEventRepository> GetSecurityEventByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SecurityEventRepository + 'static> QueryHandler<GetSecurityEventByIdQuery> for GetSecurityEventByIdHandler<R> {
    type Output = Option<SecurityEvent>;

    async fn handle(&self, query: GetSecurityEventByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list security_event entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListSecurityEventQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_session_id: Option<Uuid>,
    pub filter_event_type: Option<SecurityEventType>,
    pub filter_severity: Option<SecurityEventSeverity>,
    pub filter_ip_address: Option<String>,
    pub filter_user_agent: Option<String>,
    pub filter_description: Option<String>,
    pub filter_resolved: Option<bool>,
    pub filter_resolved_by_user_id: Option<Uuid>,
    pub filter_resolution_notes: Option<String>,
}

impl Default for ListSecurityEventQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_session_id: None,
            filter_event_type: None,
            filter_severity: None,
            filter_ip_address: None,
            filter_user_agent: None,
            filter_description: None,
            filter_resolved: None,
            filter_resolved_by_user_id: None,
            filter_resolution_notes: None,
        }
    }
}

/// Handler for ListSecurityEventQuery
pub struct ListSecurityEventHandler<R: SecurityEventRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SecurityEventRepository> ListSecurityEventHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SecurityEventRepository + 'static> QueryHandler<ListSecurityEventQuery> for ListSecurityEventHandler<R> {
    type Output = PaginatedResult<SecurityEvent>;

    async fn handle(&self, query: ListSecurityEventQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = SecurityEventFilter {
            user_id: query.filter_user_id.clone(),
            session_id: query.filter_session_id.clone(),
            event_type: query.filter_event_type.clone(),
            severity: query.filter_severity.clone(),
            ip_address: query.filter_ip_address.clone(),
            user_agent: query.filter_user_agent.clone(),
            description: query.filter_description.clone(),
            resolved: query.filter_resolved.clone(),
            resolved_by_user_id: query.filter_resolved_by_user_id.clone(),
            resolution_notes: query.filter_resolution_notes.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveSecurityEventQuery;
// <<< CUSTOM QUERIES END >>>