//! Queries for EffectivePermissionCache aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::EffectivePermissionCache;
use crate::infrastructure::persistence::{EffectivePermissionCacheRepository, PaginationParams, PaginatedResult, EffectivePermissionCacheFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for EffectivePermissionCache operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait EffectivePermissionCacheQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a effective_permission_cache by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetEffectivePermissionCacheByIdQuery {
    pub id: String,
}

/// Handler for GetEffectivePermissionCacheByIdQuery
pub struct GetEffectivePermissionCacheByIdHandler<R: EffectivePermissionCacheRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: EffectivePermissionCacheRepository> GetEffectivePermissionCacheByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: EffectivePermissionCacheRepository + 'static> QueryHandler<GetEffectivePermissionCacheByIdQuery> for GetEffectivePermissionCacheByIdHandler<R> {
    type Output = Option<EffectivePermissionCache>;

    async fn handle(&self, query: GetEffectivePermissionCacheByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list effective_permission_cache entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListEffectivePermissionCacheQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_permission_key: Option<String>,
}

impl Default for ListEffectivePermissionCacheQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_permission_key: None,
        }
    }
}

/// Handler for ListEffectivePermissionCacheQuery
pub struct ListEffectivePermissionCacheHandler<R: EffectivePermissionCacheRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: EffectivePermissionCacheRepository> ListEffectivePermissionCacheHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: EffectivePermissionCacheRepository + 'static> QueryHandler<ListEffectivePermissionCacheQuery> for ListEffectivePermissionCacheHandler<R> {
    type Output = PaginatedResult<EffectivePermissionCache>;

    async fn handle(&self, query: ListEffectivePermissionCacheQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = EffectivePermissionCacheFilter {
            user_id: query.filter_user_id.clone(),
            permission_key: query.filter_permission_key.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveEffectivePermissionCacheQuery;
// <<< CUSTOM QUERIES END >>>