//! Queries for BulkOperation aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::BulkOperation;
use crate::infrastructure::persistence::{BulkOperationRepository, PaginationParams, PaginatedResult, BulkOperationFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for BulkOperation operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait BulkOperationQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a bulk_operation by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetBulkOperationByIdQuery {
    pub id: String,
}

/// Handler for GetBulkOperationByIdQuery
pub struct GetBulkOperationByIdHandler<R: BulkOperationRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BulkOperationRepository> GetBulkOperationByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BulkOperationRepository + 'static> QueryHandler<GetBulkOperationByIdQuery> for GetBulkOperationByIdHandler<R> {
    type Output = Option<BulkOperation>;

    async fn handle(&self, query: GetBulkOperationByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list bulk_operation entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListBulkOperationQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_operation_type: Option<BulkOperationType>,
    pub filter_status: Option<BulkOperationStatus>,
    pub filter_input_file_path: Option<String>,
    pub filter_output_file_path: Option<String>,
    pub filter_error_file_path: Option<String>,
    pub filter_error_message: Option<String>,
}

impl Default for ListBulkOperationQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_operation_type: None,
            filter_status: None,
            filter_input_file_path: None,
            filter_output_file_path: None,
            filter_error_file_path: None,
            filter_error_message: None,
        }
    }
}

/// Handler for ListBulkOperationQuery
pub struct ListBulkOperationHandler<R: BulkOperationRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: BulkOperationRepository> ListBulkOperationHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: BulkOperationRepository + 'static> QueryHandler<ListBulkOperationQuery> for ListBulkOperationHandler<R> {
    type Output = PaginatedResult<BulkOperation>;

    async fn handle(&self, query: ListBulkOperationQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = BulkOperationFilter {
            operation_type: query.filter_operation_type.clone(),
            status: query.filter_status.clone(),
            input_file_path: query.filter_input_file_path.clone(),
            output_file_path: query.filter_output_file_path.clone(),
            error_file_path: query.filter_error_file_path.clone(),
            error_message: query.filter_error_message.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveBulkOperationQuery;
// <<< CUSTOM QUERIES END >>>