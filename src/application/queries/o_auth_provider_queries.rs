//! Queries for OAuthProvider aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::OAuthProvider;
use crate::infrastructure::persistence::{OAuthProviderRepository, PaginationParams, PaginatedResult, OAuthProviderFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for OAuthProvider operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait OAuthProviderQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a o_auth_provider by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetOAuthProviderByIdQuery {
    pub id: String,
}

/// Handler for GetOAuthProviderByIdQuery
pub struct GetOAuthProviderByIdHandler<R: OAuthProviderRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OAuthProviderRepository> GetOAuthProviderByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OAuthProviderRepository + 'static> QueryHandler<GetOAuthProviderByIdQuery> for GetOAuthProviderByIdHandler<R> {
    type Output = Option<OAuthProvider>;

    async fn handle(&self, query: GetOAuthProviderByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list o_auth_provider entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListOAuthProviderQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_provider_name: Option<OAuthProviderType>,
    pub filter_display_name: Option<String>,
    pub filter_client_id: Option<String>,
    pub filter_client_secret: Option<String>,
    pub filter_is_active: Option<bool>,
}

impl Default for ListOAuthProviderQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_provider_name: None,
            filter_display_name: None,
            filter_client_id: None,
            filter_client_secret: None,
            filter_is_active: None,
        }
    }
}

/// Handler for ListOAuthProviderQuery
pub struct ListOAuthProviderHandler<R: OAuthProviderRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OAuthProviderRepository> ListOAuthProviderHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OAuthProviderRepository + 'static> QueryHandler<ListOAuthProviderQuery> for ListOAuthProviderHandler<R> {
    type Output = PaginatedResult<OAuthProvider>;

    async fn handle(&self, query: ListOAuthProviderQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = OAuthProviderFilter {
            provider_name: query.filter_provider_name.clone(),
            display_name: query.filter_display_name.clone(),
            client_id: query.filter_client_id.clone(),
            client_secret: query.filter_client_secret.clone(),
            is_active: query.filter_is_active.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find o_auth_provider by provider_name
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetOAuthProviderByProviderNameQuery {
    pub provider_name: OAuthProviderType,
}

/// Handler for GetOAuthProviderByProviderNameQuery
pub struct GetOAuthProviderByProviderNameHandler<R: OAuthProviderRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OAuthProviderRepository> GetOAuthProviderByProviderNameHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OAuthProviderRepository + 'static> QueryHandler<GetOAuthProviderByProviderNameQuery> for GetOAuthProviderByProviderNameHandler<R> {
    type Output = Option<OAuthProvider>;

    async fn handle(&self, query: GetOAuthProviderByProviderNameQuery) -> Result<Self::Output> {
        self.repository.find_by_provider_name(&query.provider_name).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveOAuthProviderQuery;
// <<< CUSTOM QUERIES END >>>