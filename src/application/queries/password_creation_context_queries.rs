//! Queries for PasswordCreationContext aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::PasswordCreationContext;
use crate::infrastructure::persistence::{PasswordCreationContextRepository, PaginationParams, PaginatedResult, PasswordCreationContextFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for PasswordCreationContext operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait PasswordCreationContextQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a password_creation_context by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPasswordCreationContextByIdQuery {
    pub id: String,
}

/// Handler for GetPasswordCreationContextByIdQuery
pub struct GetPasswordCreationContextByIdHandler<R: PasswordCreationContextRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordCreationContextRepository> GetPasswordCreationContextByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordCreationContextRepository + 'static> QueryHandler<GetPasswordCreationContextByIdQuery> for GetPasswordCreationContextByIdHandler<R> {
    type Output = Option<PasswordCreationContext>;

    async fn handle(&self, query: GetPasswordCreationContextByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list password_creation_context entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListPasswordCreationContextQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_created_via: Option<PasswordCreationMethod>,
    pub filter_user_agent: Option<String>,
    pub filter_reason: Option<String>,
}

impl Default for ListPasswordCreationContextQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_created_via: None,
            filter_user_agent: None,
            filter_reason: None,
        }
    }
}

/// Handler for ListPasswordCreationContextQuery
pub struct ListPasswordCreationContextHandler<R: PasswordCreationContextRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordCreationContextRepository> ListPasswordCreationContextHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordCreationContextRepository + 'static> QueryHandler<ListPasswordCreationContextQuery> for ListPasswordCreationContextHandler<R> {
    type Output = PaginatedResult<PasswordCreationContext>;

    async fn handle(&self, query: ListPasswordCreationContextQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = PasswordCreationContextFilter {
            user_id: query.filter_user_id.clone(),
            created_via: query.filter_created_via.clone(),
            user_agent: query.filter_user_agent.clone(),
            reason: query.filter_reason.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActivePasswordCreationContextQuery;
// <<< CUSTOM QUERIES END >>>