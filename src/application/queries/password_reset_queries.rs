//! Queries for PasswordReset aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::PasswordReset;
use crate::infrastructure::persistence::{PasswordResetRepository, PaginationParams, PaginatedResult, PasswordResetFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for PasswordReset operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait PasswordResetQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a password_reset by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPasswordResetByIdQuery {
    pub id: String,
}

/// Handler for GetPasswordResetByIdQuery
pub struct GetPasswordResetByIdHandler<R: PasswordResetRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetRepository> GetPasswordResetByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetRepository + 'static> QueryHandler<GetPasswordResetByIdQuery> for GetPasswordResetByIdHandler<R> {
    type Output = Option<PasswordReset>;

    async fn handle(&self, query: GetPasswordResetByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list password_reset entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListPasswordResetQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_token: Option<String>,
    pub filter_token_hash: Option<String>,
    pub filter_email: Option<String>,
    pub filter_status: Option<PasswordResetStatus>,
    pub filter_verification_details_id: Option<Uuid>,
    pub filter_completion_details_id: Option<Uuid>,
    pub filter_security_id: Option<Uuid>,
    pub filter_device_fingerprint: Option<String>,
    pub filter_ip_address: Option<String>,
    pub filter_user_agent: Option<String>,
}

impl Default for ListPasswordResetQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_token: None,
            filter_token_hash: None,
            filter_email: None,
            filter_status: None,
            filter_verification_details_id: None,
            filter_completion_details_id: None,
            filter_security_id: None,
            filter_device_fingerprint: None,
            filter_ip_address: None,
            filter_user_agent: None,
        }
    }
}

/// Handler for ListPasswordResetQuery
pub struct ListPasswordResetHandler<R: PasswordResetRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetRepository> ListPasswordResetHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetRepository + 'static> QueryHandler<ListPasswordResetQuery> for ListPasswordResetHandler<R> {
    type Output = PaginatedResult<PasswordReset>;

    async fn handle(&self, query: ListPasswordResetQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = PasswordResetFilter {
            user_id: query.filter_user_id.clone(),
            token: query.filter_token.clone(),
            token_hash: query.filter_token_hash.clone(),
            email: query.filter_email.clone(),
            status: query.filter_status.clone(),
            verification_details_id: query.filter_verification_details_id.clone(),
            completion_details_id: query.filter_completion_details_id.clone(),
            security_id: query.filter_security_id.clone(),
            device_fingerprint: query.filter_device_fingerprint.clone(),
            ip_address: query.filter_ip_address.clone(),
            user_agent: query.filter_user_agent.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find password_reset by token
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPasswordResetByTokenQuery {
    pub token: String,
}

/// Handler for GetPasswordResetByTokenQuery
pub struct GetPasswordResetByTokenHandler<R: PasswordResetRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetRepository> GetPasswordResetByTokenHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetRepository + 'static> QueryHandler<GetPasswordResetByTokenQuery> for GetPasswordResetByTokenHandler<R> {
    type Output = Option<PasswordReset>;

    async fn handle(&self, query: GetPasswordResetByTokenQuery) -> Result<Self::Output> {
        self.repository.find_by_token(&query.token).await
    }
}

/// Query to find password_reset by token_hash
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPasswordResetByTokenHashQuery {
    pub token_hash: String,
}

/// Handler for GetPasswordResetByTokenHashQuery
pub struct GetPasswordResetByTokenHashHandler<R: PasswordResetRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PasswordResetRepository> GetPasswordResetByTokenHashHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PasswordResetRepository + 'static> QueryHandler<GetPasswordResetByTokenHashQuery> for GetPasswordResetByTokenHashHandler<R> {
    type Output = Option<PasswordReset>;

    async fn handle(&self, query: GetPasswordResetByTokenHashQuery) -> Result<Self::Output> {
        self.repository.find_by_token_hash(&query.token_hash).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActivePasswordResetQuery;
// <<< CUSTOM QUERIES END >>>