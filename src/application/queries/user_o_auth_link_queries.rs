//! Queries for UserOAuthLink aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::UserOAuthLink;
use crate::infrastructure::persistence::{UserOAuthLinkRepository, PaginationParams, PaginatedResult, UserOAuthLinkFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for UserOAuthLink operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait UserOAuthLinkQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a user_o_auth_link by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetUserOAuthLinkByIdQuery {
    pub id: String,
}

/// Handler for GetUserOAuthLinkByIdQuery
pub struct GetUserOAuthLinkByIdHandler<R: UserOAuthLinkRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserOAuthLinkRepository> GetUserOAuthLinkByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserOAuthLinkRepository + 'static> QueryHandler<GetUserOAuthLinkByIdQuery> for GetUserOAuthLinkByIdHandler<R> {
    type Output = Option<UserOAuthLink>;

    async fn handle(&self, query: GetUserOAuthLinkByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list user_o_auth_link entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListUserOAuthLinkQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_oauth_provider_id: Option<Uuid>,
    pub filter_provider_user_id: Option<String>,
    pub filter_provider_email: Option<String>,
    pub filter_provider_username: Option<String>,
    pub filter_is_active: Option<bool>,
    pub filter_access_token: Option<String>,
    pub filter_refresh_token: Option<String>,
    pub filter_is_primary: Option<bool>,
    pub filter_link_status: Option<UserOAuthLinkStatus>,
    pub filter_sync_enabled: Option<bool>,
}

impl Default for ListUserOAuthLinkQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_oauth_provider_id: None,
            filter_provider_user_id: None,
            filter_provider_email: None,
            filter_provider_username: None,
            filter_is_active: None,
            filter_access_token: None,
            filter_refresh_token: None,
            filter_is_primary: None,
            filter_link_status: None,
            filter_sync_enabled: None,
        }
    }
}

/// Handler for ListUserOAuthLinkQuery
pub struct ListUserOAuthLinkHandler<R: UserOAuthLinkRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserOAuthLinkRepository> ListUserOAuthLinkHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserOAuthLinkRepository + 'static> QueryHandler<ListUserOAuthLinkQuery> for ListUserOAuthLinkHandler<R> {
    type Output = PaginatedResult<UserOAuthLink>;

    async fn handle(&self, query: ListUserOAuthLinkQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = UserOAuthLinkFilter {
            user_id: query.filter_user_id.clone(),
            oauth_provider_id: query.filter_oauth_provider_id.clone(),
            provider_user_id: query.filter_provider_user_id.clone(),
            provider_email: query.filter_provider_email.clone(),
            provider_username: query.filter_provider_username.clone(),
            is_active: query.filter_is_active.clone(),
            access_token: query.filter_access_token.clone(),
            refresh_token: query.filter_refresh_token.clone(),
            is_primary: query.filter_is_primary.clone(),
            link_status: query.filter_link_status.clone(),
            sync_enabled: query.filter_sync_enabled.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveUserOAuthLinkQuery;
// <<< CUSTOM QUERIES END >>>