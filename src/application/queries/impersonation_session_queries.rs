//! Queries for ImpersonationSession aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::ImpersonationSession;
use crate::infrastructure::persistence::{ImpersonationSessionRepository, PaginationParams, PaginatedResult, ImpersonationSessionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for ImpersonationSession operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait ImpersonationSessionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a impersonation_session by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetImpersonationSessionByIdQuery {
    pub id: String,
}

/// Handler for GetImpersonationSessionByIdQuery
pub struct GetImpersonationSessionByIdHandler<R: ImpersonationSessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ImpersonationSessionRepository> GetImpersonationSessionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ImpersonationSessionRepository + 'static> QueryHandler<GetImpersonationSessionByIdQuery> for GetImpersonationSessionByIdHandler<R> {
    type Output = Option<ImpersonationSession>;

    async fn handle(&self, query: GetImpersonationSessionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list impersonation_session entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListImpersonationSessionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_admin_id: Option<Uuid>,
    pub filter_target_user_id: Option<Uuid>,
    pub filter_session_id: Option<Uuid>,
    pub filter_reason: Option<String>,
    pub filter_terminated_by: Option<Uuid>,
    pub filter_termination_reason: Option<String>,
    pub filter_status: Option<ImpersonationSessionStatus>,
}

impl Default for ListImpersonationSessionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_admin_id: None,
            filter_target_user_id: None,
            filter_session_id: None,
            filter_reason: None,
            filter_terminated_by: None,
            filter_termination_reason: None,
            filter_status: None,
        }
    }
}

/// Handler for ListImpersonationSessionQuery
pub struct ListImpersonationSessionHandler<R: ImpersonationSessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ImpersonationSessionRepository> ListImpersonationSessionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ImpersonationSessionRepository + 'static> QueryHandler<ListImpersonationSessionQuery> for ListImpersonationSessionHandler<R> {
    type Output = PaginatedResult<ImpersonationSession>;

    async fn handle(&self, query: ListImpersonationSessionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = ImpersonationSessionFilter {
            admin_id: query.filter_admin_id.clone(),
            target_user_id: query.filter_target_user_id.clone(),
            session_id: query.filter_session_id.clone(),
            reason: query.filter_reason.clone(),
            terminated_by: query.filter_terminated_by.clone(),
            termination_reason: query.filter_termination_reason.clone(),
            status: query.filter_status.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveImpersonationSessionQuery;
// <<< CUSTOM QUERIES END >>>