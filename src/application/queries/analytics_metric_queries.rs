//! Queries for AnalyticsMetric aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::AnalyticsMetric;
use crate::infrastructure::persistence::{AnalyticsMetricRepository, PaginationParams, PaginatedResult, AnalyticsMetricFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for AnalyticsMetric operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait AnalyticsMetricQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a analytics_metric by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetAnalyticsMetricByIdQuery {
    pub id: String,
}

/// Handler for GetAnalyticsMetricByIdQuery
pub struct GetAnalyticsMetricByIdHandler<R: AnalyticsMetricRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsMetricRepository> GetAnalyticsMetricByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsMetricRepository + 'static> QueryHandler<GetAnalyticsMetricByIdQuery> for GetAnalyticsMetricByIdHandler<R> {
    type Output = Option<AnalyticsMetric>;

    async fn handle(&self, query: GetAnalyticsMetricByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list analytics_metric entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListAnalyticsMetricQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_metric_name: Option<String>,
    pub filter_metric_type: Option<AnalyticsMetricType>,
    pub filter_unit: Option<String>,
    pub filter_aggregation_level: Option<AggregationLevel>,
    pub filter_organization_id: Option<Uuid>,
}

impl Default for ListAnalyticsMetricQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_metric_name: None,
            filter_metric_type: None,
            filter_unit: None,
            filter_aggregation_level: None,
            filter_organization_id: None,
        }
    }
}

/// Handler for ListAnalyticsMetricQuery
pub struct ListAnalyticsMetricHandler<R: AnalyticsMetricRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnalyticsMetricRepository> ListAnalyticsMetricHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnalyticsMetricRepository + 'static> QueryHandler<ListAnalyticsMetricQuery> for ListAnalyticsMetricHandler<R> {
    type Output = PaginatedResult<AnalyticsMetric>;

    async fn handle(&self, query: ListAnalyticsMetricQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = AnalyticsMetricFilter {
            metric_name: query.filter_metric_name.clone(),
            metric_type: query.filter_metric_type.clone(),
            unit: query.filter_unit.clone(),
            aggregation_level: query.filter_aggregation_level.clone(),
            organization_id: query.filter_organization_id.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveAnalyticsMetricQuery;
// <<< CUSTOM QUERIES END >>>