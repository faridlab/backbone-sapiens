//! Queries for RolePermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::RolePermission;
use crate::infrastructure::persistence::{RolePermissionRepository, PaginationParams, PaginatedResult, RolePermissionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for RolePermission operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait RolePermissionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a role_permission by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetRolePermissionByIdQuery {
    pub id: String,
}

/// Handler for GetRolePermissionByIdQuery
pub struct GetRolePermissionByIdHandler<R: RolePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RolePermissionRepository> GetRolePermissionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RolePermissionRepository + 'static> QueryHandler<GetRolePermissionByIdQuery> for GetRolePermissionByIdHandler<R> {
    type Output = Option<RolePermission>;

    async fn handle(&self, query: GetRolePermissionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list role_permission entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListRolePermissionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_role_id: Option<Uuid>,
    pub filter_permission_id: Option<Uuid>,
}

impl Default for ListRolePermissionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_role_id: None,
            filter_permission_id: None,
        }
    }
}

/// Handler for ListRolePermissionQuery
pub struct ListRolePermissionHandler<R: RolePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RolePermissionRepository> ListRolePermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RolePermissionRepository + 'static> QueryHandler<ListRolePermissionQuery> for ListRolePermissionHandler<R> {
    type Output = PaginatedResult<RolePermission>;

    async fn handle(&self, query: ListRolePermissionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = RolePermissionFilter {
            role_id: query.filter_role_id.clone(),
            permission_id: query.filter_permission_id.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveRolePermissionQuery;
// <<< CUSTOM QUERIES END >>>