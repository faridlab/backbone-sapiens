//! Queries for DirectPermissionGrant aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::DirectPermissionGrant;
use crate::infrastructure::persistence::{DirectPermissionGrantRepository, PaginationParams, PaginatedResult, DirectPermissionGrantFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for DirectPermissionGrant operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait DirectPermissionGrantQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a direct_permission_grant by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetDirectPermissionGrantByIdQuery {
    pub id: String,
}

/// Handler for GetDirectPermissionGrantByIdQuery
pub struct GetDirectPermissionGrantByIdHandler<R: DirectPermissionGrantRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DirectPermissionGrantRepository> GetDirectPermissionGrantByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DirectPermissionGrantRepository + 'static> QueryHandler<GetDirectPermissionGrantByIdQuery> for GetDirectPermissionGrantByIdHandler<R> {
    type Output = Option<DirectPermissionGrant>;

    async fn handle(&self, query: GetDirectPermissionGrantByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list direct_permission_grant entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListDirectPermissionGrantQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_permission_id: Option<Uuid>,
    pub filter_granted_by: Option<Uuid>,
    pub filter_reason: Option<String>,
    pub filter_status: Option<DirectPermissionGrantStatus>,
    pub filter_revoked_by: Option<Uuid>,
    pub filter_revocation_reason: Option<String>,
}

impl Default for ListDirectPermissionGrantQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_permission_id: None,
            filter_granted_by: None,
            filter_reason: None,
            filter_status: None,
            filter_revoked_by: None,
            filter_revocation_reason: None,
        }
    }
}

/// Handler for ListDirectPermissionGrantQuery
pub struct ListDirectPermissionGrantHandler<R: DirectPermissionGrantRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: DirectPermissionGrantRepository> ListDirectPermissionGrantHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: DirectPermissionGrantRepository + 'static> QueryHandler<ListDirectPermissionGrantQuery> for ListDirectPermissionGrantHandler<R> {
    type Output = PaginatedResult<DirectPermissionGrant>;

    async fn handle(&self, query: ListDirectPermissionGrantQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = DirectPermissionGrantFilter {
            user_id: query.filter_user_id.clone(),
            permission_id: query.filter_permission_id.clone(),
            granted_by: query.filter_granted_by.clone(),
            reason: query.filter_reason.clone(),
            status: query.filter_status.clone(),
            revoked_by: query.filter_revoked_by.clone(),
            revocation_reason: query.filter_revocation_reason.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveDirectPermissionGrantQuery;
// <<< CUSTOM QUERIES END >>>