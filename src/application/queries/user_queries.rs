//! Queries for User aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::User;
use crate::infrastructure::persistence::{UserRepository, PaginationParams, PaginatedResult, UserFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for User operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait UserQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a user by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetUserByIdQuery {
    pub id: String,
}

/// Handler for GetUserByIdQuery
pub struct GetUserByIdHandler<R: UserRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserRepository> GetUserByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserRepository + 'static> QueryHandler<GetUserByIdQuery> for GetUserByIdHandler<R> {
    type Output = Option<User>;

    async fn handle(&self, query: GetUserByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list user entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListUserQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_username: Option<String>,
    pub filter_email: Option<String>,
    pub filter_password_hash: Option<String>,
    pub filter_status: Option<UserStatus>,
    pub filter_email_verified: Option<bool>,
}

impl Default for ListUserQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_username: None,
            filter_email: None,
            filter_password_hash: None,
            filter_status: None,
            filter_email_verified: None,
        }
    }
}

/// Handler for ListUserQuery
pub struct ListUserHandler<R: UserRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserRepository> ListUserHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserRepository + 'static> QueryHandler<ListUserQuery> for ListUserHandler<R> {
    type Output = PaginatedResult<User>;

    async fn handle(&self, query: ListUserQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = UserFilter {
            username: query.filter_username.clone(),
            email: query.filter_email.clone(),
            password_hash: query.filter_password_hash.clone(),
            status: query.filter_status.clone(),
            email_verified: query.filter_email_verified.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find user by username
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetUserByUsernameQuery {
    pub username: String,
}

/// Handler for GetUserByUsernameQuery
pub struct GetUserByUsernameHandler<R: UserRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserRepository> GetUserByUsernameHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserRepository + 'static> QueryHandler<GetUserByUsernameQuery> for GetUserByUsernameHandler<R> {
    type Output = Option<User>;

    async fn handle(&self, query: GetUserByUsernameQuery) -> Result<Self::Output> {
        self.repository.find_by_username(&query.username).await
    }
}

/// Query to find user by email
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetUserByEmailQuery {
    pub email: String,
}

/// Handler for GetUserByEmailQuery
pub struct GetUserByEmailHandler<R: UserRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: UserRepository> GetUserByEmailHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: UserRepository + 'static> QueryHandler<GetUserByEmailQuery> for GetUserByEmailHandler<R> {
    type Output = Option<User>;

    async fn handle(&self, query: GetUserByEmailQuery) -> Result<Self::Output> {
        self.repository.find_by_email(&query.email).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveUserQuery;
// <<< CUSTOM QUERIES END >>>