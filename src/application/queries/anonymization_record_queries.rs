//! Queries for AnonymizationRecord aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::AnonymizationRecord;
use crate::infrastructure::persistence::{AnonymizationRecordRepository, PaginationParams, PaginatedResult, AnonymizationRecordFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for AnonymizationRecord operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait AnonymizationRecordQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a anonymization_record by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetAnonymizationRecordByIdQuery {
    pub id: String,
}

/// Handler for GetAnonymizationRecordByIdQuery
pub struct GetAnonymizationRecordByIdHandler<R: AnonymizationRecordRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnonymizationRecordRepository> GetAnonymizationRecordByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnonymizationRecordRepository + 'static> QueryHandler<GetAnonymizationRecordByIdQuery> for GetAnonymizationRecordByIdHandler<R> {
    type Output = Option<AnonymizationRecord>;

    async fn handle(&self, query: GetAnonymizationRecordByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list anonymization_record entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListAnonymizationRecordQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_original_email: Option<String>,
    pub filter_original_username: Option<String>,
    pub filter_anonymized_by: Option<Uuid>,
    pub filter_reason: Option<String>,
    pub filter_method: Option<AnonymizationMethod>,
    pub filter_status: Option<AnonymizationStatus>,
}

impl Default for ListAnonymizationRecordQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_original_email: None,
            filter_original_username: None,
            filter_anonymized_by: None,
            filter_reason: None,
            filter_method: None,
            filter_status: None,
        }
    }
}

/// Handler for ListAnonymizationRecordQuery
pub struct ListAnonymizationRecordHandler<R: AnonymizationRecordRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: AnonymizationRecordRepository> ListAnonymizationRecordHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: AnonymizationRecordRepository + 'static> QueryHandler<ListAnonymizationRecordQuery> for ListAnonymizationRecordHandler<R> {
    type Output = PaginatedResult<AnonymizationRecord>;

    async fn handle(&self, query: ListAnonymizationRecordQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = AnonymizationRecordFilter {
            user_id: query.filter_user_id.clone(),
            original_email: query.filter_original_email.clone(),
            original_username: query.filter_original_username.clone(),
            anonymized_by: query.filter_anonymized_by.clone(),
            reason: query.filter_reason.clone(),
            method: query.filter_method.clone(),
            status: query.filter_status.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveAnonymizationRecordQuery;
// <<< CUSTOM QUERIES END >>>