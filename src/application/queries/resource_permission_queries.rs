//! Queries for ResourcePermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::ResourcePermission;
use crate::infrastructure::persistence::{ResourcePermissionRepository, PaginationParams, PaginatedResult, ResourcePermissionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for ResourcePermission operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait ResourcePermissionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a resource_permission by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetResourcePermissionByIdQuery {
    pub id: String,
}

/// Handler for GetResourcePermissionByIdQuery
pub struct GetResourcePermissionByIdHandler<R: ResourcePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ResourcePermissionRepository> GetResourcePermissionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ResourcePermissionRepository + 'static> QueryHandler<GetResourcePermissionByIdQuery> for GetResourcePermissionByIdHandler<R> {
    type Output = Option<ResourcePermission>;

    async fn handle(&self, query: GetResourcePermissionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list resource_permission entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListResourcePermissionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_permission_id: Option<Uuid>,
    pub filter_resource_type: Option<String>,
    pub filter_resource_id: Option<String>,
    pub filter_granted_to_user_id: Option<Uuid>,
    pub filter_granted_to_role_id: Option<Uuid>,
    pub filter_granted_by: Option<Uuid>,
    pub filter_reason: Option<String>,
    pub filter_status: Option<ResourcePermissionStatus>,
}

impl Default for ListResourcePermissionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_permission_id: None,
            filter_resource_type: None,
            filter_resource_id: None,
            filter_granted_to_user_id: None,
            filter_granted_to_role_id: None,
            filter_granted_by: None,
            filter_reason: None,
            filter_status: None,
        }
    }
}

/// Handler for ListResourcePermissionQuery
pub struct ListResourcePermissionHandler<R: ResourcePermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: ResourcePermissionRepository> ListResourcePermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: ResourcePermissionRepository + 'static> QueryHandler<ListResourcePermissionQuery> for ListResourcePermissionHandler<R> {
    type Output = PaginatedResult<ResourcePermission>;

    async fn handle(&self, query: ListResourcePermissionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = ResourcePermissionFilter {
            permission_id: query.filter_permission_id.clone(),
            resource_type: query.filter_resource_type.clone(),
            resource_id: query.filter_resource_id.clone(),
            granted_to_user_id: query.filter_granted_to_user_id.clone(),
            granted_to_role_id: query.filter_granted_to_role_id.clone(),
            granted_by: query.filter_granted_by.clone(),
            reason: query.filter_reason.clone(),
            status: query.filter_status.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveResourcePermissionQuery;
// <<< CUSTOM QUERIES END >>>