//! Queries for WorkflowActionExecution aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::WorkflowActionExecution;
use crate::infrastructure::persistence::{WorkflowActionExecutionRepository, PaginationParams, PaginatedResult, WorkflowActionExecutionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for WorkflowActionExecution operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait WorkflowActionExecutionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a workflow_action_execution by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetWorkflowActionExecutionByIdQuery {
    pub id: String,
}

/// Handler for GetWorkflowActionExecutionByIdQuery
pub struct GetWorkflowActionExecutionByIdHandler<R: WorkflowActionExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowActionExecutionRepository> GetWorkflowActionExecutionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowActionExecutionRepository + 'static> QueryHandler<GetWorkflowActionExecutionByIdQuery> for GetWorkflowActionExecutionByIdHandler<R> {
    type Output = Option<WorkflowActionExecution>;

    async fn handle(&self, query: GetWorkflowActionExecutionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list workflow_action_execution entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListWorkflowActionExecutionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_workflow_execution_id: Option<Uuid>,
    pub filter_workflow_action_id: Option<Uuid>,
    pub filter_status: Option<WorkflowActionExecutionStatus>,
    pub filter_error_message: Option<String>,
}

impl Default for ListWorkflowActionExecutionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_workflow_execution_id: None,
            filter_workflow_action_id: None,
            filter_status: None,
            filter_error_message: None,
        }
    }
}

/// Handler for ListWorkflowActionExecutionQuery
pub struct ListWorkflowActionExecutionHandler<R: WorkflowActionExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowActionExecutionRepository> ListWorkflowActionExecutionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowActionExecutionRepository + 'static> QueryHandler<ListWorkflowActionExecutionQuery> for ListWorkflowActionExecutionHandler<R> {
    type Output = PaginatedResult<WorkflowActionExecution>;

    async fn handle(&self, query: ListWorkflowActionExecutionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = WorkflowActionExecutionFilter {
            workflow_execution_id: query.filter_workflow_execution_id.clone(),
            workflow_action_id: query.filter_workflow_action_id.clone(),
            status: query.filter_status.clone(),
            error_message: query.filter_error_message.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveWorkflowActionExecutionQuery;
// <<< CUSTOM QUERIES END >>>