//! Queries for PermissionConflict aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::PermissionConflict;
use crate::infrastructure::persistence::{PermissionConflictRepository, PaginationParams, PaginatedResult, PermissionConflictFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for PermissionConflict operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait PermissionConflictQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a permission_conflict by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetPermissionConflictByIdQuery {
    pub id: String,
}

/// Handler for GetPermissionConflictByIdQuery
pub struct GetPermissionConflictByIdHandler<R: PermissionConflictRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PermissionConflictRepository> GetPermissionConflictByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PermissionConflictRepository + 'static> QueryHandler<GetPermissionConflictByIdQuery> for GetPermissionConflictByIdHandler<R> {
    type Output = Option<PermissionConflict>;

    async fn handle(&self, query: GetPermissionConflictByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list permission_conflict entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListPermissionConflictQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_conflict_type: Option<String>,
    pub filter_severity: Option<ConflictSeverity>,
    pub filter_description: Option<String>,
    pub filter_conflicting_entity_id: Option<Uuid>,
    pub filter_resolution_status: Option<ConflictResolutionStatus>,
}

impl Default for ListPermissionConflictQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_conflict_type: None,
            filter_severity: None,
            filter_description: None,
            filter_conflicting_entity_id: None,
            filter_resolution_status: None,
        }
    }
}

/// Handler for ListPermissionConflictQuery
pub struct ListPermissionConflictHandler<R: PermissionConflictRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: PermissionConflictRepository> ListPermissionConflictHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: PermissionConflictRepository + 'static> QueryHandler<ListPermissionConflictQuery> for ListPermissionConflictHandler<R> {
    type Output = PaginatedResult<PermissionConflict>;

    async fn handle(&self, query: ListPermissionConflictQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = PermissionConflictFilter {
            conflict_type: query.filter_conflict_type.clone(),
            severity: query.filter_severity.clone(),
            description: query.filter_description.clone(),
            conflicting_entity_id: query.filter_conflicting_entity_id.clone(),
            resolution_status: query.filter_resolution_status.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActivePermissionConflictQuery;
// <<< CUSTOM QUERIES END >>>