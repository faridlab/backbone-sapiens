//! Queries for WorkflowExecution aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::WorkflowExecution;
use crate::infrastructure::persistence::{WorkflowExecutionRepository, PaginationParams, PaginatedResult, WorkflowExecutionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for WorkflowExecution operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait WorkflowExecutionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a workflow_execution by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetWorkflowExecutionByIdQuery {
    pub id: String,
}

/// Handler for GetWorkflowExecutionByIdQuery
pub struct GetWorkflowExecutionByIdHandler<R: WorkflowExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowExecutionRepository> GetWorkflowExecutionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowExecutionRepository + 'static> QueryHandler<GetWorkflowExecutionByIdQuery> for GetWorkflowExecutionByIdHandler<R> {
    type Output = Option<WorkflowExecution>;

    async fn handle(&self, query: GetWorkflowExecutionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list workflow_execution entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListWorkflowExecutionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_workflow_id: Option<Uuid>,
    pub filter_workflow_definition_id: Option<Uuid>,
    pub filter_status: Option<WorkflowExecutionStatus>,
    pub filter_error_message: Option<String>,
}

impl Default for ListWorkflowExecutionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_workflow_id: None,
            filter_workflow_definition_id: None,
            filter_status: None,
            filter_error_message: None,
        }
    }
}

/// Handler for ListWorkflowExecutionQuery
pub struct ListWorkflowExecutionHandler<R: WorkflowExecutionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: WorkflowExecutionRepository> ListWorkflowExecutionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: WorkflowExecutionRepository + 'static> QueryHandler<ListWorkflowExecutionQuery> for ListWorkflowExecutionHandler<R> {
    type Output = PaginatedResult<WorkflowExecution>;

    async fn handle(&self, query: ListWorkflowExecutionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = WorkflowExecutionFilter {
            workflow_id: query.filter_workflow_id.clone(),
            workflow_definition_id: query.filter_workflow_definition_id.clone(),
            status: query.filter_status.clone(),
            error_message: query.filter_error_message.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveWorkflowExecutionQuery;
// <<< CUSTOM QUERIES END >>>