//! Queries for OrganizationPermission aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::OrganizationPermission;
use crate::infrastructure::persistence::{OrganizationPermissionRepository, PaginationParams, PaginatedResult, OrganizationPermissionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for OrganizationPermission operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait OrganizationPermissionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a organization_permission by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetOrganizationPermissionByIdQuery {
    pub id: String,
}

/// Handler for GetOrganizationPermissionByIdQuery
pub struct GetOrganizationPermissionByIdHandler<R: OrganizationPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OrganizationPermissionRepository> GetOrganizationPermissionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OrganizationPermissionRepository + 'static> QueryHandler<GetOrganizationPermissionByIdQuery> for GetOrganizationPermissionByIdHandler<R> {
    type Output = Option<OrganizationPermission>;

    async fn handle(&self, query: GetOrganizationPermissionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list organization_permission entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListOrganizationPermissionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_organization_id: Option<Uuid>,
    pub filter_permission_id: Option<Uuid>,
    pub filter_permission_name: Option<String>,
    pub filter_resource_id: Option<Uuid>,
    pub filter_resource_type: Option<String>,
    pub filter_role_id: Option<Uuid>,
    pub filter_granted_by: Option<Uuid>,
    pub filter_reason: Option<String>,
    pub filter_is_active: Option<bool>,
}

impl Default for ListOrganizationPermissionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_organization_id: None,
            filter_permission_id: None,
            filter_permission_name: None,
            filter_resource_id: None,
            filter_resource_type: None,
            filter_role_id: None,
            filter_granted_by: None,
            filter_reason: None,
            filter_is_active: None,
        }
    }
}

/// Handler for ListOrganizationPermissionQuery
pub struct ListOrganizationPermissionHandler<R: OrganizationPermissionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: OrganizationPermissionRepository> ListOrganizationPermissionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: OrganizationPermissionRepository + 'static> QueryHandler<ListOrganizationPermissionQuery> for ListOrganizationPermissionHandler<R> {
    type Output = PaginatedResult<OrganizationPermission>;

    async fn handle(&self, query: ListOrganizationPermissionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = OrganizationPermissionFilter {
            organization_id: query.filter_organization_id.clone(),
            permission_id: query.filter_permission_id.clone(),
            permission_name: query.filter_permission_name.clone(),
            resource_id: query.filter_resource_id.clone(),
            resource_type: query.filter_resource_type.clone(),
            role_id: query.filter_role_id.clone(),
            granted_by: query.filter_granted_by.clone(),
            reason: query.filter_reason.clone(),
            is_active: query.filter_is_active.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveOrganizationPermissionQuery;
// <<< CUSTOM QUERIES END >>>