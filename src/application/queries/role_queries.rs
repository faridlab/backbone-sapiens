//! Queries for Role aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::Role;
use crate::infrastructure::persistence::{RoleRepository, PaginationParams, PaginatedResult, RoleFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for Role operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait RoleQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a role by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetRoleByIdQuery {
    pub id: String,
}

/// Handler for GetRoleByIdQuery
pub struct GetRoleByIdHandler<R: RoleRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RoleRepository> GetRoleByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RoleRepository + 'static> QueryHandler<GetRoleByIdQuery> for GetRoleByIdHandler<R> {
    type Output = Option<Role>;

    async fn handle(&self, query: GetRoleByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list role entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListRoleQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_name: Option<String>,
    pub filter_description: Option<String>,
    pub filter_is_default: Option<bool>,
}

impl Default for ListRoleQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_name: None,
            filter_description: None,
            filter_is_default: None,
        }
    }
}

/// Handler for ListRoleQuery
pub struct ListRoleHandler<R: RoleRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RoleRepository> ListRoleHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RoleRepository + 'static> QueryHandler<ListRoleQuery> for ListRoleHandler<R> {
    type Output = PaginatedResult<Role>;

    async fn handle(&self, query: ListRoleQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = RoleFilter {
            name: query.filter_name.clone(),
            description: query.filter_description.clone(),
            is_default: query.filter_is_default.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find role by name
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetRoleByNameQuery {
    pub name: String,
}

/// Handler for GetRoleByNameQuery
pub struct GetRoleByNameHandler<R: RoleRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: RoleRepository> GetRoleByNameHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: RoleRepository + 'static> QueryHandler<GetRoleByNameQuery> for GetRoleByNameHandler<R> {
    type Output = Option<Role>;

    async fn handle(&self, query: GetRoleByNameQuery) -> Result<Self::Output> {
        self.repository.find_by_name(&query.name).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveRoleQuery;
// <<< CUSTOM QUERIES END >>>