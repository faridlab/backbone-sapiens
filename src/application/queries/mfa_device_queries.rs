//! Queries for MFADevice aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::MFADevice;
use crate::infrastructure::persistence::{MFADeviceRepository, PaginationParams, PaginatedResult, MFADeviceFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for MFADevice operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait MFADeviceQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a mfa_device by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetMFADeviceByIdQuery {
    pub id: String,
}

/// Handler for GetMFADeviceByIdQuery
pub struct GetMFADeviceByIdHandler<R: MFADeviceRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: MFADeviceRepository> GetMFADeviceByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: MFADeviceRepository + 'static> QueryHandler<GetMFADeviceByIdQuery> for GetMFADeviceByIdHandler<R> {
    type Output = Option<MFADevice>;

    async fn handle(&self, query: GetMFADeviceByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list mfa_device entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListMFADeviceQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_device_type: Option<MFADeviceType>,
    pub filter_device_name: Option<String>,
    pub filter_phone_number: Option<String>,
    pub filter_email_address: Option<String>,
    pub filter_totp_secret: Option<String>,
    pub filter_secret: Option<String>,
    pub filter_hardware_key_id: Option<String>,
    pub filter_push_token: Option<String>,
    pub filter_device_fingerprint: Option<String>,
    pub filter_manufacturer: Option<String>,
    pub filter_model: Option<String>,
    pub filter_operating_system: Option<String>,
    pub filter_app_version: Option<String>,
    pub filter_is_primary: Option<bool>,
    pub filter_is_backup: Option<bool>,
    pub filter_requires_verification: Option<bool>,
    pub filter_auto_approval_enabled: Option<bool>,
    pub filter_enrolled_by: Option<Uuid>,
    pub filter_enrollment_method: Option<EnrollmentMethod>,
    pub filter_enrollment_ip: Option<String>,
    pub filter_enrollment_user_agent: Option<String>,
    pub filter_backup_codes_generated: Option<bool>,
    pub filter_is_locked: Option<bool>,
    pub filter_lock_reason: Option<String>,
    pub filter_is_active: Option<bool>,
    pub filter_status: Option<MFADeviceStatus>,
}

impl Default for ListMFADeviceQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_device_type: None,
            filter_device_name: None,
            filter_phone_number: None,
            filter_email_address: None,
            filter_totp_secret: None,
            filter_secret: None,
            filter_hardware_key_id: None,
            filter_push_token: None,
            filter_device_fingerprint: None,
            filter_manufacturer: None,
            filter_model: None,
            filter_operating_system: None,
            filter_app_version: None,
            filter_is_primary: None,
            filter_is_backup: None,
            filter_requires_verification: None,
            filter_auto_approval_enabled: None,
            filter_enrolled_by: None,
            filter_enrollment_method: None,
            filter_enrollment_ip: None,
            filter_enrollment_user_agent: None,
            filter_backup_codes_generated: None,
            filter_is_locked: None,
            filter_lock_reason: None,
            filter_is_active: None,
            filter_status: None,
        }
    }
}

/// Handler for ListMFADeviceQuery
pub struct ListMFADeviceHandler<R: MFADeviceRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: MFADeviceRepository> ListMFADeviceHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: MFADeviceRepository + 'static> QueryHandler<ListMFADeviceQuery> for ListMFADeviceHandler<R> {
    type Output = PaginatedResult<MFADevice>;

    async fn handle(&self, query: ListMFADeviceQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = MFADeviceFilter {
            user_id: query.filter_user_id.clone(),
            device_type: query.filter_device_type.clone(),
            device_name: query.filter_device_name.clone(),
            phone_number: query.filter_phone_number.clone(),
            email_address: query.filter_email_address.clone(),
            totp_secret: query.filter_totp_secret.clone(),
            secret: query.filter_secret.clone(),
            hardware_key_id: query.filter_hardware_key_id.clone(),
            push_token: query.filter_push_token.clone(),
            device_fingerprint: query.filter_device_fingerprint.clone(),
            manufacturer: query.filter_manufacturer.clone(),
            model: query.filter_model.clone(),
            operating_system: query.filter_operating_system.clone(),
            app_version: query.filter_app_version.clone(),
            is_primary: query.filter_is_primary.clone(),
            is_backup: query.filter_is_backup.clone(),
            requires_verification: query.filter_requires_verification.clone(),
            auto_approval_enabled: query.filter_auto_approval_enabled.clone(),
            enrolled_by: query.filter_enrolled_by.clone(),
            enrollment_method: query.filter_enrollment_method.clone(),
            enrollment_ip: query.filter_enrollment_ip.clone(),
            enrollment_user_agent: query.filter_enrollment_user_agent.clone(),
            backup_codes_generated: query.filter_backup_codes_generated.clone(),
            is_locked: query.filter_is_locked.clone(),
            lock_reason: query.filter_lock_reason.clone(),
            is_active: query.filter_is_active.clone(),
            status: query.filter_status.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveMFADeviceQuery;
// <<< CUSTOM QUERIES END >>>