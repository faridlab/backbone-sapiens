//! Queries for Session aggregate
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! CQRS Queries represent requests for data without side effects.

use async_trait::async_trait;
use anyhow::Result;
use serde::{Deserialize, Serialize};

use crate::domain::entity::Session;
use crate::infrastructure::persistence::{SessionRepository, PaginationParams, PaginatedResult, SessionFilter};
use super::QueryHandler;

// ============================================================================
// QUERY TRAIT (DEPRECATED)
// ============================================================================

/// Query trait for Session operations
///
/// **DEPRECATED**: Use the generic `QueryHandler<Q>` trait from `mod.rs` instead.
/// This trait is kept for backwards compatibility.
#[deprecated(since = "0.2.0", note = "Use QueryHandler<Q> trait instead")]
#[async_trait]
pub trait SessionQuery: Send + Sync {
    type Output;
    async fn execute(&self) -> Result<Self::Output>;
}

// ============================================================================
// GET BY ID QUERY
// ============================================================================

/// Query to get a session by ID
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetSessionByIdQuery {
    pub id: String,
}

/// Handler for GetSessionByIdQuery
pub struct GetSessionByIdHandler<R: SessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SessionRepository> GetSessionByIdHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SessionRepository + 'static> QueryHandler<GetSessionByIdQuery> for GetSessionByIdHandler<R> {
    type Output = Option<Session>;

    async fn handle(&self, query: GetSessionByIdQuery) -> Result<Self::Output> {
        self.repository.find_by_id(&query.id).await
    }
}

// ============================================================================
// LIST QUERY
// ============================================================================

/// Query to list session entities with pagination
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ListSessionQuery {
    pub page: u32,
    pub per_page: u32,
    pub filter_user_id: Option<Uuid>,
    pub filter_token_hash: Option<String>,
    pub filter_remember_me: Option<bool>,
    pub filter_user_agent: Option<String>,
    pub filter_device_type: Option<DeviceType>,
    pub filter_device_fingerprint: Option<String>,
    pub filter_is_active: Option<bool>,
}

impl Default for ListSessionQuery {
    fn default() -> Self {
        Self {
            page: 1,
            per_page: 20,
            filter_user_id: None,
            filter_token_hash: None,
            filter_remember_me: None,
            filter_user_agent: None,
            filter_device_type: None,
            filter_device_fingerprint: None,
            filter_is_active: None,
        }
    }
}

/// Handler for ListSessionQuery
pub struct ListSessionHandler<R: SessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SessionRepository> ListSessionHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SessionRepository + 'static> QueryHandler<ListSessionQuery> for ListSessionHandler<R> {
    type Output = PaginatedResult<Session>;

    async fn handle(&self, query: ListSessionQuery) -> Result<Self::Output> {
        let params = PaginationParams::new(query.page, query.per_page);

        // Build filter from query parameters
        let filters = SessionFilter {
            user_id: query.filter_user_id.clone(),
            token_hash: query.filter_token_hash.clone(),
            remember_me: query.filter_remember_me.clone(),
            user_agent: query.filter_user_agent.clone(),
            device_type: query.filter_device_type.clone(),
            device_fingerprint: query.filter_device_fingerprint.clone(),
            is_active: query.filter_is_active.clone(),
            ..Default::default()
        };

        // Use list_with_filters if any filters are set
        if filters.has_filters() {
            self.repository.list_with_filters(params, filters).await
        } else {
            self.repository.list(params).await
        }
    }
}

// ============================================================================
// FIND BY UNIQUE FIELDS
// ============================================================================

/// Query to find session by token_hash
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct GetSessionByTokenHashQuery {
    pub token_hash: String,
}

/// Handler for GetSessionByTokenHashQuery
pub struct GetSessionByTokenHashHandler<R: SessionRepository> {
    repository: std::sync::Arc<R>,
}

impl<R: SessionRepository> GetSessionByTokenHashHandler<R> {
    pub fn new(repository: std::sync::Arc<R>) -> Self {
        Self { repository }
    }
}

#[async_trait]
impl<R: SessionRepository + 'static> QueryHandler<GetSessionByTokenHashQuery> for GetSessionByTokenHashHandler<R> {
    type Output = Option<Session>;

    async fn handle(&self, query: GetSessionByTokenHashQuery) -> Result<Self::Output> {
        self.repository.find_by_token_hash(&query.token_hash).await
    }
}

// ============================================================================
// CUSTOM QUERIES
// ============================================================================
// <<< CUSTOM QUERIES START >>>

// <<< CUSTOM QUERIES START >>>
// Add your custom queries here
// Example:
//
// #[derive(Debug, Clone, Serialize, Deserialize)]
// pub struct GetActiveSessionQuery;
// <<< CUSTOM QUERIES END >>>