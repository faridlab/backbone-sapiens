//! Auth configuration for AnalyticsReport handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<AnalyticsReport>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AnalyticsReportService;
use crate::domain::entity::AnalyticsReport;

/// Permission string constants for AnalyticsReport operations.
pub mod permissions {
    pub const LIST:        &str = "analytics_report:list";
    pub const READ:        &str = "analytics_report:read";
    pub const CREATE:      &str = "analytics_report:create";
    pub const UPDATE:      &str = "analytics_report:update";
    pub const DELETE:      &str = "analytics_report:delete";
    pub const RESTORE:     &str = "analytics_report:restore";
    pub const TRASH:       &str = "analytics_report:trash";
    pub const EMPTY_TRASH: &str = "analytics_report:empty_trash";
    pub const BULK_CREATE: &str = "analytics_report:bulk_create";
    pub const UPSERT:      &str = "analytics_report:upsert";
}

/// Resource policy for AnalyticsReport — maps CRUD actions to permission strings.
pub struct AnalyticsReportPolicy;

#[async_trait]
impl ResourcePolicy<AnalyticsReport> for AnalyticsReportPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &AnalyticsReport,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for AnalyticsReport handlers.
///
/// Wraps `AnalyticsReportService` and checks `AnalyticsReportPolicy` before delegating.
pub type AnalyticsReportGuard = PermissionGuard<AnalyticsReport, AnalyticsReportService, AnalyticsReportPolicy>;

/// Service-integrated permission guard for AnalyticsReport (for use in generated handlers).
pub type AnalyticsReportServiceGuard = ServicePermissionGuard<AnalyticsReport, AnalyticsReportService, AnalyticsReportPolicy>;

// <<< CUSTOM
// END CUSTOM
