//! Auth configuration for SessionLimit handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<SessionLimit>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::SessionLimitService;
use crate::domain::entity::SessionLimit;

/// Permission string constants for SessionLimit operations.
pub mod permissions {
    pub const LIST:        &str = "session_limit:list";
    pub const READ:        &str = "session_limit:read";
    pub const CREATE:      &str = "session_limit:create";
    pub const UPDATE:      &str = "session_limit:update";
    pub const DELETE:      &str = "session_limit:delete";
    pub const RESTORE:     &str = "session_limit:restore";
    pub const TRASH:       &str = "session_limit:trash";
    pub const EMPTY_TRASH: &str = "session_limit:empty_trash";
    pub const BULK_CREATE: &str = "session_limit:bulk_create";
    pub const UPSERT:      &str = "session_limit:upsert";
}

/// Resource policy for SessionLimit — maps CRUD actions to permission strings.
pub struct SessionLimitPolicy;

#[async_trait]
impl ResourcePolicy<SessionLimit> for SessionLimitPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &SessionLimit,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for SessionLimit handlers.
///
/// Wraps `SessionLimitService` and checks `SessionLimitPolicy` before delegating.
pub type SessionLimitGuard = PermissionGuard<SessionLimit, SessionLimitService, SessionLimitPolicy>;

/// Service-integrated permission guard for SessionLimit (for use in generated handlers).
pub type SessionLimitServiceGuard = ServicePermissionGuard<SessionLimit, SessionLimitService, SessionLimitPolicy>;

// <<< CUSTOM
// END CUSTOM
