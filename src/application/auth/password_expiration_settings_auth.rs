//! Auth configuration for PasswordExpirationSettings handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PasswordExpirationSettings>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PasswordExpirationSettingsService;
use crate::domain::entity::PasswordExpirationSettings;

/// Permission string constants for PasswordExpirationSettings operations.
pub mod permissions {
    pub const LIST:        &str = "password_expiration_settings:list";
    pub const READ:        &str = "password_expiration_settings:read";
    pub const CREATE:      &str = "password_expiration_settings:create";
    pub const UPDATE:      &str = "password_expiration_settings:update";
    pub const DELETE:      &str = "password_expiration_settings:delete";
    pub const RESTORE:     &str = "password_expiration_settings:restore";
    pub const TRASH:       &str = "password_expiration_settings:trash";
    pub const EMPTY_TRASH: &str = "password_expiration_settings:empty_trash";
    pub const BULK_CREATE: &str = "password_expiration_settings:bulk_create";
    pub const UPSERT:      &str = "password_expiration_settings:upsert";
}

/// Resource policy for PasswordExpirationSettings — maps CRUD actions to permission strings.
pub struct PasswordExpirationSettingsPolicy;

#[async_trait]
impl ResourcePolicy<PasswordExpirationSettings> for PasswordExpirationSettingsPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PasswordExpirationSettings,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for PasswordExpirationSettings handlers.
///
/// Wraps `PasswordExpirationSettingsService` and checks `PasswordExpirationSettingsPolicy` before delegating.
pub type PasswordExpirationSettingsGuard = PermissionGuard<PasswordExpirationSettings, PasswordExpirationSettingsService, PasswordExpirationSettingsPolicy>;

/// Service-integrated permission guard for PasswordExpirationSettings (for use in generated handlers).
pub type PasswordExpirationSettingsServiceGuard = ServicePermissionGuard<PasswordExpirationSettings, PasswordExpirationSettingsService, PasswordExpirationSettingsPolicy>;

// <<< CUSTOM
// END CUSTOM
