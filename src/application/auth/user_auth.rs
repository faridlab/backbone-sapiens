//! Auth configuration for User handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<User>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::UserService;
use crate::domain::entity::User;

/// Permission string constants for User operations.
pub mod permissions {
    pub const LIST:        &str = "user:list";
    pub const READ:        &str = "user:read";
    pub const CREATE:      &str = "user:create";
    pub const UPDATE:      &str = "user:update";
    pub const DELETE:      &str = "user:delete";
    pub const RESTORE:     &str = "user:restore";
    pub const TRASH:       &str = "user:trash";
    pub const EMPTY_TRASH: &str = "user:empty_trash";
    pub const BULK_CREATE: &str = "user:bulk_create";
    pub const UPSERT:      &str = "user:upsert";
}

/// Resource policy for User — maps CRUD actions to permission strings.
pub struct UserPolicy;

#[async_trait]
impl ResourcePolicy<User> for UserPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &User,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for User handlers.
///
/// Wraps `UserService` and checks `UserPolicy` before delegating.
pub type UserGuard = PermissionGuard<User, UserService, UserPolicy>;

/// Service-integrated permission guard for User (for use in generated handlers).
pub type UserServiceGuard = ServicePermissionGuard<User, UserService, UserPolicy>;

// <<< CUSTOM
// END CUSTOM
