//! Auth configuration for BackupCode handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<BackupCode>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::BackupCodeService;
use crate::domain::entity::BackupCode;

/// Permission string constants for BackupCode operations.
pub mod permissions {
    pub const LIST:        &str = "backup_code:list";
    pub const READ:        &str = "backup_code:read";
    pub const CREATE:      &str = "backup_code:create";
    pub const UPDATE:      &str = "backup_code:update";
    pub const DELETE:      &str = "backup_code:delete";
    pub const RESTORE:     &str = "backup_code:restore";
    pub const TRASH:       &str = "backup_code:trash";
    pub const EMPTY_TRASH: &str = "backup_code:empty_trash";
    pub const BULK_CREATE: &str = "backup_code:bulk_create";
    pub const UPSERT:      &str = "backup_code:upsert";
}

/// Resource policy for BackupCode — maps CRUD actions to permission strings.
pub struct BackupCodePolicy;

#[async_trait]
impl ResourcePolicy<BackupCode> for BackupCodePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &BackupCode,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for BackupCode handlers.
///
/// Wraps `BackupCodeService` and checks `BackupCodePolicy` before delegating.
pub type BackupCodeGuard = PermissionGuard<BackupCode>;

/// Service-integrated permission guard for BackupCode (for use in generated handlers).
pub type BackupCodeServiceGuard = ServicePermissionGuard<BackupCode, BackupCodeService, BackupCodePolicy>;

// <<< CUSTOM
// END CUSTOM
