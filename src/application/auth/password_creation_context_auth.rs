//! Auth configuration for PasswordCreationContext handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PasswordCreationContext>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PasswordCreationContextService;
use crate::domain::entity::PasswordCreationContext;

/// Permission string constants for PasswordCreationContext operations.
pub mod permissions {
    pub const LIST:        &str = "password_creation_context:list";
    pub const READ:        &str = "password_creation_context:read";
    pub const CREATE:      &str = "password_creation_context:create";
    pub const UPDATE:      &str = "password_creation_context:update";
    pub const DELETE:      &str = "password_creation_context:delete";
    pub const RESTORE:     &str = "password_creation_context:restore";
    pub const TRASH:       &str = "password_creation_context:trash";
    pub const EMPTY_TRASH: &str = "password_creation_context:empty_trash";
    pub const BULK_CREATE: &str = "password_creation_context:bulk_create";
    pub const UPSERT:      &str = "password_creation_context:upsert";
}

/// Resource policy for PasswordCreationContext — maps CRUD actions to permission strings.
pub struct PasswordCreationContextPolicy;

#[async_trait]
impl ResourcePolicy<PasswordCreationContext> for PasswordCreationContextPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PasswordCreationContext,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for PasswordCreationContext handlers.
///
/// Wraps `PasswordCreationContextService` and checks `PasswordCreationContextPolicy` before delegating.
pub type PasswordCreationContextGuard = PermissionGuard<PasswordCreationContext, PasswordCreationContextService, PasswordCreationContextPolicy>;

/// Service-integrated permission guard for PasswordCreationContext (for use in generated handlers).
pub type PasswordCreationContextServiceGuard = ServicePermissionGuard<PasswordCreationContext, PasswordCreationContextService, PasswordCreationContextPolicy>;

// <<< CUSTOM
// END CUSTOM
