//! Auth configuration for OAuthProvider handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<OAuthProvider>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::OAuthProviderService;
use crate::domain::entity::OAuthProvider;

/// Permission string constants for OAuthProvider operations.
pub mod permissions {
    pub const LIST:        &str = "o_auth_provider:list";
    pub const READ:        &str = "o_auth_provider:read";
    pub const CREATE:      &str = "o_auth_provider:create";
    pub const UPDATE:      &str = "o_auth_provider:update";
    pub const DELETE:      &str = "o_auth_provider:delete";
    pub const RESTORE:     &str = "o_auth_provider:restore";
    pub const TRASH:       &str = "o_auth_provider:trash";
    pub const EMPTY_TRASH: &str = "o_auth_provider:empty_trash";
    pub const BULK_CREATE: &str = "o_auth_provider:bulk_create";
    pub const UPSERT:      &str = "o_auth_provider:upsert";
}

/// Resource policy for OAuthProvider — maps CRUD actions to permission strings.
pub struct OAuthProviderPolicy;

#[async_trait]
impl ResourcePolicy<OAuthProvider> for OAuthProviderPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &OAuthProvider,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for OAuthProvider handlers.
///
/// Wraps `OAuthProviderService` and checks `OAuthProviderPolicy` before delegating.
pub type OAuthProviderGuard = PermissionGuard<OAuthProvider, OAuthProviderService, OAuthProviderPolicy>;

/// Service-integrated permission guard for OAuthProvider (for use in generated handlers).
pub type OAuthProviderServiceGuard = ServicePermissionGuard<OAuthProvider, OAuthProviderService, OAuthProviderPolicy>;

// <<< CUSTOM
// END CUSTOM
