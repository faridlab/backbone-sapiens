//! Auth configuration for SecurityEvent handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<SecurityEvent>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::SecurityEventService;
use crate::domain::entity::SecurityEvent;

/// Permission string constants for SecurityEvent operations.
pub mod permissions {
    pub const LIST:        &str = "security_event:list";
    pub const READ:        &str = "security_event:read";
    pub const CREATE:      &str = "security_event:create";
    pub const UPDATE:      &str = "security_event:update";
    pub const DELETE:      &str = "security_event:delete";
    pub const RESTORE:     &str = "security_event:restore";
    pub const TRASH:       &str = "security_event:trash";
    pub const EMPTY_TRASH: &str = "security_event:empty_trash";
    pub const BULK_CREATE: &str = "security_event:bulk_create";
    pub const UPSERT:      &str = "security_event:upsert";
}

/// Resource policy for SecurityEvent — maps CRUD actions to permission strings.
pub struct SecurityEventPolicy;

#[async_trait]
impl ResourcePolicy<SecurityEvent> for SecurityEventPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &SecurityEvent,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for SecurityEvent handlers.
///
/// Wraps `SecurityEventService` and checks `SecurityEventPolicy` before delegating.
pub type SecurityEventGuard = PermissionGuard<SecurityEvent, SecurityEventService, SecurityEventPolicy>;

/// Service-integrated permission guard for SecurityEvent (for use in generated handlers).
pub type SecurityEventServiceGuard = ServicePermissionGuard<SecurityEvent, SecurityEventService, SecurityEventPolicy>;

// <<< CUSTOM
// END CUSTOM
