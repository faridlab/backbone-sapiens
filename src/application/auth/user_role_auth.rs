//! Auth configuration for UserRole handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<UserRole>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::UserRoleService;
use crate::domain::entity::UserRole;

/// Permission string constants for UserRole operations.
pub mod permissions {
    pub const LIST:        &str = "user_role:list";
    pub const READ:        &str = "user_role:read";
    pub const CREATE:      &str = "user_role:create";
    pub const UPDATE:      &str = "user_role:update";
    pub const DELETE:      &str = "user_role:delete";
    pub const RESTORE:     &str = "user_role:restore";
    pub const TRASH:       &str = "user_role:trash";
    pub const EMPTY_TRASH: &str = "user_role:empty_trash";
    pub const BULK_CREATE: &str = "user_role:bulk_create";
    pub const UPSERT:      &str = "user_role:upsert";
}

/// Resource policy for UserRole — maps CRUD actions to permission strings.
pub struct UserRolePolicy;

#[async_trait]
impl ResourcePolicy<UserRole> for UserRolePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &UserRole,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for UserRole handlers.
///
/// Wraps `UserRoleService` and checks `UserRolePolicy` before delegating.
pub type UserRoleGuard = PermissionGuard<UserRole, UserRoleService, UserRolePolicy>;

/// Service-integrated permission guard for UserRole (for use in generated handlers).
pub type UserRoleServiceGuard = ServicePermissionGuard<UserRole, UserRoleService, UserRolePolicy>;

// <<< CUSTOM
// END CUSTOM
