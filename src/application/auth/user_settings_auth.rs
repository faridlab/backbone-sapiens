//! Auth configuration for UserSettings handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<UserSettings>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::UserSettingsService;
use crate::domain::entity::UserSettings;

/// Permission string constants for UserSettings operations.
pub mod permissions {
    pub const LIST:        &str = "user_settings:list";
    pub const READ:        &str = "user_settings:read";
    pub const CREATE:      &str = "user_settings:create";
    pub const UPDATE:      &str = "user_settings:update";
    pub const DELETE:      &str = "user_settings:delete";
    pub const RESTORE:     &str = "user_settings:restore";
    pub const TRASH:       &str = "user_settings:trash";
    pub const EMPTY_TRASH: &str = "user_settings:empty_trash";
    pub const BULK_CREATE: &str = "user_settings:bulk_create";
    pub const UPSERT:      &str = "user_settings:upsert";
}

/// Resource policy for UserSettings — maps CRUD actions to permission strings.
pub struct UserSettingsPolicy;

#[async_trait]
impl ResourcePolicy<UserSettings> for UserSettingsPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &UserSettings,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for UserSettings handlers.
///
/// Wraps `UserSettingsService` and checks `UserSettingsPolicy` before delegating.
pub type UserSettingsGuard = PermissionGuard<UserSettings, UserSettingsService, UserSettingsPolicy>;

/// Service-integrated permission guard for UserSettings (for use in generated handlers).
pub type UserSettingsServiceGuard = ServicePermissionGuard<UserSettings, UserSettingsService, UserSettingsPolicy>;

// <<< CUSTOM
// END CUSTOM
