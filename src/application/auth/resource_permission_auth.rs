//! Auth configuration for ResourcePermission handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ResourcePermission>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ResourcePermissionService;
use crate::domain::entity::ResourcePermission;

/// Permission string constants for ResourcePermission operations.
pub mod permissions {
    pub const LIST:        &str = "resource_permission:list";
    pub const READ:        &str = "resource_permission:read";
    pub const CREATE:      &str = "resource_permission:create";
    pub const UPDATE:      &str = "resource_permission:update";
    pub const DELETE:      &str = "resource_permission:delete";
    pub const RESTORE:     &str = "resource_permission:restore";
    pub const TRASH:       &str = "resource_permission:trash";
    pub const EMPTY_TRASH: &str = "resource_permission:empty_trash";
    pub const BULK_CREATE: &str = "resource_permission:bulk_create";
    pub const UPSERT:      &str = "resource_permission:upsert";
}

/// Resource policy for ResourcePermission — maps CRUD actions to permission strings.
pub struct ResourcePermissionPolicy;

#[async_trait]
impl ResourcePolicy<ResourcePermission> for ResourcePermissionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ResourcePermission,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for ResourcePermission handlers.
///
/// Wraps `ResourcePermissionService` and checks `ResourcePermissionPolicy` before delegating.
pub type ResourcePermissionGuard = PermissionGuard<ResourcePermission, ResourcePermissionService, ResourcePermissionPolicy>;

/// Service-integrated permission guard for ResourcePermission (for use in generated handlers).
pub type ResourcePermissionServiceGuard = ServicePermissionGuard<ResourcePermission, ResourcePermissionService, ResourcePermissionPolicy>;

// <<< CUSTOM
// END CUSTOM
