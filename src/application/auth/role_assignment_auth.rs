//! Auth configuration for RoleAssignment handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<RoleAssignment>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::RoleAssignmentService;
use crate::domain::entity::RoleAssignment;

/// Permission string constants for RoleAssignment operations.
pub mod permissions {
    pub const LIST:        &str = "role_assignment:list";
    pub const READ:        &str = "role_assignment:read";
    pub const CREATE:      &str = "role_assignment:create";
    pub const UPDATE:      &str = "role_assignment:update";
    pub const DELETE:      &str = "role_assignment:delete";
    pub const RESTORE:     &str = "role_assignment:restore";
    pub const TRASH:       &str = "role_assignment:trash";
    pub const EMPTY_TRASH: &str = "role_assignment:empty_trash";
    pub const BULK_CREATE: &str = "role_assignment:bulk_create";
    pub const UPSERT:      &str = "role_assignment:upsert";
}

/// Resource policy for RoleAssignment — maps CRUD actions to permission strings.
pub struct RoleAssignmentPolicy;

#[async_trait]
impl ResourcePolicy<RoleAssignment> for RoleAssignmentPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &RoleAssignment,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for RoleAssignment handlers.
///
/// Wraps `RoleAssignmentService` and checks `RoleAssignmentPolicy` before delegating.
pub type RoleAssignmentGuard = PermissionGuard<RoleAssignment, RoleAssignmentService, RoleAssignmentPolicy>;

/// Service-integrated permission guard for RoleAssignment (for use in generated handlers).
pub type RoleAssignmentServiceGuard = ServicePermissionGuard<RoleAssignment, RoleAssignmentService, RoleAssignmentPolicy>;

// <<< CUSTOM
// END CUSTOM
