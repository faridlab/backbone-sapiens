//! Auth configuration for PermissionConflict handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PermissionConflict>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PermissionConflictService;
use crate::domain::entity::PermissionConflict;

/// Permission string constants for PermissionConflict operations.
pub mod permissions {
    pub const LIST:        &str = "permission_conflict:list";
    pub const READ:        &str = "permission_conflict:read";
    pub const CREATE:      &str = "permission_conflict:create";
    pub const UPDATE:      &str = "permission_conflict:update";
    pub const DELETE:      &str = "permission_conflict:delete";
    pub const RESTORE:     &str = "permission_conflict:restore";
    pub const TRASH:       &str = "permission_conflict:trash";
    pub const EMPTY_TRASH: &str = "permission_conflict:empty_trash";
    pub const BULK_CREATE: &str = "permission_conflict:bulk_create";
    pub const UPSERT:      &str = "permission_conflict:upsert";
}

/// Resource policy for PermissionConflict — maps CRUD actions to permission strings.
pub struct PermissionConflictPolicy;

#[async_trait]
impl ResourcePolicy<PermissionConflict> for PermissionConflictPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PermissionConflict,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for PermissionConflict handlers.
///
/// Wraps `PermissionConflictService` and checks `PermissionConflictPolicy` before delegating.
pub type PermissionConflictGuard = PermissionGuard<PermissionConflict, PermissionConflictService, PermissionConflictPolicy>;

/// Service-integrated permission guard for PermissionConflict (for use in generated handlers).
pub type PermissionConflictServiceGuard = ServicePermissionGuard<PermissionConflict, PermissionConflictService, PermissionConflictPolicy>;

// <<< CUSTOM
// END CUSTOM
