//! Auth configuration for TemporaryPermission handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<TemporaryPermission>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::TemporaryPermissionService;
use crate::domain::entity::TemporaryPermission;

/// Permission string constants for TemporaryPermission operations.
pub mod permissions {
    pub const LIST:        &str = "temporary_permission:list";
    pub const READ:        &str = "temporary_permission:read";
    pub const CREATE:      &str = "temporary_permission:create";
    pub const UPDATE:      &str = "temporary_permission:update";
    pub const DELETE:      &str = "temporary_permission:delete";
    pub const RESTORE:     &str = "temporary_permission:restore";
    pub const TRASH:       &str = "temporary_permission:trash";
    pub const EMPTY_TRASH: &str = "temporary_permission:empty_trash";
    pub const BULK_CREATE: &str = "temporary_permission:bulk_create";
    pub const UPSERT:      &str = "temporary_permission:upsert";
}

/// Resource policy for TemporaryPermission — maps CRUD actions to permission strings.
pub struct TemporaryPermissionPolicy;

#[async_trait]
impl ResourcePolicy<TemporaryPermission> for TemporaryPermissionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &TemporaryPermission,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for TemporaryPermission handlers.
///
/// Wraps `TemporaryPermissionService` and checks `TemporaryPermissionPolicy` before delegating.
pub type TemporaryPermissionGuard = PermissionGuard<TemporaryPermission>;

/// Service-integrated permission guard for TemporaryPermission (for use in generated handlers).
pub type TemporaryPermissionServiceGuard = ServicePermissionGuard<TemporaryPermission, TemporaryPermissionService, TemporaryPermissionPolicy>;

// <<< CUSTOM
// END CUSTOM
