//! Auth configuration for OrganizationRole handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<OrganizationRole>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::OrganizationRoleService;
use crate::domain::entity::OrganizationRole;

/// Permission string constants for OrganizationRole operations.
pub mod permissions {
    pub const LIST:        &str = "organization_role:list";
    pub const READ:        &str = "organization_role:read";
    pub const CREATE:      &str = "organization_role:create";
    pub const UPDATE:      &str = "organization_role:update";
    pub const DELETE:      &str = "organization_role:delete";
    pub const RESTORE:     &str = "organization_role:restore";
    pub const TRASH:       &str = "organization_role:trash";
    pub const EMPTY_TRASH: &str = "organization_role:empty_trash";
    pub const BULK_CREATE: &str = "organization_role:bulk_create";
    pub const UPSERT:      &str = "organization_role:upsert";
}

/// Resource policy for OrganizationRole — maps CRUD actions to permission strings.
pub struct OrganizationRolePolicy;

#[async_trait]
impl ResourcePolicy<OrganizationRole> for OrganizationRolePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &OrganizationRole,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for OrganizationRole handlers.
///
/// Wraps `OrganizationRoleService` and checks `OrganizationRolePolicy` before delegating.
pub type OrganizationRoleGuard = PermissionGuard<OrganizationRole, OrganizationRoleService, OrganizationRolePolicy>;

/// Service-integrated permission guard for OrganizationRole (for use in generated handlers).
pub type OrganizationRoleServiceGuard = ServicePermissionGuard<OrganizationRole, OrganizationRoleService, OrganizationRolePolicy>;

// <<< CUSTOM
// END CUSTOM
