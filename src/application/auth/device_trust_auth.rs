//! Auth configuration for DeviceTrust handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<DeviceTrust>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::DeviceTrustService;
use crate::domain::entity::DeviceTrust;

/// Permission string constants for DeviceTrust operations.
pub mod permissions {
    pub const LIST:        &str = "device_trust:list";
    pub const READ:        &str = "device_trust:read";
    pub const CREATE:      &str = "device_trust:create";
    pub const UPDATE:      &str = "device_trust:update";
    pub const DELETE:      &str = "device_trust:delete";
    pub const RESTORE:     &str = "device_trust:restore";
    pub const TRASH:       &str = "device_trust:trash";
    pub const EMPTY_TRASH: &str = "device_trust:empty_trash";
    pub const BULK_CREATE: &str = "device_trust:bulk_create";
    pub const UPSERT:      &str = "device_trust:upsert";
}

/// Resource policy for DeviceTrust — maps CRUD actions to permission strings.
pub struct DeviceTrustPolicy;

#[async_trait]
impl ResourcePolicy<DeviceTrust> for DeviceTrustPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &DeviceTrust,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for DeviceTrust handlers.
///
/// Wraps `DeviceTrustService` and checks `DeviceTrustPolicy` before delegating.
pub type DeviceTrustGuard = PermissionGuard<DeviceTrust, DeviceTrustService, DeviceTrustPolicy>;

/// Service-integrated permission guard for DeviceTrust (for use in generated handlers).
pub type DeviceTrustServiceGuard = ServicePermissionGuard<DeviceTrust, DeviceTrustService, DeviceTrustPolicy>;

// <<< CUSTOM
// END CUSTOM
