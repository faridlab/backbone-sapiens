//! Auth configuration for EmailVerificationToken handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<EmailVerificationToken>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::EmailVerificationTokenService;
use crate::domain::entity::EmailVerificationToken;

/// Permission string constants for EmailVerificationToken operations.
pub mod permissions {
    pub const LIST:        &str = "email_verification_token:list";
    pub const READ:        &str = "email_verification_token:read";
    pub const CREATE:      &str = "email_verification_token:create";
    pub const UPDATE:      &str = "email_verification_token:update";
    pub const DELETE:      &str = "email_verification_token:delete";
    pub const RESTORE:     &str = "email_verification_token:restore";
    pub const TRASH:       &str = "email_verification_token:trash";
    pub const EMPTY_TRASH: &str = "email_verification_token:empty_trash";
    pub const BULK_CREATE: &str = "email_verification_token:bulk_create";
    pub const UPSERT:      &str = "email_verification_token:upsert";
}

/// Resource policy for EmailVerificationToken — maps CRUD actions to permission strings.
pub struct EmailVerificationTokenPolicy;

#[async_trait]
impl ResourcePolicy<EmailVerificationToken> for EmailVerificationTokenPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &EmailVerificationToken,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for EmailVerificationToken handlers.
///
/// Wraps `EmailVerificationTokenService` and checks `EmailVerificationTokenPolicy` before delegating.
pub type EmailVerificationTokenGuard = PermissionGuard<EmailVerificationToken, EmailVerificationTokenService, EmailVerificationTokenPolicy>;

/// Service-integrated permission guard for EmailVerificationToken (for use in generated handlers).
pub type EmailVerificationTokenServiceGuard = ServicePermissionGuard<EmailVerificationToken, EmailVerificationTokenService, EmailVerificationTokenPolicy>;

// <<< CUSTOM
// END CUSTOM
