//! Auth configuration for MFASession handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<MFASession>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::MFASessionService;
use crate::domain::entity::MFASession;

/// Permission string constants for MFASession operations.
pub mod permissions {
    pub const LIST:        &str = "mfa_session:list";
    pub const READ:        &str = "mfa_session:read";
    pub const CREATE:      &str = "mfa_session:create";
    pub const UPDATE:      &str = "mfa_session:update";
    pub const DELETE:      &str = "mfa_session:delete";
    pub const RESTORE:     &str = "mfa_session:restore";
    pub const TRASH:       &str = "mfa_session:trash";
    pub const EMPTY_TRASH: &str = "mfa_session:empty_trash";
    pub const BULK_CREATE: &str = "mfa_session:bulk_create";
    pub const UPSERT:      &str = "mfa_session:upsert";
}

/// Resource policy for MFASession — maps CRUD actions to permission strings.
pub struct MFASessionPolicy;

#[async_trait]
impl ResourcePolicy<MFASession> for MFASessionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &MFASession,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for MFASession handlers.
///
/// Wraps `MFASessionService` and checks `MFASessionPolicy` before delegating.
pub type MFASessionGuard = PermissionGuard<MFASession, MFASessionService, MFASessionPolicy>;

/// Service-integrated permission guard for MFASession (for use in generated handlers).
pub type MFASessionServiceGuard = ServicePermissionGuard<MFASession, MFASessionService, MFASessionPolicy>;

// <<< CUSTOM
// END CUSTOM
