//! Auth configuration for ImpersonationSession handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<ImpersonationSession>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::ImpersonationSessionService;
use crate::domain::entity::ImpersonationSession;

/// Permission string constants for ImpersonationSession operations.
pub mod permissions {
    pub const LIST:        &str = "impersonation_session:list";
    pub const READ:        &str = "impersonation_session:read";
    pub const CREATE:      &str = "impersonation_session:create";
    pub const UPDATE:      &str = "impersonation_session:update";
    pub const DELETE:      &str = "impersonation_session:delete";
    pub const RESTORE:     &str = "impersonation_session:restore";
    pub const TRASH:       &str = "impersonation_session:trash";
    pub const EMPTY_TRASH: &str = "impersonation_session:empty_trash";
    pub const BULK_CREATE: &str = "impersonation_session:bulk_create";
    pub const UPSERT:      &str = "impersonation_session:upsert";
}

/// Resource policy for ImpersonationSession — maps CRUD actions to permission strings.
pub struct ImpersonationSessionPolicy;

#[async_trait]
impl ResourcePolicy<ImpersonationSession> for ImpersonationSessionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &ImpersonationSession,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for ImpersonationSession handlers.
///
/// Wraps `ImpersonationSessionService` and checks `ImpersonationSessionPolicy` before delegating.
pub type ImpersonationSessionGuard = PermissionGuard<ImpersonationSession, ImpersonationSessionService, ImpersonationSessionPolicy>;

/// Service-integrated permission guard for ImpersonationSession (for use in generated handlers).
pub type ImpersonationSessionServiceGuard = ServicePermissionGuard<ImpersonationSession, ImpersonationSessionService, ImpersonationSessionPolicy>;

// <<< CUSTOM
// END CUSTOM
