//! Auth configuration for UserSAMLLink handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<UserSAMLLink>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::UserSAMLLinkService;
use crate::domain::entity::UserSAMLLink;

/// Permission string constants for UserSAMLLink operations.
pub mod permissions {
    pub const LIST:        &str = "user_saml_link:list";
    pub const READ:        &str = "user_saml_link:read";
    pub const CREATE:      &str = "user_saml_link:create";
    pub const UPDATE:      &str = "user_saml_link:update";
    pub const DELETE:      &str = "user_saml_link:delete";
    pub const RESTORE:     &str = "user_saml_link:restore";
    pub const TRASH:       &str = "user_saml_link:trash";
    pub const EMPTY_TRASH: &str = "user_saml_link:empty_trash";
    pub const BULK_CREATE: &str = "user_saml_link:bulk_create";
    pub const UPSERT:      &str = "user_saml_link:upsert";
}

/// Resource policy for UserSAMLLink — maps CRUD actions to permission strings.
pub struct UserSAMLLinkPolicy;

#[async_trait]
impl ResourcePolicy<UserSAMLLink> for UserSAMLLinkPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &UserSAMLLink,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for UserSAMLLink handlers.
///
/// Wraps `UserSAMLLinkService` and checks `UserSAMLLinkPolicy` before delegating.
pub type UserSAMLLinkGuard = PermissionGuard<UserSAMLLink>;

/// Service-integrated permission guard for UserSAMLLink (for use in generated handlers).
pub type UserSAMLLinkServiceGuard = ServicePermissionGuard<UserSAMLLink, UserSAMLLinkService, UserSAMLLinkPolicy>;

// <<< CUSTOM
// END CUSTOM
