//! Auth configuration for EffectivePermissionCache handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<EffectivePermissionCache>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::EffectivePermissionCacheService;
use crate::domain::entity::EffectivePermissionCache;

/// Permission string constants for EffectivePermissionCache operations.
pub mod permissions {
    pub const LIST:        &str = "effective_permission_cache:list";
    pub const READ:        &str = "effective_permission_cache:read";
    pub const CREATE:      &str = "effective_permission_cache:create";
    pub const UPDATE:      &str = "effective_permission_cache:update";
    pub const DELETE:      &str = "effective_permission_cache:delete";
    pub const RESTORE:     &str = "effective_permission_cache:restore";
    pub const TRASH:       &str = "effective_permission_cache:trash";
    pub const EMPTY_TRASH: &str = "effective_permission_cache:empty_trash";
    pub const BULK_CREATE: &str = "effective_permission_cache:bulk_create";
    pub const UPSERT:      &str = "effective_permission_cache:upsert";
}

/// Resource policy for EffectivePermissionCache — maps CRUD actions to permission strings.
pub struct EffectivePermissionCachePolicy;

#[async_trait]
impl ResourcePolicy<EffectivePermissionCache> for EffectivePermissionCachePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &EffectivePermissionCache,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for EffectivePermissionCache handlers.
///
/// Wraps `EffectivePermissionCacheService` and checks `EffectivePermissionCachePolicy` before delegating.
pub type EffectivePermissionCacheGuard = PermissionGuard<EffectivePermissionCache, EffectivePermissionCacheService, EffectivePermissionCachePolicy>;

/// Service-integrated permission guard for EffectivePermissionCache (for use in generated handlers).
pub type EffectivePermissionCacheServiceGuard = ServicePermissionGuard<EffectivePermissionCache, EffectivePermissionCacheService, EffectivePermissionCachePolicy>;

// <<< CUSTOM
// END CUSTOM
