//! Auth configuration for UserPermission handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<UserPermission>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::UserPermissionService;
use crate::domain::entity::UserPermission;

/// Permission string constants for UserPermission operations.
pub mod permissions {
    pub const LIST:        &str = "user_permission:list";
    pub const READ:        &str = "user_permission:read";
    pub const CREATE:      &str = "user_permission:create";
    pub const UPDATE:      &str = "user_permission:update";
    pub const DELETE:      &str = "user_permission:delete";
    pub const RESTORE:     &str = "user_permission:restore";
    pub const TRASH:       &str = "user_permission:trash";
    pub const EMPTY_TRASH: &str = "user_permission:empty_trash";
    pub const BULK_CREATE: &str = "user_permission:bulk_create";
    pub const UPSERT:      &str = "user_permission:upsert";
}

/// Resource policy for UserPermission — maps CRUD actions to permission strings.
pub struct UserPermissionPolicy;

#[async_trait]
impl ResourcePolicy<UserPermission> for UserPermissionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &UserPermission,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for UserPermission handlers.
///
/// Wraps `UserPermissionService` and checks `UserPermissionPolicy` before delegating.
pub type UserPermissionGuard = PermissionGuard<UserPermission, UserPermissionService, UserPermissionPolicy>;

/// Service-integrated permission guard for UserPermission (for use in generated handlers).
pub type UserPermissionServiceGuard = ServicePermissionGuard<UserPermission, UserPermissionService, UserPermissionPolicy>;

// <<< CUSTOM
// END CUSTOM
