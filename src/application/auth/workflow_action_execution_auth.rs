//! Auth configuration for WorkflowActionExecution handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<WorkflowActionExecution>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::WorkflowActionExecutionService;
use crate::domain::entity::WorkflowActionExecution;

/// Permission string constants for WorkflowActionExecution operations.
pub mod permissions {
    pub const LIST:        &str = "workflow_action_execution:list";
    pub const READ:        &str = "workflow_action_execution:read";
    pub const CREATE:      &str = "workflow_action_execution:create";
    pub const UPDATE:      &str = "workflow_action_execution:update";
    pub const DELETE:      &str = "workflow_action_execution:delete";
    pub const RESTORE:     &str = "workflow_action_execution:restore";
    pub const TRASH:       &str = "workflow_action_execution:trash";
    pub const EMPTY_TRASH: &str = "workflow_action_execution:empty_trash";
    pub const BULK_CREATE: &str = "workflow_action_execution:bulk_create";
    pub const UPSERT:      &str = "workflow_action_execution:upsert";
}

/// Resource policy for WorkflowActionExecution — maps CRUD actions to permission strings.
pub struct WorkflowActionExecutionPolicy;

#[async_trait]
impl ResourcePolicy<WorkflowActionExecution> for WorkflowActionExecutionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &WorkflowActionExecution,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for WorkflowActionExecution handlers.
///
/// Wraps `WorkflowActionExecutionService` and checks `WorkflowActionExecutionPolicy` before delegating.
pub type WorkflowActionExecutionGuard = PermissionGuard<WorkflowActionExecution, WorkflowActionExecutionService, WorkflowActionExecutionPolicy>;

/// Service-integrated permission guard for WorkflowActionExecution (for use in generated handlers).
pub type WorkflowActionExecutionServiceGuard = ServicePermissionGuard<WorkflowActionExecution, WorkflowActionExecutionService, WorkflowActionExecutionPolicy>;

// <<< CUSTOM
// END CUSTOM
