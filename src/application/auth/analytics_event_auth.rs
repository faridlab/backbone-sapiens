//! Auth configuration for AnalyticsEvent handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<AnalyticsEvent>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AnalyticsEventService;
use crate::domain::entity::AnalyticsEvent;

/// Permission string constants for AnalyticsEvent operations.
pub mod permissions {
    pub const LIST:        &str = "analytics_event:list";
    pub const READ:        &str = "analytics_event:read";
    pub const CREATE:      &str = "analytics_event:create";
    pub const UPDATE:      &str = "analytics_event:update";
    pub const DELETE:      &str = "analytics_event:delete";
    pub const RESTORE:     &str = "analytics_event:restore";
    pub const TRASH:       &str = "analytics_event:trash";
    pub const EMPTY_TRASH: &str = "analytics_event:empty_trash";
    pub const BULK_CREATE: &str = "analytics_event:bulk_create";
    pub const UPSERT:      &str = "analytics_event:upsert";
}

/// Resource policy for AnalyticsEvent — maps CRUD actions to permission strings.
pub struct AnalyticsEventPolicy;

#[async_trait]
impl ResourcePolicy<AnalyticsEvent> for AnalyticsEventPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &AnalyticsEvent,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for AnalyticsEvent handlers.
///
/// Wraps `AnalyticsEventService` and checks `AnalyticsEventPolicy` before delegating.
pub type AnalyticsEventGuard = PermissionGuard<AnalyticsEvent, AnalyticsEventService, AnalyticsEventPolicy>;

/// Service-integrated permission guard for AnalyticsEvent (for use in generated handlers).
pub type AnalyticsEventServiceGuard = ServicePermissionGuard<AnalyticsEvent, AnalyticsEventService, AnalyticsEventPolicy>;

// <<< CUSTOM
// END CUSTOM
