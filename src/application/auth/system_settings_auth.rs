//! Auth configuration for SystemSettings handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<SystemSettings>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::SystemSettingsService;
use crate::domain::entity::SystemSettings;

/// Permission string constants for SystemSettings operations.
pub mod permissions {
    pub const LIST:        &str = "system_settings:list";
    pub const READ:        &str = "system_settings:read";
    pub const CREATE:      &str = "system_settings:create";
    pub const UPDATE:      &str = "system_settings:update";
    pub const DELETE:      &str = "system_settings:delete";
    pub const RESTORE:     &str = "system_settings:restore";
    pub const TRASH:       &str = "system_settings:trash";
    pub const EMPTY_TRASH: &str = "system_settings:empty_trash";
    pub const BULK_CREATE: &str = "system_settings:bulk_create";
    pub const UPSERT:      &str = "system_settings:upsert";
}

/// Resource policy for SystemSettings — maps CRUD actions to permission strings.
pub struct SystemSettingsPolicy;

#[async_trait]
impl ResourcePolicy<SystemSettings> for SystemSettingsPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &SystemSettings,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for SystemSettings handlers.
///
/// Wraps `SystemSettingsService` and checks `SystemSettingsPolicy` before delegating.
pub type SystemSettingsGuard = PermissionGuard<SystemSettings, SystemSettingsService, SystemSettingsPolicy>;

/// Service-integrated permission guard for SystemSettings (for use in generated handlers).
pub type SystemSettingsServiceGuard = ServicePermissionGuard<SystemSettings, SystemSettingsService, SystemSettingsPolicy>;

// <<< CUSTOM
// END CUSTOM
