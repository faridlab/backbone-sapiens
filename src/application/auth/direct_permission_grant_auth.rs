//! Auth configuration for DirectPermissionGrant handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<DirectPermissionGrant>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::DirectPermissionGrantService;
use crate::domain::entity::DirectPermissionGrant;

/// Permission string constants for DirectPermissionGrant operations.
pub mod permissions {
    pub const LIST:        &str = "direct_permission_grant:list";
    pub const READ:        &str = "direct_permission_grant:read";
    pub const CREATE:      &str = "direct_permission_grant:create";
    pub const UPDATE:      &str = "direct_permission_grant:update";
    pub const DELETE:      &str = "direct_permission_grant:delete";
    pub const RESTORE:     &str = "direct_permission_grant:restore";
    pub const TRASH:       &str = "direct_permission_grant:trash";
    pub const EMPTY_TRASH: &str = "direct_permission_grant:empty_trash";
    pub const BULK_CREATE: &str = "direct_permission_grant:bulk_create";
    pub const UPSERT:      &str = "direct_permission_grant:upsert";
}

/// Resource policy for DirectPermissionGrant — maps CRUD actions to permission strings.
pub struct DirectPermissionGrantPolicy;

#[async_trait]
impl ResourcePolicy<DirectPermissionGrant> for DirectPermissionGrantPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &DirectPermissionGrant,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for DirectPermissionGrant handlers.
///
/// Wraps `DirectPermissionGrantService` and checks `DirectPermissionGrantPolicy` before delegating.
pub type DirectPermissionGrantGuard = PermissionGuard<DirectPermissionGrant>;

/// Service-integrated permission guard for DirectPermissionGrant (for use in generated handlers).
pub type DirectPermissionGrantServiceGuard = ServicePermissionGuard<DirectPermissionGrant, DirectPermissionGrantService, DirectPermissionGrantPolicy>;

// <<< CUSTOM
// END CUSTOM
