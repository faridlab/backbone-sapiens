//! Auth configuration for AnalyticsMetric handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<AnalyticsMetric>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AnalyticsMetricService;
use crate::domain::entity::AnalyticsMetric;

/// Permission string constants for AnalyticsMetric operations.
pub mod permissions {
    pub const LIST:        &str = "analytics_metric:list";
    pub const READ:        &str = "analytics_metric:read";
    pub const CREATE:      &str = "analytics_metric:create";
    pub const UPDATE:      &str = "analytics_metric:update";
    pub const DELETE:      &str = "analytics_metric:delete";
    pub const RESTORE:     &str = "analytics_metric:restore";
    pub const TRASH:       &str = "analytics_metric:trash";
    pub const EMPTY_TRASH: &str = "analytics_metric:empty_trash";
    pub const BULK_CREATE: &str = "analytics_metric:bulk_create";
    pub const UPSERT:      &str = "analytics_metric:upsert";
}

/// Resource policy for AnalyticsMetric — maps CRUD actions to permission strings.
pub struct AnalyticsMetricPolicy;

#[async_trait]
impl ResourcePolicy<AnalyticsMetric> for AnalyticsMetricPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &AnalyticsMetric,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for AnalyticsMetric handlers.
///
/// Wraps `AnalyticsMetricService` and checks `AnalyticsMetricPolicy` before delegating.
pub type AnalyticsMetricGuard = PermissionGuard<AnalyticsMetric, AnalyticsMetricService, AnalyticsMetricPolicy>;

/// Service-integrated permission guard for AnalyticsMetric (for use in generated handlers).
pub type AnalyticsMetricServiceGuard = ServicePermissionGuard<AnalyticsMetric, AnalyticsMetricService, AnalyticsMetricPolicy>;

// <<< CUSTOM
// END CUSTOM
