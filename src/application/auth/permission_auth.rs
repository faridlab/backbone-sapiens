//! Auth configuration for Permission handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Permission>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PermissionService;
use crate::domain::entity::Permission;

/// Permission string constants for Permission operations.
pub mod permissions {
    pub const LIST:        &str = "permission:list";
    pub const READ:        &str = "permission:read";
    pub const CREATE:      &str = "permission:create";
    pub const UPDATE:      &str = "permission:update";
    pub const DELETE:      &str = "permission:delete";
    pub const RESTORE:     &str = "permission:restore";
    pub const TRASH:       &str = "permission:trash";
    pub const EMPTY_TRASH: &str = "permission:empty_trash";
    pub const BULK_CREATE: &str = "permission:bulk_create";
    pub const UPSERT:      &str = "permission:upsert";
}

/// Resource policy for Permission — maps CRUD actions to permission strings.
pub struct PermissionPolicy;

#[async_trait]
impl ResourcePolicy<Permission> for PermissionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Permission,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for Permission handlers.
///
/// Wraps `PermissionService` and checks `PermissionPolicy` before delegating.
pub type PermissionGuard = PermissionGuard<Permission, PermissionService, PermissionPolicy>;

/// Service-integrated permission guard for Permission (for use in generated handlers).
pub type PermissionServiceGuard = ServicePermissionGuard<Permission, PermissionService, PermissionPolicy>;

// <<< CUSTOM
// END CUSTOM
