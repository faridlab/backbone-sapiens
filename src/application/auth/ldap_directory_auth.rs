//! Auth configuration for LDAPDirectory handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<LDAPDirectory>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::LDAPDirectoryService;
use crate::domain::entity::LDAPDirectory;

/// Permission string constants for LDAPDirectory operations.
pub mod permissions {
    pub const LIST:        &str = "ldap_directory:list";
    pub const READ:        &str = "ldap_directory:read";
    pub const CREATE:      &str = "ldap_directory:create";
    pub const UPDATE:      &str = "ldap_directory:update";
    pub const DELETE:      &str = "ldap_directory:delete";
    pub const RESTORE:     &str = "ldap_directory:restore";
    pub const TRASH:       &str = "ldap_directory:trash";
    pub const EMPTY_TRASH: &str = "ldap_directory:empty_trash";
    pub const BULK_CREATE: &str = "ldap_directory:bulk_create";
    pub const UPSERT:      &str = "ldap_directory:upsert";
}

/// Resource policy for LDAPDirectory — maps CRUD actions to permission strings.
pub struct LDAPDirectoryPolicy;

#[async_trait]
impl ResourcePolicy<LDAPDirectory> for LDAPDirectoryPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &LDAPDirectory,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for LDAPDirectory handlers.
///
/// Wraps `LDAPDirectoryService` and checks `LDAPDirectoryPolicy` before delegating.
pub type LDAPDirectoryGuard = PermissionGuard<LDAPDirectory>;

/// Service-integrated permission guard for LDAPDirectory (for use in generated handlers).
pub type LDAPDirectoryServiceGuard = ServicePermissionGuard<LDAPDirectory, LDAPDirectoryService, LDAPDirectoryPolicy>;

// <<< CUSTOM
// END CUSTOM
