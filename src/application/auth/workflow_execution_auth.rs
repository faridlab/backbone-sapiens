//! Auth configuration for WorkflowExecution handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<WorkflowExecution>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::WorkflowExecutionService;
use crate::domain::entity::WorkflowExecution;

/// Permission string constants for WorkflowExecution operations.
pub mod permissions {
    pub const LIST:        &str = "workflow_execution:list";
    pub const READ:        &str = "workflow_execution:read";
    pub const CREATE:      &str = "workflow_execution:create";
    pub const UPDATE:      &str = "workflow_execution:update";
    pub const DELETE:      &str = "workflow_execution:delete";
    pub const RESTORE:     &str = "workflow_execution:restore";
    pub const TRASH:       &str = "workflow_execution:trash";
    pub const EMPTY_TRASH: &str = "workflow_execution:empty_trash";
    pub const BULK_CREATE: &str = "workflow_execution:bulk_create";
    pub const UPSERT:      &str = "workflow_execution:upsert";
}

/// Resource policy for WorkflowExecution — maps CRUD actions to permission strings.
pub struct WorkflowExecutionPolicy;

#[async_trait]
impl ResourcePolicy<WorkflowExecution> for WorkflowExecutionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &WorkflowExecution,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for WorkflowExecution handlers.
///
/// Wraps `WorkflowExecutionService` and checks `WorkflowExecutionPolicy` before delegating.
pub type WorkflowExecutionGuard = PermissionGuard<WorkflowExecution, WorkflowExecutionService, WorkflowExecutionPolicy>;

/// Service-integrated permission guard for WorkflowExecution (for use in generated handlers).
pub type WorkflowExecutionServiceGuard = ServicePermissionGuard<WorkflowExecution, WorkflowExecutionService, WorkflowExecutionPolicy>;

// <<< CUSTOM
// END CUSTOM
