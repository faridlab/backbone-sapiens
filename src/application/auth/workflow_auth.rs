//! Auth configuration for Workflow handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<Workflow>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::WorkflowService;
use crate::domain::entity::Workflow;

/// Permission string constants for Workflow operations.
pub mod permissions {
    pub const LIST:        &str = "workflow:list";
    pub const READ:        &str = "workflow:read";
    pub const CREATE:      &str = "workflow:create";
    pub const UPDATE:      &str = "workflow:update";
    pub const DELETE:      &str = "workflow:delete";
    pub const RESTORE:     &str = "workflow:restore";
    pub const TRASH:       &str = "workflow:trash";
    pub const EMPTY_TRASH: &str = "workflow:empty_trash";
    pub const BULK_CREATE: &str = "workflow:bulk_create";
    pub const UPSERT:      &str = "workflow:upsert";
}

/// Resource policy for Workflow — maps CRUD actions to permission strings.
pub struct WorkflowPolicy;

#[async_trait]
impl ResourcePolicy<Workflow> for WorkflowPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &Workflow,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for Workflow handlers.
///
/// Wraps `WorkflowService` and checks `WorkflowPolicy` before delegating.
pub type WorkflowGuard = PermissionGuard<Workflow, WorkflowService, WorkflowPolicy>;

/// Service-integrated permission guard for Workflow (for use in generated handlers).
pub type WorkflowServiceGuard = ServicePermissionGuard<Workflow, WorkflowService, WorkflowPolicy>;

// <<< CUSTOM
// END CUSTOM
