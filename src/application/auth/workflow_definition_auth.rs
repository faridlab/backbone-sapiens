//! Auth configuration for WorkflowDefinition handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<WorkflowDefinition>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::WorkflowDefinitionService;
use crate::domain::entity::WorkflowDefinition;

/// Permission string constants for WorkflowDefinition operations.
pub mod permissions {
    pub const LIST:        &str = "workflow_definition:list";
    pub const READ:        &str = "workflow_definition:read";
    pub const CREATE:      &str = "workflow_definition:create";
    pub const UPDATE:      &str = "workflow_definition:update";
    pub const DELETE:      &str = "workflow_definition:delete";
    pub const RESTORE:     &str = "workflow_definition:restore";
    pub const TRASH:       &str = "workflow_definition:trash";
    pub const EMPTY_TRASH: &str = "workflow_definition:empty_trash";
    pub const BULK_CREATE: &str = "workflow_definition:bulk_create";
    pub const UPSERT:      &str = "workflow_definition:upsert";
}

/// Resource policy for WorkflowDefinition — maps CRUD actions to permission strings.
pub struct WorkflowDefinitionPolicy;

#[async_trait]
impl ResourcePolicy<WorkflowDefinition> for WorkflowDefinitionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &WorkflowDefinition,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for WorkflowDefinition handlers.
///
/// Wraps `WorkflowDefinitionService` and checks `WorkflowDefinitionPolicy` before delegating.
pub type WorkflowDefinitionGuard = PermissionGuard<WorkflowDefinition>;

/// Service-integrated permission guard for WorkflowDefinition (for use in generated handlers).
pub type WorkflowDefinitionServiceGuard = ServicePermissionGuard<WorkflowDefinition, WorkflowDefinitionService, WorkflowDefinitionPolicy>;

// <<< CUSTOM
// END CUSTOM
