//! Auth configuration for PasswordHistory handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PasswordHistory>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PasswordHistoryService;
use crate::domain::entity::PasswordHistory;

/// Permission string constants for PasswordHistory operations.
pub mod permissions {
    pub const LIST:        &str = "password_history:list";
    pub const READ:        &str = "password_history:read";
    pub const CREATE:      &str = "password_history:create";
    pub const UPDATE:      &str = "password_history:update";
    pub const DELETE:      &str = "password_history:delete";
    pub const RESTORE:     &str = "password_history:restore";
    pub const TRASH:       &str = "password_history:trash";
    pub const EMPTY_TRASH: &str = "password_history:empty_trash";
    pub const BULK_CREATE: &str = "password_history:bulk_create";
    pub const UPSERT:      &str = "password_history:upsert";
}

/// Resource policy for PasswordHistory — maps CRUD actions to permission strings.
pub struct PasswordHistoryPolicy;

#[async_trait]
impl ResourcePolicy<PasswordHistory> for PasswordHistoryPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PasswordHistory,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for PasswordHistory handlers.
///
/// Wraps `PasswordHistoryService` and checks `PasswordHistoryPolicy` before delegating.
pub type PasswordHistoryGuard = PermissionGuard<PasswordHistory>;

/// Service-integrated permission guard for PasswordHistory (for use in generated handlers).
pub type PasswordHistoryServiceGuard = ServicePermissionGuard<PasswordHistory, PasswordHistoryService, PasswordHistoryPolicy>;

// <<< CUSTOM
// END CUSTOM
