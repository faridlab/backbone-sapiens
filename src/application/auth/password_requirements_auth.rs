//! Auth configuration for PasswordRequirements handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PasswordRequirements>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PasswordRequirementsService;
use crate::domain::entity::PasswordRequirements;

/// Permission string constants for PasswordRequirements operations.
pub mod permissions {
    pub const LIST:        &str = "password_requirements:list";
    pub const READ:        &str = "password_requirements:read";
    pub const CREATE:      &str = "password_requirements:create";
    pub const UPDATE:      &str = "password_requirements:update";
    pub const DELETE:      &str = "password_requirements:delete";
    pub const RESTORE:     &str = "password_requirements:restore";
    pub const TRASH:       &str = "password_requirements:trash";
    pub const EMPTY_TRASH: &str = "password_requirements:empty_trash";
    pub const BULK_CREATE: &str = "password_requirements:bulk_create";
    pub const UPSERT:      &str = "password_requirements:upsert";
}

/// Resource policy for PasswordRequirements — maps CRUD actions to permission strings.
pub struct PasswordRequirementsPolicy;

#[async_trait]
impl ResourcePolicy<PasswordRequirements> for PasswordRequirementsPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PasswordRequirements,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for PasswordRequirements handlers.
///
/// Wraps `PasswordRequirementsService` and checks `PasswordRequirementsPolicy` before delegating.
pub type PasswordRequirementsGuard = PermissionGuard<PasswordRequirements>;

/// Service-integrated permission guard for PasswordRequirements (for use in generated handlers).
pub type PasswordRequirementsServiceGuard = ServicePermissionGuard<PasswordRequirements, PasswordRequirementsService, PasswordRequirementsPolicy>;

// <<< CUSTOM
// END CUSTOM
