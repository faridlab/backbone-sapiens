//! Auth configuration for OrganizationPermission handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<OrganizationPermission>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::OrganizationPermissionService;
use crate::domain::entity::OrganizationPermission;

/// Permission string constants for OrganizationPermission operations.
pub mod permissions {
    pub const LIST:        &str = "organization_permission:list";
    pub const READ:        &str = "organization_permission:read";
    pub const CREATE:      &str = "organization_permission:create";
    pub const UPDATE:      &str = "organization_permission:update";
    pub const DELETE:      &str = "organization_permission:delete";
    pub const RESTORE:     &str = "organization_permission:restore";
    pub const TRASH:       &str = "organization_permission:trash";
    pub const EMPTY_TRASH: &str = "organization_permission:empty_trash";
    pub const BULK_CREATE: &str = "organization_permission:bulk_create";
    pub const UPSERT:      &str = "organization_permission:upsert";
}

/// Resource policy for OrganizationPermission — maps CRUD actions to permission strings.
pub struct OrganizationPermissionPolicy;

#[async_trait]
impl ResourcePolicy<OrganizationPermission> for OrganizationPermissionPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &OrganizationPermission,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for OrganizationPermission handlers.
///
/// Wraps `OrganizationPermissionService` and checks `OrganizationPermissionPolicy` before delegating.
pub type OrganizationPermissionGuard = PermissionGuard<OrganizationPermission, OrganizationPermissionService, OrganizationPermissionPolicy>;

/// Service-integrated permission guard for OrganizationPermission (for use in generated handlers).
pub type OrganizationPermissionServiceGuard = ServicePermissionGuard<OrganizationPermission, OrganizationPermissionService, OrganizationPermissionPolicy>;

// <<< CUSTOM
// END CUSTOM
