//! Auth configuration for NotificationLog handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<NotificationLog>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::NotificationLogService;
use crate::domain::entity::NotificationLog;

/// Permission string constants for NotificationLog operations.
pub mod permissions {
    pub const LIST:        &str = "notification_log:list";
    pub const READ:        &str = "notification_log:read";
    pub const CREATE:      &str = "notification_log:create";
    pub const UPDATE:      &str = "notification_log:update";
    pub const DELETE:      &str = "notification_log:delete";
    pub const RESTORE:     &str = "notification_log:restore";
    pub const TRASH:       &str = "notification_log:trash";
    pub const EMPTY_TRASH: &str = "notification_log:empty_trash";
    pub const BULK_CREATE: &str = "notification_log:bulk_create";
    pub const UPSERT:      &str = "notification_log:upsert";
}

/// Resource policy for NotificationLog — maps CRUD actions to permission strings.
pub struct NotificationLogPolicy;

#[async_trait]
impl ResourcePolicy<NotificationLog> for NotificationLogPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &NotificationLog,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for NotificationLog handlers.
///
/// Wraps `NotificationLogService` and checks `NotificationLogPolicy` before delegating.
pub type NotificationLogGuard = PermissionGuard<NotificationLog, NotificationLogService, NotificationLogPolicy>;

/// Service-integrated permission guard for NotificationLog (for use in generated handlers).
pub type NotificationLogServiceGuard = ServicePermissionGuard<NotificationLog, NotificationLogService, NotificationLogPolicy>;

// <<< CUSTOM
// END CUSTOM
