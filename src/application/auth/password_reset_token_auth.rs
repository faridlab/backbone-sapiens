//! Auth configuration for PasswordResetToken handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PasswordResetToken>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PasswordResetTokenService;
use crate::domain::entity::PasswordResetToken;

/// Permission string constants for PasswordResetToken operations.
pub mod permissions {
    pub const LIST:        &str = "password_reset_token:list";
    pub const READ:        &str = "password_reset_token:read";
    pub const CREATE:      &str = "password_reset_token:create";
    pub const UPDATE:      &str = "password_reset_token:update";
    pub const DELETE:      &str = "password_reset_token:delete";
    pub const RESTORE:     &str = "password_reset_token:restore";
    pub const TRASH:       &str = "password_reset_token:trash";
    pub const EMPTY_TRASH: &str = "password_reset_token:empty_trash";
    pub const BULK_CREATE: &str = "password_reset_token:bulk_create";
    pub const UPSERT:      &str = "password_reset_token:upsert";
}

/// Resource policy for PasswordResetToken — maps CRUD actions to permission strings.
pub struct PasswordResetTokenPolicy;

#[async_trait]
impl ResourcePolicy<PasswordResetToken> for PasswordResetTokenPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PasswordResetToken,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for PasswordResetToken handlers.
///
/// Wraps `PasswordResetTokenService` and checks `PasswordResetTokenPolicy` before delegating.
pub type PasswordResetTokenGuard = PermissionGuard<PasswordResetToken, PasswordResetTokenService, PasswordResetTokenPolicy>;

/// Service-integrated permission guard for PasswordResetToken (for use in generated handlers).
pub type PasswordResetTokenServiceGuard = ServicePermissionGuard<PasswordResetToken, PasswordResetTokenService, PasswordResetTokenPolicy>;

// <<< CUSTOM
// END CUSTOM
