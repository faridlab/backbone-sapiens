//! Auth configuration for PasswordPolicy handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PasswordPolicy>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PasswordPolicyService;
use crate::domain::entity::PasswordPolicy;

/// Permission string constants for PasswordPolicy operations.
pub mod permissions {
    pub const LIST:        &str = "password_policy:list";
    pub const READ:        &str = "password_policy:read";
    pub const CREATE:      &str = "password_policy:create";
    pub const UPDATE:      &str = "password_policy:update";
    pub const DELETE:      &str = "password_policy:delete";
    pub const RESTORE:     &str = "password_policy:restore";
    pub const TRASH:       &str = "password_policy:trash";
    pub const EMPTY_TRASH: &str = "password_policy:empty_trash";
    pub const BULK_CREATE: &str = "password_policy:bulk_create";
    pub const UPSERT:      &str = "password_policy:upsert";
}

/// Resource policy for PasswordPolicy — maps CRUD actions to permission strings.
pub struct PasswordPolicyPolicy;

#[async_trait]
impl ResourcePolicy<PasswordPolicy> for PasswordPolicyPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PasswordPolicy,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for PasswordPolicy handlers.
///
/// Wraps `PasswordPolicyService` and checks `PasswordPolicyPolicy` before delegating.
pub type PasswordPolicyGuard = PermissionGuard<PasswordPolicy, PasswordPolicyService, PasswordPolicyPolicy>;

/// Service-integrated permission guard for PasswordPolicy (for use in generated handlers).
pub type PasswordPolicyServiceGuard = ServicePermissionGuard<PasswordPolicy, PasswordPolicyService, PasswordPolicyPolicy>;

// <<< CUSTOM
// END CUSTOM
