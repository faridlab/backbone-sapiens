//! Auth configuration for BulkOperationResult handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<BulkOperationResult>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::BulkOperationResultService;
use crate::domain::entity::BulkOperationResult;

/// Permission string constants for BulkOperationResult operations.
pub mod permissions {
    pub const LIST:        &str = "bulk_operation_result:list";
    pub const READ:        &str = "bulk_operation_result:read";
    pub const CREATE:      &str = "bulk_operation_result:create";
    pub const UPDATE:      &str = "bulk_operation_result:update";
    pub const DELETE:      &str = "bulk_operation_result:delete";
    pub const RESTORE:     &str = "bulk_operation_result:restore";
    pub const TRASH:       &str = "bulk_operation_result:trash";
    pub const EMPTY_TRASH: &str = "bulk_operation_result:empty_trash";
    pub const BULK_CREATE: &str = "bulk_operation_result:bulk_create";
    pub const UPSERT:      &str = "bulk_operation_result:upsert";
}

/// Resource policy for BulkOperationResult — maps CRUD actions to permission strings.
pub struct BulkOperationResultPolicy;

#[async_trait]
impl ResourcePolicy<BulkOperationResult> for BulkOperationResultPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &BulkOperationResult,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for BulkOperationResult handlers.
///
/// Wraps `BulkOperationResultService` and checks `BulkOperationResultPolicy` before delegating.
pub type BulkOperationResultGuard = PermissionGuard<BulkOperationResult, BulkOperationResultService, BulkOperationResultPolicy>;

/// Service-integrated permission guard for BulkOperationResult (for use in generated handlers).
pub type BulkOperationResultServiceGuard = ServicePermissionGuard<BulkOperationResult, BulkOperationResultService, BulkOperationResultPolicy>;

// <<< CUSTOM
// END CUSTOM
