//! Auth configuration for BulkOperation handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<BulkOperation>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::BulkOperationService;
use crate::domain::entity::BulkOperation;

/// Permission string constants for BulkOperation operations.
pub mod permissions {
    pub const LIST:        &str = "bulk_operation:list";
    pub const READ:        &str = "bulk_operation:read";
    pub const CREATE:      &str = "bulk_operation:create";
    pub const UPDATE:      &str = "bulk_operation:update";
    pub const DELETE:      &str = "bulk_operation:delete";
    pub const RESTORE:     &str = "bulk_operation:restore";
    pub const TRASH:       &str = "bulk_operation:trash";
    pub const EMPTY_TRASH: &str = "bulk_operation:empty_trash";
    pub const BULK_CREATE: &str = "bulk_operation:bulk_create";
    pub const UPSERT:      &str = "bulk_operation:upsert";
}

/// Resource policy for BulkOperation — maps CRUD actions to permission strings.
pub struct BulkOperationPolicy;

#[async_trait]
impl ResourcePolicy<BulkOperation> for BulkOperationPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &BulkOperation,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for BulkOperation handlers.
///
/// Wraps `BulkOperationService` and checks `BulkOperationPolicy` before delegating.
pub type BulkOperationGuard = PermissionGuard<BulkOperation, BulkOperationService, BulkOperationPolicy>;

/// Service-integrated permission guard for BulkOperation (for use in generated handlers).
pub type BulkOperationServiceGuard = ServicePermissionGuard<BulkOperation, BulkOperationService, BulkOperationPolicy>;

// <<< CUSTOM
// END CUSTOM
