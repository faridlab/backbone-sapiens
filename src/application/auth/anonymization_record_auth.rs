//! Auth configuration for AnonymizationRecord handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<AnonymizationRecord>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AnonymizationRecordService;
use crate::domain::entity::AnonymizationRecord;

/// Permission string constants for AnonymizationRecord operations.
pub mod permissions {
    pub const LIST:        &str = "anonymization_record:list";
    pub const READ:        &str = "anonymization_record:read";
    pub const CREATE:      &str = "anonymization_record:create";
    pub const UPDATE:      &str = "anonymization_record:update";
    pub const DELETE:      &str = "anonymization_record:delete";
    pub const RESTORE:     &str = "anonymization_record:restore";
    pub const TRASH:       &str = "anonymization_record:trash";
    pub const EMPTY_TRASH: &str = "anonymization_record:empty_trash";
    pub const BULK_CREATE: &str = "anonymization_record:bulk_create";
    pub const UPSERT:      &str = "anonymization_record:upsert";
}

/// Resource policy for AnonymizationRecord — maps CRUD actions to permission strings.
pub struct AnonymizationRecordPolicy;

#[async_trait]
impl ResourcePolicy<AnonymizationRecord> for AnonymizationRecordPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &AnonymizationRecord,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for AnonymizationRecord handlers.
///
/// Wraps `AnonymizationRecordService` and checks `AnonymizationRecordPolicy` before delegating.
pub type AnonymizationRecordGuard = PermissionGuard<AnonymizationRecord, AnonymizationRecordService, AnonymizationRecordPolicy>;

/// Service-integrated permission guard for AnonymizationRecord (for use in generated handlers).
pub type AnonymizationRecordServiceGuard = ServicePermissionGuard<AnonymizationRecord, AnonymizationRecordService, AnonymizationRecordPolicy>;

// <<< CUSTOM
// END CUSTOM
