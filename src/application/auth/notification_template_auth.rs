//! Auth configuration for NotificationTemplate handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<NotificationTemplate>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::NotificationTemplateService;
use crate::domain::entity::NotificationTemplate;

/// Permission string constants for NotificationTemplate operations.
pub mod permissions {
    pub const LIST:        &str = "notification_template:list";
    pub const READ:        &str = "notification_template:read";
    pub const CREATE:      &str = "notification_template:create";
    pub const UPDATE:      &str = "notification_template:update";
    pub const DELETE:      &str = "notification_template:delete";
    pub const RESTORE:     &str = "notification_template:restore";
    pub const TRASH:       &str = "notification_template:trash";
    pub const EMPTY_TRASH: &str = "notification_template:empty_trash";
    pub const BULK_CREATE: &str = "notification_template:bulk_create";
    pub const UPSERT:      &str = "notification_template:upsert";
}

/// Resource policy for NotificationTemplate — maps CRUD actions to permission strings.
pub struct NotificationTemplatePolicy;

#[async_trait]
impl ResourcePolicy<NotificationTemplate> for NotificationTemplatePolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &NotificationTemplate,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for NotificationTemplate handlers.
///
/// Wraps `NotificationTemplateService` and checks `NotificationTemplatePolicy` before delegating.
pub type NotificationTemplateGuard = PermissionGuard<NotificationTemplate>;

/// Service-integrated permission guard for NotificationTemplate (for use in generated handlers).
pub type NotificationTemplateServiceGuard = ServicePermissionGuard<NotificationTemplate, NotificationTemplateService, NotificationTemplatePolicy>;

// <<< CUSTOM
// END CUSTOM
