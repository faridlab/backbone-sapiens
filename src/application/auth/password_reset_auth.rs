//! Auth configuration for PasswordReset handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<PasswordReset>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::PasswordResetService;
use crate::domain::entity::PasswordReset;

/// Permission string constants for PasswordReset operations.
pub mod permissions {
    pub const LIST:        &str = "password_reset:list";
    pub const READ:        &str = "password_reset:read";
    pub const CREATE:      &str = "password_reset:create";
    pub const UPDATE:      &str = "password_reset:update";
    pub const DELETE:      &str = "password_reset:delete";
    pub const RESTORE:     &str = "password_reset:restore";
    pub const TRASH:       &str = "password_reset:trash";
    pub const EMPTY_TRASH: &str = "password_reset:empty_trash";
    pub const BULK_CREATE: &str = "password_reset:bulk_create";
    pub const UPSERT:      &str = "password_reset:upsert";
}

/// Resource policy for PasswordReset — maps CRUD actions to permission strings.
pub struct PasswordResetPolicy;

#[async_trait]
impl ResourcePolicy<PasswordReset> for PasswordResetPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &PasswordReset,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for PasswordReset handlers.
///
/// Wraps `PasswordResetService` and checks `PasswordResetPolicy` before delegating.
pub type PasswordResetGuard = PermissionGuard<PasswordReset, PasswordResetService, PasswordResetPolicy>;

/// Service-integrated permission guard for PasswordReset (for use in generated handlers).
pub type PasswordResetServiceGuard = ServicePermissionGuard<PasswordReset, PasswordResetService, PasswordResetPolicy>;

// <<< CUSTOM
// END CUSTOM
