//! Auth configuration for OrganizationUser handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<OrganizationUser>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::OrganizationUserService;
use crate::domain::entity::OrganizationUser;

/// Permission string constants for OrganizationUser operations.
pub mod permissions {
    pub const LIST:        &str = "organization_user:list";
    pub const READ:        &str = "organization_user:read";
    pub const CREATE:      &str = "organization_user:create";
    pub const UPDATE:      &str = "organization_user:update";
    pub const DELETE:      &str = "organization_user:delete";
    pub const RESTORE:     &str = "organization_user:restore";
    pub const TRASH:       &str = "organization_user:trash";
    pub const EMPTY_TRASH: &str = "organization_user:empty_trash";
    pub const BULK_CREATE: &str = "organization_user:bulk_create";
    pub const UPSERT:      &str = "organization_user:upsert";
}

/// Resource policy for OrganizationUser — maps CRUD actions to permission strings.
pub struct OrganizationUserPolicy;

#[async_trait]
impl ResourcePolicy<OrganizationUser> for OrganizationUserPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &OrganizationUser,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for OrganizationUser handlers.
///
/// Wraps `OrganizationUserService` and checks `OrganizationUserPolicy` before delegating.
pub type OrganizationUserGuard = PermissionGuard<OrganizationUser, OrganizationUserService, OrganizationUserPolicy>;

/// Service-integrated permission guard for OrganizationUser (for use in generated handlers).
pub type OrganizationUserServiceGuard = ServicePermissionGuard<OrganizationUser, OrganizationUserService, OrganizationUserPolicy>;

// <<< CUSTOM
// END CUSTOM
