//! Auth configuration for DataExport handlers
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<DataExport>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::DataExportService;
use crate::domain::entity::DataExport;

/// Permission string constants for DataExport operations.
pub mod permissions {
    pub const LIST:        &str = "data_export:list";
    pub const READ:        &str = "data_export:read";
    pub const CREATE:      &str = "data_export:create";
    pub const UPDATE:      &str = "data_export:update";
    pub const DELETE:      &str = "data_export:delete";
    pub const RESTORE:     &str = "data_export:restore";
    pub const TRASH:       &str = "data_export:trash";
    pub const EMPTY_TRASH: &str = "data_export:empty_trash";
    pub const BULK_CREATE: &str = "data_export:bulk_create";
    pub const UPSERT:      &str = "data_export:upsert";
}

/// Resource policy for DataExport — maps CRUD actions to permission strings.
pub struct DataExportPolicy;

#[async_trait]
impl ResourcePolicy<DataExport> for DataExportPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &DataExport,
        ctx: &backbone_auth::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read   => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update => permissions::UPDATE,
            ResourceAction::Delete => permissions::DELETE,
        };
        ctx.has_permission(required)
    }
}

/// Permission guard for DataExport handlers.
///
/// Wraps `DataExportService` and checks `DataExportPolicy` before delegating.
pub type DataExportGuard = PermissionGuard<DataExport, DataExportService, DataExportPolicy>;

/// Service-integrated permission guard for DataExport (for use in generated handlers).
pub type DataExportServiceGuard = ServicePermissionGuard<DataExport, DataExportService, DataExportPolicy>;

// <<< CUSTOM
// END CUSTOM
