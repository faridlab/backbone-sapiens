//! Bulk operations for PermissionConflict entities
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Type alias over `GenericBulkService` — config, progress tracking, and
//! batch execution logic live in backbone-core.

use backbone_core::GenericBulkService;
pub use backbone_core::{BulkOperationConfig, BulkOperationProgress, BulkFailureMode, BulkItemResult, BulkOperationResult};

use crate::presentation::dto::CreatePermissionConflictDto;
use crate::application::service::PermissionConflictService;
use crate::domain::entity::PermissionConflict;

/// Bulk operation handler for PermissionConflict entities.
///
/// Wraps `PermissionConflictService` with configurable batch size, concurrency,
/// and `ContinueOnError` / `AbortOnFirstError` failure modes.
pub type PermissionConflictBulkService = GenericBulkService<PermissionConflict, CreatePermissionConflictDto, PermissionConflictService>;

// <<< CUSTOM
// END CUSTOM
