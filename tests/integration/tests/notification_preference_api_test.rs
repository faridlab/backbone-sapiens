//! NotificationPreference API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the NotificationPreference CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// NOTIFICATIONPREFERENCE TEST DATA GENERATOR
// ============================================================================

/// Test data generator for NotificationPreference
pub struct NotificationPreferenceTestData;

impl TestDataGenerator for NotificationPreferenceTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_id": null,
            "notification_type": null,
            "channel_enabled": null,
            "channels": null,
            "quiet_hours_start": null,
            "quiet_hours_end": null,
            "quiet_timezone": null,
            "digest_enabled": null,
            "digest_frequency": null,
            "enabled": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_id": null,
            "notification_type": null,
            "channel_enabled": null,
            "channels": null,
            "quiet_hours_start": null,
            "quiet_hours_end": null,
            "quiet_timezone": null,
            "digest_enabled": null,
            "digest_frequency": null,
            "enabled": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// NOTIFICATIONPREFERENCE API TEST
// ============================================================================

/// NotificationPreference API test suite
pub struct NotificationPreferenceApiTest {
    inner: GenericCrudTest<NotificationPreferenceTestData>,
}

impl NotificationPreferenceApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/notification_preferences", "NotificationPreference");
        Self {
            inner: GenericCrudTest::new(config, NotificationPreferenceTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for NotificationPreferenceApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_notification_preference_crud() {
        let mut test = NotificationPreferenceApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add NotificationPreference specific tests here
// <<< CUSTOM TESTS END >>>
