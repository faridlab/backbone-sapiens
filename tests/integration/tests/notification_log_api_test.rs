//! NotificationLog API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the NotificationLog CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// NOTIFICATIONLOG TEST DATA GENERATOR
// ============================================================================

/// Test data generator for NotificationLog
pub struct NotificationLogTestData;

impl TestDataGenerator for NotificationLogTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "notification_id": null,
            "template_id": null,
            "recipient_type": null,
            "recipient_id": null,
            "channel": null,
            "status": null,
            "sent_at": now,
            "delivered_at": now,
            "failed_at": now,
            "error_message": null,
            "external_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "notification_id": null,
            "template_id": null,
            "recipient_type": null,
            "recipient_id": null,
            "channel": null,
            "status": null,
            "sent_at": now,
            "delivered_at": now,
            "failed_at": now,
            "error_message": null,
            "external_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// NOTIFICATIONLOG API TEST
// ============================================================================

/// NotificationLog API test suite
pub struct NotificationLogApiTest {
    inner: GenericCrudTest<NotificationLogTestData>,
}

impl NotificationLogApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/notification_logs", "NotificationLog");
        Self {
            inner: GenericCrudTest::new(config, NotificationLogTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for NotificationLogApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_notification_log_crud() {
        let mut test = NotificationLogApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add NotificationLog specific tests here
// <<< CUSTOM TESTS END >>>
