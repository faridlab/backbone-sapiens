//! UserPermission API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the UserPermission CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// USERPERMISSION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for UserPermission
pub struct UserPermissionTestData;

impl TestDataGenerator for UserPermissionTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_id": null,
            "permission_id": null,
            "granted_at": now,
            "granted_by": null,
            "reason": null,
            "expires_at": now,
            "resource_id": null,
            "resource_type": null,
            "is_active": true,
            "revoked_at": now,
            "revoked_by": null,
            "revoked_reason": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_id": null,
            "permission_id": null,
            "granted_at": now,
            "granted_by": null,
            "reason": null,
            "expires_at": now,
            "resource_id": null,
            "resource_type": null,
            "is_active": true,
            "revoked_at": now,
            "revoked_by": null,
            "revoked_reason": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// USERPERMISSION API TEST
// ============================================================================

/// UserPermission API test suite
pub struct UserPermissionApiTest {
    inner: GenericCrudTest<UserPermissionTestData>,
}

impl UserPermissionApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/user_permissions", "UserPermission");
        Self {
            inner: GenericCrudTest::new(config, UserPermissionTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for UserPermissionApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_user_permission_crud() {
        let mut test = UserPermissionApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add UserPermission specific tests here
// <<< CUSTOM TESTS END >>>
