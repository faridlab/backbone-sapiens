//! Workflow API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the Workflow CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// WORKFLOW TEST DATA GENERATOR
// ============================================================================

/// Test data generator for Workflow
pub struct WorkflowTestData;

impl TestDataGenerator for WorkflowTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "workflow_type": null,
            "status": null,
            "initiator_id": null,
            "target_user_id": null,
            "title": null,
            "description": "Test description",
            "context": null,
            "current_step": null,
            "total_steps": null,
            "progress_percentage": null,
            "started_at": now,
            "completed_at": now,
            "failed_at": now,
            "expires_at": now,
            "retry_count": null,
            "max_retries": null,
            "error_message": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "workflow_type": null,
            "status": null,
            "initiator_id": null,
            "target_user_id": null,
            "title": null,
            "description": "Test description",
            "context": null,
            "current_step": null,
            "total_steps": null,
            "progress_percentage": null,
            "started_at": now,
            "completed_at": now,
            "failed_at": now,
            "expires_at": now,
            "retry_count": null,
            "max_retries": null,
            "error_message": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// WORKFLOW API TEST
// ============================================================================

/// Workflow API test suite
pub struct WorkflowApiTest {
    inner: GenericCrudTest<WorkflowTestData>,
}

impl WorkflowApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/workflows", "Workflow");
        Self {
            inner: GenericCrudTest::new(config, WorkflowTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for WorkflowApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_workflow_crud() {
        let mut test = WorkflowApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add Workflow specific tests here
// <<< CUSTOM TESTS END >>>
