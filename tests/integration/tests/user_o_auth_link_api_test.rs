//! UserOAuthLink API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the UserOAuthLink CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// USEROAUTHLINK TEST DATA GENERATOR
// ============================================================================

/// Test data generator for UserOAuthLink
pub struct UserOAuthLinkTestData;

impl TestDataGenerator for UserOAuthLinkTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_id": null,
            "oauth_provider_id": null,
            "provider_user_id": null,
            "provider_email": null,
            "provider_username": null,
            "is_active": true,
            "access_token": null,
            "refresh_token": null,
            "token_expires_at": now,
            "is_primary": false,
            "link_status": null,
            "last_synced": null,
            "sync_enabled": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_id": null,
            "oauth_provider_id": null,
            "provider_user_id": null,
            "provider_email": null,
            "provider_username": null,
            "is_active": true,
            "access_token": null,
            "refresh_token": null,
            "token_expires_at": now,
            "is_primary": false,
            "link_status": null,
            "last_synced": null,
            "sync_enabled": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// USEROAUTHLINK API TEST
// ============================================================================

/// UserOAuthLink API test suite
pub struct UserOAuthLinkApiTest {
    inner: GenericCrudTest<UserOAuthLinkTestData>,
}

impl UserOAuthLinkApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/user_oauth_links", "UserOAuthLink");
        Self {
            inner: GenericCrudTest::new(config, UserOAuthLinkTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for UserOAuthLinkApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_user_o_auth_link_crud() {
        let mut test = UserOAuthLinkApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add UserOAuthLink specific tests here
// <<< CUSTOM TESTS END >>>
