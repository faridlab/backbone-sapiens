//! PasswordResetToken API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the PasswordResetToken CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PASSWORDRESETTOKEN TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PasswordResetToken
pub struct PasswordResetTokenTestData;

impl TestDataGenerator for PasswordResetTokenTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_id": null,
            "token_hash": null,
            "expires_at": now,
            "is_used": false,
            "used_at": now,
            "ip_address": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_id": null,
            "token_hash": null,
            "expires_at": now,
            "is_used": false,
            "used_at": now,
            "ip_address": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PASSWORDRESETTOKEN API TEST
// ============================================================================

/// PasswordResetToken API test suite
pub struct PasswordResetTokenApiTest {
    inner: GenericCrudTest<PasswordResetTokenTestData>,
}

impl PasswordResetTokenApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/password_reset_tokens", "PasswordResetToken");
        Self {
            inner: GenericCrudTest::new(config, PasswordResetTokenTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PasswordResetTokenApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_password_reset_token_crud() {
        let mut test = PasswordResetTokenApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PasswordResetToken specific tests here
// <<< CUSTOM TESTS END >>>
