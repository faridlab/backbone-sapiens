//! WorkflowAction API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the WorkflowAction CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// WORKFLOWACTION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for WorkflowAction
pub struct WorkflowActionTestData;

impl TestDataGenerator for WorkflowActionTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "workflow_definition_id": null,
            "action_type": null,
            "action_name": null,
            "description": "Test description",
            "step_order": null,
            "parameters": null,
            "conditions": null,
            "is_required": false,
            "timeout_minutes": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "workflow_definition_id": null,
            "action_type": null,
            "action_name": null,
            "description": "Test description",
            "step_order": null,
            "parameters": null,
            "conditions": null,
            "is_required": false,
            "timeout_minutes": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// WORKFLOWACTION API TEST
// ============================================================================

/// WorkflowAction API test suite
pub struct WorkflowActionApiTest {
    inner: GenericCrudTest<WorkflowActionTestData>,
}

impl WorkflowActionApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/workflow_actions", "WorkflowAction");
        Self {
            inner: GenericCrudTest::new(config, WorkflowActionTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for WorkflowActionApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_workflow_action_crud() {
        let mut test = WorkflowActionApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add WorkflowAction specific tests here
// <<< CUSTOM TESTS END >>>
