//! UserSAMLLink API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the UserSAMLLink CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// USERSAMLLINK TEST DATA GENERATOR
// ============================================================================

/// Test data generator for UserSAMLLink
pub struct UserSAMLLinkTestData;

impl TestDataGenerator for UserSAMLLinkTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_id": null,
            "provider_id": null,
            "name_id": null,
            "session_index": null,
            "first_login_at": now,
            "last_login_at": now,
            "attributes": null,
            "is_primary": false,
            "status": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_id": null,
            "provider_id": null,
            "name_id": null,
            "session_index": null,
            "first_login_at": now,
            "last_login_at": now,
            "attributes": null,
            "is_primary": false,
            "status": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// USERSAMLLINK API TEST
// ============================================================================

/// UserSAMLLink API test suite
pub struct UserSAMLLinkApiTest {
    inner: GenericCrudTest<UserSAMLLinkTestData>,
}

impl UserSAMLLinkApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/user_saml_links", "UserSAMLLink");
        Self {
            inner: GenericCrudTest::new(config, UserSAMLLinkTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for UserSAMLLinkApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_user_saml_link_crud() {
        let mut test = UserSAMLLinkApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add UserSAMLLink specific tests here
// <<< CUSTOM TESTS END >>>
