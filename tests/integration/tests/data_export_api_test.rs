//! DataExport API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the DataExport CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// DATAEXPORT TEST DATA GENERATOR
// ============================================================================

/// Test data generator for DataExport
pub struct DataExportTestData;

impl TestDataGenerator for DataExportTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_id": null,
            "requested_by": null,
            "requested_at": now,
            "status": null,
            "file_path": null,
            "file_url": null,
            "expires_at": now,
            "completed_at": now,
            "record_count": null,
            "format": null,
            "justification": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_id": null,
            "requested_by": null,
            "requested_at": now,
            "status": null,
            "file_path": null,
            "file_url": null,
            "expires_at": now,
            "completed_at": now,
            "record_count": null,
            "format": null,
            "justification": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// DATAEXPORT API TEST
// ============================================================================

/// DataExport API test suite
pub struct DataExportApiTest {
    inner: GenericCrudTest<DataExportTestData>,
}

impl DataExportApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/data_exports", "DataExport");
        Self {
            inner: GenericCrudTest::new(config, DataExportTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for DataExportApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_data_export_crud() {
        let mut test = DataExportApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add DataExport specific tests here
// <<< CUSTOM TESTS END >>>
