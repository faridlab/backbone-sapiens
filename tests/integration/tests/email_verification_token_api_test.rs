//! EmailVerificationToken API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the EmailVerificationToken CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// EMAILVERIFICATIONTOKEN TEST DATA GENERATOR
// ============================================================================

/// Test data generator for EmailVerificationToken
pub struct EmailVerificationTokenTestData;

impl TestDataGenerator for EmailVerificationTokenTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_id": null,
            "token": null,
            "email": format!("test{}@example.com", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "token_type": null,
            "old_email": null,
            "expires_at": now,
            "verified_at": now,
            "attempts": null,
            "max_attempts": null,
            "status": null,
            "ip_address": null,
            "user_agent": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_id": null,
            "token": null,
            "email": format!("test{}@example.com", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "token_type": null,
            "old_email": null,
            "expires_at": now,
            "verified_at": now,
            "attempts": null,
            "max_attempts": null,
            "status": null,
            "ip_address": null,
            "user_agent": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// EMAILVERIFICATIONTOKEN API TEST
// ============================================================================

/// EmailVerificationToken API test suite
pub struct EmailVerificationTokenApiTest {
    inner: GenericCrudTest<EmailVerificationTokenTestData>,
}

impl EmailVerificationTokenApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/email_verification_tokens", "EmailVerificationToken");
        Self {
            inner: GenericCrudTest::new(config, EmailVerificationTokenTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for EmailVerificationTokenApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_email_verification_token_crud() {
        let mut test = EmailVerificationTokenApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add EmailVerificationToken specific tests here
// <<< CUSTOM TESTS END >>>
