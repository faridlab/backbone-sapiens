//! LDAPDirectory API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the LDAPDirectory CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// LDAPDIRECTORY TEST DATA GENERATOR
// ============================================================================

/// Test data generator for LDAPDirectory
pub struct LDAPDirectoryTestData;

impl TestDataGenerator for LDAPDirectoryTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "display_name": null,
            "host": null,
            "port": null,
            "use_ssl": null,
            "use_tls": null,
            "bind_dn": null,
            "bind_password": null,
            "search_base": null,
            "search_filter": null,
            "attribute_mapping": null,
            "sync_enabled": null,
            "sync_interval_minutes": null,
            "last_sync_at": now,
            "last_sync_result": null,
            "is_active": true,
            "status": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "name": format!("Test {}", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "display_name": null,
            "host": null,
            "port": null,
            "use_ssl": null,
            "use_tls": null,
            "bind_dn": null,
            "bind_password": null,
            "search_base": null,
            "search_filter": null,
            "attribute_mapping": null,
            "sync_enabled": null,
            "sync_interval_minutes": null,
            "last_sync_at": now,
            "last_sync_result": null,
            "is_active": true,
            "status": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// LDAPDIRECTORY API TEST
// ============================================================================

/// LDAPDirectory API test suite
pub struct LDAPDirectoryApiTest {
    inner: GenericCrudTest<LDAPDirectoryTestData>,
}

impl LDAPDirectoryApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/ldap_directories", "LDAPDirectory");
        Self {
            inner: GenericCrudTest::new(config, LDAPDirectoryTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for LDAPDirectoryApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_ldap_directory_crud() {
        let mut test = LDAPDirectoryApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add LDAPDirectory specific tests here
// <<< CUSTOM TESTS END >>>
