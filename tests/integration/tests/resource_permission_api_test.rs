//! ResourcePermission API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the ResourcePermission CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// RESOURCEPERMISSION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for ResourcePermission
pub struct ResourcePermissionTestData;

impl TestDataGenerator for ResourcePermissionTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "permission_id": null,
            "resource_type": null,
            "resource_id": null,
            "granted_to_user_id": null,
            "granted_to_role_id": null,
            "granted_by": null,
            "granted_at": now,
            "expires_at": now,
            "reason": null,
            "status": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "permission_id": null,
            "resource_type": null,
            "resource_id": null,
            "granted_to_user_id": null,
            "granted_to_role_id": null,
            "granted_by": null,
            "granted_at": now,
            "expires_at": now,
            "reason": null,
            "status": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// RESOURCEPERMISSION API TEST
// ============================================================================

/// ResourcePermission API test suite
pub struct ResourcePermissionApiTest {
    inner: GenericCrudTest<ResourcePermissionTestData>,
}

impl ResourcePermissionApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/resource_permissions", "ResourcePermission");
        Self {
            inner: GenericCrudTest::new(config, ResourcePermissionTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for ResourcePermissionApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_resource_permission_crud() {
        let mut test = ResourcePermissionApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add ResourcePermission specific tests here
// <<< CUSTOM TESTS END >>>
