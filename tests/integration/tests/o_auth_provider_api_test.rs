//! OAuthProvider API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the OAuthProvider CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// OAUTHPROVIDER TEST DATA GENERATOR
// ============================================================================

/// Test data generator for OAuthProvider
pub struct OAuthProviderTestData;

impl TestDataGenerator for OAuthProviderTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "provider_name": null,
            "display_name": null,
            "client_id": null,
            "client_secret": null,
            "redirect_uri": null,
            "scopes": null,
            "authorization_url": null,
            "token_url": null,
            "user_info_url": null,
            "is_active": true,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "provider_name": null,
            "display_name": null,
            "client_id": null,
            "client_secret": null,
            "redirect_uri": null,
            "scopes": null,
            "authorization_url": null,
            "token_url": null,
            "user_info_url": null,
            "is_active": true,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// OAUTHPROVIDER API TEST
// ============================================================================

/// OAuthProvider API test suite
pub struct OAuthProviderApiTest {
    inner: GenericCrudTest<OAuthProviderTestData>,
}

impl OAuthProviderApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/oauth_providers", "OAuthProvider");
        Self {
            inner: GenericCrudTest::new(config, OAuthProviderTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for OAuthProviderApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_o_auth_provider_crud() {
        let mut test = OAuthProviderApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add OAuthProvider specific tests here
// <<< CUSTOM TESTS END >>>
