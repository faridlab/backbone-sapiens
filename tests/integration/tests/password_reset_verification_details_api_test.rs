//! PasswordResetVerificationDetails API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the PasswordResetVerificationDetails CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PASSWORDRESETVERIFICATIONDETAILS TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PasswordResetVerificationDetails
pub struct PasswordResetVerificationDetailsTestData;

impl TestDataGenerator for PasswordResetVerificationDetailsTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "verified_at": now,
            "verification_ip": null,
            "verification_attempts": null,
            "last_attempt_at": now,
            "verification_method": null,
            "device_fingerprint": null,
            "geolocation": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "verified_at": now,
            "verification_ip": null,
            "verification_attempts": null,
            "last_attempt_at": now,
            "verification_method": null,
            "device_fingerprint": null,
            "geolocation": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PASSWORDRESETVERIFICATIONDETAILS API TEST
// ============================================================================

/// PasswordResetVerificationDetails API test suite
pub struct PasswordResetVerificationDetailsApiTest {
    inner: GenericCrudTest<PasswordResetVerificationDetailsTestData>,
}

impl PasswordResetVerificationDetailsApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/password_reset_verification_details", "PasswordResetVerificationDetails");
        Self {
            inner: GenericCrudTest::new(config, PasswordResetVerificationDetailsTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PasswordResetVerificationDetailsApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_password_reset_verification_details_crud() {
        let mut test = PasswordResetVerificationDetailsApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PasswordResetVerificationDetails specific tests here
// <<< CUSTOM TESTS END >>>
