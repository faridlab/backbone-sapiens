//! PasswordReset API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the PasswordReset CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// PASSWORDRESET TEST DATA GENERATOR
// ============================================================================

/// Test data generator for PasswordReset
pub struct PasswordResetTestData;

impl TestDataGenerator for PasswordResetTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_id": null,
            "token": null,
            "token_hash": null,
            "email": format!("test{}@example.com", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "status": null,
            "max_attempts": null,
            "attempts_remaining": null,
            "last_attempt_at": now,
            "request_metadata": null,
            "verification_details_id": null,
            "completion_details_id": null,
            "security_id": null,
            "device_fingerprint": null,
            "ip_address": null,
            "user_agent": null,
            "verified_at": now,
            "used_at": now,
            "expires_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_id": null,
            "token": null,
            "token_hash": null,
            "email": format!("test{}@example.com", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "status": null,
            "max_attempts": null,
            "attempts_remaining": null,
            "last_attempt_at": now,
            "request_metadata": null,
            "verification_details_id": null,
            "completion_details_id": null,
            "security_id": null,
            "device_fingerprint": null,
            "ip_address": null,
            "user_agent": null,
            "verified_at": now,
            "used_at": now,
            "expires_at": now,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// PASSWORDRESET API TEST
// ============================================================================

/// PasswordReset API test suite
pub struct PasswordResetApiTest {
    inner: GenericCrudTest<PasswordResetTestData>,
}

impl PasswordResetApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/password_resets", "PasswordReset");
        Self {
            inner: GenericCrudTest::new(config, PasswordResetTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for PasswordResetApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_password_reset_crud() {
        let mut test = PasswordResetApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add PasswordReset specific tests here
// <<< CUSTOM TESTS END >>>
