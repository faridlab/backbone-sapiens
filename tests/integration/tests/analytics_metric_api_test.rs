//! AnalyticsMetric API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the AnalyticsMetric CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// ANALYTICSMETRIC TEST DATA GENERATOR
// ============================================================================

/// Test data generator for AnalyticsMetric
pub struct AnalyticsMetricTestData;

impl TestDataGenerator for AnalyticsMetricTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "metric_name": null,
            "metric_type": null,
            "value": null,
            "unit": null,
            "dimensions": null,
            "period_start": null,
            "period_end": null,
            "aggregation_level": null,
            "organization_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "metric_name": null,
            "metric_type": null,
            "value": null,
            "unit": null,
            "dimensions": null,
            "period_start": null,
            "period_end": null,
            "aggregation_level": null,
            "organization_id": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// ANALYTICSMETRIC API TEST
// ============================================================================

/// AnalyticsMetric API test suite
pub struct AnalyticsMetricApiTest {
    inner: GenericCrudTest<AnalyticsMetricTestData>,
}

impl AnalyticsMetricApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/analytics_metrics", "AnalyticsMetric");
        Self {
            inner: GenericCrudTest::new(config, AnalyticsMetricTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for AnalyticsMetricApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_analytics_metric_crud() {
        let mut test = AnalyticsMetricApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add AnalyticsMetric specific tests here
// <<< CUSTOM TESTS END >>>
