//! WorkflowExecution API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the WorkflowExecution CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// WORKFLOWEXECUTION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for WorkflowExecution
pub struct WorkflowExecutionTestData;

impl TestDataGenerator for WorkflowExecutionTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "workflow_id": null,
            "workflow_definition_id": null,
            "status": null,
            "current_action": null,
            "total_actions": null,
            "progress_percentage": null,
            "timeout_minutes": null,
            "started_at": now,
            "completed_at": now,
            "failed_at": now,
            "cancelled_at": now,
            "error_message": null,
            "execution_context": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "workflow_id": null,
            "workflow_definition_id": null,
            "status": null,
            "current_action": null,
            "total_actions": null,
            "progress_percentage": null,
            "timeout_minutes": null,
            "started_at": now,
            "completed_at": now,
            "failed_at": now,
            "cancelled_at": now,
            "error_message": null,
            "execution_context": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// WORKFLOWEXECUTION API TEST
// ============================================================================

/// WorkflowExecution API test suite
pub struct WorkflowExecutionApiTest {
    inner: GenericCrudTest<WorkflowExecutionTestData>,
}

impl WorkflowExecutionApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/workflow_executions", "WorkflowExecution");
        Self {
            inner: GenericCrudTest::new(config, WorkflowExecutionTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for WorkflowExecutionApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_workflow_execution_crud() {
        let mut test = WorkflowExecutionApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add WorkflowExecution specific tests here
// <<< CUSTOM TESTS END >>>
