//! WorkflowActionExecution API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the WorkflowActionExecution CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// WORKFLOWACTIONEXECUTION TEST DATA GENERATOR
// ============================================================================

/// Test data generator for WorkflowActionExecution
pub struct WorkflowActionExecutionTestData;

impl TestDataGenerator for WorkflowActionExecutionTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "workflow_execution_id": null,
            "workflow_action_id": null,
            "action_order": null,
            "status": null,
            "input_data": null,
            "output_data": null,
            "started_at": now,
            "completed_at": now,
            "failed_at": now,
            "duration_ms": null,
            "error_message": null,
            "retry_count": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "workflow_execution_id": null,
            "workflow_action_id": null,
            "action_order": null,
            "status": null,
            "input_data": null,
            "output_data": null,
            "started_at": now,
            "completed_at": now,
            "failed_at": now,
            "duration_ms": null,
            "error_message": null,
            "retry_count": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// WORKFLOWACTIONEXECUTION API TEST
// ============================================================================

/// WorkflowActionExecution API test suite
pub struct WorkflowActionExecutionApiTest {
    inner: GenericCrudTest<WorkflowActionExecutionTestData>,
}

impl WorkflowActionExecutionApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/workflow_action_executions", "WorkflowActionExecution");
        Self {
            inner: GenericCrudTest::new(config, WorkflowActionExecutionTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for WorkflowActionExecutionApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_workflow_action_execution_crud() {
        let mut test = WorkflowActionExecutionApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add WorkflowActionExecution specific tests here
// <<< CUSTOM TESTS END >>>
