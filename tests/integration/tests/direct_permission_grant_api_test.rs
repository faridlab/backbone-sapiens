//! DirectPermissionGrant API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the DirectPermissionGrant CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// DIRECTPERMISSIONGRANT TEST DATA GENERATOR
// ============================================================================

/// Test data generator for DirectPermissionGrant
pub struct DirectPermissionGrantTestData;

impl TestDataGenerator for DirectPermissionGrantTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "user_id": null,
            "permission_id": null,
            "granted_by": null,
            "granted_at": now,
            "expires_at": now,
            "reason": null,
            "scope": null,
            "conditions": null,
            "last_used_at": now,
            "usage_count": null,
            "access_log": null,
            "status": null,
            "revoked_at": now,
            "revoked_by": null,
            "revocation_reason": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "user_id": null,
            "permission_id": null,
            "granted_by": null,
            "granted_at": now,
            "expires_at": now,
            "reason": null,
            "scope": null,
            "conditions": null,
            "last_used_at": now,
            "usage_count": null,
            "access_log": null,
            "status": null,
            "revoked_at": now,
            "revoked_by": null,
            "revocation_reason": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// DIRECTPERMISSIONGRANT API TEST
// ============================================================================

/// DirectPermissionGrant API test suite
pub struct DirectPermissionGrantApiTest {
    inner: GenericCrudTest<DirectPermissionGrantTestData>,
}

impl DirectPermissionGrantApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/direct_permission_grants", "DirectPermissionGrant");
        Self {
            inner: GenericCrudTest::new(config, DirectPermissionGrantTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for DirectPermissionGrantApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_direct_permission_grant_crud() {
        let mut test = DirectPermissionGrantApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add DirectPermissionGrant specific tests here
// <<< CUSTOM TESTS END >>>
