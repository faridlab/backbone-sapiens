//! User API Integration Tests
//!
//! Generated by backbone-schema. Do not edit manually.
//!
//! Tests the User CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::helpers::CommonUtils;

// ============================================================================
// USER TEST DATA GENERATOR
// ============================================================================

/// Test data generator for User
pub struct UserTestData;

impl TestDataGenerator for UserTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "username": null,
            "email": format!("test{}@example.com", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "password_hash": null,
            "status": null,
            "email_verified": null,
            "failed_login_attempts": null,
            "locked_until": null,
            "last_login": null,
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "username": null,
            "email": format!("test{}@example.com", Uuid::new_v4().to_string().split('-').next().unwrap()),
            "password_hash": null,
            "status": null,
            "email_verified": null,
            "failed_login_attempts": null,
            "locked_until": null,
            "last_login": null,
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }
}

// ============================================================================
// USER API TEST
// ============================================================================

/// User API test suite
pub struct UserApiTest {
    inner: GenericCrudTest<UserTestData>,
}

impl UserApiTest {
    pub fn new() -> Self {
        let config = CrudTestConfig::new("/api/v1/users", "User");
        Self {
            inner: GenericCrudTest::new(config, UserTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for UserApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_user_crud() {
        let mut test = UserApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add User specific tests here
// <<< CUSTOM TESTS END >>>
