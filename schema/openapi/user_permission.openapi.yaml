openapi: '3.0.3'
info:
  title: UserPermission API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    UserPermission entity CRUD endpoints.

servers:
  - url: http://localhost:3000
    description: Development server

tags:
  - name: UserPermissions
    description: UserPermission management endpoints

paths:
  /api/v1/user_permissions:
    get:
      tags:
        - UserPermissions
      summary: List UserPermissions
      description: Get a paginated list of UserPermissions with optional filtering and sorting
      operationId: listUserPermissions
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPermissionList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - UserPermissions
      summary: Create UserPermission
      description: Create a new UserPermission
      operationId: createUserPermission
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserPermissionRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPermission'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/user_permissions/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - UserPermissions
      summary: Get UserPermission by ID
      operationId: getUserPermission
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPermission'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - UserPermissions
      summary: Update UserPermission (full)
      operationId: updateUserPermission
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserPermissionRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPermission'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - UserPermissions
      summary: Update UserPermission (partial)
      operationId: patchUserPermission
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchUserPermissionRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPermission'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - UserPermissions
      summary: Delete UserPermission (soft)
      operationId: deleteUserPermission
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/user_permissions/bulk:
    post:
      tags:
        - UserPermissions
      summary: Bulk create UserPermissions
      operationId: bulkCreateUserPermissions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateUserPermissionRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UserPermission'

  /api/v1/user_permissions/upsert:
    post:
      tags:
        - UserPermissions
      summary: Upsert UserPermission
      operationId: upsertUserPermission
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserPermissionRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPermission'

  /api/v1/user_permissions/trash:
    get:
      tags:
        - UserPermissions
      summary: List deleted UserPermissions
      operationId: listDeletedUserPermissions
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPermissionList'

  /api/v1/user_permissions/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - UserPermissions
      summary: Restore deleted UserPermission
      operationId: restoreUserPermission
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserPermission'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/user_permissions/empty:
    delete:
      tags:
        - UserPermissions
      summary: Empty trash for UserPermissions
      operationId: emptyTrashUserPermissions
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

components:
  schemas:
    UserPermission:
      type: object
      required:
        - id
        - user_id
        - permission_id
        - granted_at
        - granted_by
        - reason
        - is_active
        - metadata
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        permission_id:
          type: string
          format: uuid
        granted_at:
          type: string
          format: date-time
        granted_by:
          type: string
          format: uuid
        reason:
          type: string
          minLength: 10
          maxLength: 500
        expires_at:
          nullable: true
          type: string
          format: date-time
        resource_id:
          nullable: true
          type: string
          maxLength: 100
        resource_type:
          nullable: true
          type: string
          maxLength: 50
        is_active:
          type: boolean
        revoked_at:
          nullable: true
          type: string
          format: date-time
        revoked_by:
          nullable: true
          type: string
          format: uuid
        revoked_reason:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    CreateUserPermissionRequest:
      type: object
      required:
        - user_id
        - permission_id
        - granted_at
        - granted_by
        - reason
        - is_active
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        permission_id:
          type: string
          format: uuid
        granted_at:
          type: string
          format: date-time
        granted_by:
          type: string
          format: uuid
        reason:
          type: string
          minLength: 10
          maxLength: 500
        expires_at:
          nullable: true
          type: string
          format: date-time
        resource_id:
          nullable: true
          type: string
          maxLength: 100
        resource_type:
          nullable: true
          type: string
          maxLength: 50
        is_active:
          type: boolean
        revoked_at:
          nullable: true
          type: string
          format: date-time
        revoked_by:
          nullable: true
          type: string
          format: uuid
        revoked_reason:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    UpdateUserPermissionRequest:
      type: object
      required:
        - user_id
        - permission_id
        - granted_at
        - granted_by
        - reason
        - is_active
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        permission_id:
          type: string
          format: uuid
        granted_at:
          type: string
          format: date-time
        granted_by:
          type: string
          format: uuid
        reason:
          type: string
          minLength: 10
          maxLength: 500
        expires_at:
          nullable: true
          type: string
          format: date-time
        resource_id:
          nullable: true
          type: string
          maxLength: 100
        resource_type:
          nullable: true
          type: string
          maxLength: 50
        is_active:
          type: boolean
        revoked_at:
          nullable: true
          type: string
          format: date-time
        revoked_by:
          nullable: true
          type: string
          format: uuid
        revoked_reason:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    PatchUserPermissionRequest:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        permission_id:
          type: string
          format: uuid
        granted_at:
          type: string
          format: date-time
        granted_by:
          type: string
          format: uuid
        reason:
          type: string
          minLength: 10
          maxLength: 500
        expires_at:
          nullable: true
          type: string
          format: date-time
        resource_id:
          nullable: true
          type: string
          maxLength: 100
        resource_type:
          nullable: true
          type: string
          maxLength: 50
        is_active:
          type: boolean
        revoked_at:
          nullable: true
          type: string
          format: date-time
        revoked_by:
          nullable: true
          type: string
          format: uuid
        revoked_reason:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    UserPermissionList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/UserPermission'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
