openapi: '3.0.3'
info:
  title: UserSettings API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    UserSettings entity CRUD endpoints.

servers:
  - url: http://localhost:3000
    description: Development server

tags:
  - name: UserSettingses
    description: UserSettings management endpoints

paths:
  /api/v1/user_settingses:
    get:
      tags:
        - UserSettingses
      summary: List UserSettingses
      description: Get a paginated list of UserSettingses with optional filtering and sorting
      operationId: listUserSettingses
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettingsList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - UserSettingses
      summary: Create UserSettings
      description: Create a new UserSettings
      operationId: createUserSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserSettingsRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettings'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/user_settingses/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - UserSettingses
      summary: Get UserSettings by ID
      operationId: getUserSettings
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettings'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - UserSettingses
      summary: Update UserSettings (full)
      operationId: updateUserSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserSettingsRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettings'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - UserSettingses
      summary: Update UserSettings (partial)
      operationId: patchUserSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchUserSettingsRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettings'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - UserSettingses
      summary: Delete UserSettings (soft)
      operationId: deleteUserSettings
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/user_settingses/bulk:
    post:
      tags:
        - UserSettingses
      summary: Bulk create UserSettingses
      operationId: bulkCreateUserSettingses
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateUserSettingsRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UserSettings'

  /api/v1/user_settingses/upsert:
    post:
      tags:
        - UserSettingses
      summary: Upsert UserSettings
      operationId: upsertUserSettings
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserSettingsRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettings'

  /api/v1/user_settingses/trash:
    get:
      tags:
        - UserSettingses
      summary: List deleted UserSettingses
      operationId: listDeletedUserSettingses
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettingsList'

  /api/v1/user_settingses/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - UserSettingses
      summary: Restore deleted UserSettings
      operationId: restoreUserSettings
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserSettings'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/user_settingses/empty:
    delete:
      tags:
        - UserSettingses
      summary: Empty trash for UserSettingses
      operationId: emptyTrashUserSettingses
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

components:
  schemas:
    UserSettings:
      type: object
      required:
        - user_id
        - theme
        - language
        - timezone
        - notifications_enabled
        - email_notifications
        - sms_notifications
        - mfa_enabled
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        theme:
          type: string
        language:
          type: string
        timezone:
          type: string
        notifications_enabled:
          type: boolean
        email_notifications:
          type: boolean
        sms_notifications:
          type: boolean
        mfa_enabled:
          type: boolean
        custom_settings:
          nullable: true
          type: object
        metadata:
          type: object

    CreateUserSettingsRequest:
      type: object
      required:
        - user_id
        - theme
        - language
        - timezone
        - notifications_enabled
        - email_notifications
        - sms_notifications
        - mfa_enabled
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        theme:
          type: string
        language:
          type: string
        timezone:
          type: string
        notifications_enabled:
          type: boolean
        email_notifications:
          type: boolean
        sms_notifications:
          type: boolean
        mfa_enabled:
          type: boolean
        custom_settings:
          nullable: true
          type: object
        metadata:
          type: object

    UpdateUserSettingsRequest:
      type: object
      required:
        - user_id
        - theme
        - language
        - timezone
        - notifications_enabled
        - email_notifications
        - sms_notifications
        - mfa_enabled
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        theme:
          type: string
        language:
          type: string
        timezone:
          type: string
        notifications_enabled:
          type: boolean
        email_notifications:
          type: boolean
        sms_notifications:
          type: boolean
        mfa_enabled:
          type: boolean
        custom_settings:
          nullable: true
          type: object
        metadata:
          type: object

    PatchUserSettingsRequest:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        theme:
          type: string
        language:
          type: string
        timezone:
          type: string
        notifications_enabled:
          type: boolean
        email_notifications:
          type: boolean
        sms_notifications:
          type: boolean
        mfa_enabled:
          type: boolean
        custom_settings:
          nullable: true
          type: object
        metadata:
          type: object

    UserSettingsList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/UserSettings'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
