openapi: '3.0.3'
info:
  title: PasswordResetToken API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    PasswordResetToken entity CRUD endpoints.

servers:
  - url: http://localhost:3000
    description: Development server

tags:
  - name: PasswordResetTokens
    description: PasswordResetToken management endpoints

paths:
  /api/v1/password_reset_tokens:
    get:
      tags:
        - PasswordResetTokens
      summary: List PasswordResetTokens
      description: Get a paginated list of PasswordResetTokens with optional filtering and sorting
      operationId: listPasswordResetTokens
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordResetTokenList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - PasswordResetTokens
      summary: Create PasswordResetToken
      description: Create a new PasswordResetToken
      operationId: createPasswordResetToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreatePasswordResetTokenRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordResetToken'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/password_reset_tokens/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - PasswordResetTokens
      summary: Get PasswordResetToken by ID
      operationId: getPasswordResetToken
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordResetToken'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - PasswordResetTokens
      summary: Update PasswordResetToken (full)
      operationId: updatePasswordResetToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePasswordResetTokenRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordResetToken'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - PasswordResetTokens
      summary: Update PasswordResetToken (partial)
      operationId: patchPasswordResetToken
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchPasswordResetTokenRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordResetToken'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - PasswordResetTokens
      summary: Delete PasswordResetToken (soft)
      operationId: deletePasswordResetToken
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/password_reset_tokens/bulk:
    post:
      tags:
        - PasswordResetTokens
      summary: Bulk create PasswordResetTokens
      operationId: bulkCreatePasswordResetTokens
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreatePasswordResetTokenRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/PasswordResetToken'

  /api/v1/password_reset_tokens/upsert:
    post:
      tags:
        - PasswordResetTokens
      summary: Upsert PasswordResetToken
      operationId: upsertPasswordResetToken
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdatePasswordResetTokenRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordResetToken'

  /api/v1/password_reset_tokens/trash:
    get:
      tags:
        - PasswordResetTokens
      summary: List deleted PasswordResetTokens
      operationId: listDeletedPasswordResetTokens
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordResetTokenList'

  /api/v1/password_reset_tokens/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - PasswordResetTokens
      summary: Restore deleted PasswordResetToken
      operationId: restorePasswordResetToken
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PasswordResetToken'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/password_reset_tokens/empty:
    delete:
      tags:
        - PasswordResetTokens
      summary: Empty trash for PasswordResetTokens
      operationId: emptyTrashPasswordResetTokens
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

components:
  schemas:
    PasswordResetToken:
      type: object
      required:
        - id
        - user_id
        - token_hash
        - expires_at
        - is_used
        - metadata
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        token_hash:
          type: string
        expires_at:
          type: string
          format: date-time
        is_used:
          type: boolean
        used_at:
          nullable: true
          type: string
          format: date-time
        ip_address:
          nullable: true
          type: string
        metadata:
          type: object

    CreatePasswordResetTokenRequest:
      type: object
      required:
        - user_id
        - token_hash
        - expires_at
        - is_used
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        token_hash:
          type: string
        expires_at:
          type: string
          format: date-time
        is_used:
          type: boolean
        used_at:
          nullable: true
          type: string
          format: date-time
        ip_address:
          nullable: true
          type: string
        metadata:
          type: object

    UpdatePasswordResetTokenRequest:
      type: object
      required:
        - user_id
        - token_hash
        - expires_at
        - is_used
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        token_hash:
          type: string
        expires_at:
          type: string
          format: date-time
        is_used:
          type: boolean
        used_at:
          nullable: true
          type: string
          format: date-time
        ip_address:
          nullable: true
          type: string
        metadata:
          type: object

    PatchPasswordResetTokenRequest:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        token_hash:
          type: string
        expires_at:
          type: string
          format: date-time
        is_used:
          type: boolean
        used_at:
          nullable: true
          type: string
          format: date-time
        ip_address:
          nullable: true
          type: string
        metadata:
          type: object

    PasswordResetTokenList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/PasswordResetToken'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
