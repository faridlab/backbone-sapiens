openapi: '3.0.3'
info:
  title: AuditLog API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    AuditLog entity CRUD endpoints.

servers:
  - url: http://localhost:3000
    description: Development server

tags:
  - name: AuditLogs
    description: AuditLog management endpoints

paths:
  /api/v1/audit_logs:
    get:
      tags:
        - AuditLogs
      summary: List AuditLogs
      description: Get a paginated list of AuditLogs with optional filtering and sorting
      operationId: listAuditLogs
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLogList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - AuditLogs
      summary: Create AuditLog
      description: Create a new AuditLog
      operationId: createAuditLog
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAuditLogRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLog'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/audit_logs/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - AuditLogs
      summary: Get AuditLog by ID
      operationId: getAuditLog
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLog'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - AuditLogs
      summary: Update AuditLog (full)
      operationId: updateAuditLog
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAuditLogRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLog'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - AuditLogs
      summary: Update AuditLog (partial)
      operationId: patchAuditLog
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchAuditLogRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLog'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - AuditLogs
      summary: Delete AuditLog (soft)
      operationId: deleteAuditLog
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/audit_logs/bulk:
    post:
      tags:
        - AuditLogs
      summary: Bulk create AuditLogs
      operationId: bulkCreateAuditLogs
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateAuditLogRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/AuditLog'

  /api/v1/audit_logs/upsert:
    post:
      tags:
        - AuditLogs
      summary: Upsert AuditLog
      operationId: upsertAuditLog
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAuditLogRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLog'

  /api/v1/audit_logs/trash:
    get:
      tags:
        - AuditLogs
      summary: List deleted AuditLogs
      operationId: listDeletedAuditLogs
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLogList'

  /api/v1/audit_logs/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - AuditLogs
      summary: Restore deleted AuditLog
      operationId: restoreAuditLog
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AuditLog'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/audit_logs/empty:
    delete:
      tags:
        - AuditLogs
      summary: Empty trash for AuditLogs
      operationId: emptyTrashAuditLogs
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

components:
  schemas:
    AuditLog:
      type: object
      required:
        - id
        - action
        - severity
        - timestamp
        - archived
        - metadata
      properties:
        id:
          type: string
          format: uuid
        user_id:
          nullable: true
          type: string
          format: uuid
        action:
          type: string
          minLength: 1
          maxLength: 100
        details:
          nullable: true
          type: object
        severity:
          type: string
        ip_address:
          nullable: true
          type: string
        user_agent:
          nullable: true
          type: string
          maxLength: 500
        session_id:
          nullable: true
          type: string
          format: uuid
        resource_type:
          nullable: true
          type: string
          maxLength: 50
        resource_id:
          nullable: true
          type: string
          maxLength: 100
        timestamp:
          type: string
          format: date-time
        archived:
          type: boolean
        archived_at:
          nullable: true
          type: string
          format: date-time
        archive_location:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    CreateAuditLogRequest:
      type: object
      required:
        - action
        - severity
        - timestamp
        - archived
        - metadata
      properties:
        user_id:
          nullable: true
          type: string
          format: uuid
        action:
          type: string
          minLength: 1
          maxLength: 100
        details:
          nullable: true
          type: object
        severity:
          type: string
        ip_address:
          nullable: true
          type: string
        user_agent:
          nullable: true
          type: string
          maxLength: 500
        session_id:
          nullable: true
          type: string
          format: uuid
        resource_type:
          nullable: true
          type: string
          maxLength: 50
        resource_id:
          nullable: true
          type: string
          maxLength: 100
        timestamp:
          type: string
          format: date-time
        archived:
          type: boolean
        archived_at:
          nullable: true
          type: string
          format: date-time
        archive_location:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    UpdateAuditLogRequest:
      type: object
      required:
        - action
        - severity
        - timestamp
        - archived
        - metadata
      properties:
        user_id:
          nullable: true
          type: string
          format: uuid
        action:
          type: string
          minLength: 1
          maxLength: 100
        details:
          nullable: true
          type: object
        severity:
          type: string
        ip_address:
          nullable: true
          type: string
        user_agent:
          nullable: true
          type: string
          maxLength: 500
        session_id:
          nullable: true
          type: string
          format: uuid
        resource_type:
          nullable: true
          type: string
          maxLength: 50
        resource_id:
          nullable: true
          type: string
          maxLength: 100
        timestamp:
          type: string
          format: date-time
        archived:
          type: boolean
        archived_at:
          nullable: true
          type: string
          format: date-time
        archive_location:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    PatchAuditLogRequest:
      type: object
      properties:
        user_id:
          nullable: true
          type: string
          format: uuid
        action:
          type: string
          minLength: 1
          maxLength: 100
        details:
          nullable: true
          type: object
        severity:
          type: string
        ip_address:
          nullable: true
          type: string
        user_agent:
          nullable: true
          type: string
          maxLength: 500
        session_id:
          nullable: true
          type: string
          format: uuid
        resource_type:
          nullable: true
          type: string
          maxLength: 50
        resource_id:
          nullable: true
          type: string
          maxLength: 100
        timestamp:
          type: string
          format: date-time
        archived:
          type: boolean
        archived_at:
          nullable: true
          type: string
          format: date-time
        archive_location:
          nullable: true
          type: string
          maxLength: 500
        metadata:
          type: object

    AuditLogList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/AuditLog'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
