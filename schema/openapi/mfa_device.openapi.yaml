openapi: '3.0.3'
info:
  title: MFADevice API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    MFADevice entity CRUD endpoints.

servers:
  - url: http://localhost:3000
    description: Development server

tags:
  - name: MFADevices
    description: MFADevice management endpoints

paths:
  /api/v1/mfa_devices:
    get:
      tags:
        - MFADevices
      summary: List MFADevices
      description: Get a paginated list of MFADevices with optional filtering and sorting
      operationId: listMFADevices
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MFADeviceList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - MFADevices
      summary: Create MFADevice
      description: Create a new MFADevice
      operationId: createMFADevice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateMFADeviceRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MFADevice'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/mfa_devices/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - MFADevices
      summary: Get MFADevice by ID
      operationId: getMFADevice
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MFADevice'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - MFADevices
      summary: Update MFADevice (full)
      operationId: updateMFADevice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMFADeviceRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MFADevice'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - MFADevices
      summary: Update MFADevice (partial)
      operationId: patchMFADevice
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchMFADeviceRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MFADevice'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - MFADevices
      summary: Delete MFADevice (soft)
      operationId: deleteMFADevice
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/mfa_devices/bulk:
    post:
      tags:
        - MFADevices
      summary: Bulk create MFADevices
      operationId: bulkCreateMFADevices
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateMFADeviceRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/MFADevice'

  /api/v1/mfa_devices/upsert:
    post:
      tags:
        - MFADevices
      summary: Upsert MFADevice
      operationId: upsertMFADevice
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateMFADeviceRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MFADevice'

  /api/v1/mfa_devices/trash:
    get:
      tags:
        - MFADevices
      summary: List deleted MFADevices
      operationId: listDeletedMFADevices
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MFADeviceList'

  /api/v1/mfa_devices/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - MFADevices
      summary: Restore deleted MFADevice
      operationId: restoreMFADevice
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MFADevice'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/mfa_devices/empty:
    delete:
      tags:
        - MFADevices
      summary: Empty trash for MFADevices
      operationId: emptyTrashMFADevices
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

components:
  schemas:
    MFADevice:
      type: object
      required:
        - id
        - user_id
        - device_type
        - device_name
        - secret
        - backup_codes
        - is_active
        - metadata
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        device_type:
          type: string
        device_name:
          type: string
          minLength: 1
          maxLength: 100
        secret:
          type: string
        backup_codes:
          type: array
        is_active:
          type: boolean
        verified_at:
          nullable: true
          type: string
          format: date-time
        last_used:
          nullable: true
          type: string
          format: date-time
        metadata:
          type: object

    CreateMFADeviceRequest:
      type: object
      required:
        - user_id
        - device_type
        - device_name
        - secret
        - backup_codes
        - is_active
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        device_type:
          type: string
        device_name:
          type: string
          minLength: 1
          maxLength: 100
        secret:
          type: string
        backup_codes:
          type: array
        is_active:
          type: boolean
        verified_at:
          nullable: true
          type: string
          format: date-time
        last_used:
          nullable: true
          type: string
          format: date-time
        metadata:
          type: object

    UpdateMFADeviceRequest:
      type: object
      required:
        - user_id
        - device_type
        - device_name
        - secret
        - backup_codes
        - is_active
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        device_type:
          type: string
        device_name:
          type: string
          minLength: 1
          maxLength: 100
        secret:
          type: string
        backup_codes:
          type: array
        is_active:
          type: boolean
        verified_at:
          nullable: true
          type: string
          format: date-time
        last_used:
          nullable: true
          type: string
          format: date-time
        metadata:
          type: object

    PatchMFADeviceRequest:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        device_type:
          type: string
        device_name:
          type: string
          minLength: 1
          maxLength: 100
        secret:
          type: string
        backup_codes:
          type: array
        is_active:
          type: boolean
        verified_at:
          nullable: true
          type: string
          format: date-time
        last_used:
          nullable: true
          type: string
          format: date-time
        metadata:
          type: object

    MFADeviceList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/MFADevice'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
