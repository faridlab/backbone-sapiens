openapi: '3.0.3'
info:
  title: UserRole API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    UserRole entity CRUD endpoints.

servers:
  - url: http://localhost:3000
    description: Development server

tags:
  - name: UserRoles
    description: UserRole management endpoints

paths:
  /api/v1/user_roles:
    get:
      tags:
        - UserRoles
      summary: List UserRoles
      description: Get a paginated list of UserRoles with optional filtering and sorting
      operationId: listUserRoles
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoleList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - UserRoles
      summary: Create UserRole
      description: Create a new UserRole
      operationId: createUserRole
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateUserRoleRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRole'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/user_roles/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - UserRoles
      summary: Get UserRole by ID
      operationId: getUserRole
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRole'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - UserRoles
      summary: Update UserRole (full)
      operationId: updateUserRole
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserRoleRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRole'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - UserRoles
      summary: Update UserRole (partial)
      operationId: patchUserRole
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchUserRoleRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRole'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - UserRoles
      summary: Delete UserRole (soft)
      operationId: deleteUserRole
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/user_roles/bulk:
    post:
      tags:
        - UserRoles
      summary: Bulk create UserRoles
      operationId: bulkCreateUserRoles
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateUserRoleRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/UserRole'

  /api/v1/user_roles/upsert:
    post:
      tags:
        - UserRoles
      summary: Upsert UserRole
      operationId: upsertUserRole
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateUserRoleRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRole'

  /api/v1/user_roles/trash:
    get:
      tags:
        - UserRoles
      summary: List deleted UserRoles
      operationId: listDeletedUserRoles
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRoleList'

  /api/v1/user_roles/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - UserRoles
      summary: Restore deleted UserRole
      operationId: restoreUserRole
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UserRole'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/user_roles/empty:
    delete:
      tags:
        - UserRoles
      summary: Empty trash for UserRoles
      operationId: emptyTrashUserRoles
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

components:
  schemas:
    UserRole:
      type: object
      required:
        - id
        - user_id
        - role_id
        - assigned_at
        - metadata
      properties:
        id:
          type: string
          format: uuid
        user_id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        assigned_at:
          type: string
          format: date-time
        assigned_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    CreateUserRoleRequest:
      type: object
      required:
        - user_id
        - role_id
        - assigned_at
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        assigned_at:
          type: string
          format: date-time
        assigned_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    UpdateUserRoleRequest:
      type: object
      required:
        - user_id
        - role_id
        - assigned_at
        - metadata
      properties:
        user_id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        assigned_at:
          type: string
          format: date-time
        assigned_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    PatchUserRoleRequest:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        role_id:
          type: string
          format: uuid
        assigned_at:
          type: string
          format: date-time
        assigned_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    UserRoleList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/UserRole'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
