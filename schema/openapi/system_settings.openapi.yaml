openapi: '3.0.3'
info:
  title: SystemSettings API
  version: '1.0.0'
  description: |
    Auto-generated by backbone-cli.
    SystemSettings entity CRUD endpoints.

servers:
  - url: http://localhost:3000
    description: Development server

tags:
  - name: SystemSettingses
    description: SystemSettings management endpoints

paths:
  /api/v1/system_settingses:
    get:
      tags:
        - SystemSettingses
      summary: List SystemSettingses
      description: Get a paginated list of SystemSettingses with optional filtering and sorting
      operationId: listSystemSettingses
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
        - $ref: '#/components/parameters/SortByParam'
        - $ref: '#/components/parameters/SortOrderParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemSettingsList'
        '401':
          $ref: '#/components/responses/Unauthorized'

    post:
      tags:
        - SystemSettingses
      summary: Create SystemSettings
      description: Create a new SystemSettings
      operationId: createSystemSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateSystemSettingsRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemSettings'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'

  /api/v1/system_settingses/{id}:
    parameters:
      - $ref: '#/components/parameters/IdParam'

    get:
      tags:
        - SystemSettingses
      summary: Get SystemSettings by ID
      operationId: getSystemSettings
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemSettings'
        '404':
          $ref: '#/components/responses/NotFound'

    put:
      tags:
        - SystemSettingses
      summary: Update SystemSettings (full)
      operationId: updateSystemSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSystemSettingsRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemSettings'
        '404':
          $ref: '#/components/responses/NotFound'

    patch:
      tags:
        - SystemSettingses
      summary: Update SystemSettings (partial)
      operationId: patchSystemSettings
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PatchSystemSettingsRequest'
      responses:
        '200':
          description: Updated
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemSettings'
        '404':
          $ref: '#/components/responses/NotFound'

    delete:
      tags:
        - SystemSettingses
      summary: Delete SystemSettings (soft)
      operationId: deleteSystemSettings
      responses:
        '204':
          description: Deleted
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/system_settingses/bulk:
    post:
      tags:
        - SystemSettingses
      summary: Bulk create SystemSettingses
      operationId: bulkCreateSystemSettingses
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: array
              items:
                $ref: '#/components/schemas/CreateSystemSettingsRequest'
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/SystemSettings'

  /api/v1/system_settingses/upsert:
    post:
      tags:
        - SystemSettingses
      summary: Upsert SystemSettings
      operationId: upsertSystemSettings
      parameters:
        - name: key_field
          in: query
          schema:
            type: string
        - name: key_value
          in: query
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateSystemSettingsRequest'
      responses:
        '200':
          description: Upserted
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemSettings'

  /api/v1/system_settingses/trash:
    get:
      tags:
        - SystemSettingses
      summary: List deleted SystemSettingses
      operationId: listDeletedSystemSettingses
      parameters:
        - $ref: '#/components/parameters/PageParam'
        - $ref: '#/components/parameters/PerPageParam'
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemSettingsList'

  /api/v1/system_settingses/{id}/restore:
    parameters:
      - $ref: '#/components/parameters/IdParam'
    post:
      tags:
        - SystemSettingses
      summary: Restore deleted SystemSettings
      operationId: restoreSystemSettings
      responses:
        '200':
          description: Restored
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/SystemSettings'
        '404':
          $ref: '#/components/responses/NotFound'

  /api/v1/system_settingses/empty:
    delete:
      tags:
        - SystemSettingses
      summary: Empty trash for SystemSettingses
      operationId: emptyTrashSystemSettingses
      responses:
        '200':
          description: Trash emptied
          content:
            application/json:
              schema:
                type: object
                properties:
                  deleted_count:
                    type: integer

components:
  schemas:
    SystemSettings:
      type: object
      required:
        - id
        - key
        - value
        - updated_at
        - metadata
      properties:
        id:
          type: string
          format: uuid
        key:
          type: string
          minLength: 1
          maxLength: 100
        value:
          type: object
        description:
          nullable: true
          type: string
          maxLength: 1000
        updated_at:
          type: string
          format: date-time
        updated_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    CreateSystemSettingsRequest:
      type: object
      required:
        - key
        - value
        - metadata
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 100
        value:
          type: object
        description:
          nullable: true
          type: string
          maxLength: 1000
        updated_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    UpdateSystemSettingsRequest:
      type: object
      required:
        - key
        - value
        - metadata
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 100
        value:
          type: object
        description:
          nullable: true
          type: string
          maxLength: 1000
        updated_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    PatchSystemSettingsRequest:
      type: object
      properties:
        key:
          type: string
          minLength: 1
          maxLength: 100
        value:
          type: object
        description:
          nullable: true
          type: string
          maxLength: 1000
        updated_by:
          nullable: true
          type: string
          format: uuid
        metadata:
          type: object

    SystemSettingsList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/SystemSettings'
        meta:
          $ref: '#/components/schemas/PaginationMeta'

    PaginationMeta:
      type: object
      properties:
        total:
          type: integer
          example: 100
        page:
          type: integer
          example: 1
        per_page:
          type: integer
          example: 20
        total_pages:
          type: integer
          example: 5

    Error:
      type: object
      required:
        - code
        - message
      properties:
        code:
          type: string
          example: VALIDATION_ERROR
        message:
          type: string
          example: Invalid input data
        details:
          type: object
          additionalProperties: true

  parameters:
    IdParam:
      name: id
      in: path
      required: true
      schema:
        type: string
        format: uuid

    PageParam:
      name: page
      in: query
      schema:
        type: integer
        minimum: 1
        default: 1

    PerPageParam:
      name: per_page
      in: query
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 20

    SortByParam:
      name: sort_by
      in: query
      schema:
        type: string

    SortOrderParam:
      name: sort_order
      in: query
      schema:
        type: string
        enum: [asc, desc]
        default: asc

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

    NotFound:
      description: Resource not found
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/Error'

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

security:
  - bearerAuth: []
